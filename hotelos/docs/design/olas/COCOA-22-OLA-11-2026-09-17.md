# Cocoa 22 · Ola 11 (Compartido, desarrollo y limpieza) — Informe de cierre · 17 de septiembre de 2026

**Para:** César. **Alcance:** última ola de la migración al sistema Cocoa 22 (spec
`docs/design/COCOA-22.md`, plan `docs/design/COCOA-22-MIGRACION.md`) sobre el árbol de
trabajo del cierre de la Tanda C (base `3e01b61`, sin commit): lote 11-A (los 10 ficheros que
quedaban en `NOT_MIGRATED`: dev, onboarding, contenedores y muertas), limpieza del CSS legacy y
de los componentes sin importador, cierre del puente L1c (`embed()` / `pageHead(embedded)`),
causas raíz del API heredadas de la QA de la Tanda C, residuos de datos, la **corrección de la
ronda 1** (8 hallazgos + 5 de regresión visual por código) y la **integración final**
(2026-09-17) que deja el contrato en `NOT_MIGRATED = []` con todas las puertas en verde.
**Sin commit**: los 39 ficheros retirados están MOVIDOS al scratchpad de la sesión (§3) y el
orquestador confirma la retirada (`git rm`) en el commit de la ola (§11). Servidores `:3000` y
`:5173` no reiniciados; la BD local solo se ha tocado para los dos residuos de datos de §5.

Método de medida: `node scripts/cocoa-22-inventory.mjs` (puntos de deuda por pantalla),
contrato `tests/cocoa-22-contract.test.mjs` (reglas 1–15) y las puertas de §7.3 del plan.
«Antes» = inventario commiteado al cerrar la Tanda C; «después» = inventario regenerado en
este cierre. Ningún lote tuvo navegador: la verificación visual (§8) la hace el orquestador.

## 1. Resumen en cifras

| Métrica | Antes (cierre Tanda C) | Después (cierre ola 11) | Δ |
|---|---|---|---|
| Pantallas inventariadas | 224 | 220 | −4 (`developer/CocoaShowcaseScreen.tsx` y `preview/CocoaGalleryScreen.tsx` muertas; `admin/InviteUserDialog.tsx` y `admin/ResetPasswordConfirmDialog.tsx` sin importador, retiradas) |
| Líneas en `screens/` | 91.375 | 89.029 | −2.346 |
| **Puntos de deuda** | **313** | **194** | **−119 (−38,0 %)** |
| `.bo-card` · otras `.bo-*` | 26 · 52 | 0 · 0 | −26 · −52 |
| `<button>` crudos (CocoaButton) | 12 (1.052) | 0 (1.039) | −12 |
| `<table>` crudas (CocoaTable) | 3 (289) | 2 (288) | −1 (las 2 que quedan son parrillas `data-cocoa-grid-table`, excepción de la regla 3) |
| Inputs crudos (Cocoa) | 4 (1.055) | 0 (1.032) | −4 |
| `style={` | 801 | 682 | −119 |
| Colores literales (fallbacks) | 2 (0) | 0 (0) | −2 |
| `<h1>` crudos · `.bo-page-title` | 6 · 3 | 0 · 0 | −6 · −3 |
| Con cabecera Cocoa · `useTabHost` | 197 · 87 | 196 · 87 | −1 · 0 (la regla 7 y el inventario ya no aceptan `pageHead(` ni `<Head`: el `<Head>` local de `operations/RoomBlockGridDialog.tsx` es una celda de parrilla y el diálogo está exento) |
| Tamaños S · M · L · XL | 95 · 104 · 20 · 3 | 93 · 105 · 19 · 3 | — |
| `NOT_MIGRATED` = `ALLOWLIST_CEILING` | 10 | **0** | cierre de la migración |
| `GLOBAL_CEILING` (boCard · rawButtons · rawTables · rawInputs · colourLiterals · inlineStyles) | 26 · 12 · 3 · 4 · 2 · 801 | **0 · 0 · 2 · 0 · 0 · 682** | = totales regenerados, sin margen; todos por debajo del techo anterior |
| `STYLE_BUDGET` | 82 entradas | 82 entradas | −2 huérfanas (`admin/InviteUserDialog.tsx`, `admin/ResetPasswordConfirmDialog.tsx`) + `dev/StyleGuideScreen.tsx` 40 + `ModuleSettingsPlaceholder.tsx` 15 |
| Pendientes en §6 del plan | 10 ficheros · 129 pts (ola 11) | **0 · 0** | `cocoa-22-waves.mjs --check` al día |

Por categoría (después): comercial 18 pantallas · 15 pts · compartido 8 · 0 · configuración 44
· 23 · cumplimiento 29 · 10 · desarrollo 2 · 9 · finanzas 33 · 36 · hoy 10 · 17 · informes 9
· 7 · operaciones 27 · 38 · público 6 · 6 · recepción 19 · 26 · revenue 15 · 7. Los 194 puntos
restantes no son legacy: `style={`·0,25 sobre 682 usos de layout u objetos con nombre con
valores del sistema (regla 6), las dos parrillas y las sub-vistas sin cabecera propia
(`HEADER_EXEMPT`). Por eso §7.4 del plan se da por cumplido con `GLOBAL_CEILING` = totales y
no con seis ceros.

## 2. Lote 11-A · pantallas dev y onboarding migradas (puntos antes → después)

| Fichero (`screens/`) | Antes | Después | Qué cambió |
|---|---|---|---|
| `onboarding/OnboardingInteractive.tsx` | 60 | 1 | asistente sobre `CocoaPage` + `CocoaFormSection`; 4 `style={` de layout; sin `.bo-*`, sin `<select>` crudo, sin `<h1>`, sin `.dp-*` (la vista clave/valor es `CocoaDataPreview`, `components/cocoa-extras/`) |
| `ModuleSettingsPlaceholder.tsx` | 16 | 0 | fábrica de los 16 placeholders `?dev=1` en Cocoa (formulario, presupuesto 15); pinta `<CocoaPage>` y por eso sale de `HEADER_EXEMPT` (integración) |
| `dev/StyleGuideScreen.tsx` | 10 | 9 | ya en `CocoaPage`; 37 `style={` de muestras (`STYLE_BUDGET` 40); la muestra de color pasa a `.c22-guide-swatch` + custom property `--c22-guide-swatch` (ronda 1, RV-02/R2) |
| `ScreenScaffold.tsx` | 9 | 0 | plantilla base sin `.bo-*` |
| `onboarding/OnboardingScreens.tsx` | 5 | 0 | 5 pantallas dev-only bajo `/desarrollo/migracion*` |
| `tabs/tab-helpers.tsx` | 1 | 0 | pierde `embed()` y `pageHead()`; `HostedHead` vive en `components/cocoa/HostedHead.tsx` |
| `tabs/NavItemTabs.tsx` · `tabs/TabHost.tsx` | 0 · 0 | 0 · 0 | fuera de la allowlist sin exención de la regla 8: `screens/tabs/**` ya no contiene ningún `<h1>` |
| `developer/CocoaShowcaseScreen.tsx` (16) · `preview/CocoaGalleryScreen.tsx` (12) | 28 | — | **muertas**, retiradas con sus exenciones de `tests/admin-web-spanish-copy-contract.test.mjs` |

## 3. Ficheros retirados (39 · copia en el scratchpad · `git rm` en el commit)

Base de las copias: `/private/tmp/claude-501/-Users-cfernandez/98e3fb6d-9746-4e88-9a84-2efd89d1bbec/scratchpad/ola11-removed/` —
cada fichero está en `<base>/<ruta relativa a hotelos/>`, y ninguno sigue en el repositorio
(comprobado fichero a fichero el 2026-09-17). Todos con 0 importadores en `apps/admin-web/src`,
`packages/` y `tests/` antes de moverlos.

| Grupo | Rutas (relativas a `hotelos/`) | Por qué |
|---|---|---|
| CSS legacy (1) | `apps/admin-web/src/styles/cocoa-22-legacy-bridge.css` | hoja puente de 617 líneas; `styles.css` ya no la importa ni declara la capa legacy |
| Componentes compartidos (10) | `apps/admin-web/src/components/ConfirmDialog.tsx` · `NarrowViewportBanner.tsx` · `SidePanel.tsx` · `States.tsx` · `SubmissionDetailPanel.tsx` · `TopBar.tsx` · `cocoa-director/DirectorKpiTile.tsx` · `cocoa-empty-state/CocoaEmptyState.tsx` · `cocoa-extras/CocoaColorWell.tsx` · `forms/FormComponents.tsx` | sin importador tras migrar `App.tsx` (`LoadingBlock` → `CocoaState`), `tabs/NavItemTabs.tsx` (`ErrorState`), `OnboardingInteractive.tsx` y `PropertySetupForms.tsx` (`DataPreview` → `cocoa-extras/CocoaDataPreview.tsx`); el cromo legacy (`TopBar`) desaparece con `USE_COCOA_LAYOUT`; `DirectorKpiTile` era alias deprecado de `CocoaKpi`; el vacío legacy se consolida en `CocoaState` |
| `components/v2/**` (8) | `apps/admin-web/src/components/v2/Avatar.tsx` · `DataTable.tsx` · `PageHeader.tsx` · `SearchInput.tsx` · `SegmentedControl.tsx` · `StatTile.tsx` · `StatusBadge.tsx` · `Tooltip.tsx` | kit «v2» anterior a Cocoa, 0 importadores |
| Pantallas (5) | `apps/admin-web/src/screens/admin/InviteUserDialog.tsx` · `admin/ResetPasswordConfirmDialog.tsx` · `admin/index.ts` · `developer/CocoaShowcaseScreen.tsx` · `preview/CocoaGalleryScreen.tsx` | barrel `admin/index.ts` sin importador (`UserRoleManager` usa sus propios diálogos); showcase y galería muertas |
| Kit de cronograma (15) | `packages/ui/src/components/timeline/index.ts` · `TimelineBottomSheet.tsx` · `TimelineDateSelector.tsx` · `TimelineDragHandle.tsx` · `TimelineFilterBar.tsx` · `TimelineGapAlert.tsx` · `TimelineGrid.tsx` · `TimelineHeader.tsx` · `TimelineOverbookingAlert.tsx` · `TimelineQuickActionBar.tsx` · `TimelineReservationBlock.tsx` · `TimelineResourceRow.tsx` · `TimelineRightDetailPanel.tsx` · `TimelineSpaceBlock.tsx` · `TimelineStatusDot.tsx` | R32: sin consumidor en admin-web desde la ola 3; alias `@hotelos/ui/timeline` fuera de `apps/admin-web/vite.config.ts` |

Además, **sin retirar pero renombrado** (aparece como `D` + `??` hasta el `git add`):
`apps/admin-web/src/components/forms/__tests__/data-preview.test.mts` →
`apps/admin-web/src/components/cocoa-extras/__tests__/data-preview.test.mts` (sigue al
componente). Y el helper `toastOffsetForBar` de `components/cocoa-rate-grid/helpers.ts` con su
test (R31) se ha borrado dentro de sus ficheros, no como fichero.

## 4. Puente L1c cerrado y CSS legacy

- **Loaders**: 10 `embed(m.X)` → `({ default: m.X })` en `tabs/configuracion/{Comunicaciones,
  PuestaEnMarcha, Sistema, Modulos, InteligenciaArtificial (×4)}Tabs.tsx` y
  `tabs/cumplimiento/{Verifactu, Sostenibilidad}Tabs.tsx`.
- **Pantallas**: 5 con `pageHead(embedded)` → `<CocoaPage state skeleton error commands>`
  (`compliance/AuthorityRoutingSettingsScreen`, `compliance/GuestRegisterRetentionSettingsScreen`,
  `compliance/PropertyTaxesScreen`, `fiscal/FiscalDashboard`, `operations/SustainabilityDashboard`)
  y 14 sin la prop `embedded` ni el wrapper `useTabHost() !== null || embedded`
  (`grep '\bembedded\b' screens/` = 4 menciones, todas en comentarios). Registran `commands` ⌘K
  y `data-hosted` a través de `CocoaPage`, que lee el contexto del host.
- **Helpers**: `tabs/tab-helpers.tsx` sin `embed()` ni `pageHead()`; `HostedHead` en
  `components/cocoa/HostedHead.tsx` (importa `HOSTED_TOOLBAR` y `useHostedEyebrow` de `TabHost`).
  Tests de `cumplimiento-tabs`, `configuracion-tabs` y `screens-fixes-contract` vaciados en el
  mismo cambio; la regla 7 del contrato y `scripts/cocoa-22-inventory.mjs` ya no aceptan
  `pageHead(` ni `<Head`.
- **CSS**: `apps/admin-web/src/styles.css` sin `@import` del puente, sin la declaración de capas
  ni el bloque legacy (115 clases `.bo-*`, 269 reglas), sin `--sidebar-w` / `--sidebar-w-collapsed`
  / `--topbar-h` (0 consumidores; el shell usa `--cocoa-sidebar-width` / `--cocoa-toolbar-height`);
  alias `.gm-grid` de `cocoa-22-layout.css` y reglas legacy de `mobile.css` fuera; `SIDEBAR_CSS`
  de `navigation/Sidebar.tsx` en `styles/cocoa-22-shell.css` (R11); paleta ⌘K y guía intrínseca
  portadas a `styles/cocoa-22-guide.css` con clases `c22-*` y solo tokens. Greps de criterio:
  `@layer` en `src/` = 0; prefijos legacy (`bo-`, `gm-`, `guide-`, `cocoa-legacy`, `legacy-bridge`)
  en `styles/` y `styles.css` = 0 (los comentarios de cabecera se reformularon en la integración);
  el único `bo-` que queda en `src/` es la guarda negativa de
  `layouts/__tests__/shell-cocoa22-contract.test.mts:148`.
- `layouts/BackOfficeLayout.tsx` sin `USE_COCOA_LAYOUT` ni cromo legacy: `<OfficeCentreBanner>`
  se monta una sola vez, en el shell Cocoa.

## 5. Causas raíz del API y datos (QA de la Tanda C)

- **qa#17 · referencia del API** — `apps/api/src/modules/developer/api-reference.service.ts`:
  `describeEndpoint()` describe los 954 endpoints en español con diccionario de segmentos
  (`SEGMENT_LABELS` / `SINGLETON_LABELS` / `ACTION_LABELS`; antes 916 de 941 descripciones eran
  eco de la ruta), rama PUT («Sustituir …») y `byMethod` con PUT. Ronda 1 (R6): 18 etiquetas
  nuevas, `DELETE …/match|reconcile` = «Deshacer la conciliación de …», `…/ai/analyze` =
  «Analizar el proyecto con IA.»; 0 descripciones que ecoen un segmento crudo (quedan
  «marketplace» y «OAuth», que son español). Test `developer/__tests__/api-reference.test.mts`
  (10/10, incluye «ola 11 · R6»). `developer/ApiReferenceScreen.tsx` tolera `byMethod.PUT ?? 0`.
- **qa#3 / qa#14 · readiness** — los mensajes de `computeReadiness`
  (`backoffice.service.ts:3492-3670`) ya nacen en español y `lib/format.readinessMessage()` queda
  con `READINESS_MESSAGE_REWRITES = []` (puente vacío); `TaxComplianceSettings.tsx` consume el texto
  del API. Integración: los 4 mensajes sembrados de `apps/api/src/lib/demo-store.ts`
  (`propertyReadinessChecks`, solo visibles hasta la primera recalculación) y los 2 de
  `moduleHealthChecks`, más `label`/`description` de las 3 exigencias de
  `getModuleSetupRequirements` (`backoffice.service.ts`, las pinta `ModuleHealthCenter`), pasan
  al español. El `checkCode` legacy `legal_profile_complete` del seed se conserva: `computeReadiness`
  lo borra al recalcular (comentario en `:3677`).
- **«sent» de SES** — `screens/fiscal/fiscal-shared.ts` `SUBMISSION_PENDING_STATUSES` incluye
  «sent» (el API lo trata como abierto: `ses-submission.service.ts SES_OPEN_STATUSES`);
  `FiscalSubmissionsCenter` y, desde la ronda 1 (R3), `compliance/SesHospedajesSettingsScreen`
  (sondeo del drawer cada 8 s) lo importan — test
  `compliance/__tests__/ses-hospedajes-pending-statuses.test.mts` (4/4).
- **Correo entrante** — `DELETE /email/connections/:id` borra la conexión nunca autorizada
  (`pending_auth` sin refresh token) y responde `deleted: true` con auditoría
  `EMAIL_CONNECTION_DELETED`; el resto se conserva como `disconnected` (`deleted: false`).
  Regla pura `disconnectOutcome()` + test `integrations/email/__tests__/email-connections.test.mts`;
  documentado en `docs/api-contracts.md` («Correo entrante») en la integración.
- **Residuos de datos** (BD local, `psql`, verificados el 2026-09-17): `email_connections` = 0
  filas (la fila «Gmail · desconectado» de Rías Altas); `guest_register_records`
  `grr_e86d63b9` (Rías Altas, `accepted`) conserva la fila con `document_number` vacío (longitud 0)
  en lugar del token cifrado de 79 caracteres. Hecho por SQL sin evento de auditoría (cadena
  in-memory): queda anotado aquí y en §0 del plan.
- `packages/compliance` gana el script `test` (`corepack pnpm --filter @hotelos/compliance test`:
  104 tests · 103 pass · 1 skipped) — hasta ahora `submitter.test.mjs` solo corría con el loader
  documentado en su cabecera.

## 6. Hallazgos y correcciones

### 6.1 Ronda 1 (2026-09-17)

| Hallazgo | Fichero:línea | Corrección |
|---|---|---|
| RV-01 · R1 (puerta front roja 1.046/1.047) | `layouts/__tests__/shell-structure-switcher.test.mts:84` | esperado 2 → **1** montaje de `<OfficeCentreBanner>` («mounted once, in the Cocoa shell») |
| RV-02 · R2 (regla 6 con `NOT_MIGRATED = []`) | `screens/dev/StyleGuideScreen.tsx` · `styles/cocoa-22-guide.css` | `background` fuera del literal: `swatchStyleFor(value, token)` escribe la custom property `--c22-guide-swatch` (la regla 6 admite claves `--*`) y `.c22-guide-swatch` la pinta; 37 `style={` (sin subir); sin exención nueva |
| R3 | `screens/compliance/SesHospedajesSettingsScreen.tsx:36,98` | `PENDING_STATUSES = SUBMISSION_PENDING_STATUSES` + test |
| RV-03 · R4 (`cocoa-22-api.mjs --check` rojo) | `docs/design/COCOA-22.md` §8 | `--write`: la re-exportación de `CocoaEmptyState` desaparece (1.894 líneas · 645 props) |
| R5 (cierre del contrato) | `tests/cocoa-22-contract.test.mjs` | `NOT_MIGRATED = []`, `ALLOWLIST_CEILING = 0`, `GLOBAL_CEILING` 0 · 0 · 2 · 0 · 0 · 682, `STYLE_BUDGET` 82, `COLOUR_EXEMPT` sin `preview/`, regla 7 sin `pageHead(` |
| RV-04 | `scripts/cocoa-22-inventory.mjs` | `pageHead\(` fuera del regex de cabecera |
| R6 (low) | `api-reference.service.ts` | 18 etiquetas + 2 casos especiales + test (§5) |
| R7 (low) | `components/guide/HelpCenter.tsx:24,62,81,163,235` | **no procede**: los cinco `<button>` son superficies-fila que ninguna primitiva cubre (cabecera de acordeón, fila de recorrido, CTA con icono, botón «×»); `components/` está fuera de la regla 2 y del inventario |
| R8 (low) | `components/README-COCOA.md` · `cocoa-rate-grid/README.md` · `COCOA-22.md` | README reescrito con la estructura real; `toastOffsetForBar`, «Vacío legacy» y `CocoaColorWell` marcados como retirados |

### 6.2 Integración final (2026-09-17) — `openIssues` de los seis lotes

| Origen | Fichero:línea | Resolución |
|---|---|---|
| componentes-muertos · dev-onboarding | `scripts/cocoa-22-inventory.mjs:106,109,111,173` | sin `<Head\b` (era el `Head` del puente) ni `<SidePanel\b`; métrica `usesGmGrid` retirada (0 usos); `HEADER_EXEMPT` sin `ModuleSettingsPlaceholder.tsx` (pinta `<CocoaPage>`) — puntos 194 sin cambio, cabecera Cocoa 197 → 196 |
| dev-onboarding | `tests/cocoa-22-contract.test.mjs:474-475, 567-568` | `HEADER_EXEMPT` sin `ModuleSettingsPlaceholder.tsx`; regla 8 documentada sin exención para `tabs/TabHost.tsx` y `tabs/tab-helpers.tsx` (R18: `screens/tabs/**` sin `<h1>`) |
| componentes-muertos · puente-l1c | `scripts/cocoa-22-waves.mjs:11-15, 76, 86-90, 94-96` | `OVERRIDE` sin `admin/InviteUserDialog.tsx` ni `admin/ResetPasswordConfirmDialog.tsx`; notas de `tab-helpers`, `TabHost`, `NavItemTabs` y `StyleGuideScreen` reescritas; `DEAD` vacío (las dos entradas de la ola 6 pasan a comentario); `--write` + `--check` al día |
| puente-l1c · componentes-muertos | `apps/admin-web/.discoverability-whitelist.json` | sin `HostedHead` (vive en `components/cocoa/`), `CocoaLoginScreen`, `CocoaServerErrorScreen` ni `CocoaOnboardingWizard` (ficheros retirados en las olas 1 y 10); 28 → 24 entradas; `$comment` actualizado; `check-discoverability` 189/189 |
| css-shell | `apps/admin-web/src/styles.css:17-18, 141-142` | comentario de cascada sin literales legacy; `--sidebar-w` / `--sidebar-w-collapsed` / `--topbar-h` retirados (0 consumidores; `shell-cocoa22-contract.test.mts:102` los prohíbe en la hoja del shell) |
| css-shell · guia-paleta | `apps/admin-web/src/styles/cocoa-22-guide.css:5-10` | cabecera sin `@layer`, `.bo-cmdk-*`, `.guide-*` ni nombres de keyframes legacy: los greps de criterio dan 0 |
| api-datos | `apps/api/src/lib/demo-store.ts:2105,2115,2125,2135,2148,2159` · `backoffice.service.ts:4628-4646` | 6 mensajes sembrados y 3 exigencias de módulo en español (§5) |
| api-datos | `docs/api-contracts.md` (§ Integration Marketplace) | nueva subsección «Correo entrante» con `deleted` y `EMAIL_CONNECTION_DELETED` |
| api-datos | `packages/compliance/package.json` | script `test` (§5) |
| puente-l1c | `docs/design/COCOA-22-MIGRACION.md` §2.5, §7.4, §8 | §2.5 con el cierre de las 8 muertas; §7.4 apunta a este informe; §8 sin R2, R11, R16, R31 y R32 (resueltos), R14 y R18 reducidos a lo que queda, R28 sin la referencia a `CocoaColorWell` |
| puente-l1c | `docs/design/COCOA-22.md:3545` (tabla §10, fila Cumplimiento) | anotada «puente L1c cerrado en la ola 11» (tabla histórica del 2026-09-15) |
| componentes-muertos | `screens/fiscal/FiscalSubmissionsCenter.tsx:9` · `styles/cocoa-22.css:85` · `components/README-COCOA.md` | ya sin `SubmissionDetailPanel` / `DirectorKpiTile` / `cocoa-empty-state` (ronda 1); verificado por grep |
| todos | `docs/design/olas/COCOA-22-OLA-11-2026-09-16.md` | renombrado a este informe (`…-2026-09-17.md`); referencias en el plan actualizadas |

## 7. Contrato, inventario y scripts (estado final)

- `tests/cocoa-22-contract.test.mjs`: `ALLOWLIST_CEILING = 0` · `NOT_MIGRATED = []` ·
  `GLOBAL_CEILING = { boCard 0, rawButtons 0, rawTables 2, rawInputs 0, colourLiterals 0,
  inlineStyles 682 }` (comentario con la historia 941/647/159/553/539/4607 → … → 26/12/3/4/2/801 →
  0/0/2/0/0/682) · `STYLE_BUDGET` 82 entradas, 0 huérfanas · `HEADER_EXEMPT` = `tabs/`, diálogos y
  drawers, `ScreenScaffold.tsx`, `operations/FrontDeskActionQueue.tsx`, `fiscal/ReportErrorCard.tsx`,
  `operations/GroupsPickupCard.tsx` · `COLOUR_EXEMPT` = `auth/`, `developer/` · 18/18.
- `tests/admin-web-spanish-copy-contract.test.mjs`: sin exenciones de showcase/galería · 6/6.
- `scripts/cocoa-22-inventory.mjs` → `docs/design/cocoa-22-inventory.json` (220 · 89.029 · 194;
  regla 15 verde). `scripts/cocoa-22-waves.mjs --write` → §6 del plan con 0 pendientes · 0 puntos
  · 0 lotes; `--check` al día. `docs/design/cocoa-22-api.mjs --check` §8 al día (no cambiaron
  primitivas en la integración); `--typecheck-examples` 11 plantillas · tsc 0 errores.

## 8. Verificación visual pendiente (orquestador con navegador · 12 URL)

Sesión de administrador de plataforma (`reception@example.com` / `hotelos-demo`; `/desarrollo/*`
exige `?dev=1`, R24). 1440 / 1024 / 390 px, claro y oscuro, sonda de §5.4 donde se indique.

| # | URL | Qué mirar |
|---|---|---|
| 1 | `/desarrollo/guia-estilo?dev=1` | tabla «Tokens · Color»: cada muestra pinta el valor declarado en claro y oscuro (clase `.c22-guide-swatch`, sin `style` de color); cambiar tema Sistema / Claro / Oscuro; 37 `style={` de muestras; secciones de primitivas sin `.bo-*` |
| 2 | `/desarrollo/migracion?dev=1` (+ `/ficheros`, `/revision`, `/lotes`) | `OnboardingScreens` + `OnboardingInteractive` en Cocoa: 4 acciones de cabecera a 1024 px, tabla clave/valor `CocoaDataPreview` (antes `.dp-*`), badges largos truncados en `/revision`, estados vacío/error, textarea «Contenido pegado (CSV / JSON)» ya no monoespaciado (handoff `mono` en `CocoaInput`) |
| 3 | `/desarrollo/crm-ajustes?dev=1` · `/desarrollo/personal-ajustes?dev=1` · `/desarrollo/informes-programados?dev=1` | `ModuleSettingsPlaceholder` en Cocoa: `CocoaPage` con eyebrow «Desarrollo · Ajustes del módulo», `CocoaState` vacío con acción primaria/secundaria, badge de estado |
| 4 | `/hoy/direccion` | shell sin cromo legacy (sin `TopBar`, sin `.bo-*`): cabecera, sidebar con `SIDEBAR_CSS` en `cocoa-22-shell.css`, drawer/velo a 200 ms; con un centro no alojativo activo, el banner «Centro no alojativo activo» una sola vez; imprimir (⌘P): `button { display: none }` del legacy oculta también los `CocoaButton` |
| 5 | ⌘K, centro de ayuda («?» del shell) y tour de bienvenida desde `/hoy` | superficies `c22-cmdk-*` / `c22-guide-*` portadas: acordeón, filas de recorrido, CTA, botón «×», tema oscuro; un toast (`--cocoa-z-toast` 800) queda ahora por encima del tour/ayuda |
| 6 | `/cumplimiento/verifactu` | `FiscalDashboard` alojado sin `pageHead(embedded)`: cabecera alojada (`HostedHead`), comandos ⌘K registrados, `data-hosted`, panel de error/skeleton de `CocoaPage` |
| 7 | `/cumplimiento/impuestos` · `/cumplimiento/sostenibilidad` | `PropertyTaxesScreen` y `SustainabilityDashboard` idem (6); subtítulo pintado también alojadas (paridad) |
| 8 | `/cumplimiento/registro-viajeros/autoridades` · `/cumplimiento/registro-viajeros/conservacion` | `AuthorityRouting` y `GuestRegisterRetention` idem (6) |
| 9 | `/cumplimiento/registro-viajeros/ses-hospedajes` · `/cumplimiento/envios` | envío en estado «Enviado» (`sent`) tratado como pendiente: el drawer sondea cada 8 s / la lista cada 12 s; «Reintentar» solo en rechazado/fallido/abandonado; `grr_e86d63b9` sin el token en «Nº de documento» |
| 10 | `/configuracion/sistema/api` | referencia del API (tras reiniciar `:3000`): descripciones en español (Tesorería «Deshacer la conciliación…», Onboarding «Analizar el proyecto con IA.», SES «Enviar el lote.»), KPI por método con PUT |
| 11 | `/configuracion/puesta-en-marcha` · `/configuracion/puesta-en-marcha/salida-en-vivo` · `/configuracion/modulos/salud` | readiness y salud de módulos en español sin puente del front (`READINESS_MESSAGE_REWRITES` vacío); pantallas que perdieron la prop `embedded` con el mismo aspecto alojado; `ModuleHealthCenter` con las exigencias «Inventario de habitaciones» etc. (reinicio de `:3000`) |
| 12 | `/finanzas/tesoreria` · `/recepcion/reservas/lista` a 390 px | cuadro de mando sin `DirectorKpiTile` (KPI, sparklines, banner de centro no alojativo); la lista de reservas a 390 px tras retirar la envoltura `[style*="display: flex"]` de `mobile.css`; `RoomBlockGridDialog` y `LiveTimelineWorkspace` con la parrilla a 13 px (reset de tabla `separate`) |

## 9. Puertas §7.3 (conteos literales, integración 2026-09-17)

| Puerta | Resultado |
|---|---|
| `corepack pnpm run typecheck:all` | **15 PASS · 0 FAIL · 0 XFAIL · 1 SKIP** (`apps/guest-web`, explícito) · 19,4 s · exit 0 |
| `corepack pnpm --filter @hotelos/api test` | **1.755 tests · 536 suites · 1.754 pass · 1 skipped · 0 fail** · 4,9 s |
| Unitarios front (`cd apps/api && TSX_TSCONFIG_PATH=../admin-web/tsconfig.json node --import tsx --test $(find ../admin-web/src -path '*/__tests__/*.test.mts')`) | **1.051 tests · 316 suites · 1.051 pass · 0 fail** (brief: 1.017) |
| `node --test tests/*.test.mjs` | **445 tests · 93 suites · 445 pass · 0 fail** (contrato Cocoa 22 18/18; copy 6/6) |
| `node scripts/check-discoverability.mjs` | OK · **220 pantallas alcanzables · 189/189 URL · 24 en whitelist · 0 enlaces rotos** · placeholders **16/20** |
| `node scripts/build-nav-tree.mjs --check` | al día (**67 ítems · 99 pestañas · 205 redirecciones**) |
| `bash .husky/pre-commit` | discoverability OK + `typecheck-all --parallel 2` **15 PASS · 0 FAIL · 1 SKIP** · 19,4 s · «Pre-commit checks passed» |
| `corepack pnpm --filter @hotelos/admin-web build` | vite: **879 módulos · 2,77 s · 0 errores / 0 avisos** · `index--L68ONgB.css` 121,22 kB (gzip 18,12 kB), sin `cocoa-22-legacy-bridge.css` |
| `node scripts/cocoa-22-inventory.mjs` | escrito · **220 pantallas · 89.029 líneas · 194 puntos** · `--summary`: bo-card 0 · bo-* 0 · `<button>` 0 (CocoaButton 1.039) · `<table>` 2 (CocoaTable 288) · inputs crudos 0 · `style={}` 682 · colores literales 0 (0 fallbacks) · cabecera Cocoa 196 · `useTabHost` 87 · S 93 · M 105 · L 19 · XL 3 |
| `node scripts/cocoa-22-waves.mjs --write` / `--check` | §6 escrito · **0 pendientes · 0 puntos · 0 lotes** · `--check`: §6 al día |
| `node docs/design/cocoa-22-api.mjs --check` / `--typecheck-examples` | §8 al día · 40 ficheros · 68 interfaces · 55 type aliases · 645 props · 163 funciones · 29 constantes · 1.894 líneas · 11 plantillas · tsc 0 errores |
| `corepack pnpm --filter @hotelos/compliance test` (nuevo) | **104 tests · 11 suites · 103 pass · 1 skipped** |
| Greps de criterio | `@layer` en `apps/admin-web/src` = 0 · prefijos legacy en `styles/` = 0 · `embed(` / `pageHead(` vivos = 0 · `SubmissionDetailPanel` / `DirectorKpiTile` / `dp-` en `src` = 0 |

Las puertas se pasaron dos veces en la integración: tras las ediciones de §6.2 (todas en verde a
la primera) y, para `pre-commit` y `build`, al final.

## 10. Lo no verificado y pendientes

- Verificación visual §5 de todo lo anterior (§8): ningún lote de la ola, la ronda 1 ni la
  integración tuvieron navegador.
- Servidores sin reiniciar: `/configuracion/sistema/api`, los mensajes de readiness/salud de
  módulos y el `deleted` del correo entrante se ven solo tras reiniciar `:3000`; `:5173` (Vite HMR)
  sirve el árbol actual.
- Handoffs de primitivas que siguen abiertos (plan §8): `CocoaSteps` (R1; `OnboardingScreens`
  pinta los pasos a mano), prop `mono` en `CocoaInput` (textarea CSV/JSON de
  `/desarrollo/migracion/ficheros`), una primitiva de fila (`CocoaListRow`) si el canon exige 0
  `<button>` fuera de `components/cocoa` (los 5 de `HelpCenter.tsx`), control de color (R28).
- `screens/tabs/NavItemTabs.tsx` sigue inyectando `STRIP_CSS` con `<style>` (misma deuda que R11,
  fuera del alcance de la ola); `styles/mobile.css:157-162` conserva el tope de controles sin
  clase como red de seguridad (R14).
- `apps/api/src/modules/onboarding/onboarding.service.ts:383` (`detail` de
  `missing_ses_hospedajes_configuration`) y los nombres sembrados de departamentos/secciones de
  `demo-store.ts` («Reception», «Fourth floor») siguen en inglés: fuera del alcance Cocoa, anotado.
- `layouts/__tests__/shell-cocoa22-contract.test.mts:148` conserva el literal `bo-` dentro de un
  `assert.doesNotMatch` (guarda de regresión): resto esperado del grep de criterio.
- `hotelos/pnpm-lock.yaml` figura modificado desde antes de `efa8c8e` (R25): no es de esta ola y no
  se ha tocado; César decide si va en el commit.

## 11. Árbol de trabajo y pasos para el commit

`git status` (raíz `~/anfitorio-demo`, todo bajo `hotelos/`, lectura del 2026-09-17 tras la
integración): **115 modificados · 40 en estado `D` · 10 nuevos (`??`)**; +2.857 / −12.305 líneas en
154 ficheros trazados sin contar el lockfile ni los nuevos.

- **`D` (40)**: los 39 de §3 (copias en `ola11-removed/`) +
  `apps/admin-web/src/components/forms/__tests__/data-preview.test.mts`, que es un renombrado a
  `components/cocoa-extras/__tests__/data-preview.test.mts` (git lo detecta como `R` al añadir).
- **`??` (10)**: `components/cocoa-extras/CocoaDataPreview.tsx`,
  `components/cocoa-extras/__tests__/`, `components/cocoa/HostedHead.tsx`,
  `screens/compliance/__tests__/`, `styles/cocoa-22-guide.css`, `styles/cocoa-22-shell.css`,
  `apps/api/src/modules/ai-operations/__tests__/`, `apps/api/src/modules/developer/__tests__/`,
  `apps/api/src/modules/integrations/email/__tests__/` y este informe.
- Pasos (orquestador, desde `~/anfitorio-demo`): (1) `git add -A hotelos` — registra los 40 `D`
  como borrados/renombrado y los 10 nuevos; decidir antes si `hotelos/pnpm-lock.yaml` entra (R25:
  si no, `git restore --staged hotelos/pnpm-lock.yaml`); (2) comprobar con
  `git status --porcelain hotelos | grep -c '^D'` = 39 y `grep -c '^R'` = 1; (3) commit
  `feat(cocoa-22/ola-11): cierre de la migración — limpieza legacy, puente L1c, dev y muertas
  (313 → 194 pts)` sin `--no-verify` (el hook repite discoverability + typecheck); (4) reiniciar
  `:3000` y hacer la pasada visual de §8; (5) opcional: borrar `ola11-removed/` del scratchpad una
  vez commiteado.
