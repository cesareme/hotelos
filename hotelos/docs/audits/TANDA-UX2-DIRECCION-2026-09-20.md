# Tanda UX-2 · «Feel» de dirección de ehotelOS — informe de cierre (2026-09-20)

**Encargo:** `docs/design/UX-DIRECCION-FEEL.md` (99 líneas; §1 principios P1-P7 leídos para dirección, §2 seis tareas medidas con baseline [V] y objetivos, §3 fricciones F-D1…F-D12, §4 lotes D0-D9, §5 decisiones D-1…D-8 y riesgos R-1…R-7) sobre el worktree `~/anfitorio-demo-wt-ux2/hotelos` (rama `tanda-ux2`, base `a069906` = main con CHK; BD propia `hotelos_ux2`, copia de Faranda + OPERA; puertos `:3929/:5199`). Plan: recon de solo lectura (`scratchpad/UX-2/recon-delta.md`, `plan.md`) → 10 lotes en 4 olas con ficheros exclusivos (ejecutados como D0-D9) → puerta rápida por ola → revisión v3 (2 revisores: funcional-runtime y seguridad-datos-regresiones) → dedupe → corrector (ronda UX2-REV) → puerta completa → este informe (integrador) → commit en la rama. **Sin migración**: 24 migraciones aplicadas en `hotelos_ux2`, `migrate status` «up to date», drift 0 en todas las puertas; la última carpeta sigue siendo `20260920160000_checkin_pago_en_recepcion`.

**Working tree al cerrar el integrador** (`git -C ~/anfitorio-demo-wt-ux2 status --porcelain -uall -- hotelos`): **40 ficheros modificados (+2.222 / −609)** y **31 nuevos (4.884 líneas)** = 71 ficheros, más `hotelos/pnpm-lock.yaml` `M` (+64/−25, md5 `f47aef23…`) que **NO entra en el commit** (`git reset -q hotelos/pnpm-lock.yaml` antes de commitear; §8): el lock base no cubre `@playwright/test`, `zod`, `@fontsource-variable/inter`, `@hotelos/ai-core` ni `qrcode-terminal` de los `package.json` de `a069906` y todavía lista el importer `apps/ai-gateway` (inexistente); un `pnpm install` ajeno al carril lo regeneró a las 11:01 (§7 R-3). El integrador solo ha ejecutado `git status/diff/log`, lecturas, la puerta rápida (§4) y el guion del pre-commit; no ha arrancado instancias ni escrito en la BD; los únicos ficheros que ha editado son este informe y el bloque UX-2 de `docs/audits/ESTADO-VERIFICADO.md`. Los API `:3000` y Vite `:5173` del orquestador no se han tocado en toda la tanda; toda medida se hizo con instancia propia (`API :3929 RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true RATE_LIMIT_MAX=5000` con `connection_limit=5` + Vite `:5199`, apagadas por PID; `:3929/:5199` sin escuchas al cerrar).

**Datos:** todo sobre el tenant aislado `UXDAY` de UX-1 ampliado con el **hotel B** (`org_uxday` / `prop_uxday_b` «Hotel UXDAY B (prueba)», `packages/database/prisma/seed-ux-direccion.ts`, usuarios `*@uxday.test` con la contraseña de demo por defecto del seed); `prop_uxday` solo se lee (comparativa de la cartera); Faranda solo lectura (una `SELECT` sobre `business_dates`); ningún nombre de persona real aparece en el código, los tests, los JSON, las capturas, los logs ni este informe.

## 0. Resumen

- **Entregado:** una verdad por dato con su ventana (`GET /dashboards/general-manager`, `portfolio` y `property-overview` resuelven «hoy» con la **fecha de negocio de cada propiedad**, campos aditivos `businessDate` / `businessDateSource`), Mi día › Dirección en español con «Riesgos de hoy» y subtítulo con la ventana, tarjeta «Pendientes · N aprobaciones · M de la IA», una primaria «Aprobar» por fila con diálogo nominal (dinero) y commit diferido + `CocoaUndoBar` 8 s en la cola de la IA, Cartera «Tabla · Comparar» con delta vs media y «PyG del hotel» desde la ficha, Centro de informes con rango real, formato honesto y catálogo en español, «Cerrar día» solo con `night_audit.run` y callout «Marcar como revisado» (⌥V) del último cierre pendiente, tarjetas de instrucciones y guía de persona honestas, seed `db:seed:ux-direccion`, medida automatizada `d1…d6`, dos contratos raíz nuevos, runbook del moderador, sección UX-2 en `docs/api-contracts.md`.
- **Medida (§3):** ninguna tarea peor que la baseline; d2 4 → 3 clics, d6 4 → 3, d1 0, d3 2 (teclado 0 clics), d4 2 (2 hoteles con delta), d5 2 (teclado 0 clics); las seis completadas en estricto desde el árbol (8/8 specs, `measure-correccion-2026-09-20.json`).
- **Revisión v3 (§5):** 9 hallazgos confirmados (1 high · 8 medium), 0 refutados; el corrector cerró 8 de los 9 y 10 low más; 1 high (lock) y 4 low quedan sin cambio de código con motivo y dueño.
- **Puertas (§4):** completa final **13/14** (`scratchpad/UX-2/gates-final.json`) y rápida del integrador (§4.3); la única roja (`nav-tree --check`) es el CSV compartido `pilots/tanda5-nav-tree.csv` con filas de los carriles RRHH/ACT sin pantalla aquí: regenerar `nav-tree.generated.json` en la fusión.
- **Decisiones para César (§6):** las 12 con la opción por defecto ya aplicada; las dos que cambian código o RBAC fuera del carril (`night_audit.review` para `general_manager`, lock de Playwright) siguen abiertas.

## 1. Qué construyó cada lote (qué · cómo · tests · verificación)

Los lotes se citan con la numeración con la que se ejecutaron (D0-D9); entre paréntesis, la del diseño §4 cuando difiere (los comentarios de cabecera de las pantallas usan una u otra). Olas: D0 → D1 ∥ D2 → D3 → D4 ∥ D5 ∥ D6 ∥ D7 ∥ D8 → D9 → ronda de corrección UX2-REV → integrador. Cifras de cada lote tal como las dejó en su informe y en `scratchpad/UX-2*/`.

### D0 · Diseño compacto
- **Qué:** `docs/design/UX-DIRECCION-FEEL.md` (99 líneas ≤ 200): §1 P1-P7 con su lectura para dirección (una verdad por dato con su ventana, una acción por fila, ⌘K como camino de teclado, diálogo nominal solo con dinero o sin reapertura), §2 tabla d1-d6 con baseline medida y objetivos (d1 0 clics; d2-d6 ≤ 3; ≥ 2 tareas solo teclado), §3 F-D1…F-D12 ancladas a fichero:línea, §4 lotes con ficheros exclusivos, tests y puerta, §5 8 decisiones + 7 riesgos.
- **Cómo:** recon de solo lectura en runtime con instancia propia como `direccion@uxday.test` (d1 0 clics pero tres cifras distintas del mismo día; d2 4 clics no completable con 0 pendientes; d3 2; d4 no completable con 1 propiedad; d5 2 con rango fijo mayo-2026 y «PDF» = `.html`; d6 4 y «Cerrar día» pintado sin clave); anclajes re-verificados por lectura sobre el worktree; numeración alineada con los ficheros que ya estaban en vuelo (D1/D2). Sin ⌥ globales (regenerarían `content/help-articles`, de DOC); panel de costes de RRHH fuera (sin ruta ni pantalla en la base).
- **Tests:** ninguno propio; scan de prosa contra `FORBIDDEN_ENGLISH` / `TILDE_REQUIRED` / `OLD_BRAND` / `MISSPELLING` de los contratos: 0 hits; marca «ehotelOS» ×1; 0 nombres de personas.
- **Verificación:** `node --test tests/*.test.mjs` 765 (763 pass · 2 skip) ×3; puerta rápida base **12/12** (`scratchpad/UX-2/gates-D0-quick.json`): typecheck 15 PASS · api 3.569 (3.568 pass) · admin-web 2.014 (2.013) · contratos 765 (763) · nav-tree 70 ítems / 104 pestañas · route-access 15 × 197 · migrate 24 + drift 0. Corregido después por la revisión (UX2-REV-01/03/04/13 y REV-10): puerta D3 rebajada a lo entregado, D-1 (solo `manager` revisa), P4 «≥ 1 acción con deshacer», recortes declarados, marcas `[V-COR]`.

### D1 · Seed del hotel B
- **Qué:** `packages/database/prisma/seed-ux-direccion.ts` (895 líneas; script `db:seed:ux-direccion`, `--dry-run`, `--reset`): `prop_uxday_b` (20 Dobles 101-120, BAR con 68 `rate_days`, política FLEX24, fecha de negocio = ayer), 17 reservas `UXDB-*` (12 alojadas que llegaron ayer y salen hoy+2 · 2 salidas · 3 llegadas), folios/líneas/pagos, cierre del día de anteayer completado por recepción y pendiente de revisión (`nar_uxdb_anteayer`, 9 pasos), 3 `approval_requests` pendientes (reembolso 60 € · ajuste de folio 25 € · pedido 900 €, `reasonCode seed-ux2`), 3 `ai_human_review_items` y 20 `revenue_daily_snapshots` de nivel superior (los dos últimos desde la corrección UX2-REV-04), `director@uxday.test` (`manager` en A y B) y `direccion@uxday.test` en B; allowlist `UX2_DEMO_PROPERTY_IDS = ["prop_uxday_b"]` concatenada a `DEMO_PROPERTY_IDS` en `prisma/lib/demo-guard.ts` (el literal de UX-1/CHK no cambia); `packages/database/package.json` +1 script.
- **Cómo:** autónomo (no importa `seed.ts` ni `seed-ux-day.ts`, que ejecutan `main()` al importarse), tipos del cierre por `import type` de `packages/shared/src/pos-types.ts`, precondición de solo lectura (`org_uxday`, `le_uxday`, `prop_uxday`, usuarios de UX-1: si falta, error y nada escrito), `assertDemoTarget` con plan impreso, guarda R9 de producción, contraseña de `SEED_UXDAY_PASSWORD` nunca impresa (UX2-REV-07 low); todos los `deleteMany` por `deleteScoped` con `propertyId = prop_uxday_b`; «ensure» idempotente (no rebobina la fecha de negocio), `--reset` la devuelve a ayer; exige el API parado (cadena de auditoría in-memory).
- **Tests:** `tests/seed-ux-direccion-contract.test.mjs` (181 líneas, **9/9**: prefijos UXDAY, sin apellidos, único `deleteMany` = `deleteScoped(prop_uxday_b)`, `--reset` nunca toca `prop_uxday`, script y allowlist, snapshots y tercer ítem IA, contraseña prohibida en la salida); contratos vecinos de la allowlist (`seed-ux-day`, `seed-checkin`, `demo-seed`, `rbac-demo-seed`) 56/56; `apps/api/src/scripts/__tests__/refresh-demo-dataset.test.mts` con `prop_uxday_b` y `UX2_DEMO_PROPERTY_IDS` (+5/−3, cerrado en la fusión de la ola); `scripts/env-contract.json` regenerado (+5/−2: `seed-ux-direccion.ts` lector de `NODE_ENV`, `SEED_UXDAY_PASSWORD`, `SEED_UXDAY_ALLOW_PRODUCTION`).
- **Verificación:** `--dry-run` exit 0; ensure → 17 reservas nuevas, cierre creado, 3/3 aprobaciones, ítems IA, 3 asignaciones; `--reset` ×2 borra y repone lo mismo (reservation 17 · nightAuditRun 1 · approvalRequest 3 · aiHumanReviewItem 2→3 · roomsRestored 20 · fecha de negocio → ayer); ensure posterior 0 nuevas / 17 existentes (huéspedes `guest_uxdb_*` = 17 sin acumular); SQL de lectura: `prop_uxday` intacto (75 reservas), 0 filas `seed-ux2` fuera de `org_uxday`; API: `GET /approvals` (x-property-id B) → 3 pendientes, `/ai-operations/review/queue` → ítems B, `/dashboards/portfolio` → `propertiesCount 2`, `/night-audit/runs` → 1 completed, `/night-audit/business-date` → ayer. Puerta del lote 10/12 (los dos rojos —censo env y allowlist del test de `refresh-demo-dataset`— eran los ficheros fuera de sus exclusivos, ya corregidos).

### D2 · Medida «antes»
- **Qué:** `apps/admin-web/e2e/_helpers-direccion.ts` (83 líneas: `loginAsDirector(page, request, { propertyId = "prop_uxday_b" })` sobre `loginAsUxDay` con propiedad activa por `localStorage`, `waitForDireccion`, `headers` Bearer + `x-property-id`; desde UX2-REV-13 sin overrides de entorno y con la guarda `assertUxDayProperty`, solo `prop_uxday*`), `e2e/measure/_measure-direccion.ts` (299 líneas: `TaskId` d1…d6, `TARGETS` d1 0/20 · d2 3/16 · d3 3/14 · d4 3/14 · d5 3/16 · d6 3/14, `countedClick/Press/Fill`, `performance.mark`, `finishVariant` → `tasks.<t>.variants.<n>`, `results-direccion.json` fusionado por tarea con escritura atómica), 6 specs `d1-revisar-dia` … `d6-revisar-cierre` (37/68/98/54/92/61 líneas, títulos «dN · …»), `.gitignore` +2 (`results-direccion.json`), baseline copiada a `docs/audits/ux-direccion/measure-baseline-2026-09-20.json`. `_measure.ts` de UX-1 intacto (lo comparte UX-3).
- **Cómo:** patrón de `_measure.ts`; caminos del recon (`goto /` → `/hoy/direccion`; tarjeta → fila → Aprobar → diálogo; Cartera → fila; Cartera → cabecera Ocupación; Centro de informes → «Generar exportación»; Cierre → fila → «Marcar como revisado» → diálogo); d2/d4/d6 acaban `completed=false` con nota si faltan pendientes / segundo hotel / run. Navegadores: el `@playwright/test` 1.63.0 del worktree exige `chromium_headless_shell-1243` y la caché solo tiene la 1228 → `PLAYWRIGHT_BROWSERS_PATH=<scratchpad>/pw-browsers` con enlaces 1243 → 1228 (nada descargado).
- **Tests:** `tests/ux-direccion-contract.test.mjs` §5 (desde la corrección: 6 specs con `createMeasure(page, "dN")`, títulos sin PII, `.gitignore`, helper sin overrides); typecheck de los ficheros e2e con un tsconfig externo (el del paquete solo incluye `src/`).
- **Verificación:** baseline grabada 09:41-09:44Z **antes de tocar pantallas**, `MEASURE_STRICT=0`, 8/8 specs: d1 0 clics · 0 teclas · 8 pet. · 1.261 ms; d2 4 · 0 · 3 · 575; d3 2 · 0 · 3 · 1.775 (teclado 0 · 40 · 3 · 1.894); d4 2 · 0 · 1 · 951; d5 2 · 0 · 4 · 956 (teclado 0 · 41 · 4 · 988); d6 4 · 0 · 5 · 1.109 — las cifras de §2 del diseño; escrituras solo en `prop_uxday_b`.

### D3 · Servicio del panel y pantalla del director (diseño D3 + D4; d1)
- **Qué:** API `apps/api/src/modules/dashboards/general-manager.service.ts` (+93/−11): `resolveGmWindow` (pura) + `readGmBusinessDate(propertyId, client)` (mejor esfuerzo, `console.warn` como el preflight), ventana «hoy» = [medianoche UTC de `business_dates."current_date"`, +1 d), sin fila → día UTC (`utc_day`), mes y año cuelgan de `today`, llegadas = `confirmed` con llegada en la ventana y salidas = `checked_in` con salida en la ventana (regla del preflight), respuesta aditiva `businessDate` + `businessDateSource`, `asOf = businessDate`. Front `apps/admin-web/src/screens/operations/GeneralManagerScreen.tsx` (+338/−100): rótulos en español (Llegadas · Salidas · Bloqueadas · Contribución neta · Ritmo 30 días · Captación 7 días · Pisos · Personal · Seguridad · TPV · Picos de demanda 14 días · «frente al año anterior»), subtítulo «Datos de la fecha de negocio DD/MM · actualizado HH:MM» (o «día UTC … sin fecha de negocio»), delta solo con base > 0 («vs hace 7 días», nunca «▲100 % vs LY»), bloque **«Riesgos de hoy»** (`buildRiskRows` puro + `RisksList` con `CocoaBadge` + `CocoaButton`: bloqueos del preflight → Revisar el cierre; pendientes de aprobación → Ir a aprobaciones; pendientes de la IA → Revisar la cola; riesgo de cancelación → Revisar reservas; anomalías), «Caja de hoy» en el hueco de la sección de anomalías retirada, comandos ⌘K `general-manager-pendientes / -cierre / -cartera / -exportar` además de `-refresh`, `density="comfortable"`, `style=` 10 → 6.
- **Cómo:** preflight con `useApiData` 60 s; aprobaciones por `listPendingApprovals` + `pendingForViewer` (criterio FX-09 de la tarjeta); IA por `/ai-operations/review/stats` solo con `ai_governance.read`; skeleton pinado por T8 intacto (spans 5/4/3); `buildGmDashboard` acepta `businessDateClient` inyectable; `buildGmPace` sigue en día UTC (fuera del brief).
- **Tests:** `apps/api/src/modules/dashboards/__tests__/general-manager-business-date.test.mts` (134 líneas; stub de Prisma: fila → `business_date`, sin fila → `utc_day`, lectura fallida → `undefined` + warn; contrato de fuente: cableado y regla del preflight compartida por regex sobre `night-audit-preflight.service.ts`) — con `general-manager-reputation` 25/25; `apps/admin-web/src/screens/operations/__tests__/general-manager-feel.test.mts` (204 líneas, 15/15: 17 literales ingleses prohibidos, 15 rótulos exigidos, ventana, deltas, bloque y fuentes, comandos, densidad, techo `style=` 6, skeleton).
- **Verificación:** instancia propia `:3931/:5201` (los puertos del carril los ocupaba otro lote): `GET /dashboards/general-manager` → `businessDate 2026-09-19`, `businessDateSource business_date`, ocupación 70,0 % (14/20), llegadas 0, salidas 0 = preflight (`arrivals_pending 0`, `departures_not_checked_out 0`, 1 bloqueo «folios abiertos con saldo»); antes, con el código viejo (día UTC 20/09): 75 %, ADR 13,53, llegadas 3, salidas 2 —la «segunda verdad» que desaparece—. Chromium (`scratchpad/UX-2/d3-direccion.png`): subtítulo, tira de 11 KPIs, «Riesgos de hoy» con 5 filas y botón; ⌘K «cierre» ofrece «Revisar el cierre del día»; 13 GET al aterrizar, 0 errores de consola. Measure d1 estricta: 0 clics · 12 pet. · completada.

### D4 · Aprobaciones y pendientes de la IA (diseño D5; d2)
- **Qué:** `screens/approvals/approvals-helpers.ts` (+88: `primaryDecisionFor` → `approve | reject | null` si no pending, caducada, propia, decidida, sin clave en el hotel de la solicitud o segunda firma ya dada; `approvalKindNoun`, `decisionDialogTitle` nominal con importe, `approvalReference` = 6 últimos caracteres del id, `decisionToast`, `pendingCardParts/Label/Total`), `ApprovalsScreen.tsx` (+116/−33): **«Aprobar» `filled small` por fila** (⌥A en la seleccionada) + «Rechazar» `bordered`, ↑↓ mueven selección e Intro abre el cajón, diálogo con `title = confirmLabel` («Aprobar reembolso de 60,00 €», Intro confirma, sin optimismo), toast «Aprobada: reembolso 60,00 € · solicitud K3M9Q2», ⌘K «Aprobar / Rechazar la solicitud seleccionada»; `screens/aiOperations/AiHumanReviewQueueScreen.tsx` (+115/−52): «Aprobar» `tinted` + «Rechazar» `bordered` (motivo en el cajón), «Asignar a mí» / «Escalar» al pie del cajón, `actionToast(key, item)` con el tipo, ⌘K «Aprobar la propuesta seleccionada» (desde la corrección: commit diferido, §5); `screens/tabs/hoy/MiDiaTabs.tsx` (+54/−24): `PendingCard` «Pendientes · N aprobaciones · M de la IA» (+ botón «IA», badge warning si N+M > 0, `useApiData("/ai-operations/review/stats")` solo con `ai_governance.read`), comentario de aterrizaje corregido (dirección → `/hoy/direccion`).
- **Cómo:** estado `detailOpen` separado de `selectedId`; diálogo con `submitOnEnter={!noteRequired}`; sin migración, rutas ni permisos nuevos; ficheros compartidos con UX-3 (`MiDiaTabs.tsx`) tocados con el mínimo (§7).
- **Tests:** `screens/approvals/__tests__/approvals-primary-action.test.mts` (189 líneas, 14), `screens/aiOperations/__tests__/ai-review-row-actions.test.mts` (reescrito en la corrección, 5), `screens/tabs/__tests__/mi-dia-pending-card.test.mts` (48 líneas, 4); con las suites hermanas 64/64.
- **Verificación:** instancia propia `:3929/:5199`, sesión de `director@uxday.test` sobre `prop_uxday_b`; log del API 0 × 403, 0 × 5xx. d2 = 3 clics (tarjeta → «Aprobar» de la fila → confirmar; `POST /approvals/apr_uxdb_refund/approve` 200, `decidedByUserId` = el director); teclado: ↓/↑ mueven `aria-selected`, Intro abre el cajón, ⌥A (keydown `Alt+KeyA` real) abre el diálogo nominal; IA: «Aprobar» en la fila → 200 → toast; el cajón conserva Asignar · Escalar · Rechazar · Aprobar. A 1.224 px la bandeja desbordaba (cerrado en UX2-REV-08).

### D5 · Cartera y detalle del hotel (diseño D6; d3, d4)
- **Qué:** `screens/operations/portfolio-compare.ts` (160 líneas, puro: `SORT_KEYS`, `TABLE_DEFAULT_SORT`, `COMPARE_DEFAULT_SORT`, `COMPARE_METRICS`, `sortKeyOf`, `nextSort`, `compareRows`, `sortRows`, `averageOf`, `deltaVsAverage` sobre la media redondeada, `compareView`, `canCompare`, `deltaTone`, `deltaLabel` «+3,2 pp» / «−10,62 €» / «en la media»), `PortfolioDashboard.tsx` (+157/−103): pestañas **«Tabla · Comparar»** (`tabs` de `CocoaPage`), comparativa = misma `CocoaTable` con columna «<métrica> vs media» (ocupación / ADR / RevPAR / ingresos del mes, `CocoaBadge` por signo, `uppercase={false}` desde UX2-REV-11), pie «Media simple de la cartera», un solo hotel → «Solo hay un hotel: la comparación aparece con dos o más», KPI «En casa» → «En el hotel», tira con caption «fecha de negocio de cada hotel» (UX2-REV-01), ⌘K «Comparar hoteles» · «Ordenar por ocupación» · «Ordenar por ingresos»; `PropertyDetailScreen.tsx` (+75/−9): **«PyG del hotel»** (`filled`, `urlForScreen("ProfitAndLossScreen")` + `withFinanceScopeParam(?ambito=<propiedad>)`), «Cierre del día» y «Exportar informe» solo para el hotel activo (desde otro: `disabled` + «Cambia a este hotel para verlo»), «Volver a la cartera», KPIs con caption «fecha de negocio DD/MM/AAAA» (UX2-REV-01), ⌘K «PyG del hotel» · «Abrir el cierre del día» · «Exportar un informe del hotel» · «Volver a la cartera de propiedades».
- **Cómo:** R-2 del diseño resuelto por lectura y runtime (`services/financeScope.ts:585-647`: la URL gana al almacenamiento en cuanto se conoce la estructura); `NightAuditScreen.tsx:249` y `ReportingCenterScreen.tsx:43` solo leen la propiedad activa ⇒ acciones deshabilitadas fuera de ella; 0 `style=` nuevos (2 → 2 en ambas pantallas).
- **Tests:** `screens/operations/__tests__/portfolio-compare.test.mts` (233 líneas, 22 = 20 + 2 de la corrección: media, deltas que suman 0, tono, etiqueta, orden inicial por vista, `sortRows` estable, un solo hotel; contratos de fuente: pestañas, 3 comandos, caption, `style=` ≤ 2, PyG con `?ambito=`, 2 botones `disabled={!isActive}`, una sola `filled`, captions de ventana).
- **Verificación:** specs D2 contra la instancia propia: d3 2 clics · 3 pet.; d3 teclado 0 · 41; d4 2 · 1 con 2 hoteles; script propio `scratchpad/UX-2-D6/verify-d6.mjs` (capturas 01-06): Cartera → «Comparar» → cabecera «Ocupación» = 3 clics · 1 petición, filas `prop_uxday` (+44,5 pp · +5,85 € · +45,46 € · +10.603,50 €, success) y `prop_uxday_b` (−44,5 pp …, danger), pie con la media simple; ⌘K «Comparar hoteles» y «Ordenar por ingresos» actúan; Intro en la fila abre `/informes/cartera/prop_uxday_b`; Cartera → fila → «PyG del hotel» = 3 clics · 20 peticiones → `/finanzas/estados-contables/perdidas-y-ganancias?ambito=prop_uxday_b` con Ámbito «Centro · Hotel UXDAY B (prueba) (UXDB)»; desde el detalle del hotel no activo los dos botones `disabled` con `title`. Deuda ajena detectada: primer `GET` de PyG con ámbito sociedad → 404 antes de aplicar `?ambito=` (§7 R-7).

### D6 · Centro de informes y catálogo (diseño D7 + parte de D3; d5)
- **Qué:** API `apps/api/src/modules/reporting/reporting.service.ts` (+61/−32): títulos del catálogo en español («Reservas: llegadas y salidas», «Facturación, facturas y cobros», «Revenue: histórico y previsión»), `reportCatalogEntries()` (array fresco por llamada, testeable sin BD), `buildReportFilename` exportada = `informe-<tipo>-<propiedad>-<desde>_<hasta>.<ext>` cuando `query.fromDate/toDate` son válidos (si no, sello del día) con la extensión real (`pdf → html`, `xlsx → csv`). Front `screens/reports/reporting-center-rows.ts` (+146: `rangeForPreset`, `RANGE_PRESET_OPTIONS`, `REPORT_TYPE_LABELS`, `FORMAT_OPTIONS` honestos, `formatLabel`, `catalogRowTitle/Detail`, `exportFormLabel`, `exportReadyDetail`, `isExportEnter`) y `ReportingCenterScreen.tsx` (+153/−73): **rango real** (`CocoaSelect` «Periodo»: Este mes —por defecto, hasta hoy— · Mes anterior · Últimos 30 días · Personalizado + dos `CocoaDatePicker` con min/max cruzados; editar una fecha pasa a Personalizado) en vez de `2026-05-01/2026-05-31`, formatos «PDF (HTML imprimible) · CSV · XLSX (se entrega CSV) · JSON», `<form onSubmit onKeyDown>` con Intro y `accessKey="E"` en «Generar exportación» (`type=submit`, `aria-keyshortcuts Alt+E`), resultado en un solo mensaje «Exportación lista: <fichero> · disponible hasta HH:MM» (callout `role=status` + toast sin anuncio), ⌘K «Generar exportación de informe» (conservado para la spec) + «Exportar informe de reservas / de facturación».
- **Cómo:** «hoy» = `isoDate(new Date())` (día natural Madrid), no la fecha de negocio del API (decisión declarada frente a D-2: evita una petición y una dependencia de `posApi`; §6); `handleExport` ignora reentradas; `style=` 3 → 3. `RevenueExportCenter.tsx` no se tocó (recorte declarado, §1 D9).
- **Tests:** `screens/reports/__tests__/reporting-center-rows.test.mts` (+140, 17/17: 4 presets con fin de mes 28/29/30/31 y cambio de año, últimos 30 días, custom, hoy malformado; `isExportEnter`; etiquetas honestas; títulos por prefijo y fallback; contrato de fuente: 0 fechas literales, POST con `range`, `<form>`, `accessKey="E"`, 4 comandos, `style=` 3); `apps/api/src/modules/reporting/__tests__/report-catalog-es.test.mts` (65 líneas, 4 nuevos; con `report-export-download` 7/7: 0 inglés, códigos/permisos/formatos intactos, catálogo inmutable, nombre de fichero con y sin rango); la integración `reports-export-download` sigue cumpliendo `/^informe-reservation-.+\.csv$/`.
- **Verificación:** instancia propia temporal `:3969/:5219`: `GET /reports/properties/prop_uxday_b/catalog` en español; `POST …/export {reservation, csv, 2026-09-01..2026-09-20}` → `informe-reservation-prop_uxday_b-2026-09-01_2026-09-20.csv`, `text/csv`, 18 líneas, `expiresAt` +15 min; `{billing, pdf, agosto}` → `…-2026-08-01_2026-08-31.html`. Navegador: form «Exportar informe de reservas (este mes)» Desde 2026-09-01 · Hasta 2026-09-20, «Mes anterior» → agosto, Intro en «Desde» → 1 POST, ⌥E → POST, ⌘K «Exportar informe de facturación» → POST `billing` y el select cambia; 1 sola región `role=status`; 0 errores de consola. Measure d5 estricta: ratón 2 · 0 · 4 · 974 ms; teclado 0 · 41 · 4 · 972 ms.

### D7 · Cierre del día (diseño D8; d6)
- **Qué:** `screens/operations/night-audit-report.ts` (+58: `closeActionFor(preflight, can)` → `run | force | blocked | review-only` con `canForceClose` como única lectura de `night_audit.run`; `CLOSE_BANNER_TITLES`, `CLOSE_REVIEW_ONLY_TEXT`, `OPEN_QUEUE_LABEL`; `latestRunToReview(runs, can)`; `reviewCalloutTitle` «Cierre del DD/MM/AAAA hecho a las HH:MM» / «Día del … reabierto, pendiente de revisión»; `reviewedToast`), `NightAuditScreen.tsx` (+85/−27): banner honesto por clave (`run` → «Cerrar día» `filled`; `force` → «Cerrar de todos modos»; `blocked` sin clave → «Abrir cola operativa» `tinted small` → `FrontDeskDashboard`; `review-only` → «Comprobaciones en verde» sin botón + texto del brief), placeholder «Cerrar día» deshabilitado eliminado, **callout del último cierre pendiente de revisión** con UNA primaria «Marcar como revisado» (`accessKey V`) + «Ver informe» `plain`, encima de «Comprobaciones previas al cierre» desde UX2-REV-09, `reviewTarget` sustituye a `reviewOpen` (el diálogo actúa sobre el run del callout o del cajón), diálogo existente «Marcar el cierre como revisado» con `submitOnEnter` y sin `initialFocus`, toast «Cierre del DD/MM/AAAA marcado como revisado», errores 409 del API en el cajón o como toast, ⌘K «Marcar el cierre como revisado» (⌥V) y «Abrir cola operativa» (solo bloqueado), una sola región viva propia.
- **Cómo:** ⌥V en vez de R (R reservada a la navegación global: `RESERVED_ACCESS_LETTERS`, `CocoaAccessKey.tsx:197`; el diseño §1 P2 fija V); título del banner review-only «Comprobaciones en verde» (no «Puedes cerrar el día», falso sin clave); 0 `style=` nuevos (4 → 4).
- **Tests:** `screens/operations/__tests__/night-audit-report.test.mts` (+153, 21/21: 7 previos + `closeActionFor` 4 + `latestRunToReview`/copy 4 + contrato estático 6: un solo «Cerrar día» y solo en la rama `run`, sin placeholder, review-only sin botón, blocked con «Abrir cola operativa», callout con 1 `filled` `accessKey V` antes de las comprobaciones, diálogo con `submitOnEnter`, comandos, `style=` ≤ 4, `role="status"` = 1); contratos admin-web que citan la pantalla 100/100.
- **Verificación:** instancia propia `:3929/:5199`, script `scratchpad/UX-2/d8-verify.mjs` (capturas `d8-*-0{1,2}-*.png`): `director@uxday.test` (`manager`, review + reopen, sin `run`): d6 ratón = 3 clics (menú → «Marcar como revisado» → confirmar), 6 peticiones (≤ 14), 0 × 403, 0 × 5xx, banner «No puedes cerrar todavía | Abrir cola operativa» y 0 botones «Cerrar día», `POST …/runs/nar_uxdb_anteayer/review` 200, `reviewedByUserId` = el director, callout desaparece; teclado ⌘K → Intro → Intro = 200 y ⌥V → Intro = 200; rama review-only (preflight en verde servido desde el navegador): 0 botones y texto exacto; ramas `run`/`force` con la clave inyectada solo en la sesión front (sin pulsar cerrar): «Cerrar día» ×1 / «Cerrar de todos modos»; `recepcion@uxday.test` en `prop_uxday`: banner bloqueado sin botón de cierre, 0 × 403. Tenant devuelto al estado del seed (un `UPDATE` acotado por id y propiedad para rearmar la revisión).

### D8 · Contenido, persona y panel de operaciones
- **Qué:** `content/screen-instructions/direccion.ts` (91 líneas: cuatro tarjetas forma B —Mi día › Dirección, Pendientes de aprobación, Cartera, Centro de informes— con `persistKey direccion-*`, `DIRECCION_ACCESS_KEYS {aprobar: A, exportar: E}`, atajos solo vía `shortcutKeys(...)`), montadas con `<CocoaScreenInstructionsCard … dismissible persistKey>` al final de `GeneralManagerScreen`, `ApprovalsScreen`, `PortfolioDashboard` y `ReportingCenterScreen`; `content/persona-guides/manager.ts` (+29/−17: `dailyFlow` = las seis tareas con sus comandos ⌘K y ⌥A/⌥E; «El cierre lo ejecuta recepción o la auditoría nocturna, no dirección»; desde UX2-REV-03 dice que solo la dirección de hotel revisa); `screens/operations/OperationsDirectorScreen.tsx` (+56/−16): `mobileDrillDown(tier, coarse)` (solo teléfono táctil → «Mi turno» / «Mis averías»; tablet y puntero fino → tableros), `DRILL_DOWN_SCREENS`, ⌘K «Ver alertas» · «Ir a pisos» · «Ir a mantenimiento», 4 `CocoaTable` de `compact` a `comfortable` (P5).
- **Cómo:** las tarjetas solo citan acciones y comandos cableados tras D3-D7 (contrato que las cruza con los literales de las pantallas); sin promesas ni tiempos; 0 glifos ⌘/⌥ escritos a mano; 0 `style=` nuevos.
- **Tests:** `content/__tests__/direccion-instructions.test.mts` (218 líneas; cada acción citada existe en su pantalla o en `approvals-helpers` / `portfolio-compare` / `reporting-center-rows` / `content/actions.ts` / `PropertyDetailScreen` como sub-URL de la cartera, cada comando en su bloque `commands`, `accessKey` A/E cableadas y no reservadas, montaje único y `persistKey` únicas; 24 verdes tras la corrección) y `screens/operations/__tests__/operations-director-feel.test.mts` (104 líneas: tabla de verdad de `mobileDrillDown`, destinos, comandos, 4 tablas `comfortable` / 0 `compact`, `style=` ≤ 3); `tests/ayuda-in-app-contract.test.mjs` 126/126 con `direccion.ts`; suites de los ficheros tocados 91/91.
- **Verificación:** instancia propia `:3935/:5205`, sesión del director sobre `prop_uxday_b`: `/hoy/direccion` pinta «Mi día en dirección» (region + h3, 4 pasos, «Cerrar instrucciones»; al descartar `localStorage cocoa-screen-instructions:direccion-panel = "1"` y tras recargar sigue oculta), `/hoy/pendientes`, `/informes/cartera` y `/informes` con su tarjeta; `/hoy/operaciones`: ⌘K lista los 4 comandos, «Ver alertas» selecciona la pestaña, «Ir a pisos» → `/operaciones/pisos`, tarjeta «Mantenimiento» → `/operaciones/mantenimiento`; emulación 375 × 812 `pointer: coarse`: «Housekeeping» → `/operaciones/pisos/mi-turno`.

### D9 · Cierre de tanda (medida final, informe, kit del moderador)
- **Qué:** medida final estricta; primera versión de este informe; `docs/audits/ux-direccion/measure-final-2026-09-20.json`; bloque UX-2 en `docs/audits/ESTADO-VERIFICADO.md`; `docs/api-contracts.md` sección «Dirección · «feel» de dirección (Tanda UX-2)» (+34: `businessDate` / `businessDateSource` y ventana de la fecha de negocio en los tres dashboards, catálogo en español, `POST …/export` con `fromDate/toDate` → nombre con rango); `docs/runbooks/ux-direccion-pruebas.md` (148 líneas, patrón de `ux-recepcion-pruebas.md`: preparación con seeds en orden y credenciales, material, guion de 45 min con las seis tareas sobre UXDAY B —escenario, éxito, camino medido—, SEQ/UMUX-Lite, cierre de sesión, hoja de registro sin nombres, medida automatizada con `--grep` sin `--`).
- **Cómo:** `db:seed:ux-direccion -- --reset` ×3 con el API parado (+ `db:seed:ux-day -- --reset` antes de la pasada completa); API `:3929` arrancado DESPUÉS de los lotes; navegadores por los enlaces del scratchpad. d2 y d6 fallaban en estricto porque sus specs (lote D2) codificaban el camino anterior: se midieron con copias en el scratchpad y se especificó el cambio de localizadores (aplicado por el corrector, UX2-REV-06).
- **Tests:** contratos que leen sus ficheros (`brand-contract`, `manual-contract`, `seed-ux-direccion-contract`, `seed-ux-day-contract`, `cocoa-22-contract`, `env-contract`) verdes.
- **Verificación:** pasada oficial `e2e:measure --grep "d[1-6] ·"` 6/8 (d1 0 clics · 11 pet. · 1.203 ms; d3 2 · 3 · 1.779 y teclado 0 · 41; d4 2 · 1 · 985; d5 2 · 4 · 977 y teclado 0 · 41) + copias d2/d6 2/2 (3 · 3 · 1.040 ms y 3 · 3 · 897 ms); pasada completa d + t 11/14 (t1 15 pet. > 12 por el cajón de check-in de CHK, no por UX-2: `GET /reservations/:id/check-in` 404, `GET /guests/:id/timeline`, `POST …/ses/submissions` 409, llamadores sin modificar; t2-t6 con clics idénticos a UX-1); API de dirección 0 × 403, 0 × 500, 0 × 429. Puerta completa 10/14 en la pasada (rbac, migrate e integración tumbadas por «too many clients» del Postgres compartido a 80-100/100) y verdes repetidas solas (§4.1). **No hecho por D9** y cerrado después: `tests/ux-direccion-contract.test.mjs` y las specs d2/d6 (corrector); bloque en `CLAUDE.md` (corrector; ver §7). Verificación en tablet emulada no realizada (UX-2 no toca densidades ni objetivos táctiles).

### COR · Ronda de corrección UX2-REV (tras la revisión v3)
- **Qué:** 8 de los 9 hallazgos confirmados + 10 low corregidos en el árbol con test por corrección (§5); ficheros: `portfolio.service.ts`, `property-overview.service.ts`, `general-manager.service.ts` (cabecera y marca), `ai-review-deferred.ts` (nuevo, 80 líneas), `AiHumanReviewQueueScreen.tsx`, `ai-review-labels.ts`, `GeneralManagerScreen.tsx`, `NightAuditScreen.tsx`, `PortfolioDashboard.tsx`, `PropertyDetailScreen.tsx`, `ApprovalsScreen.tsx`, `components/cocoa/CocoaState.tsx` (`role="none"`) y `CocoaUndoBar.tsx` (pausa por hover reseteada al cambiar de entrada), los cuatro `components/cocoa-director/*`, `persona-guides/manager.ts`, `screen-instructions/direccion.ts`, `e2e/_helpers-direccion.ts`, specs d2/d6, `seed-ux-direccion.ts` y `seed-ux-day.ts`, `tests/ux-direccion-contract.test.mjs` (nuevo, 186 líneas, 72 tests), `tests/seed-ux-direccion-contract.test.mjs` (+2), `tests/integration/l2-paginacion.test.mts`, diseño, informe, `ESTADO-VERIFICADO.md`, runbook, `api-contracts.md`, `COCOA-22-MIGRACION.md`, `cocoa-22-inventory.json`, `CLAUDE.md`.
- **Cómo:** misma instancia propia, seed `--reset` con el API parado antes de cada corrida; sin migración, rutas, permisos ni CSV; lock intacto.
- **Tests:** `dashboards-business-date-window.test.mts` (69 líneas, 4), `ai-review-deferred.test.mts` (97 líneas, 7), `ai-review-row-actions.test.mts` (reescrito, 5), `CocoaState.test.mts` +1, `CocoaUndoBar.test.mts` +1, `portfolio-compare.test.mts` +2, `direccion-instructions.test.mts` 24, `seed-ux-direccion-contract` 9, `ux-direccion-contract` 72, `l2-paginacion` 17/17 (repetido solo).
- **Verificación:** medida estricta desde el árbol **8/8** (`measure-correccion-2026-09-20.json`, 12:37Z); Chromium: commit diferido (clic → 0 POST, «Deshacer» → 0 POST, cuenta atrás → POST 200), 0 regiones vivas en `main` al aterrizar en `/hoy/direccion`, h2/h3 ingleses = [], captions de fecha de negocio ×3 en ficha y Cartera, callout del cierre en y = 350 px a 1280 × 900, bandeja sin desplazamiento horizontal a 1024 × 768, «pp» en minúsculas; API: GM · overview · portfolio del hotel B con `businessDate 2026-09-19` y la misma ocupación/ADR (51,84 % · 89 €); puerta rápida 11/12 (§4.2).

Ficheros generados compartidos regenerados en la tanda (aparecen `M`): `docs/design/cocoa-22-inventory.json` (37/37 líneas; `tests/cocoa-22-contract` 15 verde), `docs/design/COCOA-22-MIGRACION.md` (3/3: inventario §6 + la línea 190 sin nombre de pila ni ids reales), `scripts/env-contract.json` (+5/−2). Ninguna ruta nueva, ningún cambio en `route-permissions`, `permissions.ts` ni `pilots/tanda5-nav-tree.csv` por parte de UX-2 (decisión D-4).

## 2. Ficheros de la tanda (71 + lock excluido)

| Grupo | Ficheros |
|---|---|
| API (5 M · 3 nuevos) | `apps/api/src/modules/dashboards/{general-manager,portfolio,property-overview}.service.ts`, `modules/reporting/reporting.service.ts`, `scripts/__tests__/refresh-demo-dataset.test.mts`; nuevos `modules/dashboards/__tests__/{general-manager-business-date,dashboards-business-date-window}.test.mts`, `modules/reporting/__tests__/report-catalog-es.test.mts` |
| admin-web pantallas y helpers (14 M · 3 nuevos) | `screens/operations/{GeneralManagerScreen,NightAuditScreen,OperationsDirectorScreen,PortfolioDashboard,PropertyDetailScreen}.tsx`, `night-audit-report.ts`, `screens/approvals/{ApprovalsScreen.tsx,approvals-helpers.ts}`, `screens/aiOperations/{AiHumanReviewQueueScreen.tsx,ai-review-labels.ts}`, `screens/reports/{ReportingCenterScreen.tsx,reporting-center-rows.ts}`, `screens/tabs/hoy/MiDiaTabs.tsx`; nuevos `screens/operations/portfolio-compare.ts`, `screens/aiOperations/ai-review-deferred.ts`, `content/screen-instructions/direccion.ts` |
| admin-web componentes y contenido (7 M) | `components/cocoa/{CocoaState,CocoaUndoBar}.tsx`, `components/cocoa-director/{DirectorBarRecommendations,DirectorPickupBar,DirectorSegmentBars,DirectorVipList}.tsx`, `content/persona-guides/manager.ts` |
| admin-web tests (4 M · 9 nuevos) | `components/cocoa/__tests__/{CocoaState,CocoaUndoBar}.test.mts`, `screens/operations/__tests__/night-audit-report.test.mts`, `screens/reports/__tests__/reporting-center-rows.test.mts`; nuevos `screens/operations/__tests__/{general-manager-feel,operations-director-feel,portfolio-compare}.test.mts`, `screens/approvals/__tests__/approvals-primary-action.test.mts`, `screens/aiOperations/__tests__/{ai-review-deferred,ai-review-row-actions}.test.mts`, `screens/tabs/__tests__/mi-dia-pending-card.test.mts`, `content/__tests__/direccion-instructions.test.mts` |
| e2e (8 nuevos · `.gitignore` M) | `apps/admin-web/e2e/_helpers-direccion.ts`, `e2e/measure/_measure-direccion.ts`, `e2e/measure/d{1..6}-*.spec.ts` |
| database (3 M · 1 nuevo) | `packages/database/package.json`, `prisma/lib/demo-guard.ts`, `prisma/seed-ux-day.ts`; nuevo `prisma/seed-ux-direccion.ts` |
| contratos raíz e integración (1 M · 2 nuevos) | `tests/integration/l2-paginacion.test.mts`; nuevos `tests/{seed-ux-direccion-contract,ux-direccion-contract}.test.mjs` |
| docs y generados (7 M · 6 nuevos) | `CLAUDE.md`, `docs/api-contracts.md`, `docs/audits/ESTADO-VERIFICADO.md`, `docs/design/{COCOA-22-MIGRACION.md,cocoa-22-inventory.json}`, `scripts/env-contract.json`; nuevos `docs/design/UX-DIRECCION-FEEL.md`, `docs/runbooks/ux-direccion-pruebas.md`, este informe, `docs/audits/ux-direccion/measure-{baseline,final,correccion}-2026-09-20.json` |
| excluido del commit | `pnpm-lock.yaml` (+64/−25, ajeno; §7 R-3) |

## 3. Medidas antes / después por tarea (camino óptimo automatizado)

Fuente: `results-direccion.json` de `e2e/measure` copiado a `docs/audits/ux-direccion/` — baseline de D2 (`measure-baseline-2026-09-20.json`, 09:41-09:44Z, antes de tocar pantallas, `MEASURE_STRICT=0`), final de D9 (`measure-final-2026-09-20.json`, 10:58-11:03Z, estricto; d2/d6 con las copias de las specs) y **final tras la corrección** (`measure-correccion-2026-09-20.json`, **12:37Z, `MEASURE_STRICT=1`, 8/8 specs oficiales desde el árbol**, seed `--reset` con el API parado, instancia propia `:5199/:3929`). Clics = acciones de puntero contadas; teclas = pulsaciones y caracteres; peticiones = llamadas al API desde el inicio de la tarea hasta el evento de dominio (d1: desde el `goto`, shell incluido); ms = reloj de pared. Variación entre corridas: clics y teclas idénticos en las cuatro corridas de hoy; peticiones ±1 en d1 (11-12: `notifications` del shell según la caché); ms ±5 % salvo d5 (2.230 ms en la corrida de la corrección, con el Postgres compartido a 80-100/100 conexiones). Una medida solo vale si ninguna otra instancia usa `prop_uxday_b` durante la corrida (runbook §6).

| Tarea (§2 del diseño) | Objetivo | Baseline (D2) | Final (D9, estricto) | Final tras la corrección (specs del árbol, estricto) | Cumple |
|---|---|---|---|---|---|
| d1 · revisar el día y los riesgos al aterrizar | 0 clics · ≤ 20 pet. · ventana visible | 0 clics · 0 teclas · 8 pet. · 1.261 ms (tres verdades del mismo día, rótulos en inglés, sin ventana; riesgos en 4 sitios) | 0 · 0 · 11 · 1.203 ms | **0 clics · 0 teclas · 11 peticiones · 1.207 ms**; subtítulo «Datos de la fecha de negocio 19/09», «Riesgos de hoy» con acción por fila, 0 regiones vivas propias; +3 peticiones respecto a la baseline (preflight, `GET /approvals` y `GET /ai-operations/review/stats` del bloque y de la tarjeta; dos `GET /approvals`, R-6) | sí |
| d2 · aprobar una solicitud pendiente | ≤ 3 clics · ≤ 16 | 4 · 0 · 3 · 575 ms (tarjeta → fila → cajón «Aprobar» → diálogo «¿Aprobar la solicitud?») | 3 · 0 · 3 · 1.040 ms (copia de la spec) | **3 clics · 0 teclas · 3 peticiones · 757 ms** (tarjeta «Pendientes · 3 aprobaciones · 3 de la IA» → «Aprobar» en la fila → diálogo nominal con Intro; `POST /approvals/:id/approve` 200 `approved`) | sí |
| d3 · leer ocupación e ingresos del mes de un hotel | ≤ 3 clics · ≤ 14 · variante teclado | 2 · 0 · 3 · 1.775 ms (Cartera → fila; PyG solo por Finanzas) · teclado 0 · 40 · 3 · 1.894 | 2 · 0 · 3 · 1.779 · teclado 0 · 41 · 3 · 1.925 | **2 clics · 0 teclas · 3 peticiones · 1.764 ms** (Cartera → fila: ficha con «Ingresos del mes», caption «fecha de negocio 19/09/2026» y «PyG del hotel» a un clic) · **teclado 0 clics · 41 teclas · 3 · 2.156 ms** (⌘K → Cartera → Intro → 15 Tab → Intro); camino con PyG (D5): 3 clics · 20 peticiones, no medido por spec | sí |
| d4 · comparar los hoteles de la cartera por ocupación | ≤ 3 clics · ≤ 14 · 2 hoteles con delta | 2 · 0 · 1 · 951 ms (Cartera → ordenar; sin delta ni vista «Comparar»; 2 hoteles solo desde el seed) | 2 · 0 · 1 · 985 | **2 clics · 0 teclas · 1 petición · 1.002 ms** (Cartera › Tabla → cabecera «Ocupación», 2 hoteles ordenados); vista «Comparar» con delta vs media: 3 clics (verificada por D5; la spec mide la vista Tabla) | sí |
| d5 · exportar un informe del Centro de informes | ≤ 3 clics · ≤ 16 · variante teclado · rango real y formato honesto | 2 · 0 · 4 · 956 ms (rango fijo mayo-2026, «PDF» = `.html` sin avisar, catálogo en inglés) · teclado 0 · 41 · 4 · 988 | 2 · 0 · 4 · 977 · teclado 0 · 41 · 4 · 1.014 | **2 clics · 0 teclas · 4 peticiones · 2.230 ms** (→ «Generar exportación» ⌥E; rango «Este mes» 01/09-20/09, «PDF (HTML imprimible)», «Exportación lista: informe-reservation-prop_uxday_b-2026-09-01_2026-09-20.html · disponible hasta HH:MM») · **teclado 0 clics · 41 teclas · 4 · 1.174 ms** (⌘K → Centro de informes → ⌘K «Generar exportación de informe» → Intro) | sí |
| d6 · revisar el cierre del día | ≤ 3 clics · ≤ 14 · 0 respuestas 403 | 4 · 0 · 5 · 1.109 ms (Cierre → fila del historial → cajón → «Marcar como revisado» → diálogo; «Cerrar día» habilitado sin clave) | 3 · 0 · 3 · 897 ms (copia de la spec) | **3 clics · 0 teclas · 3 peticiones · 1.026 ms** (Cierre → «Marcar como revisado» del callout → diálogo «Marcar el cierre como revisado» con Intro; toast «Cierre del 18/09/2026 marcado como revisado»; `reviewedByUserId` = el director; sin botón «Cerrar día» para `manager`; 0 × 403 y 0 × 5xx en los logs del API) | sí |

Reglas de puerta del diseño §2: **ninguna tarea peor que la baseline en clics** (d2 y d6 bajan de 4 a 3; d1, d3, d4 y d5 iguales), **las seis ≤ 4 clics** (brief) y **d2-d6 ≤ 3** (objetivo), **≥ 2 tareas completas solo con teclado** (d3 y d5 con 0 clics; d2 y d6 tienen además ⌥A / ⌥V y comando ⌘K, verificados a mano, no por spec), **d1 con la ventana pintada**. Presupuestos de peticiones (`TARGETS`, provisionales [S]): los seis dentro. Lo que NO mide esta tanda (igual que UX-1 D1-D3): éxito por persona, tiempos con directores reales, errores, SEQ y UMUX-Lite → runbook y §6.

## 4. Puertas con cifras (desde la raíz del worktree; `NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv bash scripts/gates.sh [--quick] --json <fichero>`)

### 4.1 Línea base → olas → cierre D9

| Momento | Resultado | Cifras |
|---|---|---|
| Base (D0, `scratchpad/UX-2/gates-base.json`, rápida) | **12/12** | typecheck 15 PASS · api 3.569 (3.568 pass · 1 skip) · admin-web 2.014 (2.013 · 1 skip) · ai-core 119 · worker 34 · contratos 765 (763 · 2 skip) · nav-tree al día 70 ítems / 104 pestañas · route-access 15 × 197 · rbac dry-run · migrate 24 + drift 0 |
| Ola 1 (D1, `scratchpad/gates-d1-quick.json`) | 10/12 | api 3.569 (3.567 pass · 1 fail: allowlist esperada en `refresh-demo-dataset.test.mts`) · contratos 772 (769 · 1 fail: `env-contract` sin el seed nuevo en el censo) — ambos ficheros fuera de los exclusivos del lote, corregidos en la fusión de la ola |
| Ola 3 (D5, `scratchpad/UX-2-D6/gates-D6-quick.json`) | 11/12 | typecheck 15 PASS · api 3.585 (3.584) · admin-web 2.084 (2.083) · contratos 772 (769 · 1 fail: `cocoa-22-inventory.json` desactualizado por las 7 pantallas en vuelo, regenerado al cerrar la ola) · nav-tree al día · route-access OK · migrate + drift 0 |
| D9 completa (`scratchpad/UX-2/gates-final.json` de esa hora, 13:05-13:15) | 10/14 en la pasada → **13/14** con las repeticiones individuales (13:16-13:22) | typecheck 15 PASS · 0 FAIL · 1 SKIP (23,1 s) · api 3.585 (3.584 · 0 · 1) · admin-web 2.124 (2.123 · 0 · 1) · ai-core 119 · worker 34 · contratos 777 (775 · 0 · 2) · discoverability 197 · route-access 15 × 197 · cocoa waves · build 5,54 s; en la pasada fallaron `rbac:sync --dry-run`, `migrate status + drift` (error de conexión Prisma: Postgres 80-100/100 con los carriles l8/l6b/act/rrhh) e integración 983 (924 pass · 2 fail · 6 skip; `gates.sh` pierde los nombres de los `not ok` con `sum`); repetidas solas: rbac DRY-RUN OK 2.968 ms, 24 migraciones «up to date», drift «No difference detected.», integración **983 · 975 pass · 0 fail · 8 skip · 322 s**; `nav-tree --check` rojo por el CSV compartido (6 filas ACT) |
| Puerta del orquestador antes de la revisión (`scratchpad/UX-2/gates-full.json`) | **13/14** | typecheck 15 PASS · 0 FAIL · 1 SKIP (27,1 s) · api 3.585 (3.584 · 0 · 1) · admin-web 2.124 (2.123 · 0 · 1) · ai-core 119 · worker 34 · contratos 777 (775 · 0 · 2) · discoverability 197 (16/20) · route-access 15 × 197 · cocoa waves · rbac DRY-RUN 3.934 ms · migrate + drift 0 · build 10,69 s · integración 983 (975 · 0 · 8); `nav-tree --check` rojo: CSV compartido con +9 filas (RRHH: `HrEmployeesScreen`, `HrForecastScreen`, `HrOverviewScreen`; ACT: `RealEstateAssetScreen` + 5 pestañas) y la fila `PayrollScreen` reetiquetada «RRHH y nóminas» |

### 4.2 Corrección UX2-REV (rápida, `scratchpad/UX-2-COR/gates-quick.json`) y puerta completa final (`scratchpad/UX-2/gates-final.json`, tras la corrección)

| Puerta | Rápida tras la corrección | Completa final | Notas |
|---|---|---|---|
| `typecheck:all` | 15 PASS · 0 FAIL · 1 SKIP · 26,0 s | **15 PASS · 0 FAIL · 1 SKIP** explícito (apps/guest-web) · 28,1 s | — |
| api unit | 3.589 · 3.588 pass · 0 fail · 1 skip | **3.589 · 3.588 pass · 0 fail · 1 skipped** | D0 partía de 3.569: +20 (`general-manager-business-date`, `dashboards-business-date-window`, `report-catalog-es`, `refresh-demo-dataset` con `prop_uxday_b`) |
| admin-web unit | 2.133 · 2.132 · 0 · 1 | **2.133 · 2.132 pass · 0 fail · 1 skipped** | D0 partía de 2.014: +119 |
| ai-core · worker | 119/119 · 34/34 | **119/119 · 34/34** | sin cambios |
| contratos raíz (`node --test tests/*.test.mjs`) | 833 · 831 · 0 · 2 | **833 · 831 pass · 0 fail · 2 skipped** (los 2 skip preexistentes por `pilots/*.csv` fuera del worktree) | D0 partía de 765/763: +68 (`ux-direccion-contract` 72 → cuenta como 63 tests de nivel superior en la salida de `node --test`, `seed-ux-direccion-contract` 9 y los ampliados) |
| discoverability | 197 URL | **197 URL** registradas | — |
| `build-nav-tree --check` (CSV compartido) | rojo (CSV ajeno) | **rojo, ajeno a UX-2**: `nav-tree.generated.json` «stale» porque el CSV compartido (`pilots/tanda5-nav-tree.csv`, editado por RRHH a las 14:21) ya trae 294 → 304 filas: `DirectorLaborCostsScreen` `/hoy/costes-personal` y las pestañas `HrEmployeesScreen` bajo «RRHH y nóminas» (71 ítems / 113 pestañas); ninguna de esas pantallas existe en este worktree y UX-2 no añade filas (D-4) | regenerar en la fusión, tras RRHH/ACT (`node scripts/build-nav-tree.mjs --csv …`); con el CSV coherente con `a069906`, al día (como en D0-D8) |
| route-access | OK | **OK** (15 tokens × 197 URL) | — |
| cocoa waves `--check` | §6 al día | **§6 al día** | inventario y `COCOA-22-MIGRACION.md:290` regenerados en la corrección |
| `rbac:sync --dry-run` | OK | **DRY-RUN (no writes) · 187 ms** | — |
| migrate status + drift | 24 + drift 0 | **«Database schema is up to date!» · «No difference detected.»** | sin migración de UX-2 |
| admin-web build | — | **OK** (`built in 2.97s`) | — |
| integración (`tests/integration/*.test.mts`, `--test-concurrency=1`) | — | **983 · 975 pass · 0 fail · 8 skipped** | `l2-paginacion` (tocado) 17/17 |
| e2e `measure` dirección (estricto, desde el árbol) | **8/8** (§3) | — | `corepack pnpm --filter @hotelos/admin-web e2e:measure --grep "d[1-6] ·"` sin `--` |
| e2e `measure` completo (d + t, estricto) | — | 11/14 en D9 (d2/d6 ya corregidas; t1 15 pet. > 12 por CHK, no por UX-2; t2-t6 en verde) | no repetido tras la corrección |

Resultado: **13/14 en la completa final**; el único rojo es externo al carril (CSV compartido) y no tiene corrección legítima dentro de él (no se puede tocar el CSV, fusionar ni generar un JSON con pantallas inexistentes); `apps/admin-web/src/navigation/nav-tree.generated.json` se dejó intacto (`git status` limpio en `navigation/`).

### 4.3 Integrador (puerta rápida tras cerrar los documentos y guion del pre-commit)

- `NAV_TREE_CSV=… bash scripts/gates.sh --quick --json scratchpad/UX-2-INT/gates-integrador-quick.json` (`.log` al lado; 2026-09-20 tras la corrección, con el árbol completo y sin instancias levantadas): **11/12** — typecheck:all **15 PASS · 0 FAIL · 1 SKIP · 22,5 s**; api unit **3.589 · 3.588 pass · 0 fail · 1 skip**; admin-web unit **2.133 · 2.132 · 0 · 1**; ai-core **119/119**; worker **34/34**; contratos raíz **833 · 831 pass · 0 fail · 2 skip**; discoverability **197 URL**; route-access **OK 15 × 197**; cocoa waves **§6 al día**; `rbac:sync --dry-run` **OK · 249 ms**; migrate + drift **«No difference detected.»**; `nav-tree --check` **rojo** por el CSV compartido (`pilots/tanda5-nav-tree.csv`, 304 filas, mtime 14:21:49; idéntico a 4.2). Sin cambios en el árbol por la puerta (72 entradas con `-uall` antes y después; `navigation/` limpio).
- Pre-commit: el `core.hooksPath = .husky` del repo (config común de `~/anfitorio-demo/.git`) se resuelve contra la raíz del worktree (`~/anfitorio-demo-wt-ux2/.husky`, inexistente: el guion vive en `hotelos/.husky/pre-commit`), así que `git commit` no lo ejecuta por sí solo en este repositorio (misma situación en las tandas anteriores, que también lo anotan como ejecución manual); se ejecutó a mano desde `hotelos/` (`bash .husky/pre-commit`: discoverability + typecheck de los 15 workspaces → **«15 PASS · 0 FAIL · 1 SKIP · 23,0 s» · «Pre-commit checks passed»**, `scratchpad/UX-2-INT/pre-commit-manual.log`) **antes** del commit; nunca `--no-verify`.

## 5. Revisión v3: hallazgos confirmados, refutados y corregidos

Dos revisores sobre el árbol de D9 (funcional-runtime con Chromium e instancia propia; seguridad-datos-regresiones con `git`, `psql` de solo lectura y contratos). **Confirmados: 9** (1 high · 8 medium); **refutados: 0**; **low adicionales: 14**.

### 5.1 Confirmados y su estado

| # | Sev. | Hallazgo | Estado | Corrección (ficheros · test · verificación) |
|---|---|---|---|---|
| funcional REV-01 | medium | F-D1 a medias: Cartera y detalle seguían en día UTC (`portfolio.service.ts:469`, `property-overview.service.ts:198`) mientras Mi día › Dirección y el preflight iban por fecha de negocio; la tira de Cartera no decía su ventana | **corregido** | `readGmBusinessDate` + `resolveGmWindow` en los dos servicios (`businessDate`/`businessDateSource` aditivos: detalle en el sobre, Cartera por fila); ficha «fecha de negocio DD/MM/AAAA», tira «fecha de negocio de cada hotel»; puerta D3 del diseño rebajada a lo entregado; `dashboards-business-date-window.test.mts` (4) + `portfolio-compare.test.mts` (+2); runtime: GM · overview · portfolio del hotel B = 2026-09-19, 12/0/12, 51,84 % · 89 €; caption ×3 en Chromium |
| funcional REV-02 | medium | El informe y `ESTADO-VERIFICADO` decían «fecha de negocio = día UTC en Faranda» y «R-13 no aplica»: falso; se leyó la FUNCIÓN SQL `current_date` en vez de la columna | **corregido** | §6.3 y R-13 reescritos, `ESTADO-VERIFICADO.md`, runbook; `l2-paginacion.test.mts` ancla el mes y la ventana de 30 días en `businessDate` (17/17); `psql` con `"current_date"` entrecomillada: prop_123 = 2026-09-14; Faranda 2026-09-16 / 09-17 / 09-19 ×3; prop_uxday 2026-09-20; prop_uxday_b 2026-09-19 |
| funcional REV-03 | medium | `general_manager` (`direccion@uxday.test`, `direccion.general@faranda.test`) no tiene `night_audit.review` ni `reopen` (`permissions.ts:1852+`): d6 no es completable para dirección general aunque D-1 lo daba por verificado | **corregido en docs; RBAC sin cambio (decisión D-11, §6)** | diseño D-1, `persona-guides/manager.ts`, `screen-instructions/direccion.ts`, runbook; `direccion-instructions.test.mts` 24; login `direccion@uxday.test` → 0 claves `night_audit.*` |
| funcional REV-04 | medium | «Aprobar» en Pendientes de la IA era un clic irreversible (POST a los 46 ms) sin diálogo ni deshacer: contra el diseño P4 / D5 | **corregido** | `ai-review-deferred.ts` (controlador puro) + `AiHumanReviewQueueScreen.tsx`: fila «Aprobada» local, `CocoaUndoBar` 8 s, POST al expirar/cerrar la barra, al desmontar o en `beforeunload` (`keepalive`), o al sustituirla; `CocoaUndoBar` levanta la pausa por hover al cambiar de entrada (bug real expuesto: una segunda aprobación tras «Deshacer» nunca expiraba); `ai-review-deferred.test.mts` (7), `ai-review-row-actions.test.mts` (5), `CocoaUndoBar.test.mts` (+1); Chromium: clic → 0 POST, «Deshacer» → 0 POST, segundo clic → POST 200 tras la cuenta atrás; diseño P4 «≥ 1 acción con deshacer» |
| funcional REV-05 | medium | Literales ingleses en dirección («Aprobada: Rate change», «Guest message reply», «Segments», «BAR Recommendations IA», «VIPs in-house») y `tests/ux-direccion-contract.test.mjs` inexistente | **corregido** | `REVIEW_TYPE_LABELS` con todos los `reviewType` del API; `DirectorSegmentBars` / `DirectorBarRecommendations` / `DirectorVipList` con prop `title` en español («Segmentos», «Recomendaciones BAR de la IA», «VIP alojados»; estados «Llega hoy · En el hotel · Sale hoy»); `DirectorPickupBar` «frente al año anterior»; contrato nuevo (72); Chromium: h2/h3 ingleses = [] |
| funcional REV-06 | medium | Las specs oficiales d2/d6 codificaban el camino anterior; el «después» venía de copias en el scratchpad (se purga) | **corregido** | `d2-aprobar-pendiente.spec.ts`, `d6-revisar-cierre.spec.ts` al camino entregado (localizadores pinados en el contrato §5); `MEASURE_STRICT=1 … --grep "d[1-6] ·"` **8/8** desde el árbol (`measure-correccion-2026-09-20.json`) |
| seguridad REV-01 | high | `pnpm-lock.yaml` modificado en el worktree (+64/−25) e inconsistente en la base (sin entradas de Playwright pese a `^1.60.0` en `admin-web/package.json`, importer `apps/ai-gateway` inexistente, sin `packages/ai-core`) | **no corregido (regla «lock intocable»); fuera del commit (§8)** | decisión del orquestador en main (D-12, §6): regenerar el lock en un commit propio o fijar `@playwright/test` a la versión cuyos navegadores existen en la caché |
| seguridad REV-04 | medium | El seed no creaba `revenue_daily_snapshots` para el hotel B (ni el tercer ítem IA prometido): detalle 0,0 % / 0,00 € frente a Cartera 7,8 % / 89,54 € | **corregido** | 20 snapshots `rds_uxdb_<fecha>` (ayer y anteayer desde el plan, resto histórico sintético, `dataSource seed-ux-direccion`, `deleteScoped` en `--reset`) + tercer ítem `review_response`; contrato del seed +2 (9/9); seed `--reset` ×3 y ensure idempotente; Cartera = detalle (51,84 % · 89 €) |
| seguridad REV-05 | medium | Entregables del diseño ausentes: `tests/ux-direccion-contract.test.mjs`, bloque en `CLAUDE.md`, cobertura de las specs en un contrato | **corregido** | contrato nuevo (regiones vivas ≤ 1 + `role="none"` en los vacíos del panel, comandos ⌘K en 8 pantallas, 25 literales ingleses a 0 en `screens/{operations,approvals,reports,aiOperations}` + `cocoa-director`, todo `reviewType` con etiqueta, 0 fechas literales, 6 specs con `createMeasure`, `.gitignore`, helper sin overrides); bloque UX-2 en `CLAUDE.md` (23 líneas; ver §7 sobre su destino en la fusión) |

### 5.2 Low corregidos (10) y low sin cambio (4)

| # | Hallazgo | Estado |
|---|---|---|
| funcional REV-07 | 4 regiones vivas al aterrizar en el panel | corregido: `CocoaState` admite `role="none"` (+1 test); los 4 vacíos del panel y el de VIP lo usan → 0 regiones vivas en `main` |
| funcional REV-08 | bandeja de aprobaciones desborda ≤ 1.224 px | corregido: «Umbral» y «Solicitada» `showFrom: "desktop"`; a 1024 × 768 `scrollWidth` = `clientWidth` = 734 |
| funcional REV-09 | callout «Marcar como revisado» fuera del primer viewport | corregido: encima de «Comprobaciones previas al cierre»; y = 350 px a 1280 × 900 |
| funcional REV-10 | fila D3 de §1 inexacta (`operations-director-labels.ts` es de FIX-1) y recortes sin declarar | corregido: informe (§1 y recortes en §7) |
| funcional REV-11 | «+44,5 PP» en mayúsculas | corregido: `uppercase={false}` en el badge del delta |
| seguridad REV-07 | contraseña impresa en la salida de los seeds | corregido: `DEMO_PASSWORD_LABEL` en `seed-ux-direccion.ts` y `seed-ux-day.ts`; el contrato prohíbe el literal en ambos |
| seguridad REV-08 | cabecera y comentario de `productivity` describían la ventana antigua | corregido: «pendientes de llegar/salir en la fecha de negocio (regla del preflight)», nombres intactos |
| seguridad REV-11 | grafía antigua de la marca en la cabecera de `general-manager.service.ts:3`; nombre de pila e ids reales en `COCOA-22-MIGRACION.md:190` | corregido («ehotelOS»; línea 190 sin nombre ni ids) |
| seguridad REV-13 | overrides `E2E_DIRECCION_*` en el helper e2e | corregido: retirados; guarda `assertUxDayProperty` (solo `prop_uxday*`); pinado en el contrato |
| seguridad REV-09 | dos `GET /approvals?status=pending` al aterrizar (tarjeta + bloque) | **sin cambio** (R-6, dueño UX-3): exige tocar `MiDiaTabs.tsx` (compartido) y reescribir dos tests pinados; d1 queda en 11 pet. ≤ 20 |
| seguridad REV-06 / REV-10 / REV-12 | ficheros regenerados en el carril, orden de fusión de `MiDiaTabs.tsx`, `nav-tree --check` con el CSV compartido | **sin cambio de código** (dueño: orquestador): inventario y `COCOA-22-MIGRACION.md:290` regenerados de nuevo; fusión y nav-tree en §7-§8 |

## 6. Decisiones para César (opción por defecto aplicada en el árbol)

| # | Decisión | Por defecto aplicado | Estado |
|---|---|---|---|
| D-1 | ¿El director de hotel (`manager`) recibe `night_audit.run` para cerrar el día él mismo? | **No**: se mantiene la separación runner ≠ reviewer; d6 = revisar el cierre; «Cerrar día» solo se pinta con la clave (`closeActionFor`) | aplicado; cambiarlo es RBAC (`permissions.ts` + `rbac:sync`), no pantalla |
| D-2 | Segunda propiedad de prueba para comparar hoteles | **`prop_uxday_b` «Hotel UXDAY B (prueba)» dentro de `org_uxday`** (allowlist concatenada), no una organización nueva; nada se escribe en `prop_uxday` desde los lotes de dirección | aplicado |
| D-3 | Ventana «hoy» del panel del director | **Fecha de negocio de la propiedad** (la del preflight y el cierre), rotulada «Datos de la fecha de negocio DD/MM»; desde la corrección también Cartera y detalle. Cambia cifras en Faranda cuando la fecha de negocio va retrasada (hoy 09-16 / 09-17 / 09-19 frente al calendario 09-20); Mi día cuenta llegadas/salidas PENDIENTES y Cartera/detalle TODAS las del día (etiquetado); ocupación del día en Mi día y media del mes en Cartera/detalle (etiquetado) | aplicado |
| D-4 | Rango por defecto de la exportación del Centro de informes | **«Este mes» hasta hoy** con Mes anterior / Últimos 30 días / Personalizado; el rango fijo mayo-2026 desaparece; «hoy» = día natural de Madrid, no la fecha de negocio (R-12: costaría una petición a `night-audit/business-date`) | aplicado |
| D-5 | Títulos del catálogo de informes del API en español (contrato aditivo) | **Sí** («Reservas: llegadas y salidas», «Facturación, facturas y cobros», «Revenue: histórico y previsión») | aplicado |
| D-6 | Rótulos del panel del director al español | **Sí** (Llegadas · Salidas · Bloqueadas · Personal · Seguridad · TPV · Contribución neta · Ritmo 30 días · Captación 7 días · Picos de demanda 14 días · Segmentos · Recomendaciones BAR de la IA · VIP alojados); DOC retira «En construcción» en `docs/manual/10-direccion.md:88` | aplicado (DOC pendiente, §7) |
| D-7 | Atajos | **Sin ⌥ globales nuevos** (regenerarían `content/help-articles`, de DOC): comandos ⌘K de página en las 8 pantallas y `accessKey` por pantalla A (Aprobar) · E (Generar exportación) · V (Marcar como revisado; el brief citaba R, reservada a la navegación global) | aplicado |
| D-8 | Aprobar un pendiente de la IA | Por defecto del plan: directo (1 clic, sin diálogo ni deshacer). **Cambiado por la revisión (REV-04):** commit diferido 8 s con `CocoaUndoBar` (el POST sale al expirar, al desmontar, en `beforeunload` o al sustituirlo); las aprobaciones con dinero llevan diálogo nominal («Aprobar reembolso de 60,00 €»), nunca «¿Seguro?»; 0 diálogos nuevos | aplicado |
| D-9 | Sesiones con directores reales y cronometraje en OPERA (D1-D3 de UX-1) | **No caben en la tanda**; kit del moderador en `docs/runbooks/ux-direccion-pruebas.md` (2-3 directores de hotel + 1 persona de dirección general, 45 min, 6 tareas sobre UXDAY B, SEQ/UMUX-Lite, hoja sin nombres) | pendiente de César: quiénes, cuándo y autorización para cronometrar con las herramientas actuales |
| D-10 | Panel de costes de RRHH | **No se toca** (en la base `a069906` era diseño sin código; el CSV compartido ya trae `DirectorLaborCostsScreen` `/hoy/costes-personal` del carril RRHH: entra con esa tanda, no con UX-2) | aplicado |
| D-11 | `night_audit.review` (+ `reopen`) para la plantilla `general_manager` (REV-03) | **Sin cambio RBAC**: hoy solo `manager` revisa; d6 no es completable para dirección general (`direccion@uxday.test`, `direccion.general@faranda.test`), que consulta el informe; guía, tarjeta y diseño lo dicen | **abierta**: añadir las claves a `general_manager` (`permissions.ts` + `rbac:sync`, con la matriz SoD de `tests/rbac-sod-contract`) o dejarlo en `manager` |
| D-12 | `pnpm-lock.yaml` (REV-01 high) | **No se commitea desde el carril** (`git reset -q hotelos/pnpm-lock.yaml`); en main, commit propio: `corepack pnpm install --lockfile-only` (regenera y cubre los `package.json` actuales) o fijar `@playwright/test` a 1.61.x (navegadores 1228 en la caché) y reinstalar; hasta entonces las medidas dependen de `PLAYWRIGHT_BROWSERS_PATH=<scratchpad>/pw-browsers` (enlaces 1243 → 1228) o de `playwright install chromium` (~150 MB, requiere permiso) | **abierta** (orquestador / César) |

## 7. Pendientes con dueño

### 7.1 Riesgos y deuda

| # | Riesgo / deuda | Ancla | Dueño |
|---|---|---|---|
| R-1 | ~~Specs d2/d6 del camino anterior~~ **cerrado (REV-06)** | `e2e/measure/d2-*.spec.ts`, `d6-*.spec.ts` | — |
| R-2 | `build-nav-tree --check` rojo por el CSV compartido (filas RRHH/ACT sin pantalla aquí; `PayrollScreen` reetiquetada): regenerar `nav-tree.generated.json` en la fusión tras RRHH/ACT | `pilots/tanda5-nav-tree.csv`, `apps/admin-web/src/navigation/nav-tree.generated.json` | orquestador |
| R-3 | Lock ajeno (+64/−25) y navegadores 1243 ausentes en `~/Library/Caches/ms-playwright` (D-12) | `pnpm-lock.yaml` | orquestador / César |
| R-4 | `corepack pnpm … e2e:measure -- --grep` pasa el `--` literal a Playwright y corre también t1…t6 (escribe en `prop_uxday`): usar `--grep` sin `--` (runbook §6) | `apps/admin-web/package.json` (`e2e:measure`) | DOC / UX-3 |
| R-5 | t1 de recepción a 15 peticiones (presupuesto 12) desde la fusión de CHK (sondeo 404 de `check-in`, `timeline`, SES 409 en el cajón) | `screens/operations/QuickCheckInDrawer.tsx`, `checkinRunner.ts`, `e2e/measure/_measure.ts` | UX-3 / CHK |
| R-6 | Dos `GET /approvals?status=pending` al aterrizar en Mi día › Dirección (tarjeta y bloque; dedupe solo en vuelo): compartir `useApiData` | `screens/tabs/hoy/MiDiaTabs.tsx`, `GeneralManagerScreen.tsx` | UX-3 (fichero compartido) |
| R-7 | PyG desde el detalle como `manager` sin `accounting.entity.read`: primer `GET` con ámbito sociedad → 404 antes de aplicar `?ambito=` (pantalla correcta al final; 1 error de consola, 1 petición de más); camino d3 con PyG = 20 peticiones | `screens/finance/ProfitAndLossScreen.tsx:157`, `services/financeScope.ts:637-647` | Finanzas |
| R-8 | ~~Rótulos ingleses en `cocoa-director` y tipos IA~~ **cerrado (REV-05)** | — | — |
| R-9 | ~~Bandeja desborda ≤ 1.224 px~~ **cerrado (REV-08)** | — | — |
| R-10 | ~~Callout fuera del viewport~~ **cerrado (REV-09)** | — | — |
| R-11 | Ningún usuario UXDAY tiene `night_audit.run`: las ramas «Cerrar día» / «Cerrar de todos modos» solo se verificaron con la clave inyectada en la sesión front; un runner real (`night_auditor`, `front_office_manager`) solo existe en Faranda | `NightAuditScreen.tsx` (`closeActionFor`) | César (dato de Faranda) |
| R-12 | «Hoy» del rango por defecto del Centro de informes = día natural de Madrid (D-4) | `screens/reports/reporting-center-rows.ts` | aceptado |
| R-13 | ~~`l2-paginacion` con `Date.now() − 30 d`~~ **cerrado (REV-02)**: ancla en `businessDate` | — | — |
| R-14 | Postgres local a 80-100/100 conexiones con los carriles en paralelo (puertas y `/dashboards/portfolio` con «too many clients»): instancias de prueba con `connection_limit` (D3-D9 usaron 5) | entorno | orquestador |
| R-15 | Con una propiedad activa inaccesible (recepción sobre `prop_uxday_b` → 404) `/hoy/cierre-del-dia` dejó colgados sondeos de Playwright (no investigado) | shell (`layouts/BackOfficeLayout.tsx`) | lote de corrección |
| R-16 | Scripts `lint` de admin-web y api no operativos (ESLint 9 sin `eslint.config.*`), preexistente y fuera de `gates.sh` | `apps/*/package.json` | plataforma |
| R-17 | Bloque «Estado verificado (Tanda UX-2 …)» de 23 líneas en `CLAUDE.md` (añadido por el corrector, REV-05) cuando la regla del cierre lo sitúa solo en `docs/audits/ESTADO-VERIFICADO.md`; el integrador no puede editar `CLAUDE.md` | `CLAUDE.md:572-594` | orquestador (en la fusión: `merge-lane.sh` deja `CLAUDE.md` = main + deltas; retirar el bloque si main mantiene la política de L6a) |
| R-18 | Verificación en tablet emulada no realizada (UX-2 no toca densidades ni objetivos táctiles; solo el panel de operaciones cambia el drill-down por `pointer: coarse`, verificado a 375 × 812) | `OperationsDirectorScreen.tsx` | UX-3 si toca densidades |

### 7.2 Recortes declarados (REV-10)

- **D6 · `RevenueExportCenter.tsx` «solo comandos»**: no entregado; la pantalla conserva su único comando ⌘K «Actualizar el catálogo de exportaciones». d5 se mide en el Centro de informes, que sí lleva «Generar exportación de informe» y los dos «Exportar informe de …».
- **D7 · «Reabrir día» en «Más ▾»** y **D4 · «Rechazar» / Asignar / Escalar en «Más ▾»**: no entregados; Cocoa 22 no tiene primitiva de menú desplegable y la tanda no añade primitivas. «Reabrir día» sigue como botón `bordered` destructivo junto a «Marcar como revisado» en el cajón; «Rechazar» es el segundo botón (`bordered`) de la fila; Asignar / Escalar viven al pie del cajón. P1 se cumple (una sola `filled`/`tinted` por fila y región).
- **D3 · `pace`** sigue en día UTC (`buildGmPace`; fuera del brief).

### 7.3 Pendientes para DOC (`docs/manual/10-direccion.md`, ayuda en la app)

- `:87-88` — el aviso «En construcción» de los rótulos ingleses del panel queda obsoleto: todos traducidos (incluidos «Segmentos», «Recomendaciones BAR de la IA», «VIP alojados» desde la corrección); «vs LY» / «vs hace 7 días» solo con base > 0; «Descartar» de las anomalías sigue sin efecto.
- `:223` — catálogo «Reservas: llegadas y salidas», «Facturación, facturas y cobros», «Revenue: histórico y previsión»; formatos «PDF (HTML imprimible) · CSV · XLSX (se entrega CSV) · JSON»; bloque «Exportar informe» con periodo y fechas.
- `:226` y `:258` — «Generar exportación no descarga nada / Exportación lista: undefined» y «los nombres del catálogo están en inglés» obsoletos: «Exportación lista: informe-<tipo>-<propiedad>-<desde>_<hasta>.<ext> · disponible hasta HH:MM» y descarga real (FIX-1 + D6).
- `:179` (Aprobar en Pendientes de la IA no ejecuta) sigue vigente; desde la corrección «Aprobar» difiere el envío 8 s con «Deshacer» y los tipos se llaman «Respuesta a un mensaje del huésped», «Cambio de tarifa», «Respuesta a una reseña», «Acción de la IA por confirmar», «Documento clasificado por la IA».
- Nuevo por documentar: subtítulo de Mi día › Dirección, bloque «Riesgos de hoy», tarjeta «Pendientes · N aprobaciones · M de la IA», primaria «Aprobar» por fila y diálogo nominal, vistas «Tabla · Comparar», «PyG del hotel», callout «Marcar como revisado», letras ⌥A / ⌥E / ⌥V (de pantalla, generadas en el registro de atajos; sin ⌥ globales en `content/help-articles/keyboard-shortcuts.ts`), captions «fecha de negocio …» en Cartera y ficha.
- `pilots/tanda5-nav-tree.csv:23` describe un aterrizaje obsoleto de dirección (→ Recepción); la verdad es `role-tokens.ts:230-231` (`/hoy/direccion`): corrección del orquestador en el CSV (fuera del worktree).

### 7.4 Pendientes para UX-3 (ficheros compartidos que UX-2 deja modificados)

| Fichero | Cambio de UX-2 | Qué debe saber UX-3 |
|---|---|---|
| `packages/database/package.json` | +1 línea: script `db:seed:ux-direccion` | fusión trivial; el seed exige `db:seed:ux-day` antes |
| `.gitignore` | +2 líneas: comentario y `apps/admin-web/e2e/measure/results-direccion.json` | un `results-*.json` propio de UX-3 va en el mismo bloque |
| `packages/database/prisma/lib/demo-guard.ts` | `UX2_DEMO_PROPERTY_IDS = ["prop_uxday_b"]` concatenado a `DEMO_PROPERTY_IDS` (el literal de UX-1/CHK no cambia; `refresh-demo-dataset.test.mts` y los contratos de seed lo fijan) | una propiedad de prueba de UX-3 se añade de la misma forma |
| `packages/database/prisma/seed-ux-day.ts` | +3/−1: la salida imprime «contraseña: la de demo por defecto» en vez del valor (REV-07) | nada |
| `apps/admin-web/src/screens/tabs/hoy/MiDiaTabs.tsx` | `PendingApprovalsCard` → `PendingCard` (+ botón «IA»; `useApiData` de `/ai-operations/review/stats` solo con `ai_governance.read`; grupo `aria-label="Pendientes de hoy"`); comentario de aterrizaje corregido | el aterrizaje por rol no cambia; una tarjeta de pisos/mantenimiento va dentro de `actions` junto a `PendingCard`; R-6 se resuelve compartiendo `useApiData` |
| `apps/admin-web/src/components/cocoa/{CocoaState,CocoaUndoBar}.tsx` | `CocoaState` acepta `role="none"`; `CocoaUndoBar` resetea la pausa por hover al cambiar de entrada (bug real) | primitivas compartidas, cambios aditivos con test |
| `scripts/env-contract.json`, `docs/design/cocoa-22-inventory.json`, `docs/design/COCOA-22-MIGRACION.md` | regenerados en la tanda | regenerar de nuevo al cerrar UX-3 (`node scripts/env-census.mjs --write`, `node scripts/cocoa-22-inventory.mjs`, `node scripts/cocoa-22-waves.mjs --write`) |
| `apps/admin-web/e2e/measure/_measure.ts` | NO tocado por UX-2 (dirección usa `_measure-direccion.ts`) | el presupuesto de t1 (R-5) es de UX-3/CHK |

## 8. Fusión y commit

- **Commit del integrador** en la rama `tanda-ux2` (nunca en main, sin push): `git -C ~/anfitorio-demo-wt-ux2 add -A hotelos && git … reset -q hotelos/pnpm-lock.yaml && git -c user.name="cesareme" -c user.email="yakutatsa@gmail.com" commit -F <mensaje>`; guion del pre-commit ejecutado a mano antes (§4.3). Título `feat(ux-direccion): «feel» de dirección — una verdad por dato, una acción por fila, ⌘K (Tanda UX-2)`; cuerpo por lote; `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
- **Orden de fusión recomendado:** RRHH/ACT → UX-2 (o regenerar `nav-tree.generated.json` con el CSV compartido tras fusionar todo); `merge-lane.sh` (censo env, inventario Cocoa, whitelist, lock: `git checkout -- pnpm-lock.yaml` como en CHK; `ESTADO-VERIFICADO.md` concatena; `CLAUDE.md` = main + deltas, R-17); tras la fusión, en main: `node scripts/env-census.mjs --write`, `node scripts/cocoa-22-inventory.mjs`, `node scripts/cocoa-22-waves.mjs --write`, `node scripts/build-nav-tree.mjs --csv …`, reinicio de `:3000` (sirve el código anterior a la tanda: sin él Mi día › Dirección y Cartera siguen en día UTC y el catálogo en inglés) y `db:seed:ux-direccion -- --reset` con el API parado si se quiere el hotel B en la demo local (`hotelos`, no solo en `hotelos_ux2`).
- **Lo que solo César puede aportar:** D-9 (sesiones reales), D-11 (RBAC de `general_manager`), D-12 (lock), R-11 (un runner real del cierre en Faranda).
