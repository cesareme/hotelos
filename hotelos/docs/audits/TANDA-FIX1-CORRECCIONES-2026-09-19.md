# Tanda FIX-1 · Correcciones de producto tras la carga Sage y el manual — informe de cierre (2026-09-19)

**Encargo:** brief `scratchpad/fix1-brief.md` (12 lotes F1…F12 sobre los hallazgos A-01…E-10 del informe externo `pilots/faranda-celuisma/sage200-real/INFORME-CARGA-2026-09-18.md` §15, los defectos declarados «En construcción» en `docs/manual/**` y la deuda de `docs/audits/TANDA-L3-DINERO-FISCAL-2026-09-18.md`), una pasada de corrección sobre 15 hallazgos del corrector y dos rondas de puertas. Base **`150a713`** (main con T8, OPERA, manual, hotfix de cajones y runbook §19.9). Worktree exclusivo `~/anfitorio-demo-wt-fix1/hotelos`, rama **`tanda-fix1`**. Árbol SIN commit al cierre (`git status`, integrador): **94 ficheros modificados (+4.902 / −651) + 23 sin seguimiento**, más este informe. De los 94 modificados, **`pnpm-lock.yaml` (74+/25−) NO es de la tanda**: su diff (`playwright`/`playwright-core`/`fsevents`/`@fontsource-variable/inter`, `packages/ai-core`, `apps/ai-gateway`) tenía mtime 18:33:38, anterior al primer lote (19:21), ningún `package.json` ni `pnpm-workspace.yaml` está modificado y el md5 del diff no varió durante la tanda → **excluir del commit**. F12 vive fuera del repo (`~/anfitorio-demo/pilots/faranda-celuisma/sage200-real/prep/`).

Ni el árbol principal `~/anfitorio-demo/hotelos` ni los servidores del orquestador (`:3000`, `:5173`, PID 4986) ni los de UX-1 (`:3913`/`:5183`) se han tocado. Los lotes verificaron en instancias propias del worktree (`PORT=3917 RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true` + Vite `:5187`, o `:3927`/`:3937`/`:3957`/`:5197` cuando los del brief estaban ocupados por un lote hermano), matadas por PID; al cierre `:3917`/`:5187` libres. BD: solo lectura sobre Faranda (org `cmrhw9jy30002fyvb6tsdiugt`) salvo dos migraciones aditivas; escrituras de producto solo en org_123 (demo) y en tenants aislados de los tests (borrados). **Ningún apply pendiente de decisión se ha ejecutado** (reclasificación de libros de F2, re-enmascarado de F12).

## 1. Lotes: defecto · causa · corrección · test · verificación

Resumen (detalle por lote en §1.1–§1.12; hunks del corrector en §3).

| Lote | Hallazgo | Ficheros (worktree, sin tests) | Tests |
|---|---|---|---|
| F1 | A-01/E-01 cuentas anuales con apertura importada | `financial-statements/{source,annual-accounts.service}.ts`, runbook `finanzas-contabilidad.md` §9/§19.9 | +7 unit (29/29; módulo 98/98) |
| F2 | B-1/B-4/B-6 régimen de IVA en libros + reclasificación | migración `20260920100000_iva_regimen`, `schema.prisma`, `shared/fiscal-types.ts`, `vat-books{,-reclassify}.service.ts`, `modelo-303/390.service.ts`, `fiscal.routes.ts` + partial, `import/{ledger-import.canonical,ledger-import.posting,sage200.parser}.ts`, runbook `finanzas-importacion-sage200.md` §2.4/§2.5/§7.1 | +20 unit (166/166) + contratos |
| F3 | B-2/E-02/E-03/E-04/B-5 Modelo 303 | migración `20260920110000_iva_compensacion_inicial`, `schema.prisma`, `shared/fiscal-types.ts`, `vat-books.service.ts`, `modelo-303.service.ts`, `fiscal.routes.ts` + partial, front `fiscalApi.ts`, `fiscal/{fiscal-shared.ts,FiscalModelReport,VatBooksScreen}.tsx`, `AccountingSettings.tsx`, runbook §11.1/§11.2/§13/§19.9 | unit 205/205, integración `fiscal-models` 17/17, front 28/28 |
| F4 | B-7/B-8/A-04 importador de libros | `shared/ledger-import-types.ts`, `import/{ledger-import.canonical,ledger-import.posting,sage200.parser,ledger-import.service}.ts`, `fiscal-year.service.ts`, runbook Sage §1.2/§2.4/§2.5/§5.4/§9 | 66/66 unit |
| F5 | Centro de informes «Exportación lista: undefined» | `reporting/reporting.service.ts`, `server.ts`, `security/route-permissions.ts`, front `reports/{ReportingCenterScreen.tsx,reporting-center-rows.ts}`, `pmsCommerceApi.ts`, manual `10-direccion.md`/`50-comercial-revenue.md` | +4 front, integración nueva `reports-export-download` 6/6 |
| F6 | «Comparar plantillas» rompe («b is not iterable») | `users/{RoleComparePane.tsx,users-rbac.ts}`, guarda nueva `tests/shared-source-shadow-contract.test.mjs`, manual `60-sistemas.md`, `plan-de-formacion.md` | +2 unit (12/12), +1 contrato raíz |
| F7 | Webhooks: Pausar/Eliminar/entregas 404, placeholder y cabecera de marca | `webhooks/webhooks.service.ts`, `lib/tenancy.ts`, front `developer/WebhooksAdminScreen.tsx`, manual `60-sistemas.md` §4.2 | contrato de pantalla 5/5, integración `l2-modulos-plataforma` L2-08 (+4, +1 corrector) |
| F8 | Asistente: «¿Cuántas llegadas tengo hoy?» no se enruta | `assistant/assistant.tools.ts`, manual `10-direccion.md:383` | +15 unit (18/18) |
| F9 | Pisos/mantenimiento: «Tomar», toasts, concordancia, ids internos | front `operations/{MaintenanceMobileScreen,HousekeepingDashboard,OperationsDirectorScreen}.tsx` + helpers nuevos, `layouts/setup-banner.ts`, `compliance/GuestRegisterSettingsScreen.tsx`, `guestRegisterApi.ts`; API `dashboards/operations-director.service.ts`, `compliance/compliance.service.ts`; manual (8 guías/fichas) | front 20/20, API 17/17 |
| F10 | RRHH: alta de ficha de personal + promesa 2FA | `payroll/staff-profiles.{service,routes}.ts` (nuevos) + partial, `server.ts`, front `payroll/{PayrollScreen.tsx,staff-profile-form.ts}`, `payrollApi.ts`, `users/AssignmentDrawer.tsx`, manual `30-rrhh.md` §2, `60-sistemas.md` §1.2, ficha 08, `faq.md`, `plan-de-formacion.md`, runbook `finanzas-contabilidad.md` §18.13 | unit 12/12, integración `payroll-cost-routes` 14/14, front 64/64 |
| F11 | E-05 directorio «Terceros» | `import/ledger-third-parties.service.ts` (nuevo), `ledger-import.routes.ts` + partial, `shared/ledger-import-types.ts`, front `accounting/{Sage200ImportScreen.tsx,sage200-import-helpers.ts}`, `ledgerImportApi.ts`, runbook Sage §3.1/§7 | unit 7/7, integración `ledger-import-routes` 18/18 |
| F12 | C-01/C-02 PII (fuera del repo) | `prep/tools/{prep_common.py,check_pii_csv.py,pii_ids.py,remask-pii.sql,remask-pii.ts}`, `prep/plan/*`, `prep/saldos/*` (21 CSV), `prep/apply/remask-pii.{plan.json,log}` | `test_prep_common.py` 12/12, `remask-pii.test.mts` 6/6 |

### 1.1 F1 · Cuentas anuales con apertura importada (A-01/E-01)

- **Defecto.** Balance de cuentas anuales 2025 de Faranda descuadrado 640.726,85 (PN+pasivo 25.860.405,12 frente a activo 26.501.131,97) y ECPN con todas las columnas a 0,00 y avisos.
- **Causa.** `source.ts` excluye `entry_kind = opening` del modo `movements` y `annual-accounts.service.ts` lee el «antes» con `balance_at(from − 1)`: la apertura importada de Sage fechada exactamente en `from` (01/01/2025) no entra en ningún conjunto, pero su aplicación 129→113 dentro del ejercicio sí.
- **Corrección.** Tercer modo de lectura `opening_in_period` en `LedgerMode` (`source.ts`; espejo en `memory-source.mts`): asientos de apertura dentro de `[from, to]` sin cierre previo emparejado (misma organización y MISMA propiedad, `IS NOT DISTINCT FROM`, no draft, sin reversión, `c.entry_date >= from − 1 AND c.entry_date < je.entry_date`). `readLedgerSet` lee cuatro conjuntos y `rowsBefore = mergeBalanceRows(balance_at(from−1), opening_in_period)` (`mergeBalanceRows` exportada, suma por código, orden `compareAccountBalanceRows`); 129 arrastrada, ECPN columna «A» y memoria (inmovilizado, saldo inicial) cuadran por construcción. Dos desviaciones razonadas frente al SQL literal del brief: la guarda usa `>= from − 1` (un cierre fechado en `from − 1` ya queda fuera de `balance_at(from − 1)`; con `>= from` la apertura 2026-01-01 se sumaría y el 129 de 2026 se doblaría) y empareja por propiedad de la apertura, no por ámbito de la consulta.
- **Test.** `annual-accounts.test.mts` describe «apertura importada el primer día del ejercicio (A-01/E-01)»: (1) ledger cuyo primer asiento es apertura en `from` + aplicación 129→113 el 30/06 → `balanced` true, 5.125,00 ambos lados, ECPN `reconciled`; el mismo cálculo solo con `balance_at(from−1)` reproduce el bug (4.125,00); (2) periodo 2027-07..2028-06 con `closeReferenceYear` → `opening_in_period` vacío, sin duplicar; (3) `mergeBalanceRows`. `source.test.mts` describe «modo opening_in_period» (6 casos: sin cierre previo, con cierre en `from − 1`, con cierre dentro del rango, cierre anterior a `from − 1`, pareja por propiedad null=null, draft ignorado, «requires from»). 29/29; módulo 98/98.
- **Verificación.** GET `:3927` (instancia propia; `:3917` estaba ocupado por un lote hermano) `/accounting/annual-accounts/balance?fiscalYearId=cmu7jk55p0001fye48b30u3ml` → `balanced` true, **26.501.131,97** ambos lados; `/ecpn` 2025 `reconciled` true, fila A capital 4.735.729,75 / reservas 18.794.934,94 / priorResults −16.040.471,30 / total 8.209.311,63 (antes 0,00); `/annual-accounts` 2025 coherencia ok; 2026 `balanced` 26.992.112,71; `reports/balance-sheet?asOf=2025-12-31` 26.456.572,96. **La cifra de referencia del brief (26.456.572,96) es la del balance clásico, no la de cuentas anuales**: la diferencia 44.559,01 es de presentación y previa a F1 (474 = 38.989,26 y 566 = 5.569,75 activo por prefijo PGC frente a pasivo negativo por `Account.kind`; 163 = 1.180.258,40 PN frente a pasivo L_III) — cerrada en la pasada de corrección (§3, F1-CLASSIC-VS-ANNUAL-TOTALS/SEC-05) con epígrafe C.IV para la cuenta 478 (saldo acreedor 213.575,15, antes «sin clasificar» con aviso) y documentación de la divergencia en el runbook §9/§19.9; la unificación exige corregir el `kind` en el plan (decisión §8).

### 1.2 F2 · Régimen de IVA en libros y reclasificación (B-1/B-4/B-6)

- **Defecto.** Los libros importados de Sage no distinguen régimen: las autofacturas ISP/AIB, los DUA y las operaciones exentas/no sujetas entran como interiores en el 303 (01-09), en el volumen de operaciones del 390 (casilla 108) y en `/fiscal/regime` (Faranda 2025: 6.151.982,84 > umbral 6.010.121,04 → «gran empresa» falsa).
- **Causa.** `VatBookEntry` no tenía régimen; el canónico `vat_books` ignoraba la clave de operación y la calificación AEAT; el importador no las leía; `compute303` solo conocía las casillas 01-09/27-29/45-46.
- **Corrección.** Migración `20260920100000_iva_regimen` (`CREATE TYPE VatBookRegime {interior, isp, aib, importacion, exento_no_sujeto}`, columna `vat_book_entries.regime` nullable, índice `(organization_id, book, regime)`; SQL verbatim de `migrate diff`, sin backfill; copia previa `backups/hotelos-pre-fix1-20260919-192956.dump`). Shared: `VatBookRegimeCode`, `regime` en `VatBookRowDto`, DTOs de reclasificación. Canónico: `clave_operacion`, `calificacion` y (corrector) `inversion_sujeto_pasivo` como columnas OPCIONALES fuera del hash cuando vienen vacías (`HASH_OPTIONAL_WHEN_NULL_FIELDS`: un fichero anterior a F2 conserva su `contentHash`); parser AEAT con sinónimos plegados. Posting `regimeOfVatRow` (tras corrector, §3): recibidas F5 → `importacion`, ISP = S → `isp`, clave 09 → `aib`, 01 → `interior`; emitidas 09 → `isp`, 01 con NIF → `interior`, 01 sin NIF o sin datos → `null` (candidata a reclassify; **nunca `isp` por defecto**). 303: `computation.devengado` (477 por tipo) se mantiene COMPLETO para la liquidación y el cotejo; casillas 01-09 desde `devengadoInterior`; 10/11 AIB, 12/13 ISP, 32/33 importación, 36/37 AIB deducible, 28/29 ISP soportada (30/31 bienes de inversión), 38/39 AIB de bienes de inversión, 120 exento/no sujeto, 122/123 a 0; 27 y 45 incluyen las nuevas; 46/71 invariantes. 390: casillas por régimen, volumen (108) sin autofacturas, `AUTOFACTURAS_ISP_AIB_EXCLUIDAS` informativa; `/fiscal/regime` hereda el volumen. Acción de producto `POST /fiscal/vat-books/reclassify` (`accounting.configure`/high; zod strict `period | from,to`, `apply=false`, `includeZeroRate=false`; solo filas `sage200` con `regime` null; tope 200.000 filas; apply = `updateMany` en transacción con condición `regime IS NULL`, chunks de 5.000; auditoría `VAT_BOOKS_RECLASSIFIED` con recuentos). Reglas tras corrector: (1) emitida sin NIF con cuota ≠ 0 emparejada por periodo + nombre normalizado + base + cuota con recibida CON NIF extranjero de prefijo UE → ambas `aib`; (4) misma pareja con NIF no UE (CH/CO/GB…) → ambas `isp`; (2) recibidas F5 con DUA (MRN/≥ 14 dígitos/palabra clave) → `importacion`; (3) tipo 0 % con NIF extranjero → `exento_no_sujeto` solo con `includeZeroRate`.
- **Test.** `modelo-303`, `fiscal-regime`, `vat-books`, `vat-books-reclassify` (nuevo, 12 casos con razones sociales y NIF inventados), `modelo-347`, `ledger-import-canonical`, `ledger-import-posting`, `sage200-parser`, `ledger-import-service` → 166/166 en el lote; `api-route-permissions-contract` 32/32; `finance-api-surface` 16/16.
- **Verificación.** Dry-run real (`:3917`, `contabilidad@faranda.test`, 0 filas escritas, 0 auditorías): 2025-Q1 → 66 parejas `aib` (15.131,93 = clave 09 del libro real) + 10 `isp` (176,99 = ISP «S»), sin pareja 3 (17,52), DUA 9 filas 9.906,29; 2025 → 244 `aib` + 66 `isp` + 15 DUA (28.128,52), sin pareja 2.064 (46.607,98); 2026 → 129 `aib` + 48 `isp` + 10 DUA (34.679,15) + regla 3 con `includeZeroRate` 7 filas (656.000 + 132.000). 303 2025-Q3 sin cambios (27 = 281.975,20 · 45 = 256.772,71 · 71 = 25.202,49); `/fiscal/regime?year=2025` sin cambios hasta el apply (volumen 6.151.982,84). Tras el apply se espera volumen 2025 ≈ 5.584.379,55 < umbral → propuesta «general».

### 1.3 F3 · Modelo 303: compensación, históricas, vista mensual, centros, cotejo (B-2/E-02/E-03/E-04/B-5)

- **Defecto.** Casilla 110/78 no arrastraba el saldo a compensar (2025-Q2 de Faranda: 110 = 0 con LIQUI de Sage de 42.024,02 aplicada); las liquidaciones importadas de Sage no se mostraban; un mes sobre periodicidad trimestral daba 400; el periodo por defecto no era el último con libros; el desglose por centro pintaba ceros para lotes Sage sin delegación; el cotejo libro↔diario truncaba a 25.000/50.000 apuntes.
- **Causa.** `pendingVatCompensation` solo leía liquidaciones nativas; no había saldo inicial configurable; `resolveSettlementPeriod` rechazaba el mes; el cotejo cargaba apuntes en memoria con topes.
- **Corrección.** Migración `20260920110000_iva_compensacion_inicial` (`vat_settings.opening_compensation DECIMAL(12,2) NOT NULL DEFAULT 0`, `opening_compensation_period TEXT`; copia previa `backups/hotelos-pre-fix1-f3-20260919-195746.dump`). `pendingVatCompensation` = saldo inicial + UN `$queryRaw` sobre 4700x (`vat_settlement` o `sage200_journal` con 477/472 en el asiento, `entry_kind NOT IN (closing, opening)` — necesario porque el cierre 92679 salda 4700 y la apertura 2443 lo repone). `historicalSettlements` (Σ 4750 haber−debe − Σ 4700 debe; el brief decía «Σ 4750 haber», pero la LIQUI 3T25 lleva D 4750 / H 4700 y la variante neta da el 71 real 27.883,82). Tras corrector (SEC-01, §3): **110 encadenada** — `resolveCarriedCompensation` recorre hacia atrás los periodos sin asiento de liquidación hasta el último liquidado / periodo del saldo inicial / primer periodo con libros y encadena `compute303` (110 = `compensacionPendienteFinal` del anterior; aviso «Casilla 110 encadenada desde el Modelo 303 de …»; `COMPENSATION_CHAIN_MAX_DEPTH` 60); el 390 encadena trimestre a trimestre. Vista mensual informativa (`GET /fiscal/models/303?period=YYYY-MM&informativo=1`; sin flag sigue 400 `PERIOD_MISMATCH`). `GET /fiscal/vat-books/periods` (último periodo con libros materializados). Aviso `SAGE_NO_CENTRE_AVISO` (constante compartida API+front) en 303 y libros con `propertyId`. Cotejo = CTE común + dos `$queryRaw` agregados (sin topes; `LEDGER_CROSS_CHECK_MAX_*` eliminados). Front: picker mensual, callout informativa, tabla «Liquidaciones históricas (importadas de Sage)», `CocoaState` en vez de ceros para centros sin delegación, columna «Régimen» en libros, sección «Compensación inicial del IVA (casilla 110)» en Configuración › Contabilidad (`PUT /fiscal/vat-settings`, `accounting.configure`, auditoría `VAT_SETTINGS_UPDATED` existente; 400 `OPENING_COMPENSATION_PERIOD_REQUIRED` si importe > 0 sin periodo).
- **Test.** Unit accounting 205/205 (`applyOpeningCompensation`, 78 = min(110, 46), `resolveSettlementPeriod` informativo, `historicalSettlementsAviso`, `latestPeriodOf`, `resolveOpeningCompensationPatch`; tras corrector: dos periodos consecutivos sin asiento no aplican dos veces el mismo saldo, paradas en liquidado/libros/saldo inicial/maxDepth). Integración `fiscal-models` 17/17 con tenant aislado `orgfiscs_*` (LIQUI sintética 477/472/4700 → históricas y 110; saldo inicial 100 desde 2026-Q1; 400 de validación; cotejo agregado `cuadra` sin aviso de topes; `periods`; mensual informativo; E-03; reverso). Front `fiscal/__tests__` 28/28. Contratos 60/60.
- **Verificación.** `:3917` (Faranda, solo GET + un PUT inválido rechazado 400): `periods` latest «2026-Q3»; 303 2025-Q2 → 110 = 78 = 42.024,02 (LIQUI 18708); 2025-Q3 → 110 = 78 = 1.184,07, históricas [(71463, 30/09/2025, 27.883,82)], cotejo **44.790 apuntes sin truncar** (antes 25.000 con aviso), diferencias 21 % +1.054,91 / 10 % −140,80; 2026-Q1 → 110 2.948,03; mensual 2026-01 informativo 27 = 21.736,70; `propertyId=RA` → aviso E-03, 0 registros. Tras el corrector (SEC-01): 2026-Q1 110 2.948,03 · 71 −52.050,57 → **2026-Q2 110 54.998,60 · 78 45.688,28 · 71 0** → 2026-Q3 110 9.310,32 · 78 70,39 · 71 0 (antes Σ78 de 2026 = 3.018,42 > 2.948,03 disponibles); 390 2025 `resultadoLiquidaciones` −17.093,75 / pendiente fin 2.866,45 (≠ 2.948,03 de la LIQUI 4T: diferencia libro↔Sage preexistente). Navegador `:5187`: 303 abre en 3T 2026 con selector de mes, callout informativa, tabla de históricas, desglose RA con `CocoaState`; libros con columna «Régimen» («—»); configuración con la sección nueva (nada guardado en Faranda).

### 1.4 F4 · Importador de libros (B-7/B-8/A-04)

- **Defecto.** `parseRateCode` redondeaba 7,5 % a 8 (`money()` a 2 decimales sobre «0.075»); recibidas con el mismo número de proveedor en fechas distintas colisionaban en `sourceId` (renumerado «/R2», «REC-»); `fiscal_periods.closed_by` quedaba NULL al cerrar por importación (12 periodos 2025 de Faranda).
- **Causa.** `parseRateCode` pasaba por `parseSignedAmount`; `ledgerImportVatBookSourceId` no incluía fecha/nº de recepción; `markFiscalYearClosedFromImport` no recibía actor.
- **Corrección.** `parseRateCode` normaliza «,»/«%», mueve la coma sobre el texto si es fracción < 1 y devuelve `money(...).toString()` («7,5»/«7,50 %»/«0.075» → «7.5»; «21,00» → «21»); posting diario → `taxRateCode` conserva decimales (antes `Math.round`). `sourceId` = `empresa:ejercicio:serie:número[:NIF][:recepción][:R]` con columna opcional `numero_recepcion` (sinónimos AEAT, cellCode «45.0» → «45», fuera del hash si vacía); en recibidas `reception = numero_recepcion ?? fecha`; `number` conserva el número de Sage. `closedBy = input.closedBy ?? import:<importId>`; `ledger-import.service.ts:1653` pasa `createdBy`. Runbook Sage §1.2/§2.4/§2.5 (regla operativa: revertir el lote antiguo antes de reimportar libros ya cargados — las filas pre-F4 conservan la clave vieja), §5.4, §9. NADA recargado.
- **Test.** `ledger-import-canonical` + `ledger-import-posting` + `fiscal-year-import-close` 66/66 (dos fechas → dos `sourceId` sin aviso; misma fecha → fusión con aviso; `numero_recepcion` en clave; 7,5 → `rate` 7.50 y `taxRateCode` «7.5»; `closedBy` actor / `import:<id>`). `tests/integration/ledger-import.test.mts:632/650` ajustados a la clave nueva (`…:2026-09-03`) en la pasada de corrección.
- **Verificación.** `POST /accounting/ledger-imports/preview` en `:3917` (org_123, sin escritura): dos recibidas F/778 tipo 7,5 en fechas distintas → 2 asientos sin aviso; misma fecha → 1 con aviso «repetida con el mismo tipo 7.5 %»; misma fecha con `numero_recepcion` 45/46 → 2. Faranda por SELECT: 12 periodos 2025 siguen `closed` con `closed_by` NULL (hasta recarga).

### 1.5 F5 · Centro de informes: «Exportación lista: undefined»

- **Defecto.** «Generar exportación» terminaba en «Exportación lista: undefined» y el enlace apuntaba a `undefined`; el CSV de reservas salía vacío.
- **Causa.** `exportOperationalReport` devolvía `{ export:{id,…}, content }` sin `downloadUrl` (no hay object storage) y el front interpolaba `export.downloadUrl`; `buildReportContent` solo leía `data.reservations` cuando el informe expone `rows`.
- **Corrección.** `REPORT_EXPORT_STORE` en memoria (Map por id con `organizationId`/`propertyId`/contenido, TTL 15 min, máximo 100 con poda), `export.downloadUrl = /reports/exports/<id>/download` + `expiresAt`; `GET /reports/exports/:exportId/download` (`analytics.export`; tras corrector SEC-06 `riskLevel: high` como el POST que lo genera y `propertyWithinScope` → 404 opaco «Exportación no encontrada o caducada.»), cabeceras `content-type` + `content-disposition: attachment`; ramal `rows`. Front: `fetchReportExportFile` (`apiRequestBlob`), `exportStatusMessage`, `downloadReportExport` (patrón Blob de `revenueExportApi`), descarga inline tras exportar, callout «disponible hasta las HH:MM» + `CocoaButton` «Descargar exportación»; `linkStyle` y el `<a style>` eliminados (−1 `style=`). Manual §10 paso 3 y §50 sin «En construcción»; documentado que el almacén es en memoria (misma instancia, sin réplicas ni reinicios) y que XLSX degrada a CSV / PDF a HTML.
- **Test.** Front `reports/__tests__` 7/7 (+4). Integración nueva `tests/integration/reports-export-download.test.mts` (tenants A/B, `STRICT_ENV`) 6/6: manifiesto, POST export con `downloadUrl`/`expiresAt`, GET 200 con cabeceras y cuerpo idéntico, XLSX → CSV, 404 inexistente/caducado/otra organización, 403 sin permiso, poda por `REPORT_EXPORT_MAX`. Unit nuevo `reporting/__tests__/report-export-download.test.mts` 3/3 (corrector).
- **Verificación.** `:3917` (`reception@example.com`, prop_123): POST → `report_export_*`, contenido 25 líneas (antes vacío); GET → 200 `text/csv;charset=utf-8`, `attachment; filename="informe-reservation-prop_123-2026-09-19.csv"`, 3.094 B; id inexistente → 404; sin token → 401 tras SEC-06. Navegador `:5187`: estado «Exportación lista: informe-reservation-…csv», callout y botón, POST/GET 200, 0 errores de consola (la escritura a disco no es observable en el sandbox del navegador).

### 1.6 F6 · Usuarios y roles › «Comparar plantillas» («b is not iterable»)

- **Defecto.** El cajón «Comparar plantillas» rompía la interfaz en `:5173`.
- **Causa.** En el árbol principal, `packages/shared/src/permissions.js` (compilado obsoleto, 17-sep, git-ignored) sombrea `permissions.ts` (19-sep): `ROLE_PERMISSION_MAP[x]` llega `undefined` y `compareTemplates` hace `new Set(undefined)`. En el worktree (0 `.js` en `packages/*/src`) el mismo código funciona.
- **Corrección.** `users-rbac.ts` `compareTemplates(a | undefined, b | undefined)` y `RoleLike.permissions` opcional; `RoleComparePane.tsx` toma la pareja inicial de `OFFERABLE_TEMPLATES[0]/[1]` con fallback y `ROLE_PERMISSION_MAP[x] ?? []`. Guarda nueva `tests/shared-source-shadow-contract.test.mjs` (recorre `packages/*/src`, falla si un `.js` sombrea un `.ts/.tsx`; mensaje «borrar con git clean -fdX packages/*/src»). Manual `60-sistemas.md` §1.5 describe el cajón con literales verificados; fila de «Errores frecuentes» y bullet «rompe la interfaz» eliminados; `plan-de-formacion.md` :33/:287/:294.
- **Test.** `users-rbac.test.mts` 12/12 (+«tolera plantillas sin claves», +«todas las OFFERABLE_TEMPLATES tienen claves»); `shared-source-shadow-contract` 1/1 en el worktree, detección validada contra un árbol falso con 2 sombras.
- **Verificación.** `:5187` Configuración › Usuarios › «Comparar plantillas»: pareja inicial Dirección general (119 claves) vs Dirección de operaciones (110) → 18 · 101 · 9; Recepción (75) vs Jefatura de recepción (101) → 5 · 70 · 31, 18 filas por módulo, bloque «Roles con permisos idénticos»; 0 errores de consola. **El árbol principal sigue con 58 `.js` obsoletos** (shared 17, compliance 22, integrations 18, config 1): `:5173` seguirá fallando y la guarda nueva se pondrá en rojo allí hasta `git clean -fdX packages/*/src` (§6).

### 1.7 F7 · Webhooks creados desde la pantalla (404) y marca

- **Defecto.** Pausar/Eliminar/entregas de una suscripción creada desde la pantalla → 404 «Suscripción de webhook no encontrada.»; placeholder con la marca anterior y cabecera de firma con nombre heredado sin aclarar.
- **Causa.** `createSubscription` sin `developerAppId` guardaba `propertyId null` + `developerAppId` sintético `app_<org>`; el resolver de `tenancy.ts` para `webhookSubscription` solo resolvía por propiedad o `developerApp` real → sin organización → 404 del guard.
- **Corrección.** `createSubscription`: con `developerAppId` en el body verifica que la app es de la organización (400 «La aplicación indicada no pertenece a esta organización.»); sin él, `propertyId = body ?? context.propertyId`. Resolver: propiedad → developerApp → sufijo de `app_<org>` (cubre filas heredadas sin escribir). Tras corrector (SEC-02): `listSubscriptions` filtra por organización (propiedades de la organización OR developer apps OR `app_<org>`), con o sin `propertyId`. Front: `propertyId: getActivePropertyId()` al crear, placeholder `/ehotelos/webhook`, callout que aclara que el nombre de la cabecera de firma es heredado del protocolo (`sha256=…`) y no cambia con la marca. Manual §4.2 paso 6 describe «Entregas de la suscripción»; blockquote «En construcción» → «Nota».
- **Test.** `webhooks-screen-contract.test.mts` 5/5 (nuevo); `l2-modulos-plataforma` bloque «L2-08 · webhooks» (A crea/pausa/lista/borra; B recibe 404 opaco; fila heredada `propertyId null`; 403 recepción; 400 app ajena; (2b) A ve las suyas y B no ve las de A); `brand-contract` 10/10.
- **Verificación.** `:3917` `GET /webhooks/subscriptions/cmu82d5nf020bfygbcgchzfuk0/deliveries` → 200 (antes 404). `:5187`: crear `https://example.invalid/ehotelos/F7-verificacion` → 200 con `propertyId prop_123`, Pausar → PATCH 200 badge «Pausada», Eliminar → DELETE 200; org_123 vuelve a sus 2 suscripciones; Faranda lista 0 filas tras SEC-02.

### 1.8 F8 · Asistente: «¿Cuántas llegadas tengo hoy?» no se enruta

- **Defecto.** La sugerencia de llegadas (y «¿cuántas salidas tengo hoy?») devolvía `toolCalls []`; las otras cinco enrutaban.
- **Causa.** `findToolsByKeyword` casaba subcadenas literales con signos/acentos/orden; `get_arrivals_today` solo tenía «entradas hoy».
- **Corrección.** `normalizeForRouting` (minúsculas, NFD sin diacríticos, `¿?¡!.,;:` → espacio), `tokenize`, `keywordMatchesQuestion` (1 token → subcadena; ≥ 2 tokens → palabras completas en cualquier orden); keywords «cuantas llegadas»/«cuantas salidas» (+ variantes con «hoy»; tras corrector F8-TODAY-ROUTING-OVERREACH: `mentionsAnotherDate` excluye las herramientas `*_today` si la pregunta nombra mañana/ayer/semana/mes/próximo/día de la semana/fecha). Manual `10-direccion.md:383` describe la respuesta y el fallback con catálogo. Sigue sin proveedor de IA (`mode: deterministic`). Nota: la ruta real es `POST /assistant/chat` (no `/assistant/ask`).
- **Test.** `assistant-router.test.mts` 15 casos (+3 de `assistant-mode`): 7 sugerencias → exactamente 1 herramienta; variantes; «hola»/«¿Qué tengo hoy?» → []; ninguna keyword del catálogo es un único token genérico; mañana → [].
- **Verificación.** `:3917` `POST /assistant/chat` → `get_arrivals_today` «4 reservas con llegada hoy.»; salidas → `get_departures_today`; «Estado de pisos hoy» → solo `get_housekeeping_status`. `:5187` /asistente: la sugerencia dispara el POST y el hilo muestra la respuesta con «Fuentes consultadas».

### 1.9 F9 · Mantenimiento y pisos: «Tomar», toasts, concordancia, ids internos

- **Defecto.** «Tomar» un parte no asignaba a nadie; toasts «Tarea empezar./completar.»; «Faltan 1 comprobación»; `roomId`/`staffProfileId`/`reservationId` crudos en /hoy/operaciones y en el parte de viajeros manual.
- **Causa.** `MaintenanceMobileScreen.setStatus` no enviaba `assignedTo`; `HousekeepingDashboard` componía el toast con el verbo del botón; `setup-banner.ts` sin concordancia; `operations-director.service.ts` devolvía ids sin resolver y el front pintaba códigos; `GuestRegisterRecord` no tiene relación Prisma con `Reservation`.
- **Corrección.** PATCH `/work-orders/:id` con `{ status, assignedTo: user.fullName || user.email }` solo al tomar; `housekeeping-task-actions.ts` `nextTaskAction` («Tarea empezada.»/«Tarea completada.»); `Falta 1 comprobación` / `Faltan n comprobaciones`; API `withRoomNumbers` + UNA consulta `room.findMany` (`safe("details.rooms")`); `operations-director-labels.ts` (tipo/estado/prioridad —incluida `preventive`— /turno/departamento con fallback al código); `listGuestRegisterRecords` resuelve `reservationCode` con una segunda consulta por ids distintos; columna «Reserva» y ayuda en el parte manual. Manual: 40-pisos, 10-direccion, 70-recepcion, 00-primeros-pasos, README, faq, 60-sistemas, ficha 12.
- **Test.** Front `housekeeping-task-actions` + `operations-director-labels` + `maintenance-take-contract` + `setup-banner` 20/20; API `operations-director-rooms` + `guest-register-ensure` 17/17; contratos de pantalla/nav/compliance 101/101.
- **Verificación.** `:3917`: `/dashboards/operations-director?propertyId=prop_123` → `hkTasks` con `roomNumber`, `workOrders` 4 (3 con habitación); `/properties/prop_123/guest-register-records` → `reservationCode` 7/7. `:5187` (por DOM, pane minimizado): franja «Falta 1 comprobación…»; «Tomar» → «Avería … → En curso · asignada a ti», tarjeta «Asignada a <usuario demo>»; «Empezar»/«Completar» → toasts correctos; /hoy/operaciones con números de habitación y etiquetas; parte de viajeros con «RES-…». **Datos demo modificados por las acciones de producto** (org_123/prop_123): orden `cmu80n0gt014gfygbc8ar1c3n` → `in_progress` asignada; una tarea HK pendiente → en curso y una en curso → hecha.

### 1.10 F10 · RRHH: alta de ficha de personal y promesa 2FA (M6)

- **Defecto.** No se podían crear contratos (no había forma de dar de alta un `StaffProfile`); el manual prometía «2FA obligatoria a partir de N2».
- **Causa.** No existía endpoint ni cajón de alta; el login no consulta `mfaEnabled` y `createMfaChallenge` no tiene canal.
- **Corrección.** `payroll/staff-profiles.service.ts` (`listStaffProfiles` `payroll.read` con `propertyId` opcional; `createStaffProfile` `payroll.manage`: propiedad de la organización + ámbito → 404, usuario de la organización → 404, departamento de la propiedad (Prisma o seed en memoria) → 400 `STAFF_PROFILE_DEPARTMENT_MISMATCH`, duplicado activo → 409 `STAFF_PROFILE_EXISTS`, `hourlyCost` ≥ 0 «12.50», auditoría `STAFF_PROFILE_CREATED` sin nombre/correo/coste) y `staff-profiles.routes.ts` (zod strict, `grantPropertyAccess` antes del servicio, 201); partial GET medium / POST high; `server.ts` registra tras `registerPayrollCostRoutes`. Front: botón y comando «Nueva ficha», `CocoaDrawer` «Nueva ficha de personal» (Centro de trabajo con ámbito sociedad, Persona, Departamento, Modalidad, Coste hora), «Nuevo contrato» con `CocoaSelect` de fichas y preselección; etiquetas `employeeCode ?? userFullName`; 0 `style=` nuevos. 2FA: decisión por evidencia → se retira la promesa (`AssignmentDrawer.tsx:237` help «Deja la marca “2FA: Activo” … hoy el acceso no la exige»; 60-sistemas §1.2, ficha 08, faq «Otros defectos conocidos» reescrito, runbook §18.13 con referencia a `accesos-por-departamento.md` §3.2). 30-rrhh §2.1/§2.2, plan-de-formacion R-1/R1.
- **Test.** `staff-profiles.test.mts` 12/12; integración `payroll-cost-routes` 14/14 (tenant `org_sp_<run>`: 201 + auditoría, 409, 404 usuario ajeno, 400 departamento/validación, 404 opaco centro ajeno, 403, 401, GET, contrato con la ficha nueva → 200); front payroll + users 64/64; contratos raíz 32+10+8; `check-route-access` OK; `rbac:sync --dry-run` +0.
- **Verificación.** `:3917`/`:5187`: Finanzas › Nóminas › «Nueva ficha» → POST 201 «Ficha creada.» → «Nuevo contrato» con la ficha preseleccionada → POST 200 → Contratos (1). **Datos demo creados y no borrados** (org_123): `staff_profiles cmu8rc88m000ify208o377p6o` (MANUAL-F10-001) y `employment_contracts cmu8rcsv2000jfy20frpek9e7` (1.800 €, activo); ver §8 (un «Recalcular» del periodo 2026-09 generaría un recibo).

### 1.11 F11 · Terceros importados (E-05)

- **Defecto.** No había forma de consultar los terceros del lote `third_parties` (código, NIF, cuenta, rol, lote).
- **Causa.** Sin ruta ni pantalla.
- **Corrección.** `GET /accounting/ledger-imports/third-parties` (16.ª ruta del módulo; `accounting.read` → efectiva `accounting.reports.read` por el remap del manifiesto; medium; zod strict `q ≤ 80`, `role`, `limit` (default 50, máximo 200 recortado), `cursor` keyset `(role, sourceCode, id)`; cabeceras `X-Total-Count`/`X-Next-Cursor`; `lote` más reciente por tercero desde `ledger_import_entries`). Regla de nombres tras corrector (F11-EMPLOYEE-NAMES-STILL-EXPOSED): **el nombre solo se sirve para NIF de persona jurídica** (`isLegalEntityTaxId`: CIF A-H/J/N/P-S/U/V/W con o sin ES, o NIF-IVA extranjero UE/EEE/GB/CH); DNI/NIE/pasaporte/numérico/sin NIF → `null`, además de 465/460/555 y la palabra EMPLEADO (en cualquier posición: 5 clientes «<palabra> EMPLEADO nnnn» de la carga real). Front: pestaña «Terceros» en Finanzas › Contabilidad › Importar desde Sage 200 (buscador con debounce, filtro de rol, `CocoaTable`, «Cargar más», 0 `style=`; sin ruta ni comando nuevo). Runbook Sage §3.1 y §7.
- **Test.** `ledger-third-parties.test.mts` 7/7 (+ampliación del corrector); integración `ledger-import-routes` 18/18 (tenant `org_lr_<run>`: lote sintético, paginación, cursor inválido 400, `q`/`role`, 403, 401; nombres retenidos ausentes del texto); front `sage200-import-*` 82 (1 skip preexistente); `api-route-permissions-contract` 32/32.
- **Verificación.** `:3937` (`:3917` ocupado): `role=supplier&limit=20` → 200, `X-Total-Count` 3.720; clientes 18.064; total 21.784; `q=EMPLEADO` → 15 filas, 15 sin nombre; `role=x` → 400; `cursor=zzz` → 400. Tras el corrector: **4.589/21.784 con nombre (sociedades), 17.195 sin nombre**; de las 37 filas del plan remask-pii 36 salen `null` (la restante tiene CIF: la cubre el apply de F12). `:5197`: pestaña «Terceros importados · 21.784 terceros», «Cargar más», buscador «400» → 1.305, + Proveedores → 862; 0 errores de consola.

### 1.12 F12 · PII del preprocesado y de la BD (C-01/C-02) — fuera del repo

- **Defecto.** Dos títulos del mapa de cuentas (4650040312, 4650070307) con nombre de empleado en claro; ~200 descripciones de líneas, ~12 cabeceras y ~17 títulos de terceros con la forma invertida del nombre; el preprocesado trataba «SA»/«DA»/«DOS» entre palabras como marcador de sociedad y no enmascaraba la forma invertida ni el conjunto de tokens.
- **Causa.** `looks_like_person`/`COMPANY_MARKERS` de `prep_common.py`; `NameMasker` solo con bigrama directo en cuentas personales; sufijos 465 ≥ 0100 no reconocidos como subcuentas de empleado.
- **Corrección.** (a) `prep_common.py`: `COMPANY_MARKERS` (formas con puntos en cualquier posición con `(?![A-Z0-9])`; sin puntos solo al final `\.?\s*$`), `GENERIC_TITLE_WORDS` sin «SA»/«S.A», `is_employee_subaccount` (465 y sufijo ≥ 0100), `NameMasker` con formas invertidas (1.709 formas para 1.044 nombres), conjunto de tokens ≥ 3 sin partículas (958 conjuntos, ventana mínima) en cualquier cuenta, `detect()` sin mutar; **serie «EMPLEADO 0001…1042» congelada** (clasificador original conservado como `_looks_like_person_2026_09_18`; las 2 detecciones nuevas reciben «EMPLEADO <7 últimas cifras de la cuenta>»). `plan/plan-celuisma.csv` (18 títulos re-enmascarados sobre el fichero de la carga, filas sintéticas conservadas), `plan-stats.txt`, `saldos/*` (21 CSV, 122 filas cambiadas solo en `titulo`, `verificacion-saldos.json` idéntico). `check_pii_csv.py`: antes 140 hallazgos en 22 CSV (exit 1) → **0 hallazgos en 75.528 filas / 75.736 celdas (exit 0)**. (b) `pii_ids.py` (psql `default_transaction_read_only=on`) → `prep/apply/remask-pii.plan.json` **331 entradas** deterministas: `ledger_account_maps` 18, `journal_entries` 17, `journal_lines` 221, `ledger_third_parties` 37, `ledger_imports.mapping_json` 38 elementos en 14 lotes; todo `after` con token EMPLEADO, nunca el original. `remask-pii.sql` (solo lectura, ids explícitos, patrón letras→X) sirve de «antes» (aplicadas 0/331) y «después». `remask-pii.ts` (`--dry-run` por defecto; `--apply --confirm <org>` en UNA transacción, `count === 1` o rollback, `journal_lines` por `{ id, journalEntryId }` verificando asiento de la organización y `source_type sage200*`, `mapping_json` por `path`; auditoría `LEDGER_PII_REMASKED` con recuentos; log sin textos). NO se ha ejecutado `--apply`.
- **Test.** `python3 -m unittest prep/tools/test_prep_common.py` 12/12 (nombres inventados); `remask-pii.test.mts` 6/6 (`applyPlanEntry`, `patchMappingJson`, `validatePlanEntry`, `parseFlags`); `tsc --noEmit` 0 errores; plan con ids falsos → BLOQUEADO exit 1 en dry-run y en apply (antes de la transacción); `--confirm` erróneo → 1; `--apply` sin `--confirm` → 2.
- **Verificación.** Dry-run contra la BD real: 331 pendientes, 0 ya aplicadas, 0 bloqueantes, exit 0, 1,3 s (dos ejecuciones en `remask-pii.log`, 19:32 y 20:19 UTC). `masker.detect` sobre los 12 entregables + salidas = 0 líneas. BD: 1.126/1.127 títulos 465/460/555 del mapa iguales al masker; títulos 465 con sufijo ≥ 0100 sin token = 2 (los del plan).

## 2. Puertas

### 2.1 Ronda 1 (tras los 12 lotes, antes del corrector) — 15/15 en verde

typecheck:all 15 PASS · 1 SKIP (guest-web) · api unit 2.946 (2.945 pass · 1 skip) · front 1.551 (1.550 · 1 skip) · contratos raíz 543 (541 · 2 skip por CSV de pilots ausentes en el worktree) · discoverability 232 pantallas · 192/192 · nav-tree al día (69 ítems · 100 pestañas) · check-route-access 15 × 192 · Cocoa inlineStyles 647 → 646, contrato 18/18 · build 941 módulos · integración 808 (800 · 8 skip; invariantes de Faranda idénticas) · worker 34/34 · rbac:sync +0 · migrate status 20/20 + drift 0 · git status sin lockfile tocado · brand 10/10. Referencia previa (3A-final): api 2.860, front 1.505, contratos 541, integración 782.

### 2.2 Ronda final (tras la pasada de corrección) — 13/15 en verde; las 2 rojas son externas a la tanda

| Puerta | Resultado |
|---|---|
| 1 `typecheck:all` | 15 PASS · 0 FAIL · 1 SKIP (guest-web) · 22,4 s |
| 2 API unit | **2.959** tests · 2.958 pass · 0 fail · 1 skip (`PMS_HF_REAL_CSV`) |
| 3 admin-web unit | 1.551 · 1.550 pass · 1 skip (pilots/ ausente en el worktree) |
| 4 contratos raíz | 543 · 541 pass · 2 skip (`pilots/tanda5-nav-tree.csv`, `pilots/screens-inventory.csv` no están bajo el worktree) |
| 5 discoverability | 232 pantallas · 192/192 · placeholders 16/20 · route-validity OK |
| 6 `build-nav-tree --check` | **ROJO, externo**: `pilots/tanda5-nav-tree.csv` (compartido) recibió a las 23:52 la fila `DocumentCaptureScreen` de la Tanda T9; el JSON del worktree (= HEAD) es el único diff (69 → 70 ítems). Regenerar en fix1 rompería route-validity (la pantalla no existe aquí): regenerar cuando T9 se integre. |
| 7 check-route-access | OK 15 tokens × 192 URLs |
| 8 Cocoa | 232 pantallas · style={} **646** (techo 647; −1 en `ReportingCenterScreen.tsx`; las 5 líneas de `PayrollScreen.tsx` son las mismas 5 reindentadas → 0 nuevos) · rawTables 1 · inputs crudos 0 · colores literales 0 · waves «§6 al día» · contrato 18/18 (inventario regenerado en la pasada de corrección) |
| 9 build | 941 módulos · 2,58 s · 3 avisos «Circular chunk» preexistentes |
| 10 integración completa | **809 · 799 pass · 2 fail · 8 skip** — los 2 fallos (`pms-shadow-routes.test.mts:430`, `pms-shadow-sync.test.mts:225`) son un flake de ventana horaria: la puerta cruzó las 00:00 CEST y ambos tests calculan «hoy» en UTC mientras el servidor usa Europe/Madrid (`pms-shadow.service.ts:860`); ficheros idénticos a HEAD; reproducido a las 00:04. **Repetir fuera de 00:00–02:00 CEST.** En la ronda 1 (22:00) la misma puerta dio 0 fail. |
| 11 worker | 34/34 |
| 12 `rbac:sync --dry-run` | 250 claves · +0 · 0 stale · 0 behind · aviso preexistente `reservationist` (org_c) |
| 13 migrate status + drift | 20 migraciones al día · «No difference detected» (nota: los scripts raíz `db:migrate:status`/`db:drift:check` invocan `pnpm` a secas y fallan sin PATH; ejecutar `corepack pnpm --filter @hotelos/database …`) |
| 14 git status | 94 M + 23 ?? · +4.902/−651 · lockfile = baseline previo · 0 dependencias añadidas · 0 ficheros fuera de los lotes |
| 15 brand-contract | 10/10 |

Logs de ambas rondas: `scratchpad/gate{1..15}-*.log`, `scratchpad/gates/01-typecheck.log … 15-brand.log`, `10b-pms-shadow-rerun.log`, `nav-tree.fresh.json`.

## 3. Hallazgos del corrector: confirmados, refutados y corregidos

### 3.1 Confirmados (11) y estado

| Id | Sev. | Hallazgo | Estado |
|---|---|---|---|
| F2-IMPORT-ISP-DEFAULT | alta | `regimeOfVatRow` clasificaba `isp` CUALQUIER emitida sin NIF cuando el fichero no trae clave/calificación (ventas a particulares de cargas futuras fuera del régimen general). | **Corregido**: sin clave ni calificación → `null`; 09 → `isp`; 01 sin NIF → `null`. Tests posting. |
| F2-RECLASSIFY-RULE1-AMOUNT-PAIRING | media | Regla 1 emparejaba por (periodo, base, cuota) y etiquetaba toda pareja como `aib` (2025-Q1: 66 AIB clave 09 + 10 ISP «S» CH/CO). | **Corregido**: pareja por periodo + nombre normalizado + base + cuota, recibida CON NIF extranjero; prefijo UE → `aib`, no UE → regla 4 `isp`. Dry-run 2025-Q1 66 + 10 exacto. |
| F2-IMPORT-NO-ISP-RECIBIDAS | media | El importador nunca producía `isp` en recibidas e ignoraba la columna AEAT «Inversión del Sujeto Pasivo». | **Corregido**: columna opcional `inversion_sujeto_pasivo` (S/N) con sinónimos; S → `isp` antes que la clave. Fixture AEAT y runbook §2.4. |
| F1-CLASSIC-VS-ANNUAL-TOTALS / SEC-05 | media | Cuentas anuales 26.501.131,97 ≠ balance clásico 26.456.572,96 (474/566/163) y aviso de la 478. | **Corregido parcialmente**: 478 con epígrafe C.IV (sin avisos); divergencia documentada con cifras (runbook §9/§19.9). Unificar exige corregir `Account.kind` en el plan (decisión §8). |
| F11-EMPLOYEE-NAMES-STILL-EXPOSED | media | 37 terceros con nombre invertido de empleado servidos con nombre hasta el apply de F12. | **Corregido**: nombre solo con NIF de persona jurídica; 36/37 salen `null`, la restante (CIF) la cubre el apply. |
| F6-MAIN-TREE-STALE-JS | media | El árbol principal conserva 58 `.js` obsoletos: `:5173` y la guarda nueva en rojo tras el merge. | **Pendiente (acción de merge, §6)**: prohibido tocar el árbol principal desde la tanda. |
| MERGE-CONFLICTS-DESIGN-DOCS | media | main avanzó (`f77820d`, merge de UX-1): `cocoa-22-inventory.json` y `COCOA-22-MIGRACION.md` conflictúan (3-way); `HousekeepingDashboard.tsx`, `pmsCommerceApi.ts`, `server.ts` aplican limpio. | **Pendiente (acción de merge, §6)**: regenerar inventario y repetir puertas 8 y 9 en main. |
| SEC-01 | alta | 110 = saldo 4700 de asientos contabilizados + saldo inicial: un periodo sin asiento no descuenta su 78 ni arrastra su «a compensar» (2026: Σ78 3.018,42 > 2.948,03; Q2 ignoraba −52.050,57 de Q1). | **Corregido**: 110 encadenada desde el 303 calculado del periodo anterior sin asiento (§1.3). Ver decisión §8.3: contradice en parte la refutación F3-COMPENSATION-CHAIN. |
| SEC-02 | media | `GET /webhooks/subscriptions` sin `propertyId` devolvía TODAS las organizaciones (fuga latente; hoy solo 2 filas de org_123). | **Corregido**: filtro por organización + test (2b). |
| SEC-04 / R5 | media | `docs/api-contracts.md` sin las 6 rutas nuevas ni los campos/flags. | **Corregido**: bullet «Tanda FIX-1» (reclassify, periods, `informativo=1`, `liquidacionesHistoricas`, 110 encadenada, `openingCompensation*`, `regime`/columnas opcionales, casillas por régimen, download high, staff-profiles, third-parties, webhooks por organización, `reservationCode`/`roomNumber`). |

Otros hallazgos menores corregidos en la misma pasada: F2-RULE-DTO-COUNTS (`regla 1|2|3|4`, `filasEmitidas`/`filasRecibidas`), SEC-08 (ISP soportada deducida en 28/29 y 38/39 = AIB de bienes de inversión, como el formulario oficial; aviso «validar 38/39» retirado), F3-GUESSED-AVISO-NOISE (aviso «heredados sin tipo» cuenta solo `rate_source description/none`: 2025-Q4 1 apunte en vez de decenas de miles), F8-TODAY-ROUTING-OVERREACH, SEC-06 (download `high` + ámbito), F1-UNAWAITED-REJECTS (`await assert.rejects`), F5-EXPORT-STORE-IN-MEMORY (documentado), F7-LEGACY-HEADER-LITERAL (manual sin el literal de la cabecera), R2 (runbook §18: el modo demo sirve lecturas medium sin sesión), `api-reference` (segmentos «staff-profiles», «third-parties», «exports», «reclassify» en español; era el único fallo de la suite API), `ledger-import.test.mts:632/650` (clave con fecha de recepción), `structure-l5.test.mts:841` (71 = 27 − 45 − 78).

### 3.2 Refutados (4) — con lo que sí queda de cada uno

| Id | Veredicto | Residuo |
|---|---|---|
| F3-COMPENSATION-CHAIN | Refutado: 87 = 110 − 78 es la instrucción oficial; leer la 110 del saldo de 4700 (liquidaciones nativas + LIQUI de Sage + saldo inicial) es el diseño documentado y el remedio B-2; un arrastre calculado sin asiento diverge de la cadena presentada en 3 de 4 transiciones de 2025. | La pasada de corrección aplicó de todos modos SEC-01 (encadenado SOLO entre periodos sin asiento; se detiene en el último liquidado, así que la invariante 4700 = 110 se conserva cuando los periodos se liquidan). Falta un aviso en el 303 cuando el periodo anterior no tiene asiento (el 390 sí lo emite) y una frase en runbook §11.2. Decisión §8.3. |
| F11-NATURAL-PERSONS-LISTED | Refutado: 9.988 terceros con DNI/NIE ya se servían con el mismo nombre y NIF en libros de IVA, 347 y mapa de cuentas bajo la misma clave; el brief solo excluye empleados. | La pasada de corrección fue más allá de lo refutado: ahora el directorio oculta TODA persona física (autónomos incluidos). Decisión §8.4. |
| SEC-03 | Refutado: la audiencia efectiva es `accounting.reports.read` (no `accounting.read`); sin exposición nueva. | Residuo real distinto: el endpoint **no aplica `assertFinanceReadScope(context, null)`** (regla R11 que sí aplican libros, 347 y diario): plantillas con `reports.read` sin `accounting.entity.read` (manager, admin_clerk: 5 usuarios de Faranda con ámbito de centro) listan terceros de toda la sociedad. Cambio de una línea, pendiente (§6). |
| R1 | Refutado: 200 sin token con `HOTELOS_ALLOW_DEMO_AUTH=true` es el contrato H1 del fallback demo para lecturas medium (8 descargas hermanas en medium). | La pasada de corrección subió igualmente la ruta a `high` (SEC-06): queda como la única descarga `high` del manifiesto. Decisión §8.5. |

## 4. Migraciones y copias

| Migración | Contenido | Copia previa |
|---|---|---|
| `20260920100000_iva_regimen` (F2) | `CREATE TYPE VatBookRegime`; `vat_book_entries.regime` nullable; índice `(organization_id, book, regime)`. Reverso documentado en el SQL. | `~/anfitorio-demo/backups/hotelos-pre-fix1-20260919-192956.dump` (75,9 MB) |
| `20260920110000_iva_compensacion_inicial` (F3) | `vat_settings.opening_compensation DECIMAL(12,2) NOT NULL DEFAULT 0`, `opening_compensation_period TEXT`. | `~/anfitorio-demo/backups/hotelos-pre-fix1-f3-20260919-195746.dump` (75,9 MB) |

Ambas aditivas, verbatim de `migrate diff`, sin backfill; `db:migrate:deploy` en local → 20/20; `db:drift:check` 0; `scripts/check-migrations-vs-schema.mjs` OK. **El árbol principal (`:3000`) opera sin problema con la BD migrada** (columna nullable / con DEFAULT; su cliente Prisma no expone `regime` ni `openingCompensation*`), pero su `db:drift:check` marcará drift hasta el merge.

## 5. Datos escritos durante la tanda (fuera de tests aislados)

- **Faranda: 0 escrituras** (todo GET/SELECT; dry-runs sin apply; el PUT de F3 fue uno inválido rechazado con 400).
- **org_123 (demo), por acciones de producto del brief:** orden `cmu80n0gt014gfygbc8ar1c3n` en `in_progress` con `assignedTo` = usuario demo de recepción (F9); una tarea HK pendiente → en curso y una en curso → hecha (F9); `staff_profiles cmu8rc88m000ify208o377p6o` (MANUAL-F10-001) + `employment_contracts cmu8rcsv2000jfy20frpek9e7` (1.800 €, activo) (F10); 4 `audit_events STAFF_PROFILE_CREATED` (cadena global, no borrable); exportaciones en memoria (caducadas); suscripción de webhook de prueba creada y borrada (F7). Efecto colateral: `structure-l5.test.mts:867` salta porque org_123 no está en reposo (`invoices 10 / supplierBills 2 / vatBookEntries 1` frente a 8/1/0) — R3, acción del orquestador.
- **Sesiones inyectadas en `localStorage` de `localhost:5187`** por F8/F9 (token del usuario demo) no retiradas; F7/F11 sí limpiaron.

## 6. Acciones de merge (orquestador) y pendientes

1. **Commit sin `pnpm-lock.yaml`** (diff previo a la tanda, §0). Incluir los 23 ficheros sin seguimiento (§9) y este informe.
2. **Árbol principal tras el merge:** `git -C ~/anfitorio-demo/hotelos clean -fdX packages/*/src` (solo ignorados: 58 `.js` obsoletos) → reiniciar Vite `:5173` y API `:3000` → `node --test tests/shared-source-shadow-contract.test.mjs` en main; `corepack pnpm --filter @hotelos/database db:generate` (cliente con `regime`/`openingCompensation*`) y `db:drift:check` 0.
3. **Conflictos previstos con `f77820d` (merge de UX-1):** `docs/design/cocoa-22-inventory.json` y `docs/design/COCOA-22-MIGRACION.md` → resolver regenerando (`node scripts/cocoa-22-inventory.mjs` + `cocoa-22-waves.mjs`) y repetir puertas 8 y 9 en main.
4. **nav-tree:** `nav-tree.generated.json` queda desfasado por la fila `DocumentCaptureScreen` de T9 en el CSV compartido: regenerar cuando T9 se integre (no en fix1).
5. **Integración completa** repetida fuera de la ventana 00:00–02:00 CEST (2 flakes de `pms-shadow-*` por «hoy» en UTC frente a Europe/Madrid; preexistente, fuera de la tanda; candidato a arreglo aparte: calcular `TODAY` en la zona del hotel en los dos tests).
6. **Apply de F2** (`POST /fiscal/vat-books/reclassify`) y **apply de F12** (`remask-pii.ts`): §7. Hasta entonces `/fiscal/regime` 2025 sigue en «gran empresa», la columna «Régimen» de libros muestra «—» en las 53.291 filas Sage y `GET /accounting/ledger-imports/account-map` sigue exponiendo los 2 títulos 465.
7. **Residuo SEC-03 (R11):** añadir `assertFinanceReadScope(context, null)` en `listLedgerThirdParties` (`ledger-third-parties.service.ts`) + un caso en `ledger-import-routes.test.mts` con usuario de ámbito de centro sin `accounting.entity.read`.
8. **F7 residuos:** `POST /webhooks/subscriptions` no valida que el `propertyId` del body sea de la organización (un `findFirst`); `deleteSubscription` no borra `webhook_deliveries` (sin cascade).
9. **F2 residuos:** 6 recibidas F5 de 1T25 (una de 7.012,52) y 2 de 2T25 (10.626,38) con número solo alfabético sin DUA/MRN → decidir regla adicional o lista manual; regla 1 por debajo del memo en 2T25 (−1.671,43) y 4T25 (−2.375,71) por recibidas sin emitida pareja; emitidas con calificación S2 (casilla 122) sin valor en el enum; filas `bienes_inversion` con `aib/isp/importacion` simplificadas a 36/37, 28/29 y 32/33.
10. **F3 residuos:** `structure/VatAndFiscalYearTab.tsx` (PUT parcial de `vat-settings`) no muestra `openingCompensation*` (el alta vive en `AccountingSettings.tsx`); `periods.latest` = «2026-Q3» (trimestre en curso con 37 filas nativas; sigue en «No cuadra» por 17.872 apuntes Sage de Q3 sin libros de Q3, preexistente); las rutas heredadas `/accounting/reports/modelo-303` de `server.ts` no admiten `informativo=1`; el 390 calcula cada trimestre sin las contrafilas `#sustituida` en memoria (pendiente fin 2026 9.235,38 frente a 9.239,93 del 303; preexistente); aviso 303 cuando el periodo anterior no tiene asiento (§3.2).
11. **F4 residuos:** `parseRateCode` lee un tipo < 1 como fracción (0,5 % escrito «0,5» → 50 %, heredado, documentado en FAQ); `accountForRate` (`ledger-import.mapping.ts`) sigue dando 472.08 para 7,5 % con `map_by_rate`; los lotes `vat_books` de Faranda conservan `sourceId` sin recepción (reimportar exige revertir antes; sin migración de claves por decisión del brief); textos desactualizados del formato en `schema.prisma:150` (comentario `///`) y `finanzas-contabilidad.md:50` (tabla `VatBookSourceType`): añadir `[:<recepción>]`.
12. **F5:** almacén de exportaciones en memoria (misma instancia; documentado); `riskLevel high` de la descarga (§8.5).
13. **F6:** pareja inicial del comparador ahora Dirección general vs Dirección de operaciones (reversible pasando `initialA/initialB` desde `UsersRolesScreen.tsx:670`); `plan-de-formacion.md:292` («10 plantillas» en «Invitar con ámbito») era el mismo síntoma del `.js` obsoleto: revisar tras limpiar main.
14. **F8:** `get_compliance_summary` devuelve «no hay datos disponibles» en la demo (preexistente).
15. **F9:** pestaña «Incidentes» de /hoy/operaciones sigue con códigos crudos (fuera del brief); `listReservationGuestRegisterRecords`/`getGuestRegisterRecord` no resuelven `reservationCode` (la pantalla no lo necesita).
16. **F10:** la plantilla `payroll_hr` (13 claves) no tiene `users.read` → el selector «Persona» del cajón queda vacío con esa plantilla (decidir: añadir `users.read` a la plantilla o exponer personas del centro bajo `payroll.read`); `hourlyCost` viaja como texto «12.50» y `grossSalary` como número; sin atajo para crear la ficha desde «Nuevo contrato» (evitado anidar dos `CocoaDrawer`).
17. **F11:** `q` casa `name` también en filas cuyo nombre se retiene (revela la existencia de la coincidencia); `lote` es `null` para los ~18.000 clientes creados por los libros (por diseño; enlazar por `(book, counterpartyNif)` sería una consulta adicional no pedida); semántica de `limit` (recorta a 200) distinta de las otras listas del fichero (400), documentada en §3.1.
18. **F12:** `vat_book_entries.counterparty_name` con 39 filas de nombre de empleado FUERA del plan (el brief no lista la tabla; añadirla es trivial: `ALLOWED` en `remask-pii.ts` + bloque en `pii_ids.py`); `prep_plan.py` no es reproducible tras la carga (`load_chart()` lee la BD: los 35 «create» ya existen), por eso `plan/` se fusionó sobre el fichero de la carga; tokens de 7 cifras fuera de la serie; ventana mínima de la regla de tokens puede tragar palabras intermedias (sobre-enmascarado preferible a fuga).
19. **Manual: menciones obsoletas de defectos ya corregidos** (fuera de los ficheros exclusivos de los lotes que los corrigieron; lote de docs): `10-direccion.md:309` («Comparar plantillas» falla), `faq.md:423-425`, `50-comercial-revenue.md:464/478` y `plan-de-formacion.md:33/62` («Exportación lista: undefined», llegadas, webhooks, «Tomar»), `fichas/09-…md:15` («Tarea empezar.»), `faq.md:502` («no hay asignación de tareas ni de técnico… desde Pisos y Mantenimiento», parcialmente falso desde F9); las notas de «cajones laterales ocultos» (`20-administracion.md:525`, `faq.md:500`, `10-direccion.md:381`, `50-comercial-revenue.md:469/479`, `plan-de-formacion.md:14/31/37/60/138`) describen un defecto cuyo hotfix está en la base y cuyos cajones F10 abrió y completó en la demo.
20. **Puertos por lote:** cuatro lotes chocaron en `:3917`/`:5187` (mismo worktree, instancias ajenas no tocadas; verificaron en `:3927`/`:3937`/`:3957`/`:5197`); asignar puertos distintos por lote cuando corran en paralelo.
21. **Scripts raíz** `db:migrate:status`/`db:drift:check` invocan `pnpm` a secas («command not found» sin PATH): usar `corepack pnpm --filter @hotelos/database …` o corregir los scripts.
22. `docs/api-contracts.md` y runbooks no documentan el 400 nuevo de `POST /webhooks/subscriptions` con `developerAppId` ajeno ni el fallback por sufijo del guard; `finanzas-contabilidad.md` §13 (tabla de permisos) tampoco lista `GET /accounting/ledger-imports/third-parties`.

## 7. Instrucciones para César

### 7.1 Re-enmascarado de PII en la BD (F12 · `remask-pii.ts`)

Todo desde `~/anfitorio-demo/hotelos/apps/api` (árbol PRINCIPAL; `DATABASE_URL` en `../../.env`). `PREP=~/anfitorio-demo/pilots/faranda-celuisma/sage200-real/prep`. El plan `prep/apply/remask-pii.plan.json` (331 entradas, generado el 19/09 21:31) se aplica por id explícito; `after` es siempre el texto ya enmascarado.

1. **Copia previa:** `pg_dump -Fc "$DATABASE_URL" > ~/anfitorio-demo/backups/hotelos-pre-remask-$(date +%Y%m%d-%H%M%S).dump`.
2. **Antes (solo lectura):** `PGOPTIONS='-c default_transaction_read_only=on' psql "$DATABASE_URL" -X -v ON_ERROR_STOP=1 -f "$PREP/tools/remask-pii.sql"` → esperado: encontrados 331/331, aplicadas 0, pendientes 331, títulos 465 con sufijo ≥ 0100 sin token = 2, mapping del lote plan = 2, eventos `LEDGER_PII_REMASKED` = 0. (El helper `scratchpad/sage-verif/C/psql-ro.sh` que cita la cabecera del SQL es el mismo comando; el scratchpad es efímero.)
3. **Dry-run:** `node --env-file-if-exists=../../.env --import tsx "$PREP/tools/remask-pii.ts" --dry-run` → esperado: `pendientes=331 ya_aplicadas=0`, 0 ausentes / otra organización / inválidas, exit 0.
4. **Parar el API `:3000`** (o reiniciarlo después: la cadena de auditoría en memoria diverge si sigue arrancado).
5. **Apply:** `node --env-file-if-exists=../../.env --import tsx "$PREP/tools/remask-pii.ts" --apply --confirm cmrhw9jy30002fyvb6tsdiugt` → UNA transacción; si alguna fila no casa (`count !== 1`) hace rollback y sale con 1. Exit 0 = aplicado y auditado (`LEDGER_PII_REMASKED`, `entityType ledger_import`, `entityId` = lote plan, `afterJson` = recuentos).
6. **Después:** repetir el paso 2 → aplicadas 331/331, pendientes 0, títulos 465 sin token 0, eventos 1. Repetir el paso 3 → `ya_aplicadas=331`. `GET /accounting/ledger-imports/account-map` ya sin los 2 títulos con nombre; `GET /accounting/ledger-imports/third-parties?q=EMPLEADO` incluye las 37 filas del plan sin nombre. `prep/apply/remask-pii.log` registra fecha, modo, recuentos e ids (nunca textos).
7. **Reiniciar el API `:3000`.**
8. Opcional (decisión pendiente §6.18): `vat_book_entries.counterparty_name` (39 filas) no está en el plan; si se decide incluirla, regenerar el plan con `pii_ids.py` tras añadir la tabla en `ALLOWED` y repetir 2-7.

### 7.2 Reclasificación de los libros de IVA ya cargados (F2 · `POST /fiscal/vat-books/reclassify`)

Sesión de `contabilidad@faranda.test` (o cualquier cuenta con `accounting.configure`; ruta `high`: sin token responde 401). Copia previa recomendada como en 7.1 (la acción es reversible solo por SQL: `UPDATE vat_book_entries SET regime = NULL WHERE organization_id = … AND source_type = 'sage200'`; no hay endpoint de reverso).

1. **Dry-run 2025:** `POST /fiscal/vat-books/reclassify` body `{"period":"2025"}` → esperado (cifras de la última pasada): regla 1 `aib` 244 parejas, regla 4 `isp` 66 parejas, regla 2 `importacion` 15 filas (28.128,52), regla 3 0 (sin `includeZeroRate`), sin pareja 2.064, `actualizadas 0`. Comparar con el informe externo §15.5 antes de aplicar (ver §6.9: 2T25/4T25 quedan por debajo del memo; 8 DUA alfabéticos sin clasificar).
2. **Apply 2025:** `{"period":"2025","apply":true}` → auditoría `VAT_BOOKS_RECLASSIFIED` (`entityId <org>:2025`, recuentos). Solo toca filas `sage200` con `regime IS NULL` (idempotente: un segundo apply actualiza 0).
3. **Dry-run y apply 2026:** `{"period":"2026","includeZeroRate":true}` → regla 1 129 · regla 4 48 · regla 2 10 (34.679,15) · regla 3 7 filas (656.000 + 132.000, solo con `includeZeroRate`); luego `{"period":"2026","apply":true,"includeZeroRate":true}` si se confirma la regla 3 con la gestoría.
4. **Después:** `GET /fiscal/regime?year=2025` → volumen ≈ 5.584.379,55 (< 6.010.121,04 → propuesta «general», `cambia` según lo configurado); `GET /fiscal/models/390?year=2025` → `AUTOFACTURAS_ISP_AIB_EXCLUIDAS` ≈ 567.603,29; `GET /fiscal/models/303?period=2025-Q1` → casillas 10/11 (AIB) y 12/13 (ISP) informadas, 27 y 45 invariantes, `cuadra` del cotejo sin empeorar (las autofacturas siguen en la vista 477 por tipo); Libros de IVA con «Régimen» relleno en las filas afectadas.

## 8. Decisiones abiertas

1. **Apply de reclasificación (F2)** con el plan actual o tras ampliar reglas (DUA alfabéticos, recibidas sin pareja, S2/casilla 122).
2. **Apply de re-enmascarado (F12)** y si se añade `vat_book_entries.counterparty_name` al plan.
3. **Semántica de la casilla 110 (SEC-01 frente a la refutación F3-COMPENSATION-CHAIN):** el código encadena el 303 calculado entre periodos SIN asiento de liquidación (2026-Q2 pasa de 110 = 2.948,03 a 54.998,60; 390 2025 `resultadoLiquidaciones` 26.114,34 → −17.093,75). Alternativa: mantener 110 = saldo de 4700 y exigir liquidar cada periodo (`POST /fiscal/vat-settlement`), con aviso. Confirmar con la gestoría cuál es la cadena que se presenta.
4. **Directorio de terceros:** tras el corrector solo los NIF de persona jurídica muestran nombre (17.195/21.784 sin nombre, autónomos incluidos); el brief solo exigía ocultar empleados y libros/347/mapa siguen mostrando esos nombres bajo la misma clave. Mantener, volver a la regla del brief, o unificar la política en libros y mapa.
5. **`GET /reports/exports/:exportId/download` en `high`** (única descarga `high` del manifiesto; el resto medium/low): mantener o alinear con la convención H1.
6. **Balance clásico vs cuentas anuales (44.559,01):** corregir el `kind` de 474/566/163 en el plan de Sage (`PATCH /accounting/chart/:code`) o dejar la divergencia documentada.
7. **Tokens de 7 cifras** («EMPLEADO 0040312») fuera de la serie 0001…1042 (consumidores compatibles) o renumerar (rompería la serie ya cargada).
8. **Datos demo de F10** (ficha + contrato de 1.800 €): conservar (útil para formación) o retirar por producto/SQL con id explícito antes de «Recalcular» 2026-09. **Nota:** `POST /payroll/contracts/:id/deactivate` revoca las asignaciones RBAC del usuario demo → NO recomendable.
9. **Pareja inicial de «Comparar plantillas»** (Dirección general vs Dirección de operaciones) o la anterior vía `initialA/initialB`.
10. **`payroll_hr` sin `users.read`** para el selector «Persona» del cajón de ficha.
11. **Trimestre por defecto del 303:** último con libros materializados (hoy el trimestre en curso 2026-Q3, «No cuadra» por causa preexistente) o último trimestre cerrado.
12. **Recarga futura de libros** con `sourceId` nuevo (F4): revertir lotes antiguos antes, o migración de claves (no pedida).

## 9. Ficheros tocados (worktree, sin `pnpm-lock.yaml`)

**Modificados (93 del repo):** `apps/admin-web/src/layouts/{setup-banner.ts,__tests__/setup-banner.test.mts}` · `screens/AccountingSettings.tsx` · `screens/accounting/{Sage200ImportScreen.tsx,sage200-import-helpers.ts,__tests__/sage200-import-helpers.test.mts,__tests__/sage200-import-screen-contract.test.mts}` · `screens/compliance/GuestRegisterSettingsScreen.tsx` · `screens/developer/WebhooksAdminScreen.tsx` · `screens/fiscal/{FiscalModelReport.tsx,VatBooksScreen.tsx,fiscal-shared.ts,__tests__/fiscal-shared.test.mts}` · `screens/operations/{HousekeepingDashboard,MaintenanceMobileScreen,OperationsDirectorScreen}.tsx` · `screens/payroll/{PayrollScreen.tsx,__tests__/payroll-cost-screen-contract.test.mts}` · `screens/reports/{ReportingCenterScreen.tsx,reporting-center-rows.ts,__tests__/reporting-center-rows.test.mts}` · `screens/users/{AssignmentDrawer.tsx,RoleComparePane.tsx,users-rbac.ts,__tests__/users-rbac.test.mts}` · `services/{fiscalApi,guestRegisterApi,ledgerImportApi,payrollApi,pmsCommerceApi}.ts` · `apps/api/src/lib/tenancy.ts` · `security/route-permissions.ts` · `server.ts` · `modules/accounting/{fiscal-route-permissions.partial.ts,fiscal-year.service.ts,fiscal.routes.ts,ledger-import-route-permissions.partial.ts,ledger-import.routes.ts,modelo-303.service.ts,modelo-390.service.ts,vat-books.service.ts,vat-settlement.service.ts}` · `accounting/__tests__/{fiscal-regime,modelo-303,vat-books}.test.mts` · `accounting/import/{ledger-import.canonical,ledger-import.posting,ledger-import.service,sage200.parser}.ts` · `accounting/import/__tests__/{fiscal-year-import-close,ledger-import-canonical,ledger-import-posting}.test.mts` + `fixtures/sage200-fixtures.mts` · `modules/assistant/assistant.tools.ts` · `modules/compliance/{compliance.service.ts,__tests__/guest-register-ensure.test.mts}` · `modules/dashboards/operations-director.service.ts` · `modules/developer/{api-reference.service.ts,__tests__/api-reference.test.mts}` · `modules/financial-statements/{annual-accounts.service,source}.ts` + `__tests__/{annual-accounts.test,memory-source,source.test}.mts` · `modules/payroll/route-permissions.partial.ts` · `modules/reporting/reporting.service.ts` · `modules/webhooks/webhooks.service.ts` · `docs/api-contracts.md` · `docs/design/{COCOA-22-MIGRACION.md,cocoa-22-inventory.json}` · `docs/manual/{00-primeros-pasos,10-direccion,30-rrhh,40-pisos-mantenimiento,50-comercial-revenue,60-sistemas,70-recepcion,README,faq}.md` · `docs/manual/formacion/{plan-de-formacion.md,fichas/08-alta-de-usuario.md,fichas/12-parte-de-viajeros.md}` · `docs/runbooks/{finanzas-contabilidad,finanzas-importacion-sage200}.md` · `packages/database/prisma/schema.prisma` · `packages/shared/src/{fiscal-types,ledger-import-types}.ts` · `tests/integration/{fiscal-models,l2-modulos-plataforma,ledger-import-routes,ledger-import,payroll-cost-routes,structure-l5}.test.mts`.

**Sin seguimiento (23 + este informe):** `apps/admin-web/src/screens/developer/__tests__/webhooks-screen-contract.test.mts` · `screens/operations/{housekeeping-task-actions,operations-director-labels}.ts` + `__tests__/{housekeeping-task-actions,maintenance-take-contract,operations-director-labels}.test.mts` · `screens/payroll/staff-profile-form.ts` + `__tests__/staff-profile-form.test.mts` · `apps/api/src/modules/accounting/__tests__/{vat-book-periods,vat-books-reclassify}.test.mts` · `accounting/vat-books-reclassify.service.ts` · `accounting/import/ledger-third-parties.service.ts` + `__tests__/ledger-third-parties.test.mts` · `modules/assistant/__tests__/assistant-router.test.mts` · `modules/dashboards/__tests__/operations-director-rooms.test.mts` · `modules/payroll/staff-profiles.{routes,service}.ts` + `__tests__/staff-profiles.test.mts` · `modules/reporting/__tests__/report-export-download.test.mts` · `packages/database/prisma/migrations/{20260920100000_iva_regimen,20260920110000_iva_compensacion_inicial}/migration.sql` · `tests/integration/reports-export-download.test.mts` · `tests/shared-source-shadow-contract.test.mjs` · `docs/audits/TANDA-FIX1-CORRECCIONES-2026-09-19.md`.

**Fuera del repo (F12):** `pilots/faranda-celuisma/sage200-real/prep/tools/{prep_common.py,test_prep_common.py,check_pii_csv.py,pii_ids.py,remask-pii.sql,remask-pii.ts,remask-pii.test.mts}` · `prep/plan/{plan-celuisma.csv,plan-stats.txt}` · `prep/saldos/*` (21 CSV, `prep-saldos.log`, `verificacion-saldos.json`) · `prep/apply/remask-pii.{plan.json,log}`.

## 10. Mensaje de commit propuesto (sin `pnpm-lock.yaml`)

```
fix(tanda-fix1): correcciones de producto tras la carga Sage 200 y el manual

F1 · cuentas anuales: la apertura importada fechada en `from` cuenta como saldo
inicial (modo opening_in_period + mergeBalanceRows); balance 2025 cuadra y el
ECPN reconcilia; epígrafe C.IV para la 478; divergencia con el balance clásico
documentada (runbook §9/§19.9).
F2 · régimen de IVA en libros: enum VatBookRegime + columna nullable (migración
20260920100000_iva_regimen), clave_operacion/calificacion/inversion_sujeto_pasivo
opcionales en el canónico y el parser AEAT, casillas 10-13/28-39/120-123 en el
303, volumen del 390 y /fiscal/regime sin autofacturas, acción
POST /fiscal/vat-books/reclassify (dry-run por defecto, apply auditado).
F3 · modelo 303: casilla 110 desde el saldo de 4700 + saldo inicial configurable
(migración 20260920110000_iva_compensacion_inicial) y encadenada entre periodos
sin asiento; liquidaciones históricas de Sage; vista mensual informativa;
GET /fiscal/vat-books/periods; aviso de centros sin delegación; cotejo
libro↔diario agregado en BD sin topes.
F4 · importador: parseRateCode conserva decimales; sourceId de recibidas con
fecha/nº de recepción; closed_by = actor o import:<lote>.
F5 · centro de informes: exportación servida por
GET /reports/exports/:exportId/download (almacén en memoria, 15 min) y
descarga inline en la pantalla.
F6 · comparar plantillas tolera mapas incompletos; guarda
shared-source-shadow-contract contra .js compilados que sombrean .ts.
F7 · webhooks: suscripciones creadas desde la pantalla resolubles por el guard
(propiedad, app o app_<org>), listado por organización, app ajena → 400,
placeholder y aclaración de la cabecera de firma.
F8 · asistente: enrutado por palabras completas sin orden ni signos; llegadas y
salidas de hoy enrutan; fechas distintas de hoy no.
F9 · pisos y mantenimiento: «Tomar» asigna al usuario, toasts y concordancia
correctos, números de habitación, etiquetas y código de reserva en vez de ids.
F10 · RRHH: GET/POST /payroll/staff-profiles y cajón «Nueva ficha»; selector de
fichas en «Nuevo contrato»; promesa de 2FA retirada del manual y del cajón.
F11 · directorio «Terceros» (GET /accounting/ledger-imports/third-parties,
paginación keyset, nombre solo para NIF de persona jurídica) como pestaña de
Importar desde Sage 200.
Corrector · ISP nunca por defecto al importar; regla 1 por proveedor y NIF UE/no
UE; 38/39 = AIB de bienes de inversión; descarga de informes en high con ámbito;
api-reference en español para las rutas nuevas; api-contracts, runbooks y manual
actualizados; inventario Cocoa regenerado (style= 647 → 646).
Informe: docs/audits/TANDA-FIX1-CORRECCIONES-2026-09-19.md (apply de
reclassify y remask-pii pendientes de decisión).
```
