# Tanda CHK · Check-in automatizado y recepcionista IA — informe de cierre (2026-09-19 → 2026-09-20)

**Encargo:** `docs/design/CHECKIN-AUTOMATIZADO-IA.md` (flujo pre-llegada → identidad y parte de viajeros → firma → pago o garantía → asignación con motivo → llave; recepcionista IA como bot del huésped y copiloto con matriz de riesgo; modelo, API, permisos, privacidad; front, portal, kiosco) sobre el worktree `~/anfitorio-demo-wt-chk/hotelos` (rama `tanda-chk`, base `f77820d` = main con L3, L5, L6a, TL, OPERA, T8, DOC-1, UX-1 y `gates.sh`; BD propia `hotelos_chk`, copia completa de Faranda + OPERA; puertos `:3919/:5189`). Plan: `scratchpad/CHK/recon-delta.md` (recon delta sobre el dosier del 2026-09-18) → 5 olas · 19 lotes con ficheros exclusivos → puerta rápida por ola → 3 revisores (funcional/runtime, seguridad-privacidad-RBAC, regresiones y honestidad) → dedupe → corrector → puerta completa → este informe. `main` está hoy en `f2ac4f7` (5 commits por delante de la base: `60d870e` bloques de estado fuera de CLAUDE.md, `1f06771` `merge-lane.sh`, `242be35`/`f2ac4f7` FIX-1, `90ec572` DOC-2).

**Working tree SIN commit** (`git -C ~/anfitorio-demo-wt-chk status --porcelain -uall`): 141 entradas = 86 `M` + 55 `??` (con `-uall`, **92 ficheros nuevos**); sin `hotelos/pnpm-lock.yaml`: **85 modificados (+11.113 / −2.092)** y **92 nuevos (31.231 líneas)**. `pnpm-lock.yaml` aparece `M` (+74/−25) desde ANTES de la tanda (W1-A lo anotó al empezar) y ningún lote lo tocó: **fuera del commit** (`git checkout -- pnpm-lock.yaml` en la fusión, como hace `merge-lane.sh`). El integrador (este informe) solo ha ejecutado `git status/diff/log`, lecturas, los contratos que leen `CLAUDE.md`/`docs/audits` y una puerta rápida final (§3); no ha arrancado instancias ni ha escrito en la BD; los únicos ficheros que ha editado son este informe y `CLAUDE.md` (bloque compacto «Estado verificado (Tanda CHK)» + una línea en «Docs prioritarios»).

**Datos:** toda verificación con escritura se hizo en el tenant aislado `org_chk` / `prop_chk` (`packages/database/prisma/seed-checkin.ts`, `db:seed:checkin -- --reset`, usuarios `recepcion|jefe.recepcion|direccion@chk.test`, contraseña `chk-demo`) o en tenants `org_l2_*` que las suites de integración crean y destruyen; Faranda solo lectura (invariantes idénticas en cada lote: 13.457 reservas · 13.436 huéspedes de `org_faranda`, y los totales de la BD solo crecen por `org_chk`). Los nombres de los fixtures son sintéticos (nombre genérico + letra griega, especímenes ICAO Doc 9303 con titular ficticio, `PRUEBA/ANNA`); ningún nombre de persona real en código, tests, capturas, logs ni en este informe.

## 0. Resumen

- **Entregado:** módulo `apps/api/src/modules/checkin/*` (sesión de pre-check-in, captura de identidad MRZ + visión opcional, firma con evidencias y PDF del parte, OTP, pago/garantía, llegada única para huésped/kiosco/recepción con SES + llave + bienvenida, jobs del líder, kiosco, bot del huésped, contexto de servicio), motor de asignación explicable (`modules/pms/room-assignment.*`), parser MRZ ICAO 9303 (`packages/compliance/src/spain/mrz.ts`), 9 tablas nuevas + 1 columna, 44 rutas nuevas en el manifiesto (0 claves RBAC nuevas), 4 plantillas de sistema `checkin_*`, webhook de WhatsApp, front de recepción (Mi día, cola, cajón de check-in, cajón de pre-check-in, pestaña `/hoy/check-in-automatizado`), portal guest-web (asistente de 6 pasos, chat, llegada, modo kiosco), seed `org_chk`, runbook, contratos, OpenAPI.
- **Revisión:** 20 hallazgos confirmados (8 high · 12 medium), 0 refutados; el corrector cerró los 20 y 10 low más; 2 low quedan sin cambio de código con motivo (§4).
- **Puertas finales** (2026-09-20 08:17, `scratchpad/CHK/gates-final.json`): **11/14** en verde; las 3 rojas (admin-web unit 28 fail · contratos raíz 2 fail · discoverability 4 enlaces) tienen una única causa ajena al carril: 4 filas «nueva (Tanda T9)» en el CSV compartido de navegación sin componente aquí; contrafactual sin esas filas: 14/14 (§3).
- **Fusión:** `main` ya movió los bloques de estado a `docs/audits/ESTADO-VERIFICADO.md` y trae `merge-lane.sh`; el carril deja `CLAUDE.md` = main + deltas CHK y el bloque completo en `ESTADO-VERIFICADO.md` (fichero nuevo aquí; `merge-lane.sh` concatena). Orden recomendado: T9 → CHK (o regenerar `nav-tree.generated.json` tras fusionar ambos). Detalle en §7.

## 1. Qué construyó cada lote (qué · cómo · tests · verificación)

Cinco olas con ficheros exclusivos por lote (W1 modelo/MRZ/motor/seed · W2 sesión/captura/firma/asignación/plantillas · W3 llegada/rutas/jobs/cola · W4 front de recepción/portal/kiosco/bot · W5 pagos/seed final/docs). Todas las cifras son las que cada lote dejó en su informe y en `scratchpad/CHK/*`; las puertas por ola en §3.

### W1-A · Modelo de datos, migración, PII y tipos compartidos
- **Qué:** 9 modelos Prisma (`CheckInSession`, `CheckInGuest`, `DocumentCapture`, `Signature`, `AssignmentSuggestion`, `KioskDevice`, `PropertyCheckInPolicy`, `RoomBlock`, `RoomConnection`; 286 modelos / 38 enums, +9 / +0), migración `20260920150000_checkin_automatizado` (306 líneas: 9 `CREATE TABLE`, 14 índices + 3 únicos, 1 FK `checkin_guests.session_id → checkin_sessions ON DELETE CASCADE`, 0 enums, 0 columnas en tablas existentes, reversible), `PII_FIELDS.CheckInGuest` + `LOOKUP_HASH_FIELDS.CheckInGuest` (`packages/database/src/crypto-fields.ts`), `packages/shared/src/checkin-types.ts` (369 líneas: 15 vocabularios `as const`, 12 DTOs, `DOCUMENT_CAPTURE_NON_PII_FIELDS`, `CHECKIN_ERROR_CODES`).
- **Cómo:** `prisma migrate diff --from-schema-datasource` con los DROP IVA ajenos retirados a mano (cuerpo idéntico byte a byte al diff `--from-schema-datamodel`); `db:migrate:deploy` + `db:generate`; decisiones dentro del brief: `@@unique([reservationId])` en sesiones, DTOs sin PII (`documentNumberLast3`, `hasEmail`).
- **Tests:** `tests/checkin-schema-contract.test.mjs` (6; cruza los 15 vocabularios con los `///` del schema y los defaults JSON de la política; localiza la migración por sufijo para sobrevivir a un renumerado) + `tests/migrations-squash-contract.test.mjs` 8/8.
- **Verificación:** `migrate status` 18/18 → 19/19; `db:drift:check` con EXACTAMENTE los 3 ítems IVA preexistentes de otro carril (`scratchpad/CHK/drift-before/after-w1a.txt`, 0 ítems `checkin_*`); `node scripts/check-migrations-vs-schema.mjs` 286/38 OK; typecheck database/shared 0.

### W1-B · Parser MRZ ICAO 9303
- **Qué:** `packages/compliance/src/spain/mrz.ts` (396 líneas, 17 exports, 0 dependencias): `parseMrz` TD1/TD2/TD3 con dígitos de control (documento, nacimiento, caducidad, compuesto, personal), clasificación ESP DNI/NIE/TIE con número y soporte como candidatos, autocorrección `O→0`/`I→1` anotada solo en fechas y DC, siglo, normalización de cadenas de lector (90/72/88 chars); `buildMrz` para fixtures con transliteración P3 §6; sexo en vocabulario SES (H/M/O).
- **Cómo:** puro, sin BD; exportado desde `packages/compliance/src/index.ts`; la suite entra en la lista explícita de `tests/compliance-package-tests.test.mjs` (3 → 4 suites, con aserción de que la suite MRZ se ejecutó).
- **Tests:** `src/spain/__tests__/mrz.test.mjs` 21/21 (Apéndice A de ICAO P3, especímenes oficiales P4/P5/P6 con todos los DC en true como oráculo independiente del generador, DNI/TIE/NIE sintéticos, dígito alterado, corrección O/I, round-trip `buildMrz∘parseMrz`, entradas malformadas sin excepción, siglo y fechas imposibles); paquete 134 (133 pass · 1 skip preexistente node-forge).
- **Verificación:** sin runtime por diseño; sonda desde `src` con el loader strip-types.

### W1-C · Motor de asignación puro + R4 (`matchGuestToReservation` acotada)
- **Qué:** `apps/api/src/modules/pms/room-assignment.engine.ts` (652 líneas): `suggestRooms(input)` con fase A de filtros duros (inactiva, mantenimiento, no vendible, ocupada/OOO/OOS, orden de trabajo abierta, `RoomBlock` solapado, asignada a otra reserva, tipo igual o superior solo con `allowUpgrade`, capacidad, accesibilidad, inspeccionada si la política lo exige, sucia con ETA ≤ 2 h descartada con aviso a pisos) y fase B de puntuación (`DEFAULT_ASSIGNMENT_WEIGHTS`: inspeccionada 30 · limpia 20 · preferencia 15 con tope 45 · VIP 25 · recurrente 20 · grupo 15 · protección de inventario −20 · rotación 5 · mejora gratuita −10), top-3 con motivos en español, confianza y `dataNotes` honestos; `rulesVersion chk-rules-1`. `pms.service.ts` `matchGuestToReservation`: 404 opaco de propiedad y búsqueda del huésped acotada a `organizationId` (+`includeCompanions`).
- **Cómo:** sin Prisma; importa `roomStateOf` (L5) y vocabularios de `@hotelos/shared`; invariante `score === Σ reasons.weight`.
- **Tests:** `__tests__/room-assignment.engine.test.mts` 10 (136 asserts) + `match-guest-org-scope.test.mts` 4 = 14/14; `tests/pms-lifecycle.test.mjs` 5/5.
- **Verificación:** script tsx de solo lectura contra `hotelos_chk` (`scratchpad/CHK/w1c-runtime-match.mts`): documento de otra organización ya no localiza al huésped (404), propiedad inexistente 404, caso propio ok.

### W1-D · Portal L0 + seed `org_chk`
- **Qué:** `guest-portal.service.ts` con proyección ampliada (`propertyId`, `assignedRoomNumber`, `eta`, `primaryGuest {firstName, surname1Initial}`), `normalizeArrivalEta`, `submitPreCheckIn` escribe `Reservation.eta`; guest-web `client.ts` envía `propertyId` (de `?property=` o `VITE_GUEST_PROPERTY_ID`, `config/guest-config.ts`), `SignInPage` con aviso si falta el hotel; `packages/database/prisma/seed-checkin.ts` (tenant `org_chk`/`prop_chk`, 36 habitaciones en 4 plantas con vista/camas/rasgos/accesibilidad, 3 tipos, tarifas 68 días, 11 reservas `CHK-*` relativas a hoy, folios y pagos, 6 módulos, SES sandbox, 3 usuarios), allowlist de `demo-guard.ts`, script `db:seed:checkin`.
- **Cómo:** copia de la estructura de `seed-ux-day.ts` (`assertDemoTarget`, `--reset` acotado a `property_id = prop_chk`, ids fijos, idempotente).
- **Tests:** `tests/seed-checkin-contract.test.mjs` (7 → 11 tras W5-B), `tests/seed-ux-day-contract.test.mjs` (literal de la allowlist), `tests/integration/guest-portal.test.mts` 7/7.
- **Verificación:** seed `--dry-run` / ensure / re-run / `--reset` con invariantes de Faranda idénticas (`scratchpad/CHK/w1d-*.txt`); API `:3919` + Vite `:5189` con curl y navegador (sign-in con `?property=`, proyección, pre-check-in con ETA); detectado el CORS sin `x-guest-token` (cerrado por el corrector, REV3-04).

### W2-A · Sesión de pre-check-in, política, kiosco, contexto de servicio, rutas y config
- **Qué:** `modules/checkin/{service-context,checkin-session.service,checkin-policy.service,kiosk.service,checkin.schemas,checkin.routes,route-permissions.partial,env.partial,checkin-config}.ts`: `CheckInActor` guest/kiosk/system con `CHECKIN_SERVICE_PERMISSIONS` (12) y 7 permisos prohibidos (dinero/override), `paymentLinkServiceContext` solo `payment.capture` (sustituye al super-usuario demo en `POST /guest-portal/session/:token/pay`); `ensureSession`/`inviteReservation` (token opaco `randomBytes(32)` + sha256, TTL salida + 1 día, reenvío revoca, canal email → whatsapp con opt-in → sms, `template_not_found` sin lanzar), `getSessionForToken`, `updateSession` (ETA, preferencias del vocabulario a sesión y perfil, consentimientos), `upsertGuest`/`removeGuest` (menor exige adulto declarante), `applyMrz`, `completePreArrival` (validador SES sin firma → 409 `CHECKIN_INCOMPLETE {missing}`; parte por viajero idempotente), `listArrivals`; política con defaults del diseño; kiosco con código de 8 dígitos hasheado, claim único por `updateMany` condicionado, `deviceToken` solo hash; 9 variables `CHECKIN_*`/`KIOSK_*` leídas solo por `readCheckInConfig`; 18 rutas iniciales en el partial; `PUBLIC_PREFIXES` + 6 resolvers de tenencia.
- **Cómo:** perfiles `Guest` cargados con `findMany` de primer nivel porque la extensión de cifrado no descifra `include` (hallazgo del lote); gate de módulo `guest_self_service` con respaldo a `property_modules` cuando el espejo está vacío (`TENANT_BOOTSTRAP_SKIP`).
- **Tests:** `__tests__/{service-context,checkin-session,kiosk}.test.mts` 29/29; `tests/integration/checkin-session.test.mts` 16/16 (tenant aislado, STRICT_ENV); contratos api-route-permissions / spain-guest-register / env / cors 51/51; `node scripts/env-census.mjs` 167/167.
- **Verificación:** `:3919` como `recepcion@chk.test`: invitar (200, entrega simulada, token en claro solo fuera de producción), portal GET/PATCH/complete 409 con `missing` por viajero, arrivals, policy GET 200 / PUT 403 / kiosks 403 sin `kiosk.configure`; PII en reposo con envelope `v1.` + lookup hash.

### W2-B · Captura de identidad, firma, almacén provisional y PDF del parte
- **Qué:** `identity-capture.service.ts` (758 líneas): `captureDocument` con MRZ de lector/manual siempre delante y visión por `runAiTool` + `llmExtractJsonDocument` SOLO con clave (`mergeIdentityFields` con umbral 0,85, `needsReview` por campo, `identity_mismatch` frente a los nombres de la reserva, caducado = aviso), imagen nunca persistida (`DocumentCapture` con `imageStored false`, `purgeAt` + 30 d, `ID_IMAGE_DISCARDED`), `purgeExpiredCaptures`; `signature.service.ts` (361): `signGuest` (PNG por cabecera, ≥ 8 puntos, ≤ `CHECKIN_SIGNATURE_MAX_BYTES`, menor < 14 → 409 `SIGNATURE_NOT_REQUIRED`, parte incompleto → 409 `GUEST_REGISTER_INCOMPLETE`, sha256, `retentionUntil` 3 años, `markGuestRegisterSigned(signatureId)`); `signature-storage.ts` (`SignatureStorage { put, get }`, `dataUriSignatureStorage` como punto de extensión para T9); `entry-form-pdf.ts` (A4 de 1 página sobre `pdf-writer.ts` con hash/fecha/método de la firma).
- **Cómo:** límites por `readCheckInConfig` inyectados; deps inyectables (`resetIdentityCaptureForTests`).
- **Tests:** `identity-capture.test.mts` 13 + `signature.test.mts` 5 + `entry-form-pdf.test.mts` 3 = 21/21 sin BD ni red.
- **Verificación:** script tsx contra `hotelos_chk` con filas propias creadas y borradas (`scratchpad/w2b-runtime.log`, `w2b-entry-form.pdf` revisado): 3 capturas (MRZ TD1 con 4/4 checks; imagen + MRZ caducada de otra persona → `identity_mismatch` sin vincular; solo imagen sin clave → manual), firma con sha256 y PDF, purga idempotente, 0 números de documento ni base64 en `document_captures`.

### W2-C · Servicio de asignación (lector del motor)
- **Qué:** `modules/pms/room-assignment.service.ts` (992 líneas, 34 exports): `suggestForReservation` (instantánea por propiedad en 8 consultas agregadas + 4 del titular, `quoteAvailability` solo con mejora, ETA = sesión > reserva, preferencias = sesión ∪ perfil; persiste `AssignmentSuggestion` expirando las previas), `confirmSuggestion` (`assignRoom` real, `confirmed|changed`, 409 `ASSIGNMENT_SUGGESTION_DECIDED`, auditoría), `runBatchForDate` (llegadas de mañana sin habitación, VIP primero, una instantánea por lote, ocupación virtual de la primera candidata, `preassign` NO asigna), bloqueos (`ROOM_BLOCK_OVERLAP`) y comunicadas (`ROOM_CONNECTION_EXISTS`).
- **Cómo:** deps inyectables (`AssignmentDeps`), 404 opacos por organización; decisiones de lectura documentadas (protección de inventario solo para tipos distintos, rotación por propiedad, grupo desde la instantánea).
- **Tests:** `__tests__/room-assignment.service.test.mts` 18/18; `tests/integration/room-assignment.test.mts` 8/8 (permisos reales, `accountant` → `PermissionDeniedError`, rendimiento R14: lote de Los Tilos con 92 habitaciones · 41 llegadas en 68–116 ms sin escrituras, límite 3.000 ms).
- **Verificación:** script tsx sobre `prop_chk` (lote de 7 llegadas con notas honestas, confirmación real y deshecha) y Los Tilos solo lectura (`scratchpad/CHK/lt-suggestions.json`: candidatas empatadas por falta de vista/planta/rasgos en los datos reales).

### W2-D · Plantillas, proveedor WhatsApp y bienvenida real
- **Qué:** `system-templates.ts` 2 → 12 entradas (`checkin_invitation` es/en email + whatsapp, `checkin_reminder`, `checkin_welcome` email/whatsapp/sms con aviso de IA art. 50, `checkin_otp` email/sms); `providers/types.ts` con `ProviderTemplateRef`/`ProviderAttachment`; `whatsapp.provider.ts` con `type: "template"` y simulación sin credenciales; `messaging.service.ts` `sendWelcomeMessage` real por el dispatcher (whatsapp → email → sms, idempotente por `welcome:<reserva>:<canal>`, wifi/desayuno desde `PropertyAiSetting.configurationJson.faq`, auditoría `GUEST_WELCOME_MESSAGE_SENT` honesta con `simulated`).
- **Tests:** `notifications/__tests__/checkin-templates.test.mts` 11 + `messaging/__tests__/welcome-message.test.mts` 7 = 18/18; `auth/__tests__/invitations.test.mts` ajustado (`slice(0, 2)`) para admitir las plantillas nuevas.
- **Verificación:** `:3919` + dispatcher real sobre `prop_chk`: 3 entregas `checkin_welcome` `sent` con `errorMessage SIMULADO`, idempotencia comprobada, huésped de otra organización → `failed` sin escritura; `GET /notifications/deliveries` las lista.

### W3-A · Llegada (`completeCheckIn`), OTP y 11 rutas
- **Qué:** `arrival.service.ts` (819 líneas al cierre del lote): precondiciones deterministas en orden (actor/reserva/sesión → ventana ±1 día con override solo de personal → identidad del titular por método admitido → firmas de adultos con `signatureObjectKey` real (prefijo `sig_` = `legacy_signature`, R3) → saldo solo para guest/kiosk → habitación lista o reasignación de la misma categoría o 409 `ROOM_NOT_READY` con `etaReady` y sesión `arrived` + handoff) y ejecución (folio → `checkInReservation` → `issueWalletPass` → perfiles completados solo en campos vacíos → SES por parte tolerando `SES_DISABLED`/`SES_ESTABLISHMENT_INCOMPLETE`/`GUEST_REGISTER_INVALID`/`SES_SUBMISSION_IN_FLIGHT` → bienvenida → sesión `checked_in` → auditoría `GUEST_SELF_CHECKED_IN` / `GUEST_CHECKED_IN_ASSISTED`); `otp.service.ts` (código 6 dígitos, `sha256(sessionId:código)` en `consentJson.otp`, TTL, cooldown 60 s → 429, intentos → 429, `debugCode` solo con `HOTELOS_ALLOW_DEMO_AUTH` fuera de producción); rutas de huésped `document` (8 MiB, actor kiosk por `x-kiosk-token`), `signature`, `payment-link` (`at_reception` sin PSP), `otp/request|verify`, `arrive` y de personal `GET /reservations/:id/check-in`, `scan`, `signature`, `verify-identity`, `complete` (manifiesto 18 → 29).
- **Tests:** `__tests__/arrival.test.mts` 17 + `otp.test.mts` 10 = 27/27; `tests/integration/checkin-flow.test.mts` 12/12 (invitación → MRZ TD3/TD1 → complete → sugerencia confirmada → OTP con cooldown → 2 firmas → cotejo → payment-link sin PSP → arrive 200 con SES `accepted` en sandbox y llave → 409 `CHECKIN_ALREADY_DONE`); contratos api-route-permissions + pms-lifecycle 37/37.
- **Verificación:** `:3919` sobre `chk_res_01` (`scratchpad/CHK/w3a-runtime.md`): 2 capturas MRZ, 2 firmas `touch_reception`, 2 cotejos, `complete` 200 con habitación 209, llave, 2 SES `accepted`, bienvenida simulada; SQL de comprobación sin PII.

### W3-B · Rutas de asignación
- **Qué:** `modules/pms/room-assignment.routes.ts` + `room-assignment-route-permissions.partial.ts` (10 entradas: sugerencias POST/GET por reserva, `confirm` high, lote `assignments/run` high, bloqueos y comunicadas GET low / POST-DELETE medium), registro en `server.ts` (import + `registerRoomAssignmentRoutes`) y `security/route-permissions.ts`.
- **Tests:** `tests/integration/room-assignment-routes.test.mts` 9/9 (dos organizaciones aisladas, 404 opacos, contable 403, 409 `ASSIGNMENT_SUGGESTION_DECIDED`/`ROOM_BLOCK_OVERLAP`/`ROOM_CONNECTION_EXISTS`, lote sin asignar); contratos 37/37.
- **Verificación:** `app.inject` con STRICT_ENV + smoke HTTP sobre `prop_chk` (sugerencia 201 con 3 candidatas, confirm, bloqueo, lote de 7 en 44 ms).

### W3-C · Jobs del líder y adaptadores de hardware
- **Qué:** `checkin-jobs.ts` (481 líneas, patrón `reputation-sync.job.ts`): tick horario con lock `pg_try_advisory_xact_lock('checkin.jobs')` y lease del líder; por propiedad con `selfCheckInEnabled`: invitación (llegadas en `[hoy, hoy + inviteDaysBefore]` sin sesión), recordatorio (una vez, día local posterior a la invitación, `reminderAt`), lote de sugerencias a partir de `CHECKIN_ASSIGNMENT_RUN_AT` con marca diaria en `worker_job_runs` (`checkin.assignment`, `queueName api.checkin`), purga de capturas y sesiones `invited` con salida pasada; `adapters/{mrz-reader,card-encoder,lock}.adapter.ts` + `index.ts` (`resolveHardware` fail-closed: `none` en producción, `sandbox` solo explícito; proveedores `salto_space|assa_vostio|dormakaba`).
- **Tests:** `__tests__/checkin-jobs.test.mts` 15 + `hardware-adapters.test.mts` 15 = 30/30.
- **Verificación:** tick con Prisma real y reloj fijo sobre `prop_chk` (5 vueltas: 8 invitadas, lote de 7 a las 18:05 con fila `completed`, salto a las 19:00, 8 recordadas al día siguiente una sola vez; `failed=[]`); cableado en `server.ts` lo hizo W4-D (`/health checks.checkinJobs`).

### W3-D · Tablero y cola de recepción (API)
- **Qué:** `dashboards/front-desk.service.ts` (capa `preCheckIn {status, completedGuests, totalGuests, channel}`, `suggestedRoom {suggestionId, number, reasons, confidence}`, `key {issued|pending|reception}`, KPI `preCheckInCompleted`, `degraded[]`; 3 consultas agregadas en `safe()`), `front-desk-queue.service.ts` (8 kinds nuevos `precheckin_ready`, `assignment_suggested`, `self_checkin_done`, `identity_review`, `minor_without_guardian`, `room_not_ready`, `payment_failed`, `ses_rejected`; acciones `confirm_assignment` y `open_precheckin`; DI `deps.db`).
- **Tests:** `__tests__/front-desk-checkin.test.mts` 11/11 (Prisma falso propio); suite de dashboards 31/31 (el test F27 de recuento de consultas sigue intacto).
- **Verificación:** `:3919` `GET /dashboards/front-desk` y `front-desk-queue` sobre `prop_chk` (`scratchpad/CHK/w3d-front-desk*.json`): 7 `assignment_suggested` con `confirm_assignment` y alternativas.

### W4-A · Cajón de check-in de recepción
- **Qué:** `QuickCheckInDrawer.tsx` (encima del cajón y del runner de UX-1, D12): paso 1 «Escanear documento» (`CocoaFileInput`) + «Pegar MRZ» + campos con origen/confianza y número enmascarado (`···008`) + lista de viajeros; paso 2 top-3 del motor con `CocoaPopover` de motivos/pesos y «Confirmar» auditado; paso 4 firma en `SignaturePad` (`components/cocoa-extras/SignaturePad.tsx`, 0 `style=`) o estado «Firmado en el portal/kiosco/recepción», «Identidad cotejada» gateado por `guest_register.edit`; CTA por `POST …/check-in/complete` cuando hay sesión; 409 `ROOM_NOT_READY` → callout con «Buscar alternativa»; `checkinRunner.ts` con `completeCheckIn` (SES desde la respuesta); `services/checkinApi.ts` (346 líneas).
- **Tests:** `signature-pad.test.mts` 7 + `checkin-drawer.test.mts` 8 + `checkin-runner.test.mts` 18 = 33/33; contratos vecinos 32/32; `tests/cocoa-22-contract.test.mjs` 18/18 (QuickCheckInDrawer 4 `style={` de 15).
- **Verificación:** Playwright sobre `:3939/:5199` (el `:3919` lo ocupaba otra instancia de la tanda): recorrido completo CHK-06 (VIP: top-3 → confirmar 403 → escaneo honesto sin proveedor → MRZ 4/4 → pad 81 puntos → cotejo → cobrar y check-in → SES 1 · llave) y CHK-08 (409 `ROOM_NOT_READY` → alternativa → 200); 15 capturas `scratchpad/CHK/w4a-*.png`, 0 errores de consola.

### W4-B · Mi día, cola, cajón de pre-check-in y pestaña de ajustes
- **Qué:** `FrontDeskDashboard.tsx` (columnas Pre-check-in / Habitación con chip del motor / Llave, tile KPI, filtro, acciones «Invitar al pre-check-in» / «Ver pre-check-in»), `FrontDeskActionQueue.tsx` (8 kinds con etiqueta/tono, `confirm_assignment` optimista, `open_precheckin`), `ArrivalPreCheckInDrawer.tsx` (solo lectura, sin PII completa), `CheckInAutomationSettingsScreen.tsx` + `checkin-settings-view.ts` (política, pesos 0-100, vista previa con las llegadas de mañana, kioscos con emparejamiento, métricas §1.8 desde `check-in/arrivals`; guardar exige `guest_self_service.manage`, kioscos `kiosk.configure`), `frontdesk-labels.ts`, `MiDiaTabs.tsx`, `App.tsx`, fila `/hoy/check-in-automatizado` en el CSV compartido + `nav-tree.generated.json` + whitelist.
- **Tests:** `frontdesk-checkin-columns.test.mts` 17 + `checkin-settings-view.test.mts` 10 (+ labels y nav-item-tabs) = 52/52; `tests/rbac-nav-contract.test.mjs` 13/13; `cocoa-22-contract` 18/18 (ArrivalPreCheckInDrawer y CheckInAutomationSettingsScreen 0 `style=`).
- **Verificación:** Playwright sobre `:3929/:5189` con `direccion@` y `recepcion@chk.test` (14 capturas `scratchpad/CHK/w4b-*.png`): chip «Sugerida 103 · 2 motivos» → confirmar → fila con 103; cola con «Pre-check-in listo» / «Check-in autónomo»; ajustes con PUT de política 200, peso fuera de rango rechazado, emparejar kiosco (código una sola vez); recepción en solo lectura.

### W4-C · Portal guest-web: asistente, llegada y kiosco
- **Qué:** `checkin/wizard.ts` (6 pasos travellers → document → details → signature → payment → arrival; `canAdvance`, `missingFor`, menores < 14, MRZ, copy es/en, `describeArrivalError`), `kiosk/kiosk-mode.ts` (90 s de inactividad, credencial del dispositivo en `localStorage`, sesión del huésped nunca persistida), `api/client.ts` (todos los métodos, `x-guest-token` / `x-kiosk-token`, stubs sin API), `GuestSessionContext.tsx` en `sessionStorage`, `CheckInWizardPage.tsx`, `ArrivalPage.tsx`, `DocumentCamera.tsx` (input `capture=environment` + `getUserMedia` opcional, JPEG ≤ 1600 px, imagen descartada), `SignaturePad.tsx` (mismo contrato que admin-web), `KioskShell.tsx`, `StayOverviewPage` con bloque de pre-check-in, `styles.css` (0 `style=`), script `test` en `apps/guest-web/package.json`.
- **Tests:** `corepack pnpm --filter @hotelos/guest-web test` 37/37; `tests/guest-portal-ui-contract.test.mjs` 10/10; `vite build` OK; `tests/integration/kiosk-pairing.test.mts` 7/7 (pair → claim → `deviceToken` sin hashes, caducidad, kiosco ajeno ignorado, `arrive` por `mrz_checksum` solo si la política lo admite, `requireVisualCheckAtKiosk`).
- **Verificación:** Playwright 420×900 y 1024×768 (`scratchpad/CHK/w4c-r2-*.png`, logs `w4c-*.log`) con reserva sintética creada por script: magic link → token retirado de la URL → foto sin proveedor → 400 `DOCUMENT_UNREADABLE` → MRZ 200 → datos → consentimientos → complete → firma 201 → «No hay nada pendiente de pago» → ETA + preferencia → arrive 409 `CHECK_IN_DATE_OUT_OF_RANGE` («Podrás hacer el check-in el …»); kiosco: claim 409 con código falso / 200 real, asistente sin persistir la sesión. Vite con proxy same-origin fuera del repo por el CORS de entonces (cerrado después).

### W4-D · Bot del huésped, webhook de WhatsApp, herramientas de IA y copiloto
- **Qué:** `guest-bot.service.ts` (981 líneas): `handleGuestMessage` web/whatsapp, identidad por token o `Guest.phone → phoneLookupHash` con respaldo `CheckInGuest.phoneMobile` (sin coincidencia pide código + correo → `requestSignIn`), aviso de IA en el primer mensaje, `PropertyAiSetting.aiEnabled` / `Conversation.aiEnabled`, clasificación por regex + `llmClassify` con clave (confianza < 0,85 → handoff), lecturas solo de SU reserva, escrituras SIEMPRE `AiToolCall awaiting_confirmation` por `runAiTool` con contexto de servicio (late check-out → `createServiceRequest`, upgrade → `sendGuestMessage`), handoff por queja/reembolso/urgencia/2 turnos con `GUEST_CONVERSATION_HANDED_OFF`; `POST /guest-portal/chat` (rate limit 30/min/IP); `routes/webhooks-whatsapp.routes.ts` (GET verify con `timingSafeEqual`, POST con HMAC sobre el cuerpo crudo, dedupe por `message.id`, `phone_number_id → PropertyAiSetting.configurationJson.whatsappPhoneId`); herramientas `suggestRoomAssignment` (lectura) y `createServiceRequest` (escritura) con `execute` en `packages/ai-tools` + `tools/pms.tools.ts` (`POST /ai-operations/tools/sync` → 147 → 151 filas); `check-in.command.ts` con `TOLERATED_SES_CODES` y `await sendWelcomeMessage`; copiloto `assistant.tools.ts` (`get_arrivals_without_room`, `get_incomplete_precheckins`, `get_rooms_ready_for_delivery`); `server.ts` con `startCheckinJobs` bajo el líder y `/health checks.checkinJobs`; `WHATSAPP_APP_SECRET` / `WHATSAPP_VERIFY_TOKEN` en `env.partial.ts` + censo regenerado.
- **Tests:** `__tests__/guest-bot.test.mts` 16/16; `tests/integration/whatsapp-webhook.test.mts` 5/5 y `guest-bot.test.mts` 6/6; suites afectadas l6a-tool-runner 12/12, l2-modulos-ia 9/9, l2-persistencia-plataforma 5/5; contratos ai-safety-matrix + api-route-permissions + env 46/46.
- **Verificación:** `:3919` con secretos de prueba: verify 200/403, chat por reglas con aviso, late check-out → `awaiting_confirmation` → `POST /ai/tool-calls/:id/confirm` como recepción → `succeeded` + fila `service_requests`, webhook firmado con `openssl` (desconocido → pide código; duplicado → 1; firma falsa → 401; titular de CHK-02 → su reserva sin fugas), runner con `suggestRoomAssignment` sin PII en el record, copiloto con los 3 presets.

### W5-A · Contrato PSP con preautorización y pago del pre-check-in
- **Qué:** `payments/psp/psp.types.ts` con 4 métodos OPCIONALES (`authorize`, `captureAuthorization`, `releaseAuthorization`, `tokenizeFromReference`) + `pspSupportsAuthorization`, `looksLikePan`, ventana 7-30 días; Stripe (`capture_method=manual` + extended authorization, Checkout con captura manual, capture/cancel, sandbox en memoria `createStripeSandboxFetch`) y Redsys (tipos 1/2/9 REST con firma, `cardTokenFromRedsysParams`, sandbox con la clave pública de pruebas del SIS); `sandboxPspRegistry`; `checkin-payment.service.ts` (`guestPaymentAmount` pura por `depositPolicy`, `resolveGuestPayment` → `none|link|authorize|at_reception`, `authorizeGuestPayment` con `PaymentIntent` propio `authorized` y token guardado del titular, `recordPaymentEvidence` → `PaymentToken` cifrado + campos `payment*` del parte). Sin ruta HTTP nueva (D3: el portal sigue en enlace-o-recepción hasta que haya PSP).
- **Tests:** `psp-authorize.test.mts` 20 + `checkin-payment.test.mts` 8 (+47 preexistentes intactos) = 75/75; `tests/integration/payments-preauth.test.mts` 6/6 + `payments-tenancy` 5/5.
- **Verificación:** `app.inject` sobre Postgres con tenant aislado: sin PSP → `at_reception`; con `PAYMENTS_PSP_PROVIDER=stripe` + sandbox → `authorized` con `capture_method=manual`, token cifrado en reposo (SELECT crudo sin 12+ dígitos), captura parcial 60,00 y liberación posterior fallida, Checkout `pending` con redirect.

### W5-B · Seed final y walkthrough
- **Qué:** `seed-checkin.ts` (usa los servicios reales: `inviteReservation → updateSession → captureDocument(hints.mrzLines) → completePreArrival → signGuest → suggestForReservation`; 9 sesiones en los estados de §10.1: 2 `invited`, 2 `in_progress`, 3 `ready_for_arrival` con sugerencia, 1 `checked_in` con llave, 1 `handed_off identity_review`; política con textos provisionales, kiosco `chk_kiosk_01` emparejado, bloqueo de la 401 mañana, comunicadas 201-202, 8 entregas simuladas; MRZ sintéticas impresas por consola vía `buildMrz`; `--reset` borra también `checkin_*`, capturas, firmas, sugerencias, bloqueos, entregas y kioscos ajenos de `prop_chk`).
- **Tests:** `tests/seed-checkin-contract.test.mjs` 11/11; `tests/integration/checkin-seed-walkthrough.test.mts` 8/8 (arrivals, cola `precheckin_ready=3` + `identity_review=1` determinista, política, kiosco sin hashes, MRZ válidas y ausentes de `document_captures`); tras el corrector la suite se salta con motivo fuera de una BD `*_chk` (o `CHK_WALKTHROUGH=1`).
- **Verificación:** walkthrough de 5 fases con Playwright (`scratchpad/CHK/walkthrough.md`, 25 capturas en `scratchpad/CHK/w5b/`): recepción invita de nuevo a CHK-03 → asistente con la MRZ impresa → dirección confirma la 209 desde el cajón → empareja el kiosco → tablet en modo kiosco hace el check-in (llave, SES `accepted` en sandbox, bienvenida simulada, auditoría `GUEST_SELF_CHECKED_IN kiosk:chk_kiosk_01`).

### W5-C · Documentación
- **Qué:** `docs/runbooks/checkin-automatizado.md` (14 secciones: variables y qué pasa sin cada una, tablas y máquinas de estados, rutas/permisos por etapa, motor y pesos, identidad/PII/purga, firma y PDF, pagos, jobs, kiosco, bot y confirmaciones, seed y walkthrough, puertas, límites y lo que solo César puede aportar, métricas), sección «Check-in automatizado (Tanda CHK · 2026-09-19)» en `docs/api-contracts.md` (línea 517; 32 filas de rutas, códigos, auditoría, contratos aditivos) + «Assisted Check-In» ampliado, `apps/api/docs/openapi.yaml` (+42 operaciones; js-yaml parsea, 42/42 alineadas con el manifiesto de entonces; las 2 rutas del corrector y `dryRun` no llegaron al YAML → §5.6), apéndice «Estado tras la implementación» en el diseño (línea 400: 19 deltas y estado de R3-R18), CLAUDE.md (deuda 17, docs, seeds).
- **Tests:** contratos documentales (brand, manual, api-route-permissions, rbac-nav, legal, finanzas, rate-grid-docs…) 117/117 sin nav-tree.
- **Verificación:** sin runtime por instrucción; el generador `apps/api/scripts/generate-openapi.mjs` NO se ejecutó (solo parsea `server.ts` y borraría lo editado a mano; ver pendientes).

### Corrector (revisión 3, 2026-09-20 06:1x-07:5x)
- **Qué:** 20 hallazgos confirmados + 10 low corregidos (§4), migración `20260920160000_checkin_pago_en_recepcion` (1 columna `allow_pay_at_reception`, aditiva y reversible), 2 rutas nuevas (`PATCH /reservations/:id/check-in/guests/:guestId` · `POST /reservations/:id/check-in/resolve-handoff`), `dryRun` en `/complete`, redacción de secretos en `notification_deliveries`, `PII_FIELDS.Signature`, supresión RGPD del módulo, `WHATSAPP_WEBHOOK_ALLOW_UNSIGNED`, CORS, `CLAUDE.md` = main + deltas y bloque completo en `docs/audits/ESTADO-VERIFICADO.md`.
- **Tests:** `tests/integration/checkin-corrector.test.mts` (escenarios A-H) + suites actualizadas (`checkin-flow`, `checkin-session`, `whatsapp-webhook`, `checkin-seed-walkthrough`, `arrival`, `checkin-session` unit, `dispatcher-redaction`, `webhooks-whatsapp-mode`, `front-desk-checkin`, `ses-hospedajes`, `guest-register-validator`, admin-web runner/drawer/payment/settings, contratos schema/cors/guest-portal-ui/seed-checkin).
- **Verificación:** `:3919` tras `db:seed:checkin -- --reset` (`scratchpad/CHK/api-3919-corrector.log`): confirm sobre habitación bloqueada 409 `ROOM_BLOCKED`; arrive sin PSP 409 `BALANCE_DUE` + `handed_off payment_failed`; chat `{text}` 200 / `{message}` 400; `documentNumberLast3` reales; `POST` con `?token=` 401; log con `token=<redacted>`; `GET /notifications/deliveries` como recepción sin cuerpo; SQL: 0 entregas con token, 8/8 redactadas, firmas cifradas.

### Puerta completa y correcciones de la propia puerta (2026-09-20 05:5x-06:1x, antes del corrector)
- `scripts/gates.sh` (+4/−1): la puerta «integración» lanzaba `node --test tests/integration/*.test.mts` desde la raíz sin `--import tsx` ni `.env` (70/70 ficheros `ERR_MODULE_NOT_FOUND`); ahora se ejecuta como `package.json#test:integration` desde `apps/api` con `--env-file-if-exists=../../.env --import tsx --test-concurrency=1` (sin el `.env`, 34 suites sin helper caerían a la BD principal `hotelos`). Defecto idéntico en main → portar.
- `db:drift:check` rojo en la línea base por 2 migraciones IVA de FIX-1 (`20260920100000_iva_regimen`, `20260920110000_iva_compensacion_inicial`) aplicadas a `hotelos_chk` antes de W1-A y ausentes de la base `f77820d`: carpetas (42 líneas) y 21 líneas de `schema.prisma` portadas VERBATIM de main (`scratchpad/CHK/fix1-db.patch`; cmp byte a byte); fusión limpia con main. Resultado: 21 migraciones, deriva cero.
- `tests/integration/l3-cancelacion.test.mts` (+4): las candidatas a no-show llegaban hace 3-5 días y la regla `PAST_ARRIVAL_DATE` de UX-1 (ya en la base) las rechazaba con 400 → `allowPastArrival: true` solo cuando `arrivalDate < madridDay(0)` (regresión preexistente de UX-1, también en main).
- `tests/integration/api-integration.test.mts` (+4/−1): `guestRegisterRecordId` de un parte creado por el API es `grr_<16 hex>` (`lib/ids.ts createId`), no cuid; la aserción pasa a pertenencia al conjunto de partes de la reserva (más estricta).

## 2. Modelo de datos, migraciones y datos del carril

| Carpeta | Origen | Contenido | Estado en `hotelos_chk` |
|---|---|---|---|
| `20260920100000_iva_regimen` · `20260920110000_iva_compensacion_inicial` | portadas verbatim de main (FIX-1) | enum `VatBookRegime`, `vat_book_entries.regime` + índice, `vat_settings.opening_compensation*` (+21 líneas de `schema.prisma` idénticas a main) | ya aplicadas antes de la tanda; la fusión las reconoce como el mismo contenido |
| `20260920150000_checkin_automatizado` | W1-A | 9 tablas, 14 índices + 3 únicos, 1 FK en cascada, 0 enums, 0 columnas en tablas existentes, sin backfill; reversible (DROP de las 9) | aplicada (19/19 en W1-A) |
| `20260920160000_checkin_pago_en_recepcion` | corrector (REV3-02) | `property_checkin_policies.allow_pay_at_reception BOOLEAN NOT NULL DEFAULT false`; reversible | aplicada; `migrate status` 21/21 · `db:drift:check` «No difference detected.» |

Excluido a propósito (deuda 17 de CLAUDE.md): índice único `guest_register_records(reservation_id, guest_id)` (0 duplicados; dedupe en código), `MobileKey` (la llave reutiliza `issueWalletPass` / `guest_portal_actions`), `RoomFeatureAssignment` (rasgos en `Room.featuresJson`), clave `preassign_room` en la matriz de riesgo (D4). `crypto-fields.ts`: `PII_FIELDS.CheckInGuest` (5 campos) + `LOOKUP_HASH_FIELDS.CheckInGuest` (3), y desde el corrector `PII_FIELDS.Signature = [objectKey, pdfObjectKey]`. Al fusionar, renumerar las dos carpetas CHK si otro carril aporta una marca posterior (`tests/checkin-schema-contract.test.mjs` localiza la migración por sufijo).

**Datos que quedan en `hotelos_chk` (solo `org_chk`, por diseño):** tenant CHK recién sembrado por la última pasada de integración (11 reservas `CHK-*`, 13 huéspedes, 36 habitaciones, 9 sesiones, 3 sugerencias, 1 kiosco emparejado, política con `requireVisualCheckAtKiosk=false` y `selfCheckInEnabled=true`), más lo que `--reset` no borra (conversaciones/mensajes/`ai_tool_calls`/`service_requests` de las pruebas del bot, `PropertyAiSetting.configurationJson.whatsappPhoneId = w4d_phone_chk`, `ai_tool_registry` 151 filas tras el sync, auditoría append-only). Faranda: 0 escrituras en toda la tanda; ninguna organización `org_l2_*` residual.

## 3. Puertas (línea base 2026-09-19 21:20 → final 2026-09-20 08:17)

Puerta rápida por ola en `scratchpad/CHK/gates-ola{1..5}.json` (+ por lote `gates-w*.json`), completa en `gates-full.json` (06:09, tras las correcciones de la propia puerta), del corrector en `gates-corrector-full.json` (07:52) y **final** en `gates-final.json` / `.log` (08:17; primera pasada `gates-final.run1.json` 08:13 con 1 fallo transitorio de integración por carga concurrente de las puertas de T9, reejecutada aislada en verde sin cambios de código: `final-integration.log`).

| Puerta | Línea base (`gates-base.json`) | Final (`gates-final.json`) |
|---|---|---|
| typecheck:all | 15 PASS · 0 FAIL · 1 SKIP (guest-web) · 22,1 s | 15 PASS · 0 FAIL · 1 SKIP · 20,7 s |
| api unit | 2.871 (2.870 pass · 0 fail · 1 skip) | **3.099 (3.098 pass · 0 fail · 1 skip)** (+228) |
| admin-web unit | 1.849 (1.848 · 0 · 1) | 1.901 (1.872 · **28 fail** · 1 skip) — T9, §3.1 |
| ai-core | 119/119 | 119/119 |
| worker | 34/34 | 34/34 |
| contratos raíz | 589 (587 · 0 · 2 skip) | 617 (613 · **2 fail** · 2 skip) — T9, §3.1 |
| discoverability | 192 URL · 192 cubiertas | 197 URL · 193 cubiertas · **4 enlaces rotos** — T9, §3.1 |
| nav-tree --check | al día (69 · 100 · 205) | al día (70 · 104 · 205) |
| route-access | 15 × 192 | 15 × 197 |
| cocoa waves --check | §6 al día | §6 al día |
| rbac:sync --dry-run | OK · 185 ms | OK · 224 ms |
| migrate status + drift | **FAIL** (18/18 + deriva IVA de otro carril) | OK · 21/21 · «No difference detected.» |
| admin-web build | — (no en `--quick`) | ✓ 2,73 s |
| integración | — (no en `--quick`; en `gates-full.run1` 70/70 ficheros `ERR_MODULE_NOT_FOUND`) | **881 (873 pass · 0 fail · 8 skip)**, 8 skips preexistentes por datos/env |

Resultado: **11/14** (rc=1). Reejecución del integrador tras editar `CLAUDE.md` y este informe (`scratchpad/CHK/gates-integrador-quick.json`, `--quick` con `NAV_TREE_CSV`): 9/12 con cifras idénticas (api unit 3.099/3.098 · admin-web 1.901/1.872/28 fail · contratos raíz 617/613/2 fail · discoverability 197 · nav-tree 70/104/205 · route-access 15 × 197 · cocoa waves · rbac dry-run · migrate + drift «No difference detected.»); los contratos que leen `CLAUDE.md`/`docs/audits` (brand, manual, migrations-squash, rate-grid-docs, seed-ux-day) 51/51 y `nav-tree-contract` 18/21 con los 2 fallos de T9. Cocoa: `inlineStyles` dentro del techo (QuickCheckInDrawer 4/15, FrontDeskActionQueue 4/15, FrontDeskDashboard 7/25; pantallas nuevas 0), `docs/design/cocoa-22-inventory.json` y `COCOA-22-MIGRACION.md §6` regenerados por el corrector.

### 3.1 Las 3 puertas rojas: una sola causa, ajena al carril
El CSV compartido `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` (fuera del worktree) tiene desde el 2026-09-20 04:36 cuatro filas «nueva (Tanda T9)» (`DocumentCaptureScreen` `/operaciones/digitalizar`, `IncomingDocumentsScreen` `/finanzas/proveedores/documentos`, `DocumentArchiveScreen` `/finanzas/proveedores/archivo`, `GoodsReceiptsScreen` `/operaciones/compras/recepciones`) sin componente en este worktree (0 apariciones en `apps/admin-web/src` fuera del JSON generado). `nav-tree.generated.json` se mantiene sincronizado con ese CSV (si no, `nav-tree --check` y `route-access` se ponen en rojo), de modo que los 28 tests de recuento del árbol (`nav-tree.test`, `sidebar-menu.test`, `view-as`, `backoffice.routes`, `guideContent`, `tabs-b-containers`, `nav-item-tabs`), los 2 contratos («Router · registro de L1b / SCREEN_COMPONENTS» y «tab GoodsReceiptsScreen sin contenedor») y los 4 enlaces de discoverability fallan por la misma razón. Contrafactual medido con el CSV sin esas filas (`scratchpad/CHK/nav-tree-sin-t9.csv`, JSON regenerado y restaurado byte a byte): discoverability 193/193 · admin-web 0 fail · contratos raíz 0 fail → **14/14**. La única fila CHK del CSV (`/hoy/check-in-automatizado`, pestaña de Mi día) está cubierta y sus recuentos ya están en los tests. Se resuelve al fusionar T9 (o regenerando el árbol con el CSV definitivo tras fusionar ambos).

## 4. Hallazgos de la revisión (3 revisores → dedupe → corrector)

Dedupe: 20 confirmados (8 high · 12 medium; REV3-07 con 2 duplicados y REV3-08 con 1 entre revisores), **0 refutados**. Ids: REV3-nn = `funcional-runtime-CHK-REV3-nn`, SEC-n = `seguridad-privacidad-rbac-SEC-n`, REG-nn = `regresiones-honestidad-REV3-nn` de la lista consolidada. Corrector: los 20 corregidos con test por corrección, más 10 low de su segunda pasada; 2 low sin cambio de código con motivo. Fichero:línea del árbol al cierre en `docs/api-contracts.md` (sección CHK) y en el runbook §13.

| Id | Sev. | Hallazgo (resumen) | Corrección |
|---|---|---|---|
| REV3-01 | high | `RoomBlock` no era filtro duro: confirmar con `roomId` y `arrive` alojaban en habitación bloqueada | `findOverlappingRoomBlock` + `validateRoomUnderLock` → 409 `ROOM_BLOCKED` (asignar, alojar, mover, fechas); `isRoomReady({roomBlock})` reasigna la misma categoría; unit + integración + runtime |
| REV3-02 | high | Sin PSP, `at_reception` satisfacía `depositPolicy=balance` y el huésped se alojaba con todo el saldo pendiente | `allowPayAtReception` (migración `20260920160000`, DTO, PUT); si no, 409 `BALANCE_DUE {handoffKind}` + sesión `handed_off payment_failed`; recepción sin bloqueo |
| REV3-03 | high | El cajón asignaba y cobraba ANTES de que `/complete` validara firma/parte; el intento de cobro se perdía al reabrir | `precheckCheckIn` (`POST …/complete {dryRun:true}`) como paso previo del runner; sufijo con importe ya cobrado; intento persistido en `sessionStorage` y restaurado |
| REV3-04 | high | Recepción no podía cerrar sesiones que el huésped no cerró (sin ruta de personal para viajeros ni para reabrir `handed_off`) | `ensureGuestRegisterRecordsForSession` en `/complete` y en la firma del mostrador; `PATCH …/check-in/guests/:guestId`; `POST …/check-in/resolve-handoff`; el cajón cae al camino clásico si la sesión no está cerrada ni firmada |
| REV3-05 | high | «Pegar MRZ» sobrescribía al viajero con la identidad de un documento ajeno sin cotejo | `applyMrz` aplica `identityNameMatches`: discrepancia → captura con `identity_mismatch`, sin aplicar campos; aviso en guest-web |
| REV3-06 | high | Un menor < 14 no podía cerrar el pre-check-in sin documento y teléfono propios (escenario CHK-02 imposible) | validador `isSpainGuestRegisterMinor`: sin `documentType/Number` ni contacto propio; `SesGuest` con documento opcional y XML sin bloque; escenario D en integración |
| REV3-07 | medium | `documentNumberLast3` salía del envelope cifrado (viajeros cargados con `include`) | loaders con `findMany` de primer nivel; integración y runtime con últimos 3 reales |
| REV3-08 | medium | Un PATCH de consentimientos entre `otp/request` y `otp/verify` borraba el OTP pendiente | `mergeConsentJson` conserva `consentJson.otp` |
| REV3-09 | medium | KPI «Pre-check-in hecho» contaba `handed_off` | `handed_off` fuera de `PRECHECKIN_COMPLETED_STATUSES` (API y admin-web) |
| REV3-10 | medium | El bot web no tenía interfaz y `chat()` enviaba `{message}` frente a `{text}` estricto | cliente con `{ text, conversationId?, language? }`, `ChatWidget.tsx` en estancia y asistente |
| REV3-11 | medium | Con documento del perfil el paso 2 no ofrecía cámara ni MRZ | «Volver a leer el documento» siempre; etiqueta de origen perfil/MRZ |
| SEC-1 | high | Token del magic link y código OTP persistidos EN CLARO en `notification_deliveries` y legibles por cualquier usuario autenticado (`GET /notifications/deliveries`) | `dispatch` con redacción (`token=[redacted]`, `otpCode`), `retryDelivery` rechaza filas redactadas, `GET /notifications/deliveries` sin cuerpo salvo `notifications.manage`/platform admin; filas de `org_chk` limpiadas |
| SEC-2 | high | PDF del parte (PII completa) y trazo guardados en claro como data-URI en `signatures` (3 años) | `PII_FIELDS.Signature = [objectKey, pdfObjectKey]`; contrato; filas de `org_chk` re-cifradas |
| SEC-3 | medium | La supresión RGPD no cubría `checkin_guests`/`document_captures`/`signatures`/`checkin_sessions` | `eraseCheckInData` integrado en `executeErasure` (anonimiza, borra capturas, vacía objetos conservando hashes, sesión `cancelled`) |
| SEC-4 | medium | `phone_number_id → whatsappPhoneId` por `findFirst` sin unicidad ni propiedad: un admin de otra organización podía capturar mensajes ajenos | `assertWhatsappPhoneIdFree` en ambos escritores → 409 `WHATSAPP_PHONE_ID_CLAIMED`; el webhook descarta con aviso si hay > 1 |
| SEC-5 | medium | Sin `WHATSAPP_APP_SECRET` el webhook aceptaba cuerpos sin firma fuera de producción | 503 en cualquier entorno salvo `WHATSAPP_WEBHOOK_ALLOW_UNSIGNED=1` fuera de producción (variable `dangerous`, censo regenerado) |
| REG-01 | medium | Bloque de estado añadido a `CLAUDE.md` cuando main ya los movió a `docs/audits/ESTADO-VERIFICADO.md` (60d870e) | `CLAUDE.md` = main + deltas CHK; bloque completo en `ESTADO-VERIFICADO.md` (solo CHK; `merge-lane.sh` concatena) |
| REG-02 | medium | Cifras no reproducibles («19/19», «drift rojo solo IVA», api unit 3.056…) en CLAUDE.md y runbook §12 | sustituidas por las de la puerta completa del corrector; frases retiradas |
| REG-03 | medium | El walkthrough del seed dejaba `org_chk` sembrado en la BD principal y `test:integration` no cargaba `.env` | suite condicionada a BD `*_chk` o `CHK_WALKTHROUGH=1` con motivo; `--env-file-if-exists` en `package.json`; runbook |
| REG-04 | medium | CORS sin `x-guest-token`/`x-kiosk-token` (promesa reiterada por 6 lotes) | `allowedHeaders` + regex de `tests/cors-contract.test.mjs` |

Low corregidos por el corrector: REV3-12 firma idempotente (200 sin segunda fila ni PDF) · REV3-13 métrica de llaves solo con pase firmado (`ArrivalDto.key.signed`) · REV3-14 QR SVG inline y validez de la llave en la hora del hotel · REV3-15 alfa-3 y `paymentType` en el parte · SEC-6 `?token=` solo en GET y logger que redacta `token=` · SEC-7 `claimPairing` comprueba el gate del módulo antes de consumir el código · SEC-10 contrato `code` del emparejamiento y `VerifyOtpResult` en api-contracts · REG-06 comando de integración del runbook §12 · REG-07 frases «--reset … 5 tablas» retiradas · REG-08 `schema.prisma.orig` eliminado. Low sin cambio de código, con motivo: REG-09 `pnpm-lock.yaml` (intocable por las reglas; lo descarta la fusión) y REG-10 autoría de ficheros tocados fuera de la lista de un lote (se registra aquí, §7: `developer/api-reference.service.ts`, `docs/design/COCOA-22-MIGRACION.md`, `cocoa-22-inventory.json`, `scripts/env-contract.json`, `.env.example`, `deploy/.env.production.example` regenerados por el corrector; `refresh-demo-dataset.test.mts`, `invitations.test.mts`, `tools-coverage.test.mts`, `api-reference.test.mts` ajustados por los lotes que introdujeron el cambio). Los identificadores que faltan en la numeración (SEC-8, SEC-9, REG-05) no llegaron a la lista consolidada del dedupe.

## 5. Pendientes (con dueño)

**Fusión (orquestador):**
1. `bash scripts/merge-lane.sh` de main: regenera censo (`env-census --write`), Cocoa (`cocoa-22-inventory` + `waves --write`), une la whitelist, concatena `ESTADO-VERIFICADO.md` y descarta `pnpm-lock.yaml`. `CLAUDE.md` será «CONFLICTO manual»: tomar la versión de `tanda-chk` (= main `f2ac4f7` + deltas CHK: allowlist de seeds, párrafo `db:seed:checkin`, deuda 17, 3 líneas en «Docs prioritarios», bloque compacto de estado).
2. Orden T9 → CHK, o fusionar ambos y regenerar `nav-tree.generated.json` + whitelist una sola vez con el CSV definitivo (las 3 puertas rojas se ponen en verde; contrafactual §3.1).
3. Renumerar `20260920150000_checkin_automatizado` y `20260920160000_checkin_pago_en_recepcion` si otro carril fusionado antes aporta una marca posterior (grep de las dos cadenas en runbook, api-contracts, ESTADO-VERIFICADO, diseño; el contrato busca por sufijo).
4. Portar a main el arreglo de `scripts/gates.sh` (puerta de integración) y los 2 tests de integración (§1, último bloque): en main fallan igual.
5. `docs/runbooks/auditoria-eventos.md` sin las acciones nuevas (`GUEST_SELF_CHECKED_IN`, `GUEST_CHECKED_IN_ASSISTED`, `CheckInHandedOff`, `CheckInGuestProfileFilled`, `CheckInPaymentStatusChanged`, `CHECKIN_IDENTITY_VERIFIED`, `CHECKIN_OTP_*`, `CHECKIN_GUEST_SIGNED`, `ID_IMAGE_DISCARDED` (segunda entidad), `ASSIGNMENT_SUGGESTION_DECIDED`, `ROOM_BLOCK_*`, `ROOM_CONNECTION_*`, `CheckInReminderSent`, `CheckInSessionExpired`, `GUEST_WELCOME_MESSAGE_SENT`, `GUEST_CONVERSATION_HANDED_OFF`, `GUEST_ETA_UPDATED`, `PAYMENT_AUTHORIZATION_CREATED`, `PAYMENT_TOKEN_STORED`, `GUEST_REGISTER_PAYMENT_RECORDED`): la lista vive en `docs/api-contracts.md` sección CHK «Auditoría».
6. `apps/api/docs/openapi.yaml` ya no es regenerable con `apps/api/scripts/generate-openapi.mjs` (solo parsea `app.get|post|patch|delete` de `server.ts`): ampliar el generador a `registerXxxRoutes` + `put` o mantener el YAML a mano (script de reinserción idempotente `scratchpad/CHK/w5c-openapi-insert.mjs`). Además faltan en el YAML las 2 rutas del corrector (`PATCH /reservations/{id}/check-in/guests/{guestId}`, `POST /reservations/{id}/check-in/resolve-handoff`) y el campo `dryRun` del cuerpo de `POST /reservations/{id}/check-in/complete` (los tres sí están en `docs/api-contracts.md` y en el runbook; 34 paths CHK en el YAML hoy).

**Producto / deuda del módulo (runbook §13, CLAUDE.md deuda 17):** índice único de partes; almacén de firmas data-URI hasta T9 (`SignatureStorage`) y PDF sin la imagen del trazo; retención de `signatures` no purgada; `housekeepingAlerts` del motor sin tarea a pisos y notas del motor no persistidas (`AssignmentSuggestion` sin columnas `dataNotes`/`rejected`); cancelar una reserva no cierra su sesión; kiosco sin heartbeat automático ni `requireVisualCheckAtKiosk` en `/document` y `/signature`; `front_office_manager` sin `kiosk.configure` (empareja dirección); zona horaria de jobs fija a Europe/Madrid y ETA del motor en UTC; los huéspedes del seed no se reconocen por número de WhatsApp (`Guest.mobilePhone` sin hash; 1 línea en `seed-checkin.ts` para copiar a `phone`); sin PSP configurado la garantía (`authorize`) no tiene ruta HTTP (D3) y `PaymentIntentStatusWire` no incluye `authorized`; captura/liberación de garantías al check-out; plantillas *utility* de WhatsApp y `template` en el dispatcher; códigos `OTP_METHOD_NOT_ALLOWED`/`SES_QUEUE_FAILED`/`CHECKIN_ALREADY_DONE` fuera de `CHECKIN_ERROR_CODES`; `GET /ai/tool-calls` exige `audit.read` (recepción confirma pero no lista por esa ruta); guest-web sin typecheck real (SKIP conocido por `@types/react`) y sin `eslint.config.*` en el repo; `packages/database` `typecheck` no cubre `prisma/*.ts` (el seed se comprobó con `tsc` aparte).

## 6. Decisiones para César (opción por defecto aplicada en el árbol)

| # | Decisión | Aplicado por defecto | Dónde | Qué aporta César |
|---|---|---|---|---|
| D1 | Proveedor de visión para documentos | MRZ determinista siempre delante; visión por `getAiCore()` SOLO con `AI_PROVIDER` + `AI_PROVIDER_API_KEY`; sin clave, `source manual` etiquetado y la vía operativa es lector/«Pegar MRZ» | `identity-capture.service.ts`, runbook §5 | clave de organización, DPA + ZDR, `AI_MODEL`, presupuesto |
| D2 | Métodos de verificación sin recepcionista | `allowedVerificationMethods = [visual_reception, mrz_checksum, otp_email]`, `requireVisualCheckAtKiosk = true` (el seed de demo lo pone en `false` porque el kiosco no tiene lector); `identityVerified` nunca lo marca el modelo; OTP con proveedores simulados; `payment_match`, `midni_qr`, `reader_hardware` en el vocabulario sin activar | `checkin-policy.service.ts`, `otp.service.ts`, seed | qué métodos admite cada hotel; SMS/email reales |
| D3 | PSP: preautorización real o solo enlace | contrato PSP con `authorize/capture/release/tokenize` + sandbox y tests; el portal usa `createPaymentLink` o «se cobra en recepción»; `depositPolicy` por defecto `balance`; `allowPayAtReception = false` (corrector) | `payments/psp/*`, `checkin-payment.service.ts` | claves Stripe test o Redsys, elección enlace vs garantía, ruta HTTP de `authorize` |
| D4 | Pre-asignación autónoma la tarde anterior | NO: `autoAssignLevel = suggest_and_confirm`; lote a `CHECKIN_ASSIGNMENT_RUN_AT` (18:00) crea `AssignmentSuggestion` y recepción confirma en un clic; `preassign` admitido pero inactivo; sin `preassign_room` en la matriz | `checkin-jobs.ts`, `room-assignment.service.ts` | activar `preassign` y su clave de riesgo |
| D5 | Kiosco: hardware o tablet | guest-web `?kiosk=1&device=` en tablet; adaptadores `mrz-reader`/`card-encoder`/`lock` con proveedor `none` (fail-closed) y `sandbox` explícito; tarjeta física «recógela en recepción»; llave = `issueWalletPass` (QR de demo, Apple sin firmar avisado) | `adapters/*`, `KioskShell.tsx` | cerradura (Salto/Assa/dormakaba/TESA) y licencias, lector MRZ, certificado Apple Pass, `GOOGLE_WALLET_ISSUER_ID` |
| D6 | WhatsApp | proveedor con plantillas y webhook de entrada verificado por HMAC, todo simulado sin credenciales; sin firma solo con `WHATSAPP_WEBHOOK_ALLOW_UNSIGNED=1` fuera de producción; opt-in en `consentJson`; `whatsappPhoneId` único por propiedad | `whatsapp.provider.ts`, `webhooks-whatsapp.routes.ts` | BSP vs Tech Provider, número por hotel, plantillas *utility* aprobadas, `WHATSAPP_PHONE_ID`/`PROVIDER_TOKEN`/`APP_SECRET`/`VERIFY_TOKEN`, presupuesto post-01/10/2026 |
| D7 | Face-match / IDV (Veriff) y MiDNI | fuera de la tanda; el modelo los admite (`identityVerificationMethod`) sin migrar | `checkin-types.ts` | contratar IDV si se quiere identidad remota fuerte |
| D8 | Residencia de datos de IA | la que fije ai-core (`AI_INFERENCE_GEO`); nada de PII sin enmascarar en prompts | `packages/ai-core` | decidir antes del primer documento real |
| D9 | Hotel piloto del walkthrough en lectura | Los Tilos solo lectura para el motor (candidatas empatadas: sin vista/planta/rasgos en sus habitaciones); toda escritura en `org_chk` | `scratchpad/CHK/lt-suggestions.json` | piloto (LT o RA) y carga de rasgos de habitaciones |
| D10 | Email transaccional | todo `simulated` y así se muestra (`errorMessage SIMULADO`, toasts y auditoría honestos) | dispatcher | `EMAIL_PROVIDER`, `EMAIL_PROVIDER_KEY`, `EMAIL_FROM`, dominio verificado |
| D11 | SES real | sandbox `stub`; el parte se valida y encola; `paymentMethod` del contrato SES sigue fijo a `card` (ses-submission.service.ts:1191) | compliance | `sesRegistryNumber` de los 7 hoteles, certificado XAdES, `SES_HOSPEDAJES_MODE=preproduction`, XSD oficial (R1) |
| D12 | Textos legales por hotel | textos provisionales en `PropertyCheckInPolicy.guestConsentText/aiDisclosureText` marcados «texto provisional»; maqueta móvil intacta | `checkin-policy.service.ts`, seed | consentimiento RGPD, aviso de IA art. 50, tutor/menores, PDF del parte revisado por el asesor, EIPD/RAT, consulta a Interior sobre firma en tablet |
| D13 | Pantallas de ajustes | `GuestPortalSettingsScreen` (sin backend) intacta; la política vive en `/hoy/check-in-automatizado` | `CheckInAutomationSettingsScreen.tsx` | si se retira la pantalla antigua |
| D14 | Dominio del portal | `GUEST_WEB_BASE_URL` por defecto `http://localhost:5174` (magic-link test) y `http://127.0.0.1:5189` en el carril; en producción el portal es same-origin tras Caddy | `lib/env.ts`, deploy | `huesped.ehotelos.com` y la variable por entorno |

Correspondencia con el runbook §13 (necesidades N1-N8 del diseño §10.2): N1 = D1/D8 · N2 = D10 · N3 = D6 · N4 = D3 · N5 = D11 · N6 = D5 · N7 = D2/D4/D7/D9 · N8 = D12.

## 7. Fusión: ficheros tocados, exclusiones y servidores

**Ficheros** (`git status --porcelain -uall`, sin `pnpm-lock.yaml`): 85 modificados + 92 nuevos.

| Área | Modificados | Nuevos | Notas |
|---|---|---|---|
| `apps/api` | 36 (+6.479/−1.541; `docs/openapi.yaml` +3.406/−1.377) | 48 (16.401 líneas: `modules/checkin/*` 22 fuentes + 12 tests, `modules/pms/room-assignment.*` 4 + 3 tests, `routes/webhooks-whatsapp.routes.ts` + test, tests de dashboards/messaging/notifications/payments) | `server.ts` +78/−4 (registro de rutas, jobs, CORS, logger); `security/route-permissions.ts` +17 (2 partials + webhook); `lib/{auth-context,env,tenancy}.ts` |
| `apps/admin-web` | 14 (+1.972/−94) | 9 (2.728 líneas) | `QuickCheckInDrawer.tsx` +825/−18; `nav-tree.generated.json` regenerado desde el CSV compartido (incluye las 4 filas T9) |
| `apps/guest-web` | 7 (+1.478/−59) | 12 (4.024 líneas) | sin `@types/react` (SKIP preexistente); script `test` nuevo |
| `packages/database` | 4 (+326/−2: `schema.prisma` +297) | 5 (seed 1.7 k líneas + 4 carpetas de migración, 2 de ellas de FIX-1) | |
| `packages/compliance` | 4 (+37/−8) | 2 (`mrz.ts` + test) | validador y XML SES para menores |
| `packages/shared` · `packages/ai-tools` | 1 + 2 | 1 (`checkin-types.ts`) | 2 herramientas nuevas |
| `tests/` | 6 (+126/−8) | 13 (2 contratos raíz + 11 de integración) | |
| docs / scripts / env | `docs/api-contracts.md` +134, diseño +40 (apéndice), Cocoa (2), `CLAUDE.md` +34/−313 (= main + deltas), `scripts/gates.sh`, `scripts/env-contract.json`, `.env.example`, `deploy/.env.production.example`, `package.json`, `packages/database/package.json` | `docs/runbooks/checkin-automatizado.md` (556), `docs/audits/ESTADO-VERIFICADO.md` (77, solo el bloque CHK), este informe | |

**Excluido del commit:** `hotelos/pnpm-lock.yaml` (`M` +74/−25 desde antes de la tanda; ningún lote lo tocó). **Sin dependencias nuevas** (QR en guest-web copiado del codificador VeriFactu; PDF sobre `pdf-writer.ts`; MRZ y motor sin librerías).

**Servidores y datos:** `:3000` (PID 4986) y `:5173` intactos toda la tanda; cada lote arrancó su instancia desde el worktree (`PORT=3919 RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true …`, Vite `:5189`; `:3929/:3939/:5199` cuando `:3919` estaba ocupado por otro lote) y la mató por PID (logs y pids en `scratchpad/CHK/api-3919-*.log|pid`, `vite-*.log|pid`); al cierre ningún proceso residual. El integrador no arrancó nada. Sin `pkill -f`. BD: solo `org_chk` y tenants `org_l2_*` efímeros; Faranda solo lectura.

## 8. Mensaje de commit propuesto

```
feat(checkin): check-in automatizado y recepcionista IA (Tanda CHK)

Módulo apps/api/src/modules/checkin: sesión de pre-check-in con token opaco, captura de
identidad (MRZ ICAO 9303 en packages/compliance + visión opcional por ai-core, imagen nunca
persistida), firma con evidencias y PDF del parte, OTP con hash y TTL, pago del pre-check-in
(enlace o recepción; contrato PSP con authorize/capture/release y sandbox Stripe/Redsys),
llegada única para huésped, kiosco y recepción (precondiciones deterministas, SES, llave,
bienvenida real), jobs del líder (invitación, recordatorio, lote de sugerencias, purga),
kiosco con emparejamiento por código y adaptadores none/sandbox, bot del huésped web +
webhook de WhatsApp con HITL (AiToolCall awaiting_confirmation) y copiloto de recepción.
Motor de asignación explicable (modules/pms/room-assignment.*, chk-rules-1) con rutas,
bloqueos y comunicadas. 9 tablas nuevas (20260920150000_checkin_automatizado) + columna
allow_pay_at_reception (20260920160000_checkin_pago_en_recepcion); 44 rutas en el manifiesto
sin claves RBAC nuevas; 4 plantillas de sistema checkin_*.

Front: Mi día con pre-check-in, habitación sugerida y llave; cola con 8 kinds nuevos; cajón
de check-in con escaneo/MRZ, top-3 con motivos, pad de firma, cotejo y precheck antes de
cobrar; cajón de pre-check-in; pestaña /hoy/check-in-automatizado (política, pesos,
kioscos, métricas). Portal guest-web: asistente de 6 pasos, cámara, firma, chat, llegada
con llave y modo kiosco.

Seed db:seed:checkin (tenant org_chk/prop_chk, MRZ sintéticas), runbook
docs/runbooks/checkin-automatizado.md, sección CHK en docs/api-contracts.md, 42 operaciones
en openapi.yaml, apéndice del diseño y bloque en docs/audits/ESTADO-VERIFICADO.md.
Revisión 3: 20 hallazgos confirmados + 10 low corregidos (RoomBlock como filtro duro,
depósito sin PSP, precheck del cajón, cierre por recepción, cotejo de MRZ, menores,
secretos redactados en notification_deliveries, firmas y PDF cifrados, supresión RGPD,
webhook firmado, CORS x-guest-token/x-kiosk-token).

Incluye las migraciones IVA de FIX-1 portadas verbatim de main para deriva cero,
scripts/gates.sh con la puerta de integración desde apps/api (tsx + .env) y dos tests de
integración corregidos (no-show con allowPastArrival, id del parte). Puertas: 11/14 —
api unit 3.099/3.098, integración 881/873, migraciones 21/21 sin deriva; las 3 rojas son
las 4 pantallas de T9 del CSV compartido sin componente aquí. pnpm-lock.yaml fuera.
```
