# Tanda L6b · Asistente ehotelOS unificado — informe de cierre (2026-09-20, v3 · integrador)

**Encargo:** brief de la tanda (un solo asistente coherente: (1) núcleo conversacional único sobre `@hotelos/ai-core` con contexto de usuario/propiedad/pantalla, memoria por usuario, catálogo filtrado por RBAC, HITL y presupuesto existentes; (2) enrutado por intención con el modelo cuando hay proveedor y por reglas ampliadas cuando no, con cobertura 100 % de las preguntas sugeridas; (3) una sola superficie en el back office —panel desde cualquier pantalla con el contexto de la página, ⌘K integrado— sin duplicar el chat de CHK: copiloto y bot consumen el mismo núcleo con su prompt y permisos; (4) trazabilidad: herramienta/fuente, coste y si fue por reglas; (5) tests del enrutado y de los permisos, e2e mínimo del panel) sobre el worktree `~/anfitorio-demo-wt-l6b/hotelos` (rama `tanda-l6b`, base `a069906` = main con CHK fusionado; BD propia `hotelos_l6b`, copia completa del cliente piloto; puertos `:3933/:5203`).

**Cómo se ejecutó:** recon (`scratchpad/L6b/recon-delta.md`, 16 puntos de delta frente a los dosieres L6A, CHK y AI-CORE) → 4 olas · 11 lotes con ficheros exclusivos (L6b-01…L6b-11) → puerta rápida por ola (`scratchpad/L6b/gates-ola{1..4}.json`) → puertas completas (2 pasadas, con 2 correcciones de causa raíz en la integración) → ronda de revisión (2 revisores: «funcional-runtime» y «seguridad-datos-regresiones», 11 hallazgos confirmados high/medium + 8 low, 0 refutados) → corrector (35 ficheros; 17 hallazgos corregidos, 1 fuera del carril) → puertas completas finales **13/14** → este informe (v3: sustituye a la v1 de L6b-11, que se escribió antes de la revisión y del corrector).

**Working tree antes de este lote** (`git -C ~/anfitorio-demo-wt-l6b status --porcelain -uall`): **43 ficheros modificados (+2.958 / −597)** y **22 nuevos (6.538 líneas)**, sin `pnpm-lock.yaml` (estaba `M` +64/−25 desde antes de la tanda por una resincronización de `pnpm install`; la pasada de puertas completas lo restauró byte a byte desde HEAD y `git status` ya no lo lista). Los únicos ficheros de este lote son este informe y el bloque final de `docs/audits/ESTADO-VERIFICADO.md`; el commit lo hace el integrador en la rama del carril (§9), nunca en `main`, sin `push`.

**Datos:** toda verificación con escritura se hizo en tenants sintéticos (`org_uxday`/`prop_uxday` con `*@uxday.test`, `org_chk`/`prop_chk` para el bot, `org_l2_l6b*` que la suite de integración crea y destruye); el cliente piloto solo lectura (`farandaInvariants` idénticas antes/después en la suite). Ningún nombre de persona real en código, tests, capturas, logs ni en este informe; lo persistido por el asistente va cifrado (`content`, `title`) y redactado (`[NOMBRE_n]/[TEL_n]/[DOC_n]/[TARJETA_n]`).

## 0. Resumen

- **Entregado:** núcleo conversacional único `modules/assistant/assistant-core.service.ts` (`runAssistantTurn`: memoria por usuario + propiedad + superficie, catálogo filtrado por RBAC × superficie × módulos, reglas sin proveedor y tool use con proveedor en ≤ 3 turnos, puerta de propiedad `assistant-gate.ts` (aiEnabled · nivel · presupuesto) antes de cada llamada directa al modelo, escrituras SIEMPRE `awaiting_confirmation` y solo las que el usuario podría confirmar, respaldo por reglas con aviso, `AssistantTurn` v2 con `routedBy`/`citations`/`cost`/`pendingToolCalls`, una fila `answerAnalyticsQuestion` por turno con huella de la pregunta y `cost_eur` 0 por reglas), catálogo unificado de 37 lecturas (12 locales + 10 del copiloto + 15 del registro) con permisos por herramienta, router por reglas con las 19 sugeridas al 100 % y detección de «otra fecha» (dd de mes, ISO, meses, «pasado mañana»), 3 prompts por superficie publicables, memoria cifrada y redactada (2 tablas, migración `20260920170000_asistente_unificado`, purga por retención `[assistant.purge]` 90 d + supresión RGPD de los hilos `guest:<id>`), 6 rutas HTTP (+4 en el manifiesto 1.031 → 1.035, +0 claves RBAC, resolver `assistantConversation`), panel del asistente en el shell (botón, menú compacto, ⌘K «Preguntar al asistente…», `/asistente` con el mismo hilo, superficie por categoría), Pendientes IA que ejecuta al aprobar (`POST /ai/tool-calls/:id/confirm`), pantallas de IA honestas, `/copilot/ask` y el bot del huésped sobre el núcleo con el `UserContext` real / actor `guest:<conversationId>`, e2e del panel (`apps/admin-web/e2e/assistant-panel.spec.ts`), contratos (`docs/api-contracts.md`, `tests/assistant-schema-contract.test.mjs`, `tests/integration/l6b-asistente.test.mts`) y runbook `docs/runbooks/asistente-ia.md` (12 secciones).
- **Puertas** (§3): línea base 12/12 rápidas → **final completa 13/14** (`scratchpad/L6b/gates-final.json`, 15:00): typecheck 15 PASS · api unit 3.647 (3.646 pass · 0 fail · 1 skip) · admin-web unit 2.063 (2.062 · 0 · 1) · ai-core 119 · worker 34 · contratos raíz 772 (770 · 0 · 2) · discoverability 197 · route-access 15 × 197 · cocoa waves · rbac dry-run +0 · migrate 25/25 + drift 0 · build 3,07 s · integración 993 (985 · 0 · 8). La única roja, `nav-tree --check`, es externa al carril (§3.1).
- **Revisión y corrección** (§4): 11 hallazgos confirmados (3 high: cita «undefined» al reabrir una conversación, nombres sin tratamiento en claro en `title`/`input_json`, camino con modelo sin la puerta de propiedad; 8 medium) + 8 low; 0 refutados; 17 corregidos con test y verificación en runtime; 1 fuera del carril (`pnpm-lock.yaml` desincronizado con `apps/admin-web/package.json`, para la fusión).
- **Pendientes de fusión** (§6): regenerar `nav-tree.generated.json` sobre el árbol que contenga RRHH/ACT, regenerar el lock una vez, renumerar la migración si otro carril aporta una marca posterior, `CLAUDE.md` en conflicto manual (tomar `tanda-l6b`), instalar el navegador Playwright del repo para el e2e; sin humo con clave real.

## 1. Qué construyó cada lote (qué · cómo · tests · verificación)

Cifras de `git diff --numstat` al cierre y de los informes de cada lote (evidencias en `scratchpad/L6b/*`, `scratchpad/L6b-REV/*`, `scratchpad/L6b-corr-*`).

### L6b-01 · Modelo de datos, migración, PII y contrato (ola 1)
- **Qué:** 2 modelos Prisma junto a `AiToolCall` (`AssistantConversation`: `organizationId/propertyId/userId/surface/title/screenContextJson/status/lastMessageAt`; `AssistantMessage`: `role/content/toolCallsJson/routedBy/model/tokensInput/tokensOutput/costEur`; `schema.prisma` +51), migración `20260920170000_asistente_unificado` (72 líneas: 2 `CREATE TABLE`, 2 índices, 1 FK `ON DELETE CASCADE`, 0 enums, 0 columnas en tablas existentes, 0 backfill, reversible con 2 `DROP`), `PII_FIELDS.AssistantMessage = ["content"]` (`crypto-fields.ts`, sin lookup hash; el corrector añadió `AssistantConversation: ["title"]`, REV-02).
- **Cómo:** DDL generada con `prisma migrate diff --from-schema-datasource` contra `hotelos_l6b` y copiada verbatim; vocabularios `///` en vez de enums.
- **Tests:** `tests/assistant-schema-contract.test.mjs` (7: migración localizada por sufijo y posterior a `*_checkin_pago_en_recepcion`, 2 tablas exactas, aditiva/reversible, PII sin lookup hash —ahora exige `title` cifrado—, vocabularios, `check-migrations-vs-schema` sin BD).
- **Verificación:** `db:migrate:deploy` + `db:generate`; `migrate status` 25/25 «up to date», `db:drift:check` 0; script en `$transaction` con rollback: `content` en reposo como envelope `v1.`, cascada al borrar, 0 filas residuales.

### L6b-02 · Catálogo unificado y router por reglas (ola 1)
- **Qué:** `assistant-catalog.ts` (296 líneas: `ASSISTANT_CATALOG` = 12 locales + 10 `copilot_<intent>` + 15 del registro, `requiredPermissions` de `PERMISSION_KEYS`, `surfaces`, `todayScoped`, `modelInputSchema`, `run` en las locales; `catalogFor`, `toModelTools`, `copilotToolName`; `logScope` añadido por la pasada de puertas), `assistant-router.ts` (154: `routeByRules` con 4 reglas —keywords por especificidad, otra fecha, una sola del copiloto vía `detectIntent`, conflictos por palabras no genéricas— y `SUGGESTED_QUESTIONS` 6 + 3 + 10 por superficie), `assistant.tools.ts` («hoy» de las 12 lecturas en `Property.timezone`, `get_compliance_summary` sobre `getComplianceCenter` en vez de una SQL contra columnas inexistentes que siempre respondía «sin datos», keyword «por cobrar»), `copilot.service.ts` (`INTENT_KEYWORDS` exportadas, +4 frases del preset `overdue_hk_tasks` que no enrutaba, `COPILOT_RESOLVERS`).
- **Tests:** `assistant-catalog.test.mts` (8) y `assistant-router.test.mts` (las 19 sugeridas → exactamente una herramienta, presets → intent, otra fecha, composición vs especificidad; +casos REV-06 del corrector).
- **Verificación:** tenant aislado en `hotelos_l6b`: recepción ve 32 herramientas, contable 23 (0 de pisos); las 19 lecturas locales ejecutadas (18 ok; `get_compliance_summary` corregida en la segunda pasada); por HTTP `POST /copilot/ask` «¿Qué tareas de housekeeping están retrasadas?» → `overdue_hk_tasks` (antes `unknown`).

### L6b-03 · Cliente v2 y componentes del panel (ola 1)
- **Qué:** `services/assistantApi.ts` (+204/−4: tipos v2 aditivos `AssistantTurn`/`AssistantCitation`/`AssistantCost`/`AssistantPendingToolCall`/conversaciones, `askAssistant(string | { question, conversationId?, surface?, screen? })`, `listConversations/getConversation/deleteConversation/listPending/confirmToolCall`), `components/assistant/AssistantPanel.tsx` (488), `AssistantThread.tsx` (218), `assistant-context.ts` (99: `buildScreenContext` sin PII ni query, `SAFE_ID`), `assistant-panel-store.ts` (149: store fuera de React, evento `hotelos-open-assistant`).
- **Tests:** `components/__tests__/assistant-panel.test.mts` (25 al cierre: contexto sin PII, store, citas y coste, `turnsFromMessages` con `toolCalls` guardados (REV-01), 19 sugeridas cubiertas por keywords, cliente v2, 0 `style=` y solo Cocoa, listado por superficie, ⌘K abre hilo nuevo).
- **Verificación:** SSR sin DOM del hilo (badge «Por reglas», coste, cita `get_arrivals_today · Prisma · reservations`, tarjeta «Escritura pendiente», `role="log" aria-live="polite"`); 10 sugeridas por HTTP → 10/10 por reglas; tolerancia al 404 de los endpoints v2 mientras no existían.

### L6b-04 · Front honesto de las pantallas de IA (ola 1)
- **Qué:** `ai-operations-labels.ts` (checks `provider`/`budget` en español, `skipped` = «omitida (sin modelo)», `costLabel` «—» con título para NULL, `costCallsTotal`, `aiUsageStatus` «En uso» solo con llamadas reales), `AiOwnerSummaryScreen.tsx` (tile «Estado de la IA» `Encendida · Sin modelo` / `En uso · N acciones en 30 días`, proyección «—» sin coste real), `AiPipelineStatusScreen.tsx` (coste NULL como «—»).
- **Tests:** `ai-operations-labels.test.mts` (19).
- **Verificación:** Playwright sobre `:3933/:5203` con UXDAY (`scratchpad/L6b/l6b04-*.png`): `/hoy/informe-ia` «Encendida · Sin modelo», KPIs «0,00 € sin coste real», proyección «—»; `/configuracion/ia/actividad` celda «—» con título «hubo llamada sin tipo de cambio»; readiness 5 de 6 con `provider` en aviso.

### L6b-05 · Núcleo conversacional, memoria y prompts (ola 2)
- **Qué:** `assistant-core.service.ts` (774 al cierre: `runAssistantTurn`, `visibleCatalogFor` con `canExecuteToolForModules`, `writeToolsFor`, `answerByRules`/`answerByModel` con `MAX_MODEL_TURNS` 3 · 700 tokens · 6 000 caracteres por resultado, `RulesFallback` ante `AiError`/`configured:false`/403/429/rechazo, redacción antes de persistir, fila `answerAnalyticsQuestion` por turno), `assistant-memory.service.ts` (561: puerto `AssistantMemoryStore` Prisma + en memoria, `openAssistantConversation` con 404 opaco, ventana de 10 mensajes, listado/detalle/borrado acotados a usuario + propiedad + organización), `assistant-prompts.ts` (156: `assistant_backoffice/reception/guest`, `normalizeScreenContext`, `composeUserMessage`), `assistant.service.ts` reducido a fachada (54 líneas).
- **Tests:** `assistant-core.test.mts` (21 al cierre) y `assistant-memory.test.mts` (8).
- **Verificación:** 3 turnos por HTTP con UXDAY (reglas, cita, coste 0, `conversationId`); tablas `assistant_*` con `content` cifrado y 0 apariciones de nombre/teléfono; 404 entre usuarios; detectó que las rutas aún no pasaban `conversationId` (cerrado por L6b-06).

### L6b-06 · Rutas HTTP, manifiesto, tenencia y contratos (ola 3)
- **Qué:** `routes/assistant.routes.ts` (298: `POST /assistant/chat` con `conversationId/surface/screen` y `x-correlation-id`, `GET /assistant/tools?surface=` con `visibleCatalogFor` + sugeridas, `GET/DELETE /assistant/conversations[/:id]`, `GET /assistant/pending` con input redactado), `security/route-permissions.ts` (+10: 3 rutas `authenticated` + `pending` con `ai.tool.execute`), `lib/tenancy.ts` (+16: resolver `assistantConversation`, solo filas del propio usuario), `docs/api-contracts.md` (sección «Asistente unificado» l.735, manifiesto 1.031 → 1.035), `developer/api-reference.service.ts` (`pending: "la escritura pendiente"`).
- **Tests:** `routes/__tests__/assistant-routes.test.mts` (15) y `tests/integration/l6b-asistente.test.mts` (10: catálogo recepción vs contabilidad, 401 en las cinco rutas, chat por reglas, memoria privada entre dos usuarios de la misma propiedad, 204 con cascada, 404 opaco, modelo simulado propone `createWorkOrder` → `pending` solo a quien preguntó → `confirm` ejecuta `work_orders` +1).
- **Verificación:** instancia `:3933` con UXDAY: 31 herramientas · 19 sugeridas, `?surface=cocina` → 400, memoria por `conversationId` (4 mensajes), `pending` `{ items: [] }`, DELETE 204 → GET 404, `api-route-permissions-contract` 32/32.

### L6b-07 · Shell, ⌘K y página `/asistente` (ola 2)
- **Qué:** `layouts/BackOfficeLayout.tsx` (`AssistantButton` en la barra con `aria-haspopup="dialog"`, fila «Asistente ehotelOS» del menú compacto, `assistantSurfaceForScreen` (categoría «recepcion» → `reception`), montaje único de `<AssistantPanel>`, ⌘K y `hotelos-open-search` cierran el panel, «Abrir asistente completo»), `components/CommandPalette.tsx` (ítem `assistant:ask` último con badge «IA», único sin coincidencias, guardia `focusin`), `screens/assistant/AssistantChatScreen.tsx` (mismo hilo y conversaciones que el panel; 15 → 0 `style=`).
- **Tests:** `CommandPalette.test.mts` (18) y la aserción de montaje en `assistant-panel.test.mts`.
- **Verificación:** Playwright con UXDAY (`scratchpad/L6b/l6b07-*`): ⌘K «cuántas llegadas tengo hoy» → único ítem → panel con «POR REGLAS», «sin coste», cita `get_arrivals_today · prisma:Reservation.arrivalDate=today(Property.timezone)`; Esc devuelve el foco; 390 px → hoja inferior; en `/recepcion/reservas/lista` cuerpo `{ surface: "reception", screen: { screenKey: "ReservationWorkspace", url, commands: [...] } }` sin query ni nombres.

### L6b-08 · Copiloto y bot del huésped sobre el núcleo (ola 3)
- **Qué:** `copilot/copilot.service.ts` (873 al cierre: `answerCopilot` delega en `runAssistantTurn` con superficie `reception`, forma `CopilotAnswer` intacta gracias a una captura `AsyncLocalStorage` alrededor de los resolvers, `unknown`/`router` histórico, carga diferida del núcleo por el ciclo de imports; tras el corrector usa el `UserContext` de la petición y el actor `system:checkin:copilot` solo si el `propertyId` pedido no es el activo), `checkin/guest-bot.service.ts` (1.055: `answerWithModelViaCore` —superficie `guest`, actor `guest:<conversationId>` con solo `ai.tool.execute`, `stripAssistantNotices`— y `classifyWithCore` con el prompt `assistant_guest`; retirados `llmClassify` del shim y `recordAs guest_bot_reply`).
- **Tests:** `copilot/__tests__/copilot-core.test.mts` (8) y `checkin/__tests__/guest-bot.test.mts` (22).
- **Verificación:** instancia propia con `org_chk` (`scratchpad/L6b/l6b08-runtime.json`): 10 presets → su intent con `degraded []` y claves exactas sin marca de modelo; bot por reglas con aviso de IA, `late_checkout` → `pending_confirmation` con `toolCallId`; 0 filas fuera de `org_chk`/`org_uxday`.

### L6b-09 · Pendientes IA: aprobar ejecuta (ola 2)
- **Qué:** `AiHumanReviewQueueScreen.tsx` (ítems `ai_tool_call` con badge «Ejecuta al aprobar», «Aprobar y ejecutar» / «Rechazar» → `confirmToolCall`, resultado como callout + badge), `ai-review-labels.ts` (`CONFIRM_OUTCOME_LABELS`, `confirmResultOutcome`/`confirmErrorOutcome`, etiquetas del payload). Confirm-only: el runner cierra la revisión (AI-CORE §10.3), no se llama a `/review/:id/approve` (D7).
- **Tests:** `ai-review-labels.test.mts` (19).
- **Verificación:** Playwright como dirección de UXDAY (`scratchpad/L6b/l6b09-*`): aprobar → «Ejecutada … `blockRoomForMaintenance`» con `ai_tool_calls.succeeded`, `work_orders.blocks_room`, habitación `out_of_order`, auditoría REQUESTED:ai → CONFIRMATION_APPROVED:user → EXECUTED:ai; rechazar → «Rechazada»; > 24 h → «Caducada» (409); sin permiso → «Sin permiso … `maintenance.workorder.manage`, `ai.high_risk.confirm`» (403). Para la escena se concedió `maintenance.workorder.manage` al rol «Dirección general» de `org_uxday` (`role_permissions` `cml6b09uxdaygmwomanage0001`; §6).

### L6b-10 · e2e mínimo del panel (ola 4)
- **Qué:** `apps/admin-web/e2e/assistant-panel.spec.ts` (334 líneas, 6 casos: recepción → toolbar → sugerida «Por reglas» con cita y turno del API `routedBy rules`/`cost.model null`; ⌘K → «Preguntar al asistente…» → Intro → panel; memoria tras recargar (lista y reapertura); privacidad entre dos usuarios de la misma propiedad (lista sin ella, 404); accesibilidad (`aria-modal`, `aria-expanded`, foco en «Pregunta», `role=log`, Esc); (3b) cita conservada al reabrir). Sesión API propia, `retirePreviousRun` por `DELETE` del producto, locator `[data-tour="assistant"]`.
- **Verificación:** `corepack pnpm --filter @hotelos/admin-web e2e assistant-panel` sobre instancias propias `:3963/:5233` (los `:3933/:5203` estaban ocupados por otro carril) con `PLAYWRIGHT_BROWSERS_PATH` de symlinks a chromium 1228 (el lock del worktree pedía 1243): **5/6**; (3b) rojo a propósito por el defecto REV-01 (cita «undefined» al reabrir), corregido después por el corrector y verificado con navegador y test unitario, sin volver a ejecutar la spec (§6). No corre en las puertas.

### L6b-11 · Runbook, diseño, CLAUDE.md, estado verificado e informe v1 (ola 4)
- **Qué:** `docs/runbooks/asistente-ia.md` (381 líneas al cierre, 12 secciones: configuración, superficies, catálogo y permisos, enrutado, memoria y PII, trazabilidad, HITL, rutas, front, puertas, runtime, límites y tabla N1-N9 de lo que solo César aporta), `docs/design/AI-CORE.md` (§5.3 consumidor de tool use, §14 fila `assistant_*`, §16.11 hecho, §17 filas 5 y 16 con la decisión aplicada), `CLAUDE.md` (deuda 19 + 2 líneas en «Docs prioritarios»), bloque L6b en `ESTADO-VERIFICADO.md`, informe v1 (sustituido por este).
- **Verificación:** instancia `:3943` con `HOTELOS_ALLOW_DEMO_AUTH=false RBAC_STRICT=true` y UXDAY, 10 comprobaciones del runbook §11 (`scratchpad/L6b/l6b11-runtime.log`); `brand-contract` + `manual-contract` 25/25.

### Puertas completas (runner, 13:33 y 13:53)
- Pasada 1 `12/14` (`gates-full.log`): `nav-tree --check` + 2 fallos en `tests/integration/l2-robustez.test.mts`. Causas raíz corregidas en código sin debilitar tests: (a) L2-06 (3) exige el aviso «[copilot.ask] work_orders failed» y el colector unificado emitía «[assistant.copilot]» → `logScope` en `AssistantRunContext`/`RunAssistantTurnInput` y `COPILOT_LOG_SCOPE = "copilot.ask"` en el alias; (b) L2-06 (4) contaba 5 `auditEvent.create` de la cola de auditoría encadenada por suites anteriores → `await flushAuditQueues()` antes de cada `countPrismaOps` (umbrales intactos). Pasada 2 **13/14** (`gates-full.json`, integración 993 · 985 · 0 · 8). `pnpm-lock.yaml` restaurado desde HEAD; `nav-tree.generated.json` regenerado en pruebas y restaurado (idéntico a la base).

### Revisión (2 revisores, 13:55-14:14) y corrector (14:34-14:53)
- Ver §4. Evidencias: `scratchpad/L6b-REV/{api-walk-report.json, ui-walk-report.json, write-tools.mts, redact-check.mts, hitl-verify.json, ui-*.png}` y `scratchpad/L6b-corr-{walk.json, gates-quick.json, gates-full.json}`.

## 2. Delta frente al dosier (`scratchpad/L6b/recon-delta.md`, 16 puntos) → estado al cierre

| # | Recon (2026-09-20 mañana) | Al cierre |
|---|---|---|
| 1 | ruta de confirmación y `checks.ai` ya aplicados (L6a fusionada) | sin cambio; la cola Pendientes IA y el panel la consumen (L6b-09, L6b-03) |
| 2 | 28 herramientas con `execute` (15 lecturas + 13 escrituras) | las 15 lecturas en el catálogo; las 13 escrituras expuestas al modelo por `writeToolsFor` solo si el usuario podría confirmarlas (REV-04) |
| 3 | puertas base 12/12 | §3: final completa 13/14 (única roja externa) |
| 4 | `deriveAssistantMode` corregido; «L7 pasa `/assistant` a `ai.tool.execute`» no aplicado | `tools`/`chat`/`conversations` siguen `authenticated`; `pending` con `ai.tool.execute` (D11) |
| 5 | CSV fila 79 «AssistantChat también en barra superior y ⌘K» NO existía; `/copilot/*` sin consumidor | hecho (L6b-07); `/copilot/*` alias sobre el núcleo con el `UserContext` real (D8) |
| 6 | AI-CORE §5.3 «ningún flujo pasa `tools` al núcleo» | el núcleo pasa `tools` + `toolChoice: "auto"`; `insight` sigue sin consumidor |
| 7 | deuda admin-web L6a (`skipped`, readiness provider/budget, owner summary) | cerrada (L6b-04) |
| 8 | aprobar en Pendientes IA no ejecutaba | cerrado (L6b-09; AI-CORE §17.16) |
| 9 | asistente con 12 herramientas y router por keywords; telemetría directa | catálogo 37 + router por reglas + núcleo con memoria; fila por turno con huella de la pregunta y coste 0 por reglas |
| 10 | recepción sin `analytics.ai_ask` → el turno no puede ir por `runAiTool` | el turno se registra con `recordToolCall`; las puertas de propiedad del runner se replican en `assistant-gate.ts` antes de la llamada directa al modelo (L6B-REV-01); solo las herramientas del registro pasan por `runAiTool` |
| 11 | bot con `llmClassify` del shim; `Conversation`/`Message` no valen como memoria | bot sobre `ai.classify` + turno `guest` del núcleo; memoria propia `assistant_*` |
| 12 | ai-core con `tools`/`toolResultBlock`/`complete` multi-turno disponibles | consumidos por el núcleo (solo `fetch` simulado, sin clave) |
| 13 | front: `CocoaDrawer`, page commands, `useApiData`, `apiRequest`, 6 sugeridas | panel, contexto y store nuevos; `AssistantChatScreen` sobre los mismos componentes |
| 14 | PII: `PII_FIELDS` y `ENCRYPTION_KEY` presentes | `PII_FIELDS.AssistantMessage` + `AssistantConversation.title`; redacción antes de persistir; el redactor NO reconoce nombres sin tratamiento (documentado en AI-CORE §17.8, runbook §5) |
| 15 | tenencia: patrón `aiToolCallConfirmation`; contrato de rutas por `routes/*.ts` | resolver `assistantConversation`; 4 rutas nuevas en el manifiesto |
| 16 | docs: api-contracts sin sección, CLAUDE.md deuda 18, ESTADO con bloque CHK al final | sección «Asistente unificado» (l.735), deuda 19, bloque L6b, runbook, notas en CHK design §5 y runbook checkin §10 |

## 3. Puertas (línea base 11:05 → final 15:00)

Todas con `bash scripts/gates.sh [--quick] --json <f>` y `NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv`. Rápidas por ola: `gates-ola1.json` 12/12 (11:46) · `gates-ola2.json` 12/12 (12:16) · `gates-ola3.json` 11/12 (12:51, nav-tree) · `gates-ola4.json` 11/12 (13:24). Completas: runner 12/14 → 13/14 (13:33 / 13:53), revisor rápida 11/12 (`L6b-REV/gates-quick-rev2.json`), corrector rápida 11/12 + completa 13/14 (`L6b-corr-gates-quick.json`, `L6b-corr-gates-full.json`, 14:43 / 14:53), **final completa 13/14** (`L6b/gates-final.json`, 15:00; logs por puerta `gates-final.<puerta>.log`).

| Puerta | Línea base (`gates-base.json`) | Final completa (`gates-final.json`) |
|---|---|---|
| typecheck:all | 15 PASS · 0 FAIL · 1 SKIP · 51,6 s | 15 PASS · 0 FAIL · 1 SKIP · 22,6 s |
| api unit | 3.569 (3.568 pass · 0 fail · 1 skip) | **3.647** (3.646 pass · 0 fail · 1 skip) |
| admin-web unit | 2.014 (2.013 · 0 · 1) | **2.063** (2.062 · 0 · 1) |
| ai-core | 119 (119 · 0 · 0) | 119 (119 · 0 · 0) |
| worker | 34 (34 · 0 · 0) | 34 (34 · 0 · 0) |
| contratos raíz | 765 (763 · 0 · 2) | **772** (770 · 0 · 2) |
| discoverability | 197 URL registradas | 197 URL registradas |
| nav-tree --check | al día (70 ítems · 104 pestañas · 205 redirecciones) | «stale» frente al CSV compartido — **rojo, externo** (§3.1) |
| route-access | 15 tokens × 197 URLs | 15 tokens × 197 URLs |
| cocoa waves --check | §6 al día | §6 al día |
| rbac:sync --dry-run | OK · 1.116 ms | OK · 236 ms (+0 claves) |
| migrate status + drift | No difference detected. (24/24) | No difference detected. (25/25) |
| admin-web build | — (no medida en la base) | ✓ built in 3,07 s |
| integración | — (no medida en la base) | 993 (985 pass · 0 fail · 8 skip) |

Puerta rápida del integrador tras escribir este informe y el bloque de estado (solo `.md`): ver §3.2.

### 3.1 La única puerta roja: `nav-tree --check`, externa al carril

`apps/admin-web/src/navigation/nav-tree.generated.json` del worktree es idéntico a HEAD (`git diff` vacío; L6b no lo toca). El CSV compartido `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` (fuera del worktree) fue modificado durante el día por otros carriles: 12:19 (filas 19/31 `retire → AssistantChat`, que sí corresponden a esta tanda), 13:30 (Tanda RRHH: `HrEmployeesScreen`, `HrForecastScreen`, `HrOverviewScreen` bajo `/finanzas/nominas/*`) y 14:21 (Tanda ACT: `DirectorLaborCostsScreen`, `RealEstate{Asset,Documents,Group,Inspections,Taxes,Works}Screen`); 294 → 304 filas, 70 → 71 ítems, 104 → 113 pestañas. Esas pantallas no existen en este worktree (grep sin coincidencias en `apps/admin-web/src`): regenerar aquí produciría rutas a componentes inexistentes y tiraría `route-access`/`discoverability`. Se probó, se inspeccionó el diff y se restauró el fichero original (copia en `scratchpad/L6b/nav-tree.before.json`). Se resuelve en la fusión, regenerando sobre el árbol que ya contenga RRHH/ACT (§6), o pasando a la puerta un CSV congelado en la base. Rojo desde la ola 3 (12:51) por la misma causa.

### 3.2 Rojos transitorios de las olas, todos cerrados en el árbol

`cocoa-22-contract` regla 15 (inventario regenerado: 245 pantallas · 107.680 líneas · 171 puntos, `inlineStyles` 624 → 609, `cocoaCards` 35 → 32; `COCOA-22-MIGRACION.md` §6 al día), `api-reference.test` (segmento `pending` etiquetado), `assistant-panel.test` (aserción del montaje en el shell invertida cuando L6b-07 montó el panel), `seed-ux-day-contract` («las 12 specs de recepción», incluida `assistant-panel.spec.ts`), `api unit`/`rbac:sync`/`migrate` por «too many clients» de Postgres con varias instancias de carril simultáneas (verdes en la repetición), `l2-robustez` (causas raíz corregidas por el runner, §1). Cifras de la puerta rápida del integrador (solo docs cambiadas después de la final): se rellenan en el bloque «Puerta rápida del integrador» al final de esta sección.

**Puerta rápida del integrador** (`scratchpad/L6b/gates-integrador-quick.json`, 15:11-15:21, con dos `typecheck-all` y suites de otro carril corriendo a la vez): **10/12** — typecheck:all 15 PASS · 0 FAIL · 1 SKIP · 53,8 s · api unit 3.647 (3.645 pass · **1 fail** · 1 skip) · admin-web unit 2.063 (2.062 · 0 · 1) · ai-core 119/119 · worker 34/34 · contratos raíz 772 (770 · 0 · 2; incluye `brand-contract` y `manual-contract` sobre las docs nuevas) · discoverability 197 · nav-tree --check ROJO (externo, §3.1) · route-access 15 × 197 · cocoa waves §6 al día · rbac:sync dry-run 475 ms · migrate + drift «No difference detected.». El fallo de `api unit` es **transitorio**: el resumidor `| sum` de `gates.sh` descarta el nombre del test (el log por puerta `gates-integrador-quick.api_unit.log` guarda la salida ya resumida, 55 bytes) y la repetición aislada inmediata de la suite completa (`node --import tsx --test "src/**/__tests__/*.test.mts"` desde `apps/api`, `scratchpad/L6b/api-unit-integrador.log`) da 3.647 · 3.646 pass · 0 fail · 1 skip · 27,2 s; este lote solo cambia `.md` y la puerta completa de las 15:00 sobre el mismo código es 13/14. Hook `.husky/pre-commit` ejecutado a mano desde `hotelos/` (`scratchpad/L6b/pre-commit-integrador.log`): discoverability OK + `typecheck-all` 15 PASS · 0 FAIL · 1 SKIP · 37,4 s → «Pre-commit checks passed».

## 4. Hallazgos de la revisión: confirmados, refutados, corregidos

Dos revisores en paralelo sobre el árbol de la ola 4 (instancias propias `:3933/:5203`, tenant UXDAY, SQL de solo lectura). **Refutados: ninguno.** Duplicados fusionados: funcional REV-02 ≡ seguridad L6B-REV-03.

| Id | Sev. | Hallazgo (fichero) | Corrección del corrector · evidencia |
|---|---|---|---|
| REV-01 | high | Conversación reabierta pinta cada cita «undefined · fuente»: `turnsFromMessages` copiaba los `toolCalls` guardados (`{tool, source, ok, summary}`) a `turn.toolCalls` tipados `{name, …}` (`AssistantPanel.tsx`, `AssistantThread.tsx`); e2e (3b) rojo por esto | **Corregido**: convierte a `toolCalls {name,…}` + `citations`; `AssistantMessage.toolCalls` tipado como `AssistantStoredToolCall`; test «REV-01» en `assistant-panel.test.mts`; navegador: reabierta cita `get_arrivals_today · prisma:…` sin «undefined» |
| REV-02 ≡ L6B-REV-03 | high | Nombres sin tratamiento («¿Tiene reserva Nombre Apellido?») en claro en `assistant_conversations.title` (columna no cifrada) y en `ai_tool_calls.input_json.question`; el redactor (`packages/ai-core/src/redaction.ts` `NAME_RE`) solo reconoce nombres tras Sr./Sra./presentación | **Corregido**: `PII_FIELDS.AssistantConversation = ["title"]` (sin migración; envelope `v1.` en filas nuevas), la fila `answerAnalyticsQuestion` guarda solo `questionChars` + `questionSha256` (nunca el texto), docs rebajadas a «documentos, teléfonos, correos, tarjetas y nombres con tratamiento/presentación» (api-contracts, runbook §5/§12, AI-CORE §14/§17.8); tests `assistant-core.test.mts` «REV-02» y `assistant-schema-contract` (exige `title` cifrado); SQL en el carril: `left(title,3)='v1.'`, `input_json ? 'question' = f` |
| REV-03 | medium | El núcleo no sanea `screen`: `normalizeScreenContext` solo recortaba a 120 caracteres → nombres y query strings en `screen_context_json` y en `input_json.screen` desde clientes distintos del panel (`assistant-prompts.ts`) | **Corregido**: `SAFE_ID` en `screenKey`, `entity.type/id` (entidad descartada si no cumple) y `commands` (≤ 20, sin duplicados); `url` = ruta sin `?`/`#` con segmentos no seguros → `_`; runtime: `url '/recepcion/huespedes/_'`, entidad vacía; test «REV-03» |
| REV-04 | medium | `writeToolsFor` ofrecía al modelo escrituras que el runner deniega siempre o que el usuario nunca podrá confirmar (`createWorkOrder` con `create` vs `manage` de la matriz; `prepareGuestRegisterRecord`/`queueSesHospedajesSubmission` high sin `ai.high_risk.confirm`) (`assistant-core.service.ts`) | **Corregido**: descarta las escrituras con `evaluateAiSafetyForTool(...).denied` y las `high|critical` sin `ai.high_risk.confirm`; `write-tools.mts` del revisor: recepción → `[assignRoom(medium)]` (antes 4); test «REV-04»; matriz de riesgo sin tocar |
| REV-05 | medium | La superficie de una conversación no se respetaba: un `conversationId` `reception` aceptaba turnos `backoffice` (catálogo y prompt distintos) y el panel listaba conversaciones de todas las superficies (`assistant-memory.service.ts`, `AssistantPanel.tsx`) | **Corregido**: `effectiveSurface` de la conversación guardada para catálogo, prompt, telemetría y `turn.surface`; el panel lista `listConversations({ surface })`; runtime: t2 con `surface backoffice` → `reception`, `?surface=backoffice` no la incluye; test «REV-05» + contrato de fuente |
| REV-06 | medium | Regla 2 del router («otra fecha» excluye `*_today`) no reconocía «15 de septiembre» ni ISO: respondía el dato de HOY como si fuera el del 15 (`assistant.tools.ts` `mentionsAnotherDate`) | **Corregido**: dd de mes, `aaaa-mm-dd`, `dd-mm-aaaa`, meses y «pasado mañana»; las lecturas `*_today` devuelven `data.date` y el resumen dice «hoy (20/09/2026)»; runtime: «¿cuántas llegadas hubo el 15 de septiembre?» → sin citas; tests en `assistant-router.test.mts` y `assistant-core.test.mts` |
| L6B-REV-01 | high | El camino con modelo (`answerByModel`, hasta 3 llamadas) y la clasificación del bot llamaban al proveedor sin las puertas 3/4/7 del runner (`aiEnabled`, nivel `off`, presupuesto): con clave real, una propiedad apagada o sin presupuesto seguía generando llamadas facturables (`assistant-core.service.ts`, `guest-bot.service.ts`) | **Corregido**: `assistant-gate.ts` nuevo (`decideAssistantPropertyGate` pura + `evaluateAssistantPropertyGate` con `getPropertyAiSettings`, `PropertyAiToolSetting`, `monthToDateCostEur`, `monthlyBudgetEurOf`; fallo de lectura = puerta cerrada); el núcleo la evalúa antes de `answerByModel` (cerrada → reglas con aviso, 0 llamadas) y `classifyWithCore` la consulta; tests: 3 casos en `assistant-core.test.mts` (aiEnabled=false / presupuesto / nivel off → `captured.length 0`) y `guest-bot.test.mts` |
| L6B-REV-04 | medium | `/copilot/ask` (`authenticated`) ejecutaba el núcleo con un actor sintético con 5 claves de lectura fijas y memoria compartida por propiedad, ignorando el RBAC del usuario real (`copilot.service.ts`, `server.ts`) | **Corregido**: `server.ts` pasa `request.userContext`; `answerCopilot` lo usa cuando su `propertyId` es el pedido (si no, contexto de servicio → 404 opaco como antes); SQL: filas y conversación a nombre del usuario; test «L6B-REV-04» (sin `folio.read` → `unknown`); manifiesto sin cambio |
| L6B-REV-06 | medium | `pnpm-lock.yaml` (restaurado desde HEAD) no contiene `@playwright/test`, `zod` ni `@fontsource-variable/inter` que `apps/admin-web/package.json` declara: `pnpm install --frozen-lockfile` falla; preexistente a L6b (base CHK) | **No corregido en el carril** (lock intocable): en la fusión `corepack pnpm install --lockfile-only` una sola vez y fijar `@playwright/test` a la versión cuyo navegador está instalado (1.61.x → chromium 1228) o instalar el 1243 (§6) |
| L6B-REV-07 | medium | `assistant_conversations/assistant_messages` fuera de la supresión RGPD y sin job ni columna de retención: «90 días» era SQL manual (`gdpr.service.ts`) | **Corregido**: `purgeAssistantConversations({ retentionDays })` + scheduler `[assistant.purge]` en `server.ts` bajo el líder de schedulers (`ASSISTANT_MEMORY_RETENTION_DAYS` 90 · `ASSISTANT_MEMORY_PURGE_INTERVAL_MS` 6 h · `ASSISTANT_MEMORY_PURGE_DISABLED`; censo `env.ts`/`.env.example`/`deploy/.env.production.example`/`scripts/env-contract.json` regenerado); `executeErasure` borra los hilos `guest:<conversationId>` y `compileDossier` los incluye; tests en `assistant-memory.test.mts` |

Low (todos corregidos salvo el que ya no aplicaba): REV-07 `GET /assistant/pending` incluye las propuestas `awaiting_confirmation` cuyo `user_id` es el propio usuario aunque la conversación ya no exista (D6 acotada al proponente; test en `assistant-routes.test.mts`); REV-08 ⌘K con el panel cerrado abre un hilo nuevo (`threadForPendingQuestion`; test); REV-09 el compositor del pie del `CocoaDrawer` se estira (`c22-assistant-composer` en `cocoa-22-shell.css`, 0 `style=`; runtime 591/640 px); L6B-REV-08 geometría de las filas del menú compacto movida a `.cocoa-shell .cocoa-menu-item` (5 `style={menuItemStyle}` retirados: `BackOfficeLayout.tsx` 52 → 47, HEAD 51); L6B-REV-10 `gates.sh --json` guarda la salida completa de cada puerta en `<json>.<puerta>.log`; L6B-REV-11 docs (api-contracts L2 «1035 entradas», `AssistantConversationStatus = open | archived`, import muerto `getAvailableTools` retirado de `server.ts`, notas L6b en `docs/runbooks/checkin-automatizado.md` §10 y `docs/design/CHECKIN-AUTOMATIZADO-IA.md` §5, CLAUDE.md deuda 19 actualizada); L6B-REV-12 `knownGuestPii` acotado a `ctx.user.propertyId` y, en `guest`, `input.conversationId` fijado al actor (test «L6B-REV-12»); L6B-REV-09 instancias huérfanas `:3933/:5203` de un lote anterior: ya no existían al empezar la corrección.

Desvíos del corrector respecto a los «fix» sugeridos, documentados: REV-02 cifra `title` en vez de derivar el título sin texto libre (e2e e integración fijan `title` = pregunta); `screen_context_json` no se cifra: REV-03 garantiza solo ids seguros; REV-05 superficie efectiva en el servidor sin 409; REV-07 alternativa D6 acotada al proponente; L6B-REV-04 manifiesto de `/copilot/ask` sigue `authenticated` (el RBAC lo aplica el catálogo: sin lectura → `unknown`, nunca 403); L6B-REV-01 puerta con fallo de lectura = cerrada (nunca fail-open), sin tocar el runner. `tests/assistant-schema-contract.test.mjs` invierte la aserción «AssistantConversation no se cifra» → exige `title` cifrado (cambio de contrato, no debilitación).

## 5. Decisiones para César (opción por defecto aplicada en el árbol)

| # | Decisión | Aplicado por defecto | Dónde | Qué aporta César |
|---|---|---|---|---|
| D1 | Clave, DPA, ZDR, residencia, presupuesto | sin `AI_PROVIDER`: todo por reglas y así se dice (`routedBy rules`, «Por reglas», «Sin modelo»); con clave, `AI_MODEL` para el turno y `AI_MODEL_CLASSIFY` para el bot; el camino con modelo solo tiene `fetch` simulado | `lib/ai-config.ts`, runbook §1/§12 (N1) | cuenta, DPA, ZDR, `AI_USD_EUR_RATE`, límite de gasto, humo real |
| D2 | Superficies y cómo se eligen | 3 (`backoffice` · `reception` · `guest`); en el shell solo la categoría «Recepción» del árbol usa `reception` («Mi día» vive en «Hoy» → `backoffice`); la superficie de una conversación guardada manda sobre la de la pantalla (REV-05) | `assistantSurfaceForScreen` (BackOfficeLayout), `assistant-catalog.ts`, núcleo | si «Mi día» debe usar el prompt de recepción |
| D3 | Escrituras que ve el modelo | las 13 con implementación cuyo módulo está activo, cuyos permisos tiene el usuario y que el usuario podría confirmar (matriz de riesgo no denegada; `high|critical` solo con `ai.high_risk.confirm`); ninguna en `guest` | `writeToolsFor` (assistant-core.service.ts) | subconjunto por superficie de CHK §5 o alinear `create_maintenance_task` de la matriz a `maintenance.workorder.create` (N6) |
| D4 | Autonomía | ninguna: `WRITE_ALWAYS_CONFIRMS`, toda escritura `awaiting_confirmation` (AI-CORE §17.5 aplicada) | runner | si `createHousekeepingTask`/`markRoomClean` pueden ser autónomas con aprobador registrado (N2) |
| D5 | Memoria y retención | por usuario + propiedad + superficie, cifrada (`content`, `title`) y redactada (solo tratamientos, documentos, teléfonos, correos, tarjetas); purga `[assistant.purge]` a los 90 días por `last_message_at` (6 h, desactivable); RGPD borra los hilos `guest:<id>` del sujeto; las conversaciones del personal que lo mencionan solo las agota la retención | `assistant-memory.service.ts`, `server.ts`, `gdpr.service.ts`, `env.ts` | plazo por hotel (`ASSISTANT_MEMORY_RETENTION_DAYS`) (N3) |
| D6 | Alcance de `GET /assistant/pending` | propuestas `awaiting_confirmation` de la propiedad activa cuyo `conversation_id` es una conversación del propio usuario o cuyo `user_id` es el propio usuario; el resto sigue en `GET /ai/tool-calls` (`audit.read`) | `pendingToolCallsWhere` (assistant.routes.ts) | «todo lo pendiente de la propiedad para quien tenga `ai.tool.execute`» = quitar un filtro (N6) |
| D7 | Pendientes IA | aprobar un ítem `ai_tool_call` llama a `POST /ai/tool-calls/:id/confirm` y ejecuta (confirm-only: el runner cierra la revisión); ítems ya aprobados antes de la tanda quedan con botones deshabilitados | `AiHumanReviewQueueScreen.tsx`, AI-CORE §17.16 | si la cola debe llamar además a `/review/:id/approve` (tolerando el 409 posterior) |
| D8 | `/copilot/*` | alias sobre el núcleo con el `UserContext` de la petición (claves y memoria del usuario; actor `system:checkin:copilot` solo si el `propertyId` pedido no es el activo), forma `CopilotAnswer` intacta, manifiesto `authenticated`; sin consumidor en admin-web ni móvil; resolvers en UTC | `copilot.service.ts`, `server.ts` | retirarlo en L7 o conservarlo para la app móvil (N7) |
| D9 | Bot del huésped | núcleo solo con proveedor (clasificación con `assistant_guest` + turno `guest` con `answerGuestQuestion`, ambos tras la puerta de propiedad); actor `guest:<conversationId>`; reglas y plantillas del bot sin proveedor; escrituras del bot por su propio `runAiTool`; hilo duplicado en `assistant_*` (cifrado, con supresión RGPD) | `guest-bot.service.ts` | si el hilo del huésped debe vivir solo en `conversations`/`messages` |
| D10 | Entrada al asistente | botón de la barra, menú compacto, ⌘K («Preguntar al asistente…» como último ítem; único sin coincidencias; abre hilo nuevo si el panel estaba cerrado) y evento `hotelos-open-assistant`; sin atajo nuevo; ⌘K cierra el panel | `BackOfficeLayout.tsx`, `CommandPalette.tsx` | otro atajo dedicado o no |
| D11 | Permisos de las rutas | `tools`/`chat`/`conversations` `authenticated` (catálogo filtrado por las claves reales, memoria privada); `pending` con `ai.tool.execute`; `rbac:sync` +0 claves | `route-permissions.ts` | pasar `/assistant/chat` a `ai.tool.execute` (rompe `l2-modulos-ia` y `l6a-integrador`, que lo fijan `authenticated`) |
| D12 | «Hoy» | las 12 lecturas locales en `Property.timezone` (`propertyToday`, con la fecha en el resumen); los 10 resolvers del copiloto siguen en UTC (`startOfDayUtc`) | `assistant.tools.ts`, `copilot.service.ts` | unificar los resolvers (cambio pequeño en `Ctx`) |

## 6. Pendientes (con dueño)

**Fusión (orquestador):**
1. `nav-tree --check`: regenerar `node scripts/build-nav-tree.mjs --csv /Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` SOBRE el árbol que ya contenga las pantallas RRHH/ACT (o congelar el CSV en la base para la puerta); en L6b el generado es idéntico a HEAD.
2. `pnpm-lock.yaml`: regenerar una sola vez en la fusión (`corepack pnpm install --lockfile-only`) y commitearlo; hoy `--frozen-lockfile` falla por `@playwright/test`/`zod`/`@fontsource-variable/inter` de `apps/admin-web` (L6B-REV-06, preexistente). Fijar `@playwright/test` a 1.61.x (chromium 1228 instalado) o instalar 1243 (N9, descarga).
3. Renumerar `20260920170000_asistente_unificado` si otro carril aporta una marca posterior (grep de la cadena en runbook, api-contracts, ESTADO-VERIFICADO, AI-CORE §14; el contrato localiza la migración por sufijo).
4. `CLAUDE.md` en conflicto manual: tomar `tanda-l6b` (= base + deuda 19 + 2 líneas en «Docs prioritarios»); `ESTADO-VERIFICADO.md` concatena; `docs/design/cocoa-22-inventory.json` + `COCOA-22-MIGRACION.md` §6 regenerar si otro carril mueve líneas de `screens/`; `scripts/env-contract.json`/`.env.example`/`deploy/.env.production.example` regenerar con `env-census --write` si otro carril añade variables (3 nuevas `ASSISTANT_MEMORY_*`).
5. Ficheros compartidos con anclas en §7.
6. e2e `assistant-panel.spec.ts`: volver a ejecutar tras la fusión con el navegador instalado (5/6 medidos por L6b-10 antes de la corrección de REV-01; (3b) verificado después solo con navegador manual y test unitario).
7. Hook de commit: `core.hooksPath = .husky` es relativo a la raíz del worktree (`anfitorio-demo-wt-l6b/`), donde no existe `.husky` (vive en `hotelos/`): `git hook run pre-commit` → «cannot find a hook named pre-commit», así que git no lo ejecuta en este layout; el integrador lo ejecutó a mano (`bash .husky/pre-commit` desde `hotelos/`, §9). Decidir si `core.hooksPath` debe ser `hotelos/.husky` (o mover la carpeta).

**Datos residuales en `hotelos_l6b`** (tenants sintéticos, append-only, sin PII real; borrables por id si se quiere el carril como estaba): `role_permissions` `cml6b09uxdaygmwomanage0001` (`maintenance.workorder.manage` para «Dirección general» de `org_uxday`), 2 `work_orders` de prueba en `prop_uxday` (habitación 104 `out_of_order` por la ejecución real de L6b-09), filas `ai_tool_calls` de las verificaciones (`answerAnalyticsQuestion` de `org_uxday`/`org_chk`/`org_123`, 4 `blockRoomForMaintenance` de la escena, 1 `createServiceRequest` `awaiting_confirmation` del bot en `org_chk`), conversaciones `assistant_*` de las pasadas (2 del revisor con `title` anterior al cifrado: `cmu9rtzxd00c4fyp2btqaylut` y la telemetría `cmu9rtzxl00c9fyp2v6u7qyyj` con `input_json.question` en claro con un nombre ficticio → borrar por id), `org_chk` sembrado.

**Producto / deuda del módulo (runbook §12, CLAUDE.md deuda 19):** tool use, respaldo ante 403/429 del proveedor, coste real, clasificación del bot y prompts publicados sin humo con clave; `AiIntent`/`AiIntentName` sin consumidor (retirar); `/copilot/*` alias sin consumidor (resolvers en UTC; lectura ajena al copiloto → `intent unknown`); bot: pregunta compuesta > 2 000 caracteres → reglas, hilo duplicado; router sin extracción de parámetros (lecturas del registro con entrada obligatoria responden «necesita datos concretos»); redactor sin nombres sueltos (mitigado con cifrado y huella; `knownPii` por propiedad sería el siguiente paso); plantilla `general_manager` sin `maintenance.workorder.manage` (N8); `AssistantChatScreen` duplica ~110 líneas de estado del panel (extraer `useAssistantConversation`); `as const` de superficies/estados/roles/`routedBy` en `packages/shared`; `pipeline.service.ts` agrega `dec(costEur) ?? 0` y oculta filas NULL en `kpis.costMtdEur`/`byTool[].costEur`; `GET /ai-operations/governance/cost` sin `callsTotal` (derivado en el front); badge «OK» de las citas recortado en pantallas estrechas; aviso React preexistente en `/hoy/informe-ia` (shorthand `border`); `AI_ERROR_LABELS_ES` no mapeadas en admin-web; `gates.sh --json` guarda por puerta la salida ya pasada por `sum` (un fallo intermitente de una suite sigue sin nombre: capturar antes del resumidor).

## 7. Fusión: ficheros compartidos, tocados y excluidos

Ficheros compartidos con otros carriles (anclas de texto para re-anclar; aditivos salvo donde se indica):

| Fichero | Cambio L6b | Ancla |
|---|---|---|
| `apps/api/src/server.ts` (+22/−6) | `POST /copilot/ask` pasa `context: request.userContext`; scheduler `[assistant.purge]` bajo `schedulerLeader` + `holdsSchedulerLease`; import muerto `getAvailableTools` retirado | bloque de schedulers (`MAILBOX_POLL`), handler de `/copilot/ask` (≈l.7535) |
| `apps/api/src/security/route-permissions.ts` (+10) | 3 entradas `/assistant/conversations[/:id]` y `GET /assistant/pending` (`ai.tool.execute`, low) con comentario «Asistente unificado (Tanda L6b · L6b-06 …)» | tras `{ method: "POST", path: "/webhooks/whatsapp", … }` y las entradas existentes de `/assistant/tools` y `/assistant/chat` |
| `apps/api/src/lib/tenancy.ts` (+16) | resolver `assistantConversation` en `RESOLVERS` (solo filas del propio usuario) | antes del resolver compuesto de L2 |
| `apps/api/src/lib/env.ts` (+3) · `.env.example` (+12) · `deploy/.env.production.example` (+12) · `scripts/env-contract.json` (+31) | `ASSISTANT_MEMORY_PURGE_DISABLED`, `ASSISTANT_MEMORY_PURGE_INTERVAL_MS`, `ASSISTANT_MEMORY_RETENTION_DAYS` (sección Schedulers; censo regenerado con `scripts/env-census.mjs --write`) | tras `MAILBOX_POLL_INTERVAL_MS` |
| `apps/api/src/modules/gdpr/gdpr.service.ts` (+23) | `compileDossier` incluye `AssistantConversation`/`AssistantMessage`; `executeErasure` borra los hilos `guest:<conversationId>` | import `eraseGuestDocuments`; sección `Message` del dosier; bloque `SurveyResponse` de la supresión |
| `apps/api/src/modules/ai-operations/tools/messaging.tools.ts` (+7/−4) | `knownGuestPii(conversationId, propertyId)` acotado a la propiedad del contexto (L6B-REV-12) | `knownGuestPii` |
| `apps/admin-web/src/layouts/BackOfficeLayout.tsx` (+93/−18) | imports de `components/assistant`, `ASSISTANT_SCREEN`, `assistantSurfaceForScreen`, `AssistantButton`, fila del menú compacto, `useCanOpenItem`, `closeAssistant()` en ⌘K / `hotelos-open-search` / `openPaletteWith`, `openFullAssistant`, `<AssistantButton />` antes de `<HelpButton />`, `<AssistantPanel …/>` antes de `<GuideProvider />`; 5 `style={menuItemStyle}` retirados de las filas del menú (`.cocoa-menu-item`) | `NEW_RESERVATION_SCREEN`, `function HelpButton`, `useCanCreateReservation`, `<NotificationsBell />`, `<GuideProvider />` |
| `apps/admin-web/src/styles/cocoa-22-shell.css` (+22/−2) | `.cocoa-shell .cocoa-menu-item` (geometría de las filas) y `.c22-drawer__foot > .c22-assistant-composer` | `.cocoa-menu-item`, `.c22-drawer__foot` |
| `apps/admin-web/src/components/CommandPalette.tsx` (+66/−6) | `CommandItemSource` + `"assistant"`, `ASSISTANT_ITEM_ID`, `assistantAskItem`, ítem al final de `buildPaletteItems`, `overlayRef` + guardia `focusin`, `onlyAssistant`, badge «IA» | `liveHitItems`, `buildPaletteItems`, `inputRef`, «Sin resultados para» |
| `apps/api/src/modules/checkin/guest-bot.service.ts` (+134/−48) | `answerWithModelViaCore` y `classifyWithCore` (tras la puerta `assistantPropertyGate`) sustituyen a `answerWithModelViaRunner`/`classifyWithModel`; constantes `GUEST_ASSISTANT_*`, `guestAssistantContext`, `stripAssistantNotices` | «Contratos», `RULES_CONFIDENCE`, `defaultGuestBotDeps` |
| `apps/api/src/modules/copilot/copilot.service.ts` (+227/−33) | `INTENT_KEYWORDS` exportadas, `COPILOT_RESOLVERS` con captura `AsyncLocalStorage`, `COPILOT_LOG_SCOPE`, `answerCopilot` reescrito con `context?` (el `switch` histórico desaparece); `COPILOT_PRESET_QUESTIONS` intacto | «intent detection», «resolvers», «dispatcher», `COPILOT_PRESET_QUESTIONS` |
| `apps/api/src/modules/assistant/assistant.service.ts` (+35/−161) · `assistant.tools.ts` (+65/−29) | fachada sobre `runAssistantTurn`; `propertyToday` + `data.date` en las lecturas de hoy, `get_compliance_summary` sobre `getComplianceCenter`, `mentionsAnotherDate` ampliado | «Compliance: critical controls», `ASSISTANT_TOOLS`, `OTHER_DATE_TOKENS` |
| `apps/api/src/modules/developer/api-reference.service.ts` (+3/−1) | `pending: "la escritura pendiente"` en `SEGMENT_LABELS` | `"room-connections"` |
| `packages/database/prisma/schema.prisma` (+51) · `packages/database/src/crypto-fields.ts` (+10) | modelos tras `AiToolCall`; `PII_FIELDS.AssistantMessage` y `AssistantConversation` tras `Signature` | `model EventStream`, `EmailConnection` |
| `docs/api-contracts.md` (+61/−4) | sección «Asistente unificado (Tanda L6b · 2026-09-20)» (l.735) antes de «Tanda L2 · Persistencia y API»; L2 «1035 entradas» | l.735, l.800 |
| `docs/design/AI-CORE.md` (+8/−3) · `docs/design/CHECKIN-AUTOMATIZADO-IA.md` (+5) · `docs/runbooks/checkin-automatizado.md` (+6) · `CLAUDE.md` (+29) | §5.3, §14, §16.11, §17 filas 5/8/16; notas L6b en CHK §5 y runbook §10; deuda 19 + Docs prioritarios | — |
| `docs/design/cocoa-22-inventory.json` · `COCOA-22-MIGRACION.md` | regenerados (107.680 líneas · 171 puntos; `inlineStyles` 609) | — |
| `scripts/gates.sh` (+4) | salida completa por puerta con `--json` | `run()` |
| `tests/integration/l2-robustez.test.mts` (+6) · `tests/seed-ux-day-contract.test.mjs` (+3/−2) | `flushAuditQueues()` antes de `countPrismaOps`; 12 specs de recepción | `MAX_OPS_GUEST_ACTIVITY`, «las 12 specs» |
| `apps/admin-web/src/screens/aiOperations/*` (5 + 2 tests) · `services/assistantApi.ts` · `screens/assistant/AssistantChatScreen.tsx` · tests `CommandPalette`, `guest-bot`, `assistant-router` | propios de la tanda, sin otro carril encima | — |

**Nuevos (22):** `modules/assistant/{assistant-catalog,assistant-core.service,assistant-gate,assistant-memory.service,assistant-prompts,assistant-router}.ts` + 3 tests, `routes/__tests__/assistant-routes.test.mts`, `copilot/__tests__/copilot-core.test.mts`, `components/assistant/*` (4) + `components/__tests__/assistant-panel.test.mts`, `e2e/assistant-panel.spec.ts`, migración, `tests/assistant-schema-contract.test.mjs`, `tests/integration/l6b-asistente.test.mts`, `docs/runbooks/asistente-ia.md`, este informe. **Excluido:** `pnpm-lock.yaml` (restaurado a HEAD). **Sin dependencias nuevas** (memoria sobre Prisma, captura con `node:async_hooks`, front solo Cocoa; 0 `style=` nuevos: `BackOfficeLayout` 51 → 47, `AssistantChatScreen` 15 → 0, panel/hilo/paleta 0).

**Servidores:** `:3000` (PID 4986) y `:5173` intactos; cada lote arrancó su instancia desde el worktree y la mató por PID; sin `pkill -f`. BD `hotelos_l6b` al cierre: 25 migraciones sin deriva; cliente piloto sin escrituras.

## 8. Modelo de datos y migración

| Carpeta | Contenido | Estado en `hotelos_l6b` |
|---|---|---|
| `20260920170000_asistente_unificado` (L6b-01) | `assistant_conversations` (11 columnas, índice `organization_id, property_id, user_id, last_message_at`) y `assistant_messages` (11 columnas, índice `conversation_id, created_at`, FK `ON DELETE CASCADE`); 0 enums, 0 columnas en tablas existentes, 0 backfill; reversible (`DROP TABLE assistant_messages; DROP TABLE assistant_conversations;`) | aplicada; `migrate status` 25/25 «Database schema is up to date!» · `db:drift:check` «No difference detected.» |

`PII_FIELDS.AssistantMessage = ["content"]` y `AssistantConversation = ["title"]` (envelope `v1.`, sin lookup hash; el cifrado de `title` no necesita migración: filas anteriores se leen tal cual hasta reescribirse). `ai_tool_calls.conversation_id` lleva el id de `assistant_conversations` en la fila del turno y en las llamadas del runner lanzadas desde el asistente (sin FK; AI-CORE §14). Purga por retención y supresión RGPD en §5 D5. Contrato `tests/assistant-schema-contract.test.mjs` (7/7).

## 9. Commit en la rama del carril

Comando del integrador (excepción documentada a las reglas comunes): `git -C ~/anfitorio-demo-wt-l6b add -A hotelos && git -C ~/anfitorio-demo-wt-l6b reset -q hotelos/pnpm-lock.yaml && git -C ~/anfitorio-demo-wt-l6b -c user.name=… -c user.email=… commit -F <mensaje>`; sin `--no-verify`; el hook `.husky/pre-commit` (discoverability + `typecheck-all`) ejecutado a mano desde `hotelos/` porque git no lo resuelve en este layout (§6.7). El sha va en el informe estructurado del integrador, no aquí (este fichero forma parte del commit).

```
feat(asistente): asistente ehotelOS unificado sobre ai-core (Tanda L6b)

L6b-01 modelo y migración 20260920170000_asistente_unificado (assistant_conversations /
assistant_messages, content y title cifrados por PII_FIELDS, contrato de schema).
L6b-02 catálogo unificado de 37 lecturas (12 locales + 10 copiloto + 15 registro) con
permisos por herramienta y router por reglas con las 19 sugeridas al 100 % (otra fecha,
especificidad, un solo intent del copiloto).
L6b-03 cliente v2 (AssistantTurn con routedBy, citas, coste, pendientes) y componentes
del panel (AssistantPanel, AssistantThread, contexto de pantalla sin PII, store).
L6b-04 pantallas de IA honestas (Sin modelo / En uso, coste NULL como «—», skipped).
L6b-05 núcleo conversacional único runAssistantTurn sobre ai-core: memoria por usuario +
propiedad + superficie, reglas sin proveedor y tool use con proveedor (≤ 3 turnos,
respaldo con aviso), escrituras solo como propuesta awaiting_confirmation, prompts por
superficie, una fila answerAnalyticsQuestion por turno.
L6b-06 rutas GET /assistant/tools, POST /assistant/chat, GET/DELETE
/assistant/conversations[/:id], GET /assistant/pending (+4 manifiesto, 0 claves nuevas,
resolver assistantConversation), contratos api-contracts y l6b-asistente.
L6b-07 panel en el shell (botón, menú compacto, ⌘K «Preguntar al asistente»),
/asistente con el mismo hilo, superficie por categoría.
L6b-08 /copilot/ask y bot del huésped sobre el núcleo (prompt y permisos propios).
L6b-09 Pendientes IA ejecuta al aprobar (POST /ai/tool-calls/:id/confirm).
L6b-10 e2e del panel (apps/admin-web/e2e/assistant-panel.spec.ts).
L6b-11 runbook docs/runbooks/asistente-ia.md, AI-CORE §16.11/§17, CLAUDE.md deuda 19.
Revisión + corrector: cita al reabrir conversaciones, title cifrado y huella de la
pregunta en telemetría, screen saneado en el núcleo, escrituras solo confirmables,
superficie efectiva de la conversación, fechas explícitas en el router, puerta de
propiedad (aiEnabled · nivel · presupuesto) antes de cada llamada al modelo, copiloto
con el UserContext real, purga por retención [assistant.purge] y supresión RGPD de los
hilos del huésped, pending acotado al proponente, gates.sh con logs por puerta.
Puertas completas 13/14 (única roja nav-tree --check, CSV compartido con pantallas de
otros carriles). Sin dependencias nuevas; pnpm-lock.yaml fuera.

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
```
