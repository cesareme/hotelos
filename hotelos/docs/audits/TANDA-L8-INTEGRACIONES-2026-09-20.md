# Tanda L8 · Integraciones honestas — informe de cierre (2026-09-20)

**Encargo** (`scratchpad/tandaL8-brief.md`): que cada integración diga la verdad sobre su estado y sea operable sin sorpresas — (1) modo declarado `none | sandbox | real` en configuración y en `/health` con mensaje veraz, (2) pantalla Configuración › Integraciones con el estado real, qué falta para pasar a real y la última actividad / errores, (3) ningún «éxito» falso (descargas vacías, «enviado» sin envío, contadores inventados), (4) contrato de tests modo ↔ comportamiento, (5) runbook `docs/runbooks/integraciones.md`. Worktree `~/anfitorio-demo-wt-l8/hotelos` (rama `tanda-l8`, base `a069906` = main con CHK), BD propia `hotelos_l8` (copia de la demo local con los datos reales de Faranda), puertos `:3935` / `:5205`. Plan: recon (`scratchpad/L8/recon-delta.md`) → 3 olas · 9 lotes con ficheros exclusivos (L8-01 contrato + servicio + hub heredado honesto; L8-02 hub heredado `simulated`; L8-03 `simulado` en Comunicaciones; L8-04 auditoría de textos + runbook; L8-05 cableado de `/health` y `GET /integrations/status`; L8-06 panel de la pestaña; L8-07 pantallas vecinas y manual; L8-08/L8-09 documentación de estado) → puerta rápida por ola → este lote (L8-10: verificación en runtime, puerta completa e informe).

**Todos los datos de este informe son de configuración o agregados**: no se cita ningún huésped, usuario ni credencial; los tenants de verificación son `org_uxday` / `prop_uxday` (seed `db:seed:ux-day`, roles direccion / recepcion / sistemas) y la demo `org_123` / `prop_123`.

**Working tree SIN commit** (`git -C ~/anfitorio-demo-wt-l8 status --porcelain -uall`, 2026-09-20 13:2x): **24 ficheros modificados (+637 / −229 sin `pnpm-lock.yaml`) y 14 nuevos (3.533 líneas)** antes de este informe y del bloque de CLAUDE.md que escribe el lote de documentación en paralelo. `pnpm-lock.yaml` aparece `M` (+64 / −25) desde ANTES de la tanda (el recon lo anotó al empezar) y ningún lote lo tocó: **fuera del commit** (`git checkout -- pnpm-lock.yaml` en la fusión, como hace `scripts/merge-lane.sh`). **Sin migraciones** (`migrate status` 24/24 «up to date», `db:drift:check` «No difference detected.»): todo el estado se deriva de configuración y de tablas existentes; el nombre reservado `20260920180000_integraciones_honestas` no se usó. Sin dependencias nuevas, sin claves RBAC nuevas (`integrations.read` ya existía), sin rutas nuevas de front (el panel vive en `/configuracion/modulos/integraciones`, fila 85 del CSV de navegación).

## 0. Resumen

- **Contrato compartido** `packages/shared/src/integrations-status-types.ts` (161 líneas): `INTEGRATION_MODES` (`none | sandbox | real`, `:33`), `INTEGRATION_MODE_LABELS_ES` (`:35`), **18 claves** `INTEGRATION_KEYS` (`:52`), etiquetas (`:74`), pantalla por clave (`:100`), `IntegrationStatusDto` (`:121`), `IntegrationsHealthBlock` (`:144`, 14 claves sin dependencia de propiedad).
- **Servicio** `apps/api/src/modules/integrations/integrations-status.service.ts` (946 líneas): una función pura por integración + `collectIntegrationsStatus` (`:881`, BD por propiedad, colector degradado que nunca convierte un fallo en éxito) + `describeIntegrationsHealth` (`:738`, sin BD, para `/health`). Solo lectores YA existentes de `process.env` (`readChannelEnv`, `aiConfigSummary`, `emailProvidersStatus`, `emailStatus`, `isWhatsappConfigured`, `isSmsConfigured`, `unsignedWebhookMode`, `pspStatusFor`, `getComplianceHealth`, `describeDocumentStorageHealth`); Sentry y Redis llegan como parámetros desde `server.ts`.
- **Superficie HTTP**: `GET /integrations/status?propertyId=` (`integrations.read`, low; `integrations-status.routes.ts`, permisos en `route-permissions.partial.ts:21` fusionados en `security/route-permissions.ts:200`) y clave superior `integrations` de `GET /health` (`server.ts:1876-1914`), fuera de `checks` (nunca degrada `status`).
- **Éxitos falsos corregidos**: Redis «ok» sin consumidor, `dependencies.redis`, prueba del hub heredado «ok» sin proveedor, `PATCH` de estado sin persistir, readiness `payment_provider_connected` con la pasarela ficticia, Pagos «Conectado» con el hub, KPI «Enviadas · entregadas» que sumaba los envíos «SIMULADO», marketplace «certificadas / verificadas», toast «parte enviado a SES», guía de revenue «sincronizadas con las agencias», descripción «Descargar las reservas del canal» (§2).
- **Pantalla**: Configuración › Módulos e integraciones › **Integraciones** = `IntegrationsStatusPanel` (18 filas por área, badge por modo, «qué falta para real», última actividad, último error, `n consultas sin datos`) encima del catálogo de terceros vacío; Pagos y Comunicaciones alineadas (§3).
- **Verificado en runtime** en este lote con instancia propia (`:3935` / `:5205`, PIDs 86954/86958 y 291/293, matadas por PID): `/health` con `integrations` (14 claves, sin URLs ni secretos, `checks` idénticos a la línea base salvo el mensaje honesto de Redis), `GET /integrations/status` 200 (direccion, sistemas) / 403 (recepcion) / 404 (propiedad ajena) / 400 (`propertyId` vacío), prueba del hub `simulated`, Pagos sin «Conectado», Comunicaciones con «1 simulada (sin proveedor)» y badge «SIMULADO», SQL solo lectura del runbook §5 (§3).
- **Puertas**: línea base 12/12 en verde → olas 1-2 11/12 (única roja `nav-tree --check`, ajena: filas de la Tanda ACT en el CSV compartido) → **puerta completa final 13/14** (`scratchpad/L8/gates-full.json`): typecheck 15 PASS, api 3.622, admin-web 2.045, ai-core 119, worker 34, contratos raíz 786 (784 pass · 0 fail · 2 skip, tras añadir `integrations-status.spec.ts` a `seed-ux-day-contract` l.164/188 y corregir el flaky de `payments-tenancy` #1: `correlationId` fuera del cuerpo comparado), discoverability 197, route-access 15 × 197, cocoa al día, rbac OK, 24/24 + drift 0, build OK, **integración 993 (985 pass · 8 skip · 0 fail)**; `nav-tree --check` sigue en rojo por la Tanda ACT (§5). Corrección posterior del corrector L8 en §9; **puerta completa tras el corrector (15:09, `scratchpad/L8/gates-final.json`) 13/14**: typecheck 15 PASS · 0 FAIL · 1 SKIP (34,3 s), api 3.636 (3.635 pass · 1 skip), admin-web 2.053 (2.052 · 1 skip), ai-core 119, worker 34, contratos raíz 787 (785 · 0 fail · 2 skip), discoverability 197, route-access 15 × 197, cocoa §6 al día, rbac OK, 24/24 + drift 0, build 3,24 s, integración 993 (985 · 0 fail · 8 skip); único rojo `nav-tree --check` (CSV compartido con 304 filas: pantallas de L6b / L7 / ACT inexistentes aquí). Detalle por lote (§10), hallazgos de la revisión (§11), decisiones para César con el defecto aplicado (§12) y cierre del integrador con el commit (§13).
- **Contrato de honestidad (punto 4 del brief)**: `tests/integrations-honesty-contract.test.mjs` (L8-08, 21 casos, fuente sin BD: una función por clave en el servicio, una fila por clave en el runbook §1 y una entrada en `api-contracts.md`, 14 claves de `/health`, `none` / `sandbox` nunca simulan un éxito, Pagos sin `gatewayReady` del hub) + `tests/integration/integraciones-estado.test.mts` (10 casos, Postgres, organización aislada con `RBAC_STRICT`) + `apps/admin-web/e2e/integrations-status.spec.ts`; entraron en el árbol a las 13:27-13:29, en paralelo con la puerta completa de este lote (§5).

## 1. Tabla modo ↔ comportamiento por integración

Modo «hoy» = el que devolvió `GET /integrations/status` en este lote (2026-09-20 13:19-13:24, BD `hotelos_l8`) para `prop_uxday` y para `prop_123`; `/health` = bloque `integrations` público (14 claves). Regla común (`finalizeIntegrationStatus`, `integrations-status.service.ts:137`): `readyForReal ≡ mode === "real" && missingForReal.length === 0`; `none` ⇒ `lastActivityAt: null`; `message` y `missingForReal` sin URLs, rutas, buckets ni nombres de variables secretas (comprobado en runtime con los patrones `https?:|://|www\.` y `[A-Z]+_(SECRET|KEY|TOKEN|PASSWORD|DSN|URL)`: 0 violaciones en 18 × 2 DTO y en el bloque de `/health`).

| Clave · etiqueta | Cómo se decide el modo (ancla) | Modo hoy `prop_uxday` / `prop_123` / `/health` | Comportamiento por modo (lo que el usuario ve) |
| --- | --- | --- | --- |
| `opera` · OPERA Cloud (modo sombra) | `operaStatus` (`:159`): `none` sin perfil `active` (`pms_shadow_profiles`); `real`/`files` con perfil activo; `lastError` del último run `failed`/`partial`; `missingForReal` = clave de ingest o buzón `pms_shadow` y «Primer informe recibido» | `none` / `none` / — (por propiedad) | «Sin perfil de modo sombra para la propiedad: no se reciben informes de OPERA.» En Faranda (Rías Altas) es `real` por ficheros (1 perfil, runs `api_key` 5 done · 2 partial · 1 failed, `cli` 1 done) sin que exista conexión OHIP: el runbook §3.1 lo declara ingesta de informes por diseño |
| `sage200` · Sage 200 (importación contable) | `sage200Status` (`:195`): `none` con 0 `ledger_imports`; `real`/`files` con ≥ 1 lote (`missingForReal` «Contabilizar al menos un lote» si ninguno `posted`) | `none` / `none` / — | «Sin lotes importados desde Sage 200: la contabilidad no se ha cargado.» Faranda: `real` (50 lotes `posted`). No existe conexión directa: carga por ficheros |
| `gestoria_export` · Exportación a gestoría | `gestoriaExportStatus` (`:218`): siempre `real`/`manual`, `missingForReal` vacío; mensaje «Formatos A3 y Sage 200 no implementados» | `real` / `real` / `real` | Único «Real» en el tenant de pruebas: exportación manual de datos reales (CSV universal, libros de IVA, diario compatible ContaPlus / Sage 50); `a3` → 409 y la pantalla lo dice (`GestoriaExportScreen.tsx:137-141`). `lastActivityAt` siempre `null` (sin consulta a `gestoria_exports`, decisión de L8-01) |
| `channels` · Canales de venta (OTAs) | `channelsStatus` (`:240`): `none` sin canales o sin activos; `sandbox` si todos los activos van al simulador (`readChannelEnv().maxMode`, `channel-manager/env.partial.ts:85`); `real` si alguno activo queda en `real` (`:267`) | `none` / `sandbox` / `sandbox` | `prop_123`: «6 de 7 canales activos, todos en simulador local», transporte `http`, `lastActivityAt` = último `last_sync_at` (2026-09-19). `/health` habla del tope: «Tope de modo de canales en simulador local: ningún canal sale a Internet.» |
| `psp` · Pasarela de pago (PSP) | `pspStatus` (`:286`) sobre `pspStatusFor` (`payments/psp/index.ts:71`): `none` sin proveedor; `sandbox` con Stripe `sk_test_` / Redsys `test` (`:300`); `real` con `live` (`:308`) | `none` / `none` / — | «Ningún PSP configurado: sin cobros con tarjeta en línea ni enlaces de pago.» `missingForReal` «Credenciales de Stripe o Redsys (clave del comercio y secreto del webhook)» · «Contrato con el PSP». El readiness `payment_provider_connected` lee lo mismo (`backoffice.service.ts:3564`, `:3795-3796`): `prop_123` **bloqueado** (antes pasaba con la pasarela ficticia) |
| `whatsapp` · WhatsApp | `whatsappStatus` (`:325`): `real` con salida configurada (`:330`; `missingForReal` = secreto del webhook si `unsignedWebhookMode` ≠ `signed`); sin salida `none` en producción (`:341`) y `sandbox` fuera (`:350`) | `sandbox` / `sandbox` / `sandbox` | «Sin credenciales de WhatsApp: los envíos se registran como SIMULADO y no salen del sistema; webhook de entrada rechazado hasta configurar el secreto.» |
| `sms` · SMS | `smsStatus` (`:362`): `real` con `isSmsConfigured()` (`:367`); `none` en producción sin credenciales (`:378`); `sandbox` fuera (`:387`) | `sandbox` / `sandbox` / `sandbox` | «Sin credenciales de SMS: los envíos se registran como SIMULADO y no salen del sistema.» |
| `email_out` · Correo saliente | `emailOutStatus` (`:400`) sobre `emailStatus()` (`email.provider.ts:42`): `real` (`:406`) / `disabled` → `none` (`:417`) / `simulated` → `sandbox` (`:426`); `lastActivityAt` de `notification_deliveries` reales o «SIMULADO» | `sandbox` / `sandbox` / `sandbox` | «Sin proveedor de correo saliente: los envíos se registran como SIMULADO y no salen del sistema.» Las filas simuladas llevan `SIMULATED_ERROR_MESSAGE` (`dispatcher.service.ts:24`) y cuentan aparte (§2 A3) |
| `email_in` · Correo entrante (OAuth) | `emailInStatus` (`:442`): `none` sin credenciales OAuth de Google ni Microsoft (`:451`, transporte `manual` si hay buzón manual); `real` con credenciales (`:463`; «Autorizar al menos un buzón» si no hay `email_connections`) | `none` / `none` / `none` | «Sin credenciales OAuth de Google ni Microsoft: solo queda el buzón manual (pegar correos a mano).» |
| `gbp` · Google Business Profile | `gbpStatus` (`:479`) sobre `review_sources`: `real`/`http` con fuente `google` `api` operativa (`:490`); `real`/`files` con `csv` o `email` operativa (`:501`); `sandbox` con `demo` (`:512`); `none` sin fuentes (`:522`) | `none` / `real` (files) / — | `prop_123`: «Reseñas cargadas por CSV (1 fuente conectada); la API de Google Business Profile no está autorizada», `readyForReal:false` con 3 pendientes (la ruta OAuth no existe: decisión). Cuenta como «Real» en los KPI del panel de `prop_123` (2 reales) |
| `ses` · SES.Hospedajes | `complianceStatus("ses")` (`:554`) sobre `getComplianceHealth().integrations`: `sandbox` → `sandbox` (`:572`); `preproduction` → `real` con «Decisión: pasar a producción» (`:583`); `production` → `real` (`:593`); `null` (consulta fallida) → `none` «no disponible» (`:556`) | `sandbox` / `sandbox` / `sandbox` | «Modo de pruebas local: ninguna comunicación de viajeros se envía al Ministerio del Interior; las respuestas son simuladas.» `prop_123`: `lastActivityAt` 2026-09-18 y `lastError` (149 `failed` de Faranda: registro turístico incompleto y «sustituidas») |
| `verifactu` · VeriFactu | ídem (`complianceStatus("verifactu")`); `missingForReal` incluye los 3 errores del bloque `SistemaInformatico` (`resolveVerifactuSoftware`) | `sandbox` / `sandbox` / `sandbox` | «Modo de pruebas local: ningún registro de facturación se envía a la AEAT; las respuestas son simuladas.» 5 pendientes (certificado, razón social, NIF e instalación del productor, decisión preproducción → producción); `lastActivityAt` 2026-09-19 (acuses del stub) |
| `tbai` · TicketBAI | `complianceStatus("tbai")`: `enabled:false` → `none` (`:560`) | `none` / `none` / `none` | «No aplica: ninguna propiedad declara territorio foral (Bizkaia, Gipuzkoa, Araba o Navarra).» |
| `igic` · IGIC (Canarias) | `igicStatus` (`:603`): siempre `none`, `missingForReal` vacío | `none` / `none` / `none` | «No es una integración propia: Canarias declara por VeriFactu con Impuesto=03; el estado real es el de VeriFactu.» |
| `storage` · Almacén de documentos (S3) | `storageStatus` (`:617`) sobre `describeDocumentStorageHealth()`: `s3` → `real` «configurado, no comprobado» (`:621`); `inline` / `disk` → `sandbox` (`:630`); `unconfigured` → `none` (`:638`) | `sandbox` / `sandbox` / `sandbox` | «Almacén local en la base de datos (inline): funciona, pero nada se guarda en S3.» `configured:true` con `mode sandbox` (el contrato separa ambas cosas) |
| `ai` · Proveedor de IA | `aiStatus` (`:650`) sobre `aiConfigSummary()`: `none` sin proveedor operativo (`:653`); `real` con `configured:true` (`:664`). Sin `sandbox`: o hay modelo o hay reglas | `none` / `none` / `none` | «Sin proveedor de IA: las funciones de IA responden «no configurado» y no inventan resultados.» |
| `sentry` · Sentry | `sentryStatus` (`:672`): `real` con DSN, `none` sin él (`:674`); parámetro derivado en `server.ts` con la regla de `checks.sentry` | `none` / `none` / `none` | «Sentry desactivado: los errores solo quedan en el log del proceso.» `screen: null` |
| `redis` · Redis | `redisStatus` (`:682`): `real` solo con `consumerCount > 0` (`:685`); `none` aunque haya `REDIS_URL` (`:693`; `server.ts` pasa siempre 0: ningún componente abre un cliente) | `none` (`configured:true`) / ídem / `none` | «Dirección de Redis configurada, pero ningún componente del API la usa: hoy no aporta nada (el dato de /health «configured» solo significa eso).» `/health`: `checks.redis` «configured (sin consumidor en este build)» (`server.ts:1756`), `dependencies.redis` `unconfigured` (`:1896`) |

Fuera de las 18 claves: el **hub heredado** (`IntegrationConnection` / marketplace) queda en modo demostración explícito — proveedores «Demo OTA Adapter / Demo Payment Gateway / Demo Guest Messaging (demostración)» con `demo:true` y `mode:"sandbox"` (`lib/demo-store.ts:1858-1883`), prueba de conexión siempre `{ status: "simulated", simulated: true }` con evento `IntegrationTestSimulated` `status simulated` (`integrations.service.ts:369-375`; fixture `demo-store.ts:1908`), `PATCH` de estado persistido y auditado (`updateIntegrationConnectionStatus`, `:303`; valor inválido → 400). Las 6 rutas y `credentialsSecretRef` siguen por contrato (`tests/modular-suite-contract.test.mjs`).

## 2. Éxitos falsos corregidos (antes → después)

Fuente: auditoría léxica de L8-04 (`scratchpad/L8/audit-textos.md`: 454 hits en 112 ficheros, 15 llamadas `apiRequestBlob` + 15 descargas en cliente, **0 descargas vacías**) y recon nº 1-8. Todo verificado en el árbol y, donde se indica, en runtime en este lote.

| # | Dónde (antes) | Antes | Después (ancla) | Verificado |
| --- | --- | --- | --- | --- |
| A4 | `/health` (`server.ts`) | `checks.redis` «configured» y `dependencies.redis` «ok» sin ningún cliente Redis en el código | `checks.redis` «configured (sin consumidor en este build)» / «not configured (optional · sin consumidor)» (`server.ts:1756`); `dependencies.redis` siempre `unconfigured` (`:1896`); `redis` `mode none` en el bloque `integrations` | runtime `:3935` (`scratchpad/L8/health-final.json` frente a `health-base.json`) |
| A7 | `/health` | `dependencies.objectStorage` legible como «alcanzable» con `s3` | `storage` en `integrations`: `s3` = «configurado, no comprobado» con `missingForReal` «Comprobación de acceso al bucket»; `inline` / `disk` = `sandbox` (`integrations-status.service.ts:617-638`); `dependencies.objectStorage` sigue diciendo solo el tipo | runtime (`inline` → `sandbox`) |
| A5 | Hub heredado (`integrations.service.ts`, `server.ts` PATCH) | `POST …/integrations/:id/test` → `status: "ok"` + evento `IntegrationSyncStarted` `accepted` sin tocar red; `PATCH` solo en memoria | `buildConnectionTestResult` → `status: "simulated"` + `IntegrationTestSimulated` `simulated`, sin mover `lastSyncAt` (`:369-375`); `PATCH` persistido por `updateIntegrationConnectionStatus` (`:303`, 400 con estado inválido); proveedores «… (demostración)» `demo:true` `mode sandbox` (`demo-store.ts:1858-1883`) | runtime: `POST /properties/prop_123/integrations/iconn_mock_payments/test` → `{"status":"simulated","simulated":true,…,"event":{"eventType":"IntegrationTestSimulated","status":"simulated"}}`; `lastSyncAt` intacto (2026-05-14); `integration_events` `IntegrationTestSimulated` 2 (una de L8-02, una de este lote) |
| A2 | Readiness `payment_provider_connected` (`backoffice.service.ts`) + `GoLiveChecklist.tsx:72` | «Pasarela de pago conectada.» → `pass` con la conexión ficticia `ip_mock_payments`; `integrationErrors` contaba espejos en memoria; etiqueta «Proveedor de pago conectado (módulo Payment Vault)» | `paymentProviderConnected = pspStatusFor(propertyId).configured` (`:3564`), `integrationErrors = countPropertyIntegrationErrors` (`:42`, `:2845`); «Pasarela de pago configurada (<proveedor> · <modo>).» / «Se necesita una pasarela de pago (PSP) configurada —Stripe o Redsys con credenciales— con el módulo Payment Vault activado; el proveedor de demostración del hub heredado no cuenta.» (`:3795-3796`); etiqueta «PSP configurado (Stripe o Redsys · módulo Payment Vault)» (`GoLiveChecklist.tsx:72`) | runtime: `GET /backoffice/properties/prop_123/readiness` → `status blocked`, `blockingCount 2` (`payment_provider_connected` fail + `ses_hospedajes_credentials` fail); dashboard `paymentProviderStatus: "missing"`; captura `shots/11-go-live-checklist-demo-prop123.png` |
| A1 | Configuración › Facturación y pagos › **Pagos** (`PaymentSettings.tsx`) | «Conectado» (verde) y «1 conectado» leídos del hub heredado (`billingApi.ts` → `/backoffice/properties/:id/integrations`) mientras `psp-status` decía «Ningún PSP configurado» | Fuente única `GET /integrations/status` fila `psp` (`PaymentSettings.tsx:21-22`, `:93-95`; `billingApi.ts:135-147` `pspIntegrationStatus`): badge «Sin integración» / «Pruebas (sin efecto real)» / «Real», callout «Qué falta para cobrar en real» (`:149`), «Última actividad: sin actividad registrada. Las credenciales del PSP … se configuran en el servidor»; el hub pasa a sección «Conexiones de demostración (catálogo heredado)» (`:170`) con badge «Demostración · no cobra» (`:70`) y nota «son de demostración y no cobran» | runtime `:5205` `prop_123` y `prop_uxday`: capturas `shots/05-pagos-demo-prop123.png`, `06-pagos-uxday.png`; ninguna aparición de «Conectado» en el texto de la pantalla |
| A3 | Configuración › Comunicaciones › **Plantillas y envíos** (`NotificationsScreen.tsx`, `dispatcher.service.ts`) | `sent` → badge «enviado» (verde) y KPI «Enviadas (30 días) · entregadas» sumaban las filas `sent` grabadas con «SIMULADO: …»; `template-stats` sin distinguir | `delivery-outcome.ts` (`isSimulatedDelivery` `:29`, `deliveryOutcome` `:34`, badge «simulado» en warning `:38-48`); KPI «Enviadas» solo reales con caption «N simuladas (sin proveedor)» (`NotificationsScreen.tsx:135-137`, `:261-268`); API `GET /notifications/template-stats` con `simulated` separado y `sent` solo reales (`dispatcher.service.ts:24`, `:31`, `:370`) | runtime: `template-stats` `org_123/prop_123` → `sent 0 · simulated 1 · failed 0 · queued 0`; pantalla «ENVIADAS (30 DÍAS) 0 · 1 simulada (sin proveedor)», pestaña Envíos con badge «SIMULADO» y el motivo, Estadísticas con columna «SIMULADAS» (capturas `07`, `09`, `10`) |
| A6 | Configuración › Módulos e integraciones › **Integraciones** (`MarketplaceCatalogScreen.tsx`) | Subtítulo «Aplicaciones certificadas que extienden tu PMS…», vacío «Las aplicaciones verificadas aparecerán aquí…», badge success «verificada» sin proceso alguno (`marketplace_listings` 0, `app_installations` 0) | Subtítulo «Estado real de cada integración; debajo, el catálogo de aplicaciones de terceros, hoy vacío.» (`:167`); `IntegrationsStatusPanel` encima (`:29`, `:182`); vacío «Sin aplicaciones de terceros publicadas · Hoy no hay ninguna aplicación de terceros en el catálogo. Puedes crear tu propia aplicación en Configuración › Sistema › Aplicaciones.» (`:204`); badge solo si `l.verified` (`:221`, nunca con catálogo vacío) | runtime: capturas `01`-`04`; texto de la pestaña sin «certificad» ni «verificad» |
| B7 | Toast de check-in (`content/actions.ts:348`) | «… · parte enviado a SES (n)» | «… · parte encolado para SES (n)» (`:348-349`; test `primary-action.test.mts:96` actualizado) | unitario (admin-web 43/43) |
| B8 | Guía de revenue (`persona-guides/revenue.ts:14`) | «… sincronizadas con las agencias en línea» | «… sincronizadas con los canales dados de alta (en modo de pruebas van al simulador local hasta activar un canal real)» | contrato de copy (`integrations-copy.test.mts`) |
| B9 | Referencia del API (`developer/api-reference.service.ts:734`) | «pull-reservations: Descargar las reservas del canal.» | «Traer las reservas del canal al buzón de reservas externas (no crea reservas; en modo de pruebas vienen del simulador).» | ídem |
| — | Exportar a gestoría (`GestoriaExportScreen.tsx`) | «Ese formato aún no está disponible…» y descripción que no nombraba el formato validable | «Ese formato no está disponible: el servidor lo rechaza (409) y no genera ningún fichero; usa el CSV universal de asientos.» (`:137-141`); descripción con «Diario compatible ContaPlus / Sage 50 … validar con la gestoría» (`:260`) | unitario |
| — | Manual `docs/manual/60-sistemas.md` | §3.3 describía el catálogo «certificadas»; tabla de `/health` con `redis ok`; «Integraciones: sin aplicaciones certificadas» | §3.3 describe el panel (contadores, columnas, «cómo leerlo», `:256-276`); `dependencies.redis` `unconfigured` explicado (`:449`); «postgres» como único bloqueo (`:458`); errores frecuentes (`:486`) | lectura |

**Pendientes de la misma auditoría (no abordados en la tanda, dueño en §7)**: A8 `marketing/CampaignManagerScreen.tsx:347` (KPI «Enviadas · ya entregadas» sin motor de envío), A9 `components/cocoa-global/CocoaStatusBar.tsx:29` («Conectado» fijo; sin consumidor en el árbol), B1 `compliance/SesHospedajesSettingsScreen.tsx:66-93` y `GuestRegisterSettingsScreen.tsx` (acuse `accepted` en verde sin mirar `endpoint stub://`), B2 `fiscal/TbaiForalScreen.tsx:93, :228` («Enviado», «envíos verificados»), B3 `channelManager/ChannelAggregatorHub.tsx:1082, :1103` («Canales conectados»), B4 `cocoa-rate-grid/helpers.ts:422-446` («Enviado a <canal>…»), B5 `ChannelMappingsScreen.tsx:140-668` («Conectado»), B6 `operations/ConciergeInboxDashboard.tsx:187` («Recibidos y enviados»), comentarios `QuickCheckInDrawer.tsx:45` y `e2e/quick-checkin.spec.ts:18` con el literal antiguo, `docs/manual/10-direccion.md` / `20-administracion.md` («Demo Payment Gateway · CONECTADO») y `packages/integrations/src/registry/integration-provider-manifest.ts` (nombres sin «(demostración)», lo consume `apps/mobile`).

## 3. Verificación en runtime (este lote)

Instancia propia desde el worktree: API `PORT=3935 RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true node --env-file-if-exists=../../.env --import tsx src/server.ts` (PID 86954; segunda pasada PID 291) y Vite `--port 5205 --host 127.0.0.1 --strictPort` con `VITE_API_URL=http://127.0.0.1:3935` (PID 86958; 293); ambas paradas con `kill <PID>` y puertos comprobados libres. Tokens de `org_uxday` (`direccion@` / `recepcion@` / `sistemas@uxday.test`) y de la demo `org_123` obtenidos por `POST /auth/login` y guardados solo en el scratchpad (`scratchpad/L8/tok-*.txt`, `login-*.json`, `sessions.json`, modo 0600). Script: `scratchpad/L8/l8-10-verify.py` (salida `l8-10-verify.log`); capturas: `scratchpad/L8/l8-10-shots.mjs` (Playwright headless de `apps/admin-web`, sesión sembrada en `localStorage` como hace `e2e/_helpers.ts loginAsUxDay`).

| # | Comprobación | Resultado |
| --- | --- | --- |
| 1 | `GET /health` frente a `scratchpad/L8/health-base.json` (línea base del recon, 12:05) → `health-final.json` | `status healthy` · `ok true`; **`checks` con las mismas 11 claves** (`ai, audit, checkinJobs, database, env, redis, reputationSync, schedulers, sentry, sesHospedajes, verifactu`) y los mismos valores salvo `database.latencyMs` (1 → 0) y `redis.message` («configured» → «configured (sin consumidor en este build)»); `dependencies` `{ postgres ok, redis unconfigured (antes ok), objectStorage inline }`; **una sola clave superior nueva: `integrations`** con **14 entradas** (`gestoria_export real`, `channels sandbox`, `whatsapp sandbox`, `email_out sandbox`, `sms sandbox`, `email_in none`, `ses sandbox`, `verifactu sandbox`, `tbai none`, `igic none`, `storage sandbox`, `ai none`, `sentry none`, `redis none`), mensajes de 66-198 caracteres (tope 260), 0 URLs, 0 nombres de variables secretas, 0 «enviado/a» fuera de `real`; el JSON completo no contiene `http`, `://` ni `change-me`. 3.832 bytes |
| 2 | `GET /integrations/status` por rol | direccion **200** (`prop_uxday`, 18 DTO, `degraded: []`), sistemas **200**, recepcion **403** «requiere: integrations.read»; `?propertyId=prop_123` con sesión de `org_uxday` → **404** «Propiedad no encontrada.»; `?propertyId=` → **400** «propertyId debe ser un identificador de propiedad no vacío.»; `?propertyId=nope` → 404; sin token (demo `HOTELOS_ALLOW_DEMO_AUTH=true`) → 200 `prop_123` (18 DTO, `degraded: []`). Invariantes (`readyForReal`, `none` sin actividad, sin URL / secreto, sin «enviado» en no-real): **0 violaciones** en los 3 JSON (`status-final-*.json`). `prop_123`: reales `gestoria_export`, `gbp`; sandbox `channels, whatsapp, email_out, sms, ses, verifactu, storage`; `prop_uxday`: real `gestoria_export`; sandbox `whatsapp, email_out, sms, ses, verifactu, storage` |
| 3 | Pestaña Configuración › Módulos e integraciones › **Integraciones** (`:5205`, sesión direccion `org_uxday` y demo `org_123`) | «Estado leído el 20/09/2026, 13:2x · 18 integraciones»; KPI `prop_uxday` **REALES 1 · EN PRUEBAS 6 · SIN INTEGRACIÓN 11**; `prop_123` **2 · 7 · 9**; 5 áreas (PMS y contabilidad 3, Ventas cobros y reputación 3, Comunicaciones 4, Cumplimiento 4, Plataforma 4) con columnas Integración / Modo / Estado / Qué falta para real / Última actividad / Último error / Configurar; badge verde solo en «Real»; catálogo debajo «0 aplicaciones publicadas · Sin aplicaciones de terceros publicadas» y «Aplicaciones instaladas 0». 0 errores de consola. Capturas `shots/01-integraciones-tabla-uxday.png`, `02-…-completa.png` (página entera), `03-integraciones-catalogo-uxday.png`, `04-integraciones-tabla-demo-prop123.png` (+ `.txt` con el texto de cada pantalla) |
| 4 | Facturación y pagos › **Pagos** | `prop_123`: bloque «Pasarela de pago (PSP) · SIN INTEGRACIÓN · Ningún PSP configurado…», «Qué falta para cobrar en real» (2 líneas), sección «Conexiones de demostración (catálogo heredado) · 1 conexión» con «Demo Payment Gateway (demostración) · mock_payments · DEMOSTRACIÓN · NO COBRA · api_key · 14/05/2026»; `prop_uxday`: «Sin conexiones de pago en el catálogo heredado». **Ninguna aparición de «Conectado»**. Capturas `05`, `06`. API: `GET /properties/prop_123/payments/psp-status` `configured:false`; con `org_uxday` 403 (`payment.capture` no está en sus plantillas: preexistente) |
| 5 | Comunicaciones › **Plantillas y envíos** | `prop_123`: KPI «ENVIADAS (30 DÍAS) 0 · 1 simulada (sin proveedor)»; pestaña Envíos «1 envío · 30 días» con badge **SIMULADO** y «SIMULADO: proveedor de email no configurado; no se envió de verdad.»; Estadísticas con columnas ENVIADAS 0 / **SIMULADAS 1**; `prop_uxday`: 0 / «entregadas». Capturas `07`, `09`, `10`, `08`. Las 222 filas «SIMULADO» del carril pertenecen a tenants aislados de las suites de integración (1 por tenant), no a `org_123` |
| 6 | `POST /properties/prop_123/integrations/iconn_mock_payments/test` (`{}`) | **`status: "simulated"`**, `simulated: true`, mensaje «Prueba simulada (proveedor de demostración): no se ha contactado con ningún servicio externo de «Demo Payment Gateway (demostración)».», evento `IntegrationTestSimulated` `status simulated`; `lastSyncAt` de la conexión sin cambios; `GET /integrations/providers` → 3 proveedores `demo:true` `mode sandbox` con «(demostración)». Sin cuerpo JSON la ruta responde 400 (validación general del API) |
| 7 | SQL solo lectura del runbook §5 (`scratchpad/L8/sql-runbook-s5-final.txt`) | `pms_shadow_profiles` opera_cloud active 1 · `pms_shadow_runs` api_key done 5 / failed 1 / partial 2, cli done 1 · `developer_apps` con `pms.shadow.ingest` 1 · `email_connections` 0 · `ledger_imports` sage200 posted 50 · `channels` airbnb sandbox inactive 1, booking_com sandbox active 2, channex 2, expedia 2 · `payment_provider_connections` 0 · `notification_deliveries` email sent SIMULADO 222 + 1 real · `review_sources` csv connected 1 · `verifactu_submissions` accepted 66 (47 sandbox + 19 sin modo), **66/66 `stub://`** · `ses_hospedajes_submissions` accepted 9 (9/9 `stub://`), failed 149 · `integration_connections` ip_mock_payments connected 1 · `integration_events` ConnectionTested accepted 1 (histórico), IntegrationTestSimulated simulated 2 · `marketplace_listings` 0, `app_installations` 0. Idéntico al runbook salvo el evento simulado nuevo. Postgres 37 → 56 conexiones de 100 durante la verificación (varios carriles) |

Efectos secundarios en la BD del carril: 1 fila nueva en `integration_events` (`IntegrationTestSimulated`, `iconn_mock_payments`, `prop_123`) y 4 sesiones de login (`org_uxday` × 3 + demo); ninguna escritura sobre datos de Faranda.

## 4. Rutas, permisos, contratos y documentos nuevos o cambiados

| Tipo | Elemento | Dónde |
| --- | --- | --- |
| Ruta nueva | `GET /integrations/status?propertyId=` → `IntegrationsStatusResponse { generatedAt, propertyId, integrations[18], degraded[] }`; `propertyId` opcional (defecto: propiedad del contexto), vacío → 400, ajena / desconocida → 404 opaco, sin permiso → 403 | `apps/api/src/modules/integrations/integrations-status.routes.ts` (57 líneas, `resolveRequestedPropertyId` exportada a propósito), registrada en `server.ts:3050` |
| Permiso | `integrations.read`, riskLevel `low` (clave existente: manager, operations_director, accountant, controller, compliance, general_manager, auditor, admin; Recepción no) | `modules/integrations/route-permissions.partial.ts:21` → `security/route-permissions.ts:42` (import) y `:200` (spread) |
| `/health` | Clave superior `integrations` (`IntegrationsHealthBlock`, 14 claves), fuera de `checks`; `checks.redis` y `dependencies.redis` honestos | `server.ts:1756`, `:1876-1914`, `:1896` |
| Respuesta cambiada | `GET /notifications/template-stats`: `sent` solo reales + `simulated` nuevo, `total = sent + simulated + failed + queued`, `lastSentAt` sin simuladas | `dispatcher.service.ts:357-398` |
| Respuesta cambiada | `POST /properties/:propertyId/integrations/:connectionId/test` → siempre `{ status: "simulated", simulated: true, message, connectionId, event }`; `PATCH …/integrations/:connectionId` persiste (`400` con estado inválido); `GET /integrations/providers` con `demo`, `mode` y «(demostración)» | `integrations.service.ts:303`, `:369-375`; `lib/demo-store.ts:1858-1908` |
| Contrato compartido | `packages/shared/src/integrations-status-types.ts` (nuevo) exportado desde `packages/shared/src/index.ts:54` | — |
| Front | `services/integrationsApi.ts` (nuevo, `fetchIntegrationsStatus`), `screens/integrations/IntegrationsStatusPanel.tsx` + `integrations-status-helpers.ts` (nuevos; `MODE_LABELS` «Sin integración» / «Pruebas (sin efecto real)» / «Real», `:27`), `screens/notifications/delivery-outcome.ts` (nuevo), `PaymentSettings.tsx`, `GoLiveChecklist.tsx`, `MarketplaceCatalogScreen.tsx`, `NotificationsScreen.tsx`, `GestoriaExportScreen.tsx`, `billingApi.ts`, `content/actions.ts`, `persona-guides/revenue.ts` | `apps/admin-web/src/…` |
| Discoverability | `IntegrationsStatusPanel` en `apps/admin-web/.discoverability-whitelist.json:44` (componente hijo sin URL propia, con `$comment` fechado) | — |
| Contrato de entorno | `scripts/env-contract.json`: `integrations-status.service.ts` añadido como lector (readBy) — sin variables nuevas | — |
| Tests nuevos | `apps/api/src/modules/integrations/__tests__/integrations-status.test.mts` (**44** casos: una función por integración, invariantes `readyForReal` / `none` sin actividad / sin URL ni secretos / frases sandbox con `simul|prueba|ficticia|local` y sin «enviado», `describeIntegrationsHealth` = 14 claves) y `legacy-hub.test.mts` (**9**: `simulated` nunca `ok`/`accepted`, `PATCH` persistido, proveedores demo); `apps/admin-web/src/screens/integrations/__tests__/integrations-status-helpers.test.mts` (**15**), `screens/notifications/__tests__/delivery-outcome.test.mts` (**8**), `screens/__tests__/integrations-copy.test.mts` (**8**, contratos de fuente sobre los textos corregidos); `operations/__tests__/primary-action.test.mts` (1 aserción actualizada). Ejecutados en este lote: api 53/53, admin-web 43/43 (con `primary-action`). De L8-08 (13:27-13:29): `tests/integrations-honesty-contract.test.mjs` (**21**, en verde dentro de «contratos raíz» de la puerta final), `tests/integration/integraciones-estado.test.mts` (**10**, 401 / 403 recepción y owner / 200 dirección y sistemas, `withEnv` con tope `stub`, `/health.integrations`, hub `simulated`, invariantes de Faranda) y `apps/admin-web/e2e/integrations-status.spec.ts` (1 caso: 18 filas, badge de modo del contrato, «Real» solo en `gestoria_export` con transporte «Manual», contador «Reales» = badges, ninguna fila en pruebas dice «enviado») | — |
| Docs | `docs/api-contracts.md` (hub heredado honesto `:347`; «Estado de las integraciones (Tanda L8 · L8-05)» `:349-356`; `template-stats` `:374`; `/health.integrations` `:784`), **`docs/runbooks/integraciones.md` (nuevo, 496 líneas: §0 vocabulario, §1 tabla de 18 claves, §2 lectura de `/health` y de la pestaña, §3 activación por integración con variables exactas, §4 lo que solo César puede aportar (D-01…), §5 SQL solo lectura, §6 puertas)**, `docs/manual/60-sistemas.md` (§3.3, tabla de `/health`, errores frecuentes), `docs/design/COCOA-22-MIGRACION.md` y `docs/design/cocoa-22-inventory.json` (regenerados: 246 pantallas · 107.721 líneas · 175 puntos, sin deuda nueva), `CLAUDE.md` (bloque 19 «Tanda L8» + Docs prioritarios, escrito por el lote de documentación en paralelo), este informe | — |

## 5. Puertas (línea base 2026-09-20 11:56 → puerta completa final)

Puerta completa de este lote: `NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv bash scripts/gates.sh --json scratchpad/L8/gates-final.json` (13:27:41 → 13:34:39, con las instancias del carril ya paradas; `scratchpad/L8/gates-final.log`). Mientras corría, L8-08/L8-09 dejaron en el árbol el contrato de honestidad, el test de integración, la spec e2e, el runbook v2, `CLAUDE.md` y `ESTADO-VERIFICADO.md` (mtimes 13:27-13:29): las cifras de «contratos raíz» e «integración» ya los incluyen.

| Puerta | Base (`gates-base.json`, `--quick`, 11:56) | Ola 1 (`gates-ola1.json`) | Ola 2 (`gates-ola2.json`) | **Completa L8-10 → orquestador (`gates-full.json`, 13:34 → 14:16)** | **Final tras corrector (`gates-final.json`, completa, 15:09)** |
| --- | --- | --- | --- | --- | --- |
| typecheck:all | 15 PASS · 0 FAIL · 1 SKIP | ídem | ídem | **15 PASS · 0 FAIL · 1 SKIP** (36,9 s) | **15 PASS · 0 FAIL · 1 SKIP** (34,3 s) |
| api unit | 3.569 (1 skip) | 3.622 (1 skip) | 3.622 (1 skip) | **3.622 · 3.621 pass · 0 fail · 1 skip** (+53 frente a la base: los 44 + 9 de la tanda) | **3.636 · 3.635 pass · 0 fail · 1 skip** (+14 del corrector: servicio 50, rutas 3, hub 11, compliance-health 6) |
| admin-web unit | 2.014 (1 skip) | 2.022 (1 skip) | 2.045 (1 skip) | **2.045 · 2.044 pass · 0 fail · 1 skip** (+31: 15 + 8 + 8) | **2.053 · 2.052 pass · 0 fail · 1 skip** (+8 netos: +10 casos, −2 por `CocoaStatusBar` borrado) |
| ai-core / worker | 119 / 34 | 119 / 34 | 119 / 34 | **119 / 34** | **119 / 34** |
| contratos raíz | 765 (2 skip) | 765 (2 skip) | 765 (2 skip) | **786 · 784 pass · 0 fail · 2 skip** (+21 del contrato de honestidad; `seed-ux-day-contract` corregido en la misma puerta, l.164/188 — §7 P1) | **787 · 785 pass · 0 fail · 2 skip** (+1 del contrato de honestidad: el bloque de `/health` no llama a `getComplianceHealth(`) |
| discoverability | 197 URLs | 197 | 197 | **197 URLs** (16/20 del presupuesto de whitelist) | **197 URLs** |
| nav-tree --check | up to date (70 ítems · 104 pestañas · 205) | **rojo** | **rojo** | **rojo** (`stale`, causa ajena — abajo) | **rojo** (`stale`: CSV compartido 304 filas frente a 294 del JSON; filas de L6b / L7 —`/hoy/costes-personal`, `/finanzas/nominas/*`, «RRHH y nóminas»— y de ACT; `nav-tree.generated.json` del worktree sin diff) |
| route-access | 15 tokens × 197 URLs | ídem | ídem | **OK 15 × 197** | **OK 15 × 197** |
| cocoa waves --check | §6 al día | ídem | ídem | **§6 al día** (inventario regenerado: 246 · 107.721 · 175 puntos, `equal: true` con `--stdout`) | **§6 al día** (246 · 107.741 · 175 puntos tras el corrector) |
| rbac:sync --dry-run | OK | OK | OK | **OK** (466 ms; aviso preexistente de plantillas v4) | **OK** (1.565 ms) |
| migrate status + drift | 24/24 · No difference | ídem | ídem | **24/24 · No difference detected.** | **24/24 · No difference detected.** |
| admin-web build | (no en `--quick`) | — | — | **✓ built in 4.04 s** | **✓ built in 3.24 s** |
| integración | (no en `--quick`) | — | — | **993 · 985 pass · 0 fail · 8 skip** (incluye `integraciones-estado.test.mts`, 10 casos; Postgres a 25 conexiones de 100 al empezar) | **993 · 985 pass · 0 fail · 8 skip** |
| **Total** | **12/12** | **11/12** | **11/12** | **13/14** (`rc=1`, solo `nav-tree --check`; `scratchpad/L8/gates-full.json`) | **13/14** (`rc=1`, solo `nav-tree --check`) |

La única puerta roja de la final tiene causa conocida y **no está en código de la tanda** (la de contratos raíz se corrigió en la misma puerta):

1. `contratos raíz`: el fallo de `tests/seed-ux-day-contract.test.mjs` (lista cerrada de las specs e2e sin `integrations-status.spec.ts`) se corrigió en la puerta completa (l.164/188, caso «las 12 specs…»), junto con el flaky #1 de `tests/integration/payments-tenancy.test.mts:202-205` (`correlationId` fuera del cuerpo comparado): 786 · 784 pass · 0 fail · 2 skip.
2. `nav-tree --check` en rojo desde la ola 1 por una causa **ajena al carril**: el CSV compartido `~/anfitorio-demo/pilots/tanda5-nav-tree.csv` (mtime 12:19, 294 → 300 filas) incorpora las filas 295-300 de la Tanda ACT (`RealEstateAssetScreen` y sus pestañas `/finanzas/activo-inmobiliario/*`), cuyas pantallas no existen en este worktree; regenerar `nav-tree.generated.json` aquí rompería `route-access` / `discoverability`. Le toca al carril ACT al fusionar (o al orquestador con `merge-lane.sh`).
3. A las 15:09 (puerta tras el corrector) el mismo CSV tiene **304 filas**: a las de ACT se suman las de L6b / L7 (`DirectorLaborCostsScreen` `/hoy/costes-personal`, `HrEmployeesScreen` `/finanzas/nominas/plantilla`, ítem «Nóminas» → «RRHH y nóminas»), también inexistentes en este worktree. Mismo tratamiento: en la rama fusionada, `node scripts/build-nav-tree.mjs --csv /Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` y después `route-access` / `discoverability`; `nav-tree.generated.json` de L8 se entrega sin diff.

## 6. Lo que solo César puede aportar (por integración; detalle en el runbook §4)

| Integración | Qué falta (lo dice `missingForReal` / el runbook) | Quién |
| --- | --- | --- |
| OPERA (modo sombra) | Corte diario automático (agente de carpeta o Report Scheduler hacia el buzón `pms_shadow`), muestras reales de los informes 3-7 y del XML de ingresos, criterio contable por transaction code; OHIP en fase 2 (Enterprise ID, Chain Code, Hotel IDs, app key) | César (+ gestoría para el criterio contable) |
| Sage 200 / exportación | Nada para el modo (ya es real por ficheros); especificación del fichero de importación de Sage 200 si se quiere ese formato (D-04); decisiones §10 del runbook Sage | César + gestoría |
| Canales (OTAs) | Cuenta Channex (staging + API key), ids de propiedad / productos, extranet ids de Booking / Expedia; `CHANNEL_MAX_MODE=real` solo en producción; certificación con el agregador o con cada OTA | César |
| PSP | Cuenta Stripe (`sk_live_` + `whsec_`) **o** comercio Redsys (FUC, terminal, clave), origen público HTTPS, decisión enlace de pago vs preautorización, política de reembolsos | César |
| WhatsApp | Cuenta WhatsApp Business (Tech Provider o BSP), número por hotel, plantillas *utility* aprobadas, opt-in, secreto de la app para el webhook | César |
| Correo saliente / SMS | Cuenta Postmark o SendGrid con dominio verificado (SPF/DKIM) y clave; remitente por hotel o único; cuenta Twilio y número E.164 si el SMS se usa | César |
| Correo entrante (OAuth) | Cliente OAuth de Google Workspace o Microsoft 365 (id, secreto, URI de retorno), un buzón por propósito y centro, autorización por el dueño del buzón | César |
| Google Business Profile | Decisión de construir la ruta de autorización OAuth (no existe: producto), proyecto Google (cliente y secreto), id de ubicación por hotel, activar el módulo `reputation_quality` por propiedad | César + producto |
| SES.Hospedajes | Alta por establecimiento (número de registro, usuario del WS), certificado XAdES del titular y contraseña, `preproduction` antes de `production`, bloque de establecimiento completo (Rías Altas sigue bloqueada) | César |
| VeriFactu | Declaración responsable del productor (razón social, NIF, id / versión del sistema), número de instalación por despliegue, certificado del obligado tributario, `preproduction` → `production`, política de cadena por sociedad (SII) | César + asesor fiscal |
| TicketBAI / IGIC | Solo si un hotel tributa en territorio foral (territorio en la ficha, licencia, serie del dispositivo, certificado); IGIC: decidir si se retiran las variables `IGIC_*` sin conector | César (si aplica) / producto |
| Almacén (S3) | Proveedor S3 compatible en la UE, bucket privado, credenciales; decisión `disk` (VPS con backup) vs S3; sonda de acceso al bucket (producto) | César + producto |
| IA | Clave de organización con DPA + ZDR, residencia (API directa vs Bedrock UE), presupuesto por hotel, `AI_USD_EUR_RATE`, proveedor de visión | César |
| Sentry / Redis | DSN del proyecto de Sentry (cuenta); decidir si Redis entra (cola o limitador multi-réplica) o se retira del contrato (D-16) | César / producto |

## 7. Pendientes (con dueño)

| # | Pendiente | Dueño |
| --- | --- | --- |
| P1 | **Cerrado en la puerta completa (786 · 784 · 0 fail)**: `integrations-status.spec.ts` añadido a la lista (l.164, 188) y caso «las 12 specs…»; `payments-tenancy.test.mts:202-205` (flaky #1) corregido. Texto original: `tests/seed-ux-day-contract.test.mjs:157-171` («las 11 specs de recepción…») fija por `deepEqual` la lista de ficheros de `apps/admin-web/e2e/` y L8-08 añadió `integrations-status.spec.ts` sin tocarla (786 tests · 783 pass · **1 fail** · 2 skip). Corrección de una línea: añadir `"integrations-status.spec.ts"` a la lista (orden alfabético, entre `frontdesk-cockpit` y `login`) y renombrar el caso a «las 12 specs…»; la spec nueva ya cumple el bucle del contrato (sin `test.skip`, sin `/backoffice/`). Después, `node --test tests/*.test.mjs` y la puerta completa deben quedar en 786/786 (2 skip) | L8-08 (o corrector) — fichero fuera de este lote |
| P2 | `docs/runbooks/integraciones.md` §6 («Resultado al cierre») cita la puerta completa del orquestador (`gates-full.json`: api 3.622 · admin-web 2.045 · contratos raíz 786) y la `--quick` del corrector (11/12); la puerta completa definitiva tras el corrector es `scratchpad/L8/gates-final.json` (15:09, §13): api 3.636 · admin-web 2.053 · contratos raíz 787 · integración 993 · 13/14. Una línea en las filas «Suites completas» y «Puertas del carril» (fichero fuera del integrador, que solo edita este informe y ESTADO-VERIFICADO) | lote de docs siguiente |
| P3 | `nav-tree --check` rojo por el CSV compartido (304 filas a las 15:09 frente a 294 del JSON del worktree): filas de la Tanda ACT (`/finanzas/activo-inmobiliario/*`) y de L6b / L7 (`/hoy/costes-personal`, `/finanzas/nominas/*`, «RRHH y nóminas»), pantallas inexistentes aquí; en la rama fusionada, `node scripts/build-nav-tree.mjs --csv /Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` y `route-access` / `discoverability` de nuevo; `nav-tree.generated.json` de este worktree se entrega sin diff | orquestador (fusión) |
| P4 | Textos pendientes de la auditoría (§2, lista final): **cerrados por el corrector L8 (§9)** A8 campañas, A9 `CocoaStatusBar`, B1 (SES), manuales 10/20 y `integration-provider-manifest.ts` (apps/mobile); quedan B2-B6 y los comentarios con «parte enviado a SES» | tanda de textos siguiente |
| P5 | `gestoria_export` se declara `real`/`manual` con `readyForReal true` aunque nada cruce por red y `lastActivityAt` sea siempre `null` (sin consulta a `gestoria_exports`); `psp.lastActivityAt` siempre `null`. Decidir si «real» se reserva a datos que cruzan y, si no, añadir `lastExportAt` (la función ya acepta el parámetro) | producto (L8-01 lo dejó documentado) |
| P6 | Divergencia deliberada: `verifactu` / `ses` en `preproduction` son `real` con `readyForReal:false` («Decisión: pasar a producción») mientras `GET /compliance/health` declara `readyForReal:true`; documentada en `api-contracts.md:354` y runbook §0 | producto |
| P7 | **Cerrado por el corrector L8 (§9, REV-03)**: `/health` usa `describeComplianceEnvironment()` (solo entorno, sin Prisma); TicketBAI habla del proceso y su aplicabilidad se evalúa por propiedad | — |
| P8 | `integrations-status.service.ts` importa `routes/webhooks-whatsapp.routes.ts` por `unsignedWebhookMode` (cadena de imports pesada; hoy sin ciclo porque `server.ts` importa el servicio y la ruta por separado): mover `unsignedWebhookMode` a un módulo sin Fastify | mantenimiento |
| P9 | Sin test de render React del panel (la spec e2e `integrations-status.spec.ts` de L8-08 lo cubre en navegador real, pero no se ejecuta en `gates.sh`: correrla a mano con API y Vite propios según el runbook §6); sin test unitario del API para `templateStats.simulated` (verificado por HTTP en §3 y por la integración `integraciones-estado.test.mts` solo en lo que toca a `/integrations/status`) | lote de tests |
| P10 | Roles `direccion` / `sistemas` de `org_uxday` reciben 403 en `GET /properties/:id/payments/psp-status` (`payment.capture`) — la pantalla Pagos ya no depende de esa ruta, pero la sonda del runbook §2.4 sí; y `GET /integrations/status` sin token responde 200 con el contexto demo (`HOTELOS_ALLOW_DEMO_AUTH=true`, común a todo GET low del build) | RBAC / producto |
| P11 | ESLint no ejecutable en el repo (falta `eslint.config.*` con ESLint 9): preexistente | mantenimiento |
| P12 | `rbac:sync --dry-run` avisa de 69 roles de plantilla por detrás de la versión 4 (`--upgrade-templates`, runbook `rbac-sync.md`): preexistente, ajeno | operación |
| P13 | Panel de navegador compartido durante la ola: lotes anteriores sembraron claves `localStorage` (`hotelos.auth.*`, `hotelos-active-*`) en orígenes de otros carriles (`127.0.0.1:5201`, `localhost:5203`) sin limpiarlas; este lote usó una pestaña propia (`tab-9`, `127.0.0.1:5205`, sesión `org_uxday`) y capturas con Playwright headless (sin tocar el panel) | carriles afectados: `localStorage.clear()` si ven 401 |

## 8. Fusión: ficheros tocados, exclusiones y servidores

Estado del worktree al commit del integrador (`git status --porcelain -uall`, 2026-09-20 tarde; `git diff --stat` sobre `a069906`: 42 rutas con diff, 1.052 inserciones · 404 borrados, más 19 ficheros nuevos):

- **Modificados (40, sin `pnpm-lock.yaml`)**: `CLAUDE.md`; `apps/admin-web/.discoverability-whitelist.json`, `src/components/cocoa-global/index.ts`, `src/content/actions.ts`, `src/content/persona-guides/revenue.ts`, `src/layouts/__tests__/shell-cocoa22-contract.test.mts`, `src/screens/GoLiveChecklist.tsx`, `src/screens/PaymentSettings.tsx`, `src/screens/accounting/GestoriaExportScreen.tsx`, `src/screens/compliance/SesHospedajesSettingsScreen.tsx`, `src/screens/compliance/__tests__/ses-hospedajes-pending-statuses.test.mts`, `src/screens/fiscal/fiscal-shared.ts`, `src/screens/fiscal/__tests__/fiscal-shared.test.mts`, `src/screens/marketing/CampaignManagerScreen.tsx`, `src/screens/marketplace/MarketplaceCatalogScreen.tsx`, `src/screens/notifications/NotificationsScreen.tsx`, `src/screens/operations/__tests__/primary-action.test.mts`, `src/services/billingApi.ts`; `apps/api/src/lib/demo-store.ts`, `modules/backoffice/backoffice.service.ts`, `modules/compliance/compliance-health.service.ts`, `modules/compliance/__tests__/compliance-health.test.mts`, `modules/developer/api-reference.service.ts`, `modules/integrations/integrations.service.ts`, `modules/notifications/dispatcher.service.ts`, `security/route-permissions.ts`, `server.ts`; `apps/mobile/src/screens/more/IntegrationMarketplaceScreen.tsx`; `packages/integrations/src/registry/integration-provider-manifest.ts`; `packages/shared/src/index.ts`; `docs/api-contracts.md`, `docs/audits/ESTADO-VERIFICADO.md`, `docs/design/COCOA-22-MIGRACION.md`, `docs/design/cocoa-22-inventory.json`, `docs/manual/10-direccion.md`, `docs/manual/20-administracion.md`, `docs/manual/60-sistemas.md`; `scripts/env-contract.json`; `tests/integration/payments-tenancy.test.mts` (flaky #1 corregido en la puerta: `correlationId` fuera del cuerpo comparado) y `tests/seed-ux-day-contract.test.mjs` (lista de specs con `integrations-status.spec.ts`, l.164/188).
- **Borrado (1)**: `apps/admin-web/src/components/cocoa-global/CocoaStatusBar.tsx` (corrector, REV-10: «Conectado» fijo y contadores inventados, sin consumidor).
- **Nuevos (19)**: `apps/admin-web/e2e/integrations-status.spec.ts`; `apps/admin-web/src/screens/__tests__/integrations-copy.test.mts`, `src/screens/integrations/{IntegrationsStatusPanel.tsx, integrations-status-helpers.ts, __tests__/integrations-status-helpers.test.mts}`, `src/screens/notifications/{delivery-outcome.ts, __tests__/delivery-outcome.test.mts}`, `src/services/integrationsApi.ts`; `apps/api/src/modules/integrations/{integrations-status.routes.ts, integrations-status.service.ts, route-permissions.partial.ts, __tests__/integrations-status.test.mts, __tests__/integrations-status-routes.test.mts, __tests__/legacy-hub.test.mts}`; `packages/shared/src/integrations-status-types.ts`; `tests/integrations-honesty-contract.test.mjs`, `tests/integration/integraciones-estado.test.mts`; `docs/runbooks/integraciones.md`; este informe.
- **Excluidos del commit**: `pnpm-lock.yaml` (modificación heredada, +64 / −25; `git reset -q hotelos/pnpm-lock.yaml` antes de commitear; la decisión sobre el lock de `main` está en §11, seguridad REV-02). Ignorados por git y no versionados: `apps/admin-web/dist/`, `apps/admin-web/test-results/`, `.env`; el scratchpad (`scratchpad/L8*/`: capturas, JSON de puertas, logs; tokens borrados) tampoco se versiona.
- **Servidores**: ninguna instancia del carril viva al cerrar (`lsof` en `:3935` / `:5205` vacío); `:3000`, `:5173` y el proceso 4986 no se tocaron en toda la tanda. BD `hotelos_l8` (48/100 conexiones al cierre): conserva las filas de §3 (1 evento simulado, sesiones de login de las verificaciones) y `last_sync_at = NULL` en la fixture demo `iconn_mock_payments` (§9); Faranda solo lectura en todos los lotes.
- **Commit** (integrador, rama `tanda-l8`, sin push ni fusión): `git -C ~/anfitorio-demo-wt-l8 add -A hotelos && git -C ~/anfitorio-demo-wt-l8 reset -q hotelos/pnpm-lock.yaml && git -C ~/anfitorio-demo-wt-l8 commit -F <mensaje>` con el pre-commit del repo (`check-discoverability` + `typecheck-all`, sin `--no-verify`); título `feat(integraciones): estado honesto none|sandbox|real con GET /integrations/status, bloque integrations en /health, panel de la pestaña Integraciones y fin de los éxitos falsos (Tanda L8)`, cuerpo por lote (§10). Sha en el informe del integrador al orquestador (§13).

## 9. Corrección posterior (corrector L8 · 2026-09-20, tarde)

Hallazgos de la revisión funcional y de seguridad corregidos en el mismo worktree (cifras de puertas en ESTADO-VERIFICADO y runbook §6):

| Id | Corrección (ancla) | Test que la fija |
| --- | --- | --- |
| funcional REV-01 | `ses` / `verifactu` / `tbai` por propiedad aplican el interruptor del establecimiento (`properties` ∪ `property_compliance_settings`) o el uso real (envíos SES 180 días, facturas emitidas, territorio foral): `complianceGates` + parámetro `gate` de `complianceStatus` (`integrations-status.service.ts`); apagado y sin uso → `none` «Desactivado para este establecimiento…» con «Activar … para el establecimiento» al frente de `missingForReal`; activo solo por uso → nota en la frase. `prop_uxday`: `ses` `none`, `verifactu` `sandbox` «activo solo por uso (24 facturas emitidas)» | `integrations-status.test.mts` (+6 casos: gate apagado/por uso, `complianceGates` puro, colector con flags, sin fila de propiedad no gatea, nota TBAI de /health) |
| funcional REV-02 | Campañas KPI «Enviadas · registradas como enviadas (sin motor de envío)»; SES «Aceptado (simulador)» / «Enviado (simulador)» bajo `endpoint stub://` y «Simulador local (sin envío a la autoridad)» en «Punto de acceso» (`fiscal-shared.ts`: `isSimulatedSubmission`, `simulatorAwareStatusLabel`, `SIMULATED_ENDPOINT_LABEL`); manifiesto `packages/integrations` con `demo:true` / `mode:"sandbox"` / «(demostración)» y `apps/mobile` sin «connected»; manuales 10/20 con la pestaña Pagos real | `fiscal-shared.test.mts` (+3), `integrations-copy.test.mts` (+7) |
| seguridad REV-01 | `/health.integrations` sin BD: `describeComplianceEnvironment()` (`compliance-health.service.ts`, `getTbaiHealth(null)` = modo del proceso + «Aplica solo a los establecimientos con territorio foral; el modo efectivo se lee por propiedad.») sustituye a `getComplianceHealth()` sin organización en `server.ts` | `compliance-health.test.mts` (+3), `integrations-status.test.mts` (nota de tbai), `integrations-honesty-contract.test.mjs` (+1: el bloque no llama a `getComplianceHealth(`) |
| seguridad REV-02 | `pnpm-lock.yaml`: no se toca (regla); decisión del orquestador / César (fijar `@playwright/test` 1.61.x o instalar el navegador 1243) | — |
| seguridad REV-03 / funcional REV-08 | Cifras de puertas alineadas (13/14, contratos raíz 786 · 784 · 0) en §0, §5, §7 de este informe, ESTADO-VERIFICADO y runbook §6; `api-contracts.md` cita el contrato de honestidad como existente (22 casos) | `brand-contract`, `manual-contract` (docs) |
| low REV-03 | «Actualizar todo» de la cabecera de Integraciones recarga también el panel (`IntegrationsStatusPanel refreshKey`); vacío «Ninguna aplicación instalada; el catálogo de terceros está vacío.» | `integrations-copy.test.mts` |
| low REV-04 | Pagos: badge de estado persistido (`error` / `disconnected`) junto a «Demostración · no cobra»; fixture `iconn_mock_payments` sin `lastSyncAt` (fila de `hotelos_l8` a NULL) | `legacy-hub.test.mts` (+1), `integrations-copy.test.mts` |
| low REV-05 | `sage200` «en la sociedad (la importación contable es por sociedad, común a todos sus centros)»; runbook §3.2 punto 4 | `integrations-status.test.mts` |
| low REV-06 | `missingForReal` sin punto final (`finalizeIntegrationStatus`) | `integrations-status.test.mts` |
| low REV-07 | `resolveRequestedPropertyId` sin `trim` (espacios ⇒ 404 opaco del guard), documentado en `api-contracts.md` | `integrations-status-routes.test.mts` (nuevo, 3 casos) |
| low REV-10 | `CocoaStatusBar.tsx` borrado (sin consumidor; «Conectado» y contadores inventados), `cocoa-global/index.ts` y `shell-cocoa22-contract` actualizados | `integrations-copy.test.mts` |
| seguridad low REV-05 | `countPropertyIntegrationErrors` solo siembra las fixtures del hub cuando la propiedad es la demo (`legacyFixturesApplyTo`) | `legacy-hub.test.mts` (+1) |
| low REV-09 · seguridad low REV-04 / REV-06 | Sin corregir aquí: e2e no ejecutable (Playwright 1.63 vs navegador 1228: decisión de César), `nav-tree --check` por el CSV compartido (carril dueño) y ficheros sin dueño (`.discoverability-whitelist.json`, `scripts/env-contract.json`, `docs/design/cocoa-22-inventory.json`, `COCOA-22-MIGRACION.md`, `tests/seed-ux-day-contract.test.mjs`): el orquestador los incluye en el commit y excluye `pnpm-lock.yaml` | — |

**Ficheros tocados por el corrector L8** (además de los de §8; el orquestador los incluye en el commit): `apps/api/src/modules/integrations/{integrations-status.service.ts, integrations-status.routes.ts, integrations.service.ts, __tests__/integrations-status.test.mts, __tests__/legacy-hub.test.mts, __tests__/integrations-status-routes.test.mts (nuevo)}`, `apps/api/src/modules/compliance/{compliance-health.service.ts, __tests__/compliance-health.test.mts}`, `apps/api/src/server.ts`, `apps/api/src/lib/demo-store.ts`, `apps/admin-web/src/screens/{marketing/CampaignManagerScreen.tsx, compliance/SesHospedajesSettingsScreen.tsx, compliance/__tests__/ses-hospedajes-pending-statuses.test.mts, fiscal/fiscal-shared.ts, fiscal/__tests__/fiscal-shared.test.mts, marketplace/MarketplaceCatalogScreen.tsx, integrations/IntegrationsStatusPanel.tsx, PaymentSettings.tsx, __tests__/integrations-copy.test.mts}`, `apps/admin-web/src/components/cocoa-global/index.ts` (+ `CocoaStatusBar.tsx` borrado), `apps/admin-web/src/layouts/__tests__/shell-cocoa22-contract.test.mts`, `packages/integrations/src/registry/integration-provider-manifest.ts`, `apps/mobile/src/screens/more/IntegrationMarketplaceScreen.tsx`, `tests/integrations-honesty-contract.test.mjs`, `docs/{api-contracts.md, runbooks/integraciones.md, audits/ESTADO-VERIFICADO.md, audits/TANDA-L8-INTEGRACIONES-2026-09-20.md, manual/10-direccion.md, manual/20-administracion.md, design/cocoa-22-inventory.json, design/COCOA-22-MIGRACION.md}` (inventario y §6 regenerados con `scripts/cocoa-22-inventory.mjs` y `cocoa-22-waves.mjs --write`), `CLAUDE.md`. BD `hotelos_l8`: `integration_connections.last_sync_at = NULL` en la fila `iconn_mock_payments` (fixture demo, `prop_123`). Verificado en runtime con instancia propia `:3935` (PID 23863, matada): `/health` 3 ms tras el arranque con `integrations.tbai` «Modo de pruebas local … Aplica solo a los establecimientos con territorio foral…», sin «ninguna propiedad», sin `http` ni `stub`; `GET /integrations/status?propertyId=prop_uxday` (direccion@uxday.test) → 200, `degraded: []`, 1 real · 5 sandbox · 12 none, `ses` `none` «Desactivado para este establecimiento y sin envíos en los últimos 180 días…», `verifactu` `sandbox` «… Activo solo por uso (24 facturas emitidas)…» con «Activar VeriFactu para el establecimiento» primero, `tbai` `none` «No aplica: el establecimiento no declara territorio foral…», ningún pendiente con punto final; `?propertyId=%20prop_uxday%20` → 404, `?propertyId=` → 400, `?propertyId=%20%20` → 404; `/backoffice/properties/prop_uxday/dashboard` → `integrationErrors 0` y 0 filas de `integration_connections` para `prop_uxday` (las fixtures ya no se siembran fuera de la demo).

## 10. Por lote: qué, cómo, tests, verificación

Ficheros por lote exclusivos (ningún fichero editado por dos lotes de la misma ola); puertas `--quick` tras cada ola (§5) y completa al final (§13). Todas las verificaciones con instancias propias del worktree (`PORT=<api> RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true node --env-file-if-exists=../../.env --import tsx src/server.ts`; Vite con `VITE_API_URL`), matadas por PID; BD `hotelos_l8` solo lectura sobre Faranda.

| Lote | Qué | Cómo | Tests (del lote) | Verificación |
| --- | --- | --- | --- | --- |
| L8-01 · contrato + servicio | `packages/shared/src/integrations-status-types.ts` (18 `INTEGRATION_KEYS` en orden de pantalla, `IntegrationStatusDto`, `IntegrationsHealthBlock`, etiquetas y pantallas en español) y `apps/api/src/modules/integrations/integrations-status.service.ts` (una función pura por integración, `finalizeIntegrationStatus`, `describeIntegrationsHealth` 14 claves sin BD, `collectIntegrationsStatus` por propiedad) | Entradas explícitas y lectores existentes inyectables (`readChannelEnv`, `aiConfigSummary`, `emailProvidersStatus`, `emailStatus`, `pspStatusFor`, `getComplianceHealth`, `describeDocumentStorageHealth`, `isWhatsappConfigured`, `isSmsConfigured`, `unsignedWebhookMode`); Prisma solo en el colector (`Promise.all` bajo `createDegradedCollector`: fallo ⇒ «sin datos» + `degraded[]`, nunca éxito); 0 lecturas nuevas de `process.env` (censo intacto); semántica `mode` = cómo cruzan los datos, `configured` independiente | `integrations-status.test.mts` 44 (→ 50 tras el corrector): none / sandbox / real por clave, invariantes comunes, redis sin consumidor, s3 no comprobado, errores de software de VeriFactu → `missingForReal`, 14 claves de `/health`, colector con stubs; api unit 3.622 | Script tsx contra `hotelos_l8` (solo lectura): `org_uxday/prop_uxday` 18 integraciones · 79-99 ms · `degraded []` · 0 violaciones; propiedad de Faranda 18 · 13-18 ms (`sage200` real/files «50 lotes importados, 50 contabilizados», `channels` sandbox «3 de 4 canales activos, todos en simulador local»); `describeIntegrationsHealth` con los lectores reales 14 claves, 0 frases con URL o variable secreta |
| L8-02 · hub heredado + Redis + readiness | Proveedores «… (demostración)» con `demo:true` / `mode:"sandbox"`; prueba de conexión `{ status: "simulated", simulated: true }` + evento `IntegrationTestSimulated` sin mover `lastSyncAt`; `PATCH` de estado persistido y auditado (400 si inválido); `checks.redis` «configured (sin consumidor en este build)» y `dependencies.redis` `unconfigured`; readiness `payment_provider_connected` por `pspStatusFor` e `integrationErrors` sin mocks ni otras propiedades | `lib/demo-store.ts`, `integrations.service.ts` (`buildConnectionTestResult`, `updateIntegrationConnectionStatus`, `countPropertyIntegrationErrors`), `server.ts` (`PATCH` delega; `/health`), `backoffice.service.ts` (`paymentProviderConnectedFor` eliminada) | `legacy-hub.test.mts` 9 (→ 11); `modular-suite-contract` + `cors-contract` + `env-contract` 28/28; api unit 3.578 en su momento | Instancia `:3937`: `/health` Redis honesto; `connect` + `test` de `mock_payments` → `simulated` con fila en `integration_events`; `PATCH {status:"error"}` → fila en Postgres + `IntegrationStatusUpdated`; `PATCH {status:"bogus"}` → 400; dashboard `prop_123` `paymentProviderStatus missing` (antes `connected`), readiness `fail/blocking`; filas de prueba borradas por id |
| L8-03 · Comunicaciones sin «enviado» falso | `delivery-outcome.ts` (`simulated` = `sent` + «SIMULADO»), badge y filtro «simulado», KPI «Enviadas (30 días)» solo reales con «n simuladas (sin proveedor)», Estadísticas con columna «Simuladas»; `GET /notifications/template-stats` con `simulated` y `sent` solo reales | `NotificationsScreen.tsx` (0 `style=` nuevos, 6 = 6), `dispatcher.service.ts` (`SIMULATED_ERROR_MESSAGE` única fuente del texto, `isSimulatedDelivery`, `templateStats`) | `delivery-outcome.test.mts` 8; suites que tocan el dispatcher 64/64; `--quick` de ola 10/12 (rojos ajenos: inventario cocoa y CSV) | `:3945` / `:5205`: 2 `dispatch` simulados + 1 fallido → KPI 0 · «2 simuladas (sin proveedor)», `template-stats` `sent 0 · simulated 1`, badges «simulado» / «fallido», filtro «enviado» → 0 filas; Playwright headless (chromium ya en caché), 0 errores de consola; filas de prueba borradas por id |
| L8-04 · auditoría de textos + runbook v1 | `docs/runbooks/integraciones.md` v1 (§0 vocabulario, §1 tabla de 18 claves, §2 lectura de `/health` y de la pestaña, §3 activación con variables exactas, §4 D-01…D-18, §5 SQL solo lectura, §6 puertas) y `scratchpad/L8/audit-textos.md` (454 hits en 112 ficheros → A1-A9 éxitos falsos, B1-B9 parciales, 0 descargas vacías en 15 `apiRequestBlob` + 15 descargas en cliente) | Solo documentación; anclas re-verificadas por grep sobre el árbol; claves re-ancladas a `INTEGRATION_KEYS` | `brand-contract` 10/10 (0 hits de marca antigua) | Instancia `:3935` antes y después de L8-01: `/health` (sin `integrations` aún), `/integrations/status` 404 (sin cablear), `/compliance/health` `sandbox_only`, `email-status` `simulated`, `email/providers` todo `false` salvo manual; SQL agregados de §5 (corrige el recon: los 149 `failed` son de SES, VeriFactu 0) |
| L8-05 · cableado | `GET /integrations/status?propertyId=` (`integrations.read`, low) y clave superior `integrations` de `/health` (14 claves, fuera de `checks`); `platformIntegrationsConfigured()`; `docs/api-contracts.md` | `integrations-status.routes.ts` (`resolveRequestedPropertyId`, tenencia por `grantPropertyAccess`: 404 opaco), `route-permissions.partial.ts` → `security/route-permissions.ts` (sin claves nuevas), `server.ts` (registro tras `registerPmsShadowRoutes`; `getComplianceHealth` estático, sustituido después por `describeComplianceEnvironment`, §9) | `api-route-permissions` + `cors` + `rbac-nav` + `modular-suite` 64/65 (1 skip preexistente); `env-contract` 9/9; `rbac:sync --dry-run` +0; `documents-health` 4/4 (BD) | `:3947`: `/health` healthy con 14 claves (none 6 · sandbox 7 · real 1), `checks` intacto, sin secretos, 22 ms de media; `GET /integrations/status` 200 direccion (18 DTO, invariantes 0 violaciones) / 403 recepción / 404 `prop_123` con sesión uxday / 400 vacío; modos coherentes con `/health` en las 12 claves comparables |
| L8-06 · panel de la pestaña | `IntegrationsStatusPanel.tsx` + `integrations-status-helpers.ts` + `services/integrationsApi.ts` montados sobre el catálogo en `MarketplaceCatalogScreen` (KPI Reales / En pruebas / Sin integración, 5 áreas, badge de modo, «qué falta», última actividad, último error, «Configurar», callout de consultas degradadas, 404 honesto) | Cocoa (0 `style=`), `useTabHost`, `shellNavigate` / `requestTabNavigation` (OPERA → pestaña Modo sombra); etiquetas = `INTEGRATION_MODE_LABELS_ES` (el contrato de copy prohíbe «sandbox» en pantalla) | `integrations-status-helpers.test.mts` 15; `admin-web-spanish-copy` + `no-raw-fetch` + `cocoa-22` 27/28 (inventario regenerado después); `check-discoverability` (whitelist añadida después) | `:3947` / `:5217`: pestaña con «18 integraciones», KPI 1 / 6 / 11, 5 tablas, «Configurar» OPERA → `/configuracion/modulos/modo-sombra`, Canales → `/comercial/canales`; móvil 375 px sin scroll horizontal; 0 errores de consola |
| L8-07 · pantallas vecinas + manual | Pagos lee la fila `psp` (sin `gatewayReady` del hub) y muestra el hub como «Conexiones de demostración (catálogo heredado)»; Gestoría «compatible ContaPlus / Sage 50», A3 «(no disponible)» con el 409; `GoLiveChecklist` «PSP configurado (Stripe o Redsys · módulo Payment Vault)»; toast «parte encolado para SES»; guía de revenue y `api-reference` sin «agencias» / «descargar»; `docs/manual/60-sistemas.md` §3.3 y tabla de `/health` | `PaymentSettings.tsx` (`Promise.allSettled`, cada bloque degrada por separado), `billingApi.ts` (`pspIntegrationStatus`), `GestoriaExportScreen.tsx`, `content/actions.ts`, `persona-guides/revenue.ts`, `developer/api-reference.service.ts` | `integrations-copy.test.mts` 8 (→ 15); `primary-action` + `screens-fixes-contract` 51/51; `api-reference.test.mts` 13/13; admin-web 2.044 / 2.045 | `:3935` / `:5205`: Pagos `prop_uxday` «SIN INTEGRACIÓN» con «Qué falta para cobrar en real» (2 ítems), `prop_123` «Demo Payment Gateway (demostración) · DEMOSTRACIÓN · NO COBRA»; sin «Conectado»; gestoría sin el nombre del producto de importación; literales del manual §3.3 contrastados en la pestaña |
| L8-08 · tests de la tanda | `tests/integrations-honesty-contract.test.mjs` (21 → 22), `tests/integration/integraciones-estado.test.mts` (10), `apps/admin-web/e2e/integrations-status.spec.ts` (1) | Contrato solo fuente (regex anclados a literales del servicio, `server.ts`, PSP, webhook, proveedores, panel, runbook §1, `api-contracts.md`); integración con organización aislada `org_l2_l808…`, `withEnv(STRICT_ENV)` y `resetAiConfigForTests`; e2e con `loginAsUxDay` y selectores por rol / `data-cocoa` | 21/21 (0,27 s) · 10/10 (2,7 s, BD) · 1/1 (1,4 s con configuración temporal del scratchpad por el navegador 1228) | `:3935` / `:5205` propias (PIDs 9491 / 9504, matadas): `real` solo `gestoria_export` (transporte manual), 0 `real` por red, KPI = badges; 401 sin token solo bajo `STRICT_ENV` (el carril tiene auth de demo); 0 organizaciones `org_l2_*` residuales |
| L8-09 · documentación de estado | Runbook v2 (línea base real del carril, códigos 200/403/404/400, `degraded`, panel real, comandos exactos), `CLAUDE.md` (deuda 19 + «Docs prioritarios») y el bloque «Estado verificado (Tanda L8)» de `ESTADO-VERIFICADO.md` | Solo documentación; anclas por grep; SQL agregados solo lectura | `brand-contract` + `integrations-honesty-contract` 31/31; contratos que leen `CLAUDE.md` 35/35 | `:3949` (PID 88698, matado): `/health` 14 claves (1 real · 7 sandbox · 6 none, frase más larga 198), `GET /integrations/status` `prop_123` 2 · 7 · 9 y `prop_uxday` 1 · 6 · 11, 403 recepción, 404 ajena, 400 vacío |
| L8-10 · cierre | Este informe (§0-§8), puerta completa del lote (13:34, entonces 12/14) y verificación en runtime con 11 capturas (§3) | Script `l8-10-verify.py` + Playwright headless (`l8-10-shots.mjs`), sesión sembrada en `localStorage` como `e2e/_helpers.ts` | Tests de la tanda aparte: api 53/53, admin-web 43/43, contratos brand + audit + honesty 37/37 | §3: `/health` con `checks` idénticos a la línea base salvo Redis, `GET /integrations/status` por rol, hub `simulated`, readiness `prop_123` bloqueado, Pagos sin «Conectado», Comunicaciones con «simulada», SQL §5 del runbook |
| Puertas (orquestador) | Tres ejecuciones completas de `gates.sh --json` → **13/14** (`gates-full.json`, 14:16) | `tests/seed-ux-day-contract.test.mjs` con la spec nueva (l.164/188); flaky de `tests/integration/payments-tenancy.test.mts:202-205` corregido sin debilitarlo | contratos raíz 786 (784 · 2 skip); integración 993 (985 · 8 skip) | §5 |
| Corrector L8 | 5 hallazgos medium confirmados: 4 corregidos + 7 low corregidos (§9, §11) | `--quick` **11/12** (`scratchpad/L8-fix/gates-quick.json`): api 3.636 · admin-web 2.053 · contratos raíz 787; `integraciones-estado` 10/10 a mano; typecheck de `packages/integrations` y `apps/mobile` rc = 0 | +14 api · +8 admin-web · +1 contrato | `:3935` (PID 23863, matado): `/health` 3 ms sin BD, `prop_uxday` 1 · 5 · 12 con `ses` `none`, `propertyId` con espacios → 404, dashboard `prop_uxday` `integrationErrors 0` |
| Puerta final tras el corrector | `gates.sh --json` completa (15:09) → **13/14** | único rojo `nav-tree --check` (CSV externo, 304 filas) | api 3.636 · admin-web 2.053 · ai-core 119 · worker 34 · contratos raíz 787 · integración 993 | §13 |
| Integrador | Este cierre (§8, §10-§13), bloque ESTADO-VERIFICADO y commit en `tanda-l8` | `git status` / `git diff --stat` del worktree; solo edita los dos ficheros de auditoría | contratos que leen `docs/audits` tras la edición y `--quick` final (§13) | §13 |

## 11. Hallazgos de la revisión (confirmados / refutados / corregidos)

Revisión funcional en runtime y de seguridad / datos / regresiones sobre el árbol de la ola 3. **Refutados: ninguno** (0 hallazgos descartados). Detalle de cada corrección con su test en §9.

| Id | Sev. | Hallazgo | Estado |
| --- | --- | --- | --- |
| funcional REV-01 | medium | `ses` / `verifactu` / `tbai` de `GET /integrations/status` ignoraban el interruptor del establecimiento (`compliance-health.service.ts` fijaba `enabled: true` por código y leía el modo del proceso): `prop_uxday` salía «Pruebas (sin efecto real)» mientras el readiness decía «SES.HOSPEDAJES desactivado para este establecimiento»; en producción una propiedad con SES apagado se habría pintado «Real» | **Corregido**: `complianceGates` + parámetro `gate` de `complianceStatus`; apagado y sin uso → `none` «Desactivado para este establecimiento…» con «Activar … para el establecimiento» al frente de `missingForReal`; activo solo por uso → nota en la frase; `/health` habla del proceso. Runtime `prop_uxday`: `ses` `none`, `verifactu` `sandbox` «Activo solo por uso (24 facturas emitidas)», `tbai` `none` |
| funcional REV-02 | medium | Objetivo (3) parcial: textos de éxito sin motor real seguían servidos (Campañas «Enviadas · ya entregadas», SES «Enviado» / «Aceptado» y `stub://` como «Punto de acceso», manifiesto de `packages/integrations` sin `demo` / `mode` consumido por `apps/mobile`, manual 20 «PSP conectados») | **Corregido**: KPI «registradas como enviadas (sin motor de envío)», «Aceptado (simulador)» / «Simulador local (sin envío a la autoridad)» (`fiscal-shared.ts`), manifiesto `demo:true` / `mode:"sandbox"` / «(demostración)», `apps/mobile` sin «connected», manuales 10 / 20 con la pestaña Pagos real |
| seguridad REV-01 | medium | El bloque `integrations` de `GET /health` (público) ejecutaba Prisma sobre todas las organizaciones en cada petición (`getComplianceHealth()` sin `organizationId`: `property.findMany` global + `resolveSesEstablishment` + 7 `count`) y exponía un dato cruzado de tenants, contra el diseño «sin BD» del servicio, el contrato y el test | **Corregido**: `describeComplianceEnvironment()` (solo entorno; `getTbaiHealth(null)` = modo del proceso + «Aplica solo a los establecimientos con territorio foral; el modo efectivo se lee por propiedad.») sustituye a `getComplianceHealth()` en `server.ts`; `/health` 3 ms; contrato de honestidad +1 caso (el bloque no llama a `getComplianceHealth(`; `describeComplianceEnvironment` no toca Prisma) |
| seguridad REV-02 | medium | `pnpm-lock.yaml` del worktree difiere de HEAD porque el lock de HEAD está desincronizado con los `package.json` (0 menciones a `playwright`, `@fontsource-variable/inter` ni importer `packages/ai-core`): `pnpm install --frozen-lockfile` no puede pasar en HEAD; el lock regenerado sube `@playwright/test` a 1.63.0, que exige `chromium_headless_shell-1243` (la caché tiene 1228) | **No corregido** (fichero prohibido): excluido del commit. Decisión en `main` (orquestador / César): regenerar el lock fijando `@playwright/test` a la 1.61.x instalada, o aceptar 1.63 y `pnpm exec playwright install`; hasta entonces `corepack pnpm --filter @hotelos/admin-web e2e` falla en `browserType.launch` para todas las specs |
| seguridad REV-03 (+ funcional REV-08) | medium | El informe y los dos bloques de estado daban por roja la puerta «contratos raíz» y cifraban la final en 12/14 cuando el árbol ya llevaba la corrección (`seed-ux-day-contract` l.164/188) y la puerta real era 13/14 | **Corregido** por el corrector (13/14, 786 · 784 · 0) y ampliado por el integrador con la puerta completa tras el corrector (15:09: 787 · 785 · 0, api 3.636, admin-web 2.053; §5, §13) |
| funcional REV-03 … REV-07, REV-10 · seguridad REV-05 | low | «Actualizar todo» no recargaba el panel; vacío del catálogo engañoso; badge del estado persistido ausente en Pagos y fixture con `lastSyncAt`; `sage200` sin decir que es por sociedad; `missingForReal` con punto final; `resolveRequestedPropertyId` con `trim`; `CocoaStatusBar` «Conectado» fijo; fixtures del hub sembradas para cualquier propiedad | **Corregidos** (§9) con test cada uno (`integrations-copy` +7, `legacy-hub` +2, `integrations-status` +6, `integrations-status-routes` 3 nuevo) |
| funcional REV-09 | low | La spec e2e `integrations-status.spec.ts` no es ejecutable con el lock heredado (Playwright 1.63 vs navegador 1228) y no entra en `gates.sh` | **No corregido**: depende de seguridad REV-02; se ejecuta a mano con configuración temporal (L8-08: 1/1) |
| seguridad REV-04 | low | `nav-tree --check` rojo por el CSV compartido | **No corregible desde L8** (§5 punto 3; P3): regenerar en la rama fusionada |
| seguridad REV-06 | low | Ficheros sin lote dueño en el árbol (`.discoverability-whitelist.json`, `scripts/env-contract.json`, `tests/seed-ux-day-contract.test.mjs`, `docs/design/cocoa-22-inventory.json`, `COCOA-22-MIGRACION.md`) | **Resuelto en el commit del integrador**: los cinco entran (regenerados / corregidos por lotes de la tanda: whitelist L8-06, censo L8-01, contrato de specs y flaky por la puerta, inventario y §6 por el corrector); `pnpm-lock.yaml` fuera |

## 12. Decisiones para César (opción por defecto aplicada)

Lista única del plan (`decisionsForOwner`; detalle operativo en el runbook §4 D-01…D-18 y en §6 de este informe). La opción por defecto ya está aplicada en el árbol: nada espera a la decisión para funcionar, y el panel / `/health` dicen exactamente en qué modo está cada cosa.

| # | Decisión (lo que solo César puede aportar) | Opción por defecto aplicada | Dónde se ve |
| --- | --- | --- | --- |
| 1 | Canales: cuenta staging de Channex + API key e ids de propiedad / room type / rate plan; `CHANNEL_MAX_MODE=real` solo en producción | Todo en `sandbox` (validación estructural local, `readChannelEnv().maxMode` nacido en `sandbox`); el panel lo dice: «n de n canales activos, todos en simulador local» | `channelsStatus`; fila `channels` del panel; `/health.integrations.channels` «Tope de modo de canales en simulador local» |
| 2 | PSP: Stripe test (`STRIPE_SECRET_KEY` `sk_test_` + `STRIPE_WEBHOOK_SECRET`) y/o Redsys con el banco | Modo `none`: cobros → `409 PSP_NOT_CONFIGURED`; «Ningún PSP configurado…» en panel y Pagos; readiness `payment_provider_connected` bloqueado (`prop_123`, antes pasaba con la pasarela ficticia) | `pspStatus` / `pspStatusFor`; Pagos badge «Sin integración»; `GoLiveChecklist` |
| 3 | WhatsApp Business: Tech Provider o BSP, número por hotel, plantillas *utility*, `WHATSAPP_PHONE_ID` / `PROVIDER_TOKEN` / `APP_SECRET` / `VERIFY_TOKEN` | Salida «SIMULADO» fuera de producción (`sandbox`), `none` en producción; webhook 503 en producción sin secreto, `simulated:true` fuera | `whatsappStatus`; `unsignedWebhookMode`; fila `whatsapp` |
| 4 | Correo transaccional (`EMAIL_PROVIDER` / `KEY` / `FROM` con dominio verificado) y Twilio | Envíos «simulado» (nunca «enviado»): KPI «Enviadas» solo reales, `template-stats.simulated`, badge «SIMULADO» | `emailOutStatus` / `smsStatus`; Comunicaciones › Plantillas y envíos |
| 5 | OAuth de buzones: `GMAIL_CLIENT_ID` / `SECRET` o `MS_CLIENT_ID` / `SECRET` y un buzón por centro | `email_in` `none`: «solo queda el buzón manual (pegar correos a mano)»; `imap` «no disponible» | `emailInStatus`; `GET /integrations/email/providers` |
| 6 | OPERA: Hotel Codes, hora de night audit, listados `cf_*`, XML de ingresos, programador / SFTP; para OHIP Enterprise ID / Chain Code / app key | «Real por ficheros» solo con perfil `pms_shadow` activo + clave de ingest o buzón; sin perfil `none`; OHIP no existe y el panel no lo insinúa | `operaStatus`; Modo sombra OPERA |
| 7 | VeriFactu: `VERIFACTU_SOFTWARE_NAME` / `NIF`, `VERIFACTU_INSTALL_NUMBER` o instalación declarada, certificado, `VERIFACTU_MODE=preproduction` | `sandbox` con «declaración del software incompleta» (3 campos) en `missingForReal` y en `checks.verifactu` de `/health`; por propiedad `none` si el interruptor del establecimiento está apagado y no hay facturas (tras el corrector) | `complianceStatus("verifactu")`; Cumplimiento › VeriFactu |
| 8 | SES.Hospedajes: alta en la sede del MIR, `sesRegistryNumber` por hotel, certificado, `SES_HOSPEDAJES_MODE=preproduction` | `sandbox` (stub local, acuses `stub://` etiquetados «(simulador)»); por propiedad `none` si desactivado y sin envíos en 180 días (`prop_uxday`) | `complianceStatus("ses")`; Cumplimiento › Envíos |
| 9 | Google Business Profile: propietario del perfil de cada hotel y proyecto OAuth `business.manage`; la ruta de autorización (T8-L5) sigue sin existir | `none` sin fuentes; con CSV `real` / `files` y `readyForReal:false` («la API de Google Business Profile no está autorizada») | `gbpStatus`; Reputación |
| 10 | Almacén de documentos: `disk` en el VPS o S3 compatible UE (proveedor, región, bucket, credenciales) | `inline` en la demo → `sandbox` «nada se guarda en S3»; `s3` → `real` «configurado, no comprobado» (sin sonda de red) | `storageStatus`; `dependencies.objectStorage` solo dice el tipo |
| 11 | IA: `AI_PROVIDER=anthropic` + `AI_PROVIDER_API_KEY` + `AI_USD_EUR_RATE`, DPA / ZDR | `none` (reglas): «las funciones de IA responden «no configurado» y no inventan resultados» | `aiStatus`; Configuración › IA |
| 12 | Sentry DSN y decisión sobre Redis (sin consumidor: retirar `REDIS_URL` o mantenerlo opcional) | Sentry `none` «solo quedan en el log del proceso»; `checks.redis` «configured (sin consumidor en este build)», `dependencies.redis` `unconfigured`, `redis` `none` aunque haya `REDIS_URL` | `sentryStatus` / `redisStatus`; `/health` |
| 13 | Sage 200: cadencia mensual por Excel confirmada; ¿hace falta un formato de salida Sage 200 (IME)? | El panel llama «Sage 200 (importación contable)» y «Exportación a gestoría» (compatible ContaPlus / Sage 50), nunca «exportación Sage 200»; A3 → 409 y la pantalla lo dice | `sage200Status` / `gestoriaExportStatus`; Finanzas › Contabilidad |
| 14 | Hub heredado `IntegrationConnection` (proveedores «Demo …»): mantener como catálogo de demostración marcado o retirarlo en una tanda posterior (rutas fijadas por `modular-suite-contract`) | Se mantiene marcado `demo:true` / `mode:"sandbox"`, prueba → «simulado», «Demostración · no cobra» en Pagos, fixtures solo para la propiedad demo | `integrations.service.ts`; Pagos › «Conexiones de demostración (catálogo heredado)» |

## 13. Cierre del integrador (puerta final, contratos de los documentos y commit)

- **Puerta completa tras el corrector** (`NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv bash scripts/gates.sh --json scratchpad/L8/gates-final.json`, 2026-09-20 15:09): **13/14** — typecheck:all 15 PASS · 0 FAIL · 1 SKIP (34,3 s) · api unit 3.636 (3.635 pass · 0 fail · 1 skip) · admin-web unit 2.053 (2.052 · 0 · 1) · ai-core 119/119 · worker 34/34 · contratos raíz 787 (785 · 0 fail · 2 skip) · discoverability 197 URLs · route-access 15 tokens × 197 URLs · cocoa waves §6 al día · rbac:sync dry-run OK (1.565 ms) · migrate status + drift «No difference detected.» · admin-web build 3,24 s · integración 993 (985 pass · 0 fail · 8 skip). Único rojo `nav-tree --check` (`stale`: CSV compartido 304 filas frente a 294 del JSON del worktree, filas de L6b / L7 / ACT; JSON sin diff). Sin cambios de código entre esa puerta y el commit: el integrador solo edita este informe y `docs/audits/ESTADO-VERIFICADO.md`.
- **Contratos que leen estos dos documentos, tras la edición del integrador**: `node --test tests/brand-contract.test.mjs tests/manual-contract.test.mjs tests/integrations-honesty-contract.test.mjs` → 47/47 (brand 10 · manual 15 · honesty 22).
- **Puerta `--quick` del integrador** (`scratchpad/L8-int/gates-quick.json`): **11/12** — typecheck:all 15 PASS · 0 FAIL · 1 SKIP (23,6 s); api unit 3.636 (3.635 pass · 1 skip); admin-web unit 2.053 (2.052 · 1 skip); ai-core 119; worker 34; contratos raíz 787 (785 · 2 skip); discoverability 197 URLs; route-access 15 × 197; cocoa waves §6 al día; rbac:sync dry-run OK; migrate status + drift «No difference detected.»; único rojo `nav-tree --check` (externo, sin cambio en el árbol tras la puerta).
- **Commit** en `tanda-l8` (sin push, sin fusión en `main`): un solo commit con `git add -A hotelos` + `git reset -q hotelos/pnpm-lock.yaml` + `git commit -F <mensaje>` (autor cesareme), 60 ficheros (40 modificados · 1 borrado · 19 nuevos), título «feat(integraciones): estado honesto none|sandbox|real, GET /integrations/status, bloque integrations en /health y panel de la pestaña Integraciones (Tanda L8)», cuerpo por lote, pre-commit (`check-discoverability` + `typecheck-all`) sin `--no-verify`; el sha va en el informe estructurado del integrador al orquestador.
- **Queda para el orquestador / César**: (1) regenerar `nav-tree.generated.json` en la rama fusionada con el CSV compartido y repetir `route-access` / `discoverability` (P3); (2) `pnpm-lock.yaml` de `main` (seguridad REV-02: fijar `@playwright/test` 1.61.x o instalar el navegador 1243) — hasta entonces la spec e2e de L8-08 se ejecuta a mano; (3) una línea en el runbook §6 con las cifras de `gates-final.json` 15:09 (P2); (4) B2-B6 de la auditoría de textos y los comentarios `QuickCheckInDrawer.tsx:45` / `e2e/quick-checkin.spec.ts:18` (P4); (5) decisiones de producto P5 / P6 (`gestoria_export` «real / manual», `preproduction` con `readyForReal:false`) y D-01…D-18 del runbook §4 (§12); (6) `:3000` sigue sirviendo el código anterior a la tanda hasta fusionar y reiniciar (decisión de César).
