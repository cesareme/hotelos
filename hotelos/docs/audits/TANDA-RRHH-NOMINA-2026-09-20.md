# Tanda RRHH · Plantilla, previsión, nómina y panel de costes de dirección — informe de cierre (2026-09-20)

**Encargo:** `docs/design/RRHH-PLANTILLA-NOMINA.md` (expediente cifrado, convenios, contratos con jornada y modalidad, estándares de dotación y motor de previsión por ocupación, plantilla máxima aprobada con SoD, turnos / fichajes / ausencias por ficha, nómina en modo externo con aprobación e incidencias, RBAC v5) + `docs/design/PANEL-COSTES-DIRECCION.md` (costes de personal por hotel y departamento sobre Sage + nómina frente a USALI, como última ola) sobre el worktree `~/anfitorio-demo-wt-rrhh/hotelos` (rama `tanda-rrhh`, base `a069906` = main con L3, L5, L6a, TL, OPERA, T8, UX-1, FIX-1, T9 y CHK; BD propia `hotelos_rrhh`, copia de la BD viva tras CHK con la carga real de Sage —640/641/642 ene-jul 2026 sin centro de coste y el lote de agosto por centro × USALI—; puertos `:3923/:5193`, slots B/C/D `:3933/:5203`, `:3943/:5213`, `:3953/:5223`). Plan: `scratchpad/RRHH/recon-delta.md` (§0 estado, §1 deltas fichero:línea, §2 hechos, §3 especificación por lote) y `scratchpad/RRHH/plan-olas.md` (12 lotes en 6 olas, ficheros exclusivos por lote). Brief de César: nómina sin integración externa (importación de fichero como hoy), datos personales nunca en repo / tests / informes (expedientes ficticios en el tenant de prueba), coste por departamento desde el reparto del lote (agosto) y 640/642 sin departamento en ene-jul («sin desglose»).

**Working tree antes del commit del integrador** (`git -C ~/anfitorio-demo-wt-rrhh status --porcelain -uall`): 139 entradas = 79 `M` + 60 `??`; sin `hotelos/pnpm-lock.yaml`: **78 modificados (+2.971 / −364)** y **60 nuevos (21.696 líneas)**. `pnpm-lock.yaml` aparece `M` (+64 / −25) desde ANTES de la tanda (mtime 08:53, anterior al recon; los `package.json` no cambian frente a la base: es la regeneración de `pnpm install` sobre un lock desfasado, último commit del lock 2026-05-31) y ningún lote lo tocó: **fuera del commit** (`git reset -q hotelos/pnpm-lock.yaml` antes de commitear, como hace `scripts/merge-lane.sh`). El integrador (este informe) solo ha escrito este fichero y el bloque de `docs/audits/ESTADO-VERIFICADO.md`, ha ejecutado el guion del pre-commit (`hotelos/.husky/pre-commit`: discoverability + `typecheck-all`) y ha commiteado en la rama; ni API ni Vite propios; `:3923` / `:5193` sin escuchas; `:3000`, `:5173` y el PID 4986 intactos.

**Datos:** toda escritura fue sobre el tenant aislado `org_hr` / `le_hr` / `prop_hr` (`packages/database/prisma/seed-hr.ts`, `corepack pnpm --filter @hotelos/database db:seed:hr -- --reset`, 12 usuarios `*@hr.test` con contraseña `hr-demo` / `SEED_HR_PASSWORD`, 24 expedientes ficticios con NIE sintético `Z9999nnn` cifrado) o sobre tenants `org_l2_*` que las suites de integración crean y destruyen (0 residuales tras cada lote); Faranda solo lectura (recuentos idénticos antes / después en cada lote: `scratchpad/RRHH/rrhh1-counts-*`, `rrhh7-counts-*`, invariantes de las suites). Ningún nombre de persona real en código, tests, JSON, capturas ni en este informe; las cifras del piloto se citan solo por código de centro (AS, LT, MC, PG, RA, FN, LL, OC).

## 0. Resumen

- **Entregado:** módulo `apps/api/src/modules/hr/*` (12 ficheros: expedientes con PII cifrada, convenios y reglas versionadas, motor de previsión puro + drivers + estándares por estrellas, plantilla máxima con SoD, KPIs y alertas, motor de reglas ET/convenio, 22 rutas en su manifiesto), nómina ampliada en `modules/payroll/*` (aprobación con SoD, incidencias del mes JSON/CSV, tipos 2026 con temporales y jornada parcial, bloqueo de modo, panel de costes de personal con lector SQL propio), migración aditiva `20260920173000_rrhh_plantilla_nomina` (6 tablas, 21 columnas, 1 CHECK, 296 → 302 modelos), 5 claves RBAC `hr.*` (catálogo 254 → 259, plantillas v4 → v5, +1 pareja SoD estática), 4 pantallas Cocoa nuevas (Plantilla, Previsión de plantilla, Panel RRHH como pestañas de «RRHH y nóminas»; Costes de personal como pestaña de Mi día) + `EmployeeDrawer` + contenedor `NominasTabs`, selector de ficha en Personal y turnos, seed `org_hr`, runbook `docs/runbooks/rrhh-plantilla-nomina.md`, manual `docs/manual/30-rrhh.md` (386 → 512 l.) y `10-direccion.md` §11, `docs/api-contracts.md` (3 viñetas), runbook finanzas §20, apéndice del diseño.
- **Revisión:** 18 hallazgos confirmados (5 high · 13 medium) + 14 low, **0 refutados**; el corrector cerró en código 16 de los 18 y 12 de los 14 low; los 4 restantes son decisiones de integración (nav-tree con el CSV compartido, `pnpm-lock.yaml`) o no exigen código (§4).
- **Puertas finales** (2026-09-20 16:52, `scratchpad/RRHH/gates-final.json`): **13/14** en verde — typecheck 15 PASS · api unit 3.730 (3.729 pass · 0 fail · 1 skip) · admin-web unit 2.119 (2.118 · 0 · 1) · ai-core 119 · worker 34 · contratos raíz 800 (798 · 0 · 2) · discoverability 201 URL · route-access 15 × 201 · cocoa waves · rbac dry-run · migrate 25/25 + drift «No difference detected.» · build · integración 1.029 (1.021 pass · 0 fail · 8 skip). La única roja, `nav-tree --check`, tiene causa externa: 6 filas «nueva (Tanda ACT)» (`RealEstateAssetScreen` + 5 pestañas `/finanzas/activo-inmobiliario/*`) en el CSV compartido sin componente en esta rama; contra el CSV sin esas filas la puerta es «up to date» (70 · 108 · 205) y **14/14** (§3.1).
- **Fusión:** `merge-lane.sh` (censo, Cocoa, whitelist, lock; `ESTADO-VERIFICADO.md` concatena); regenerar `nav-tree.generated.json` con el CSV consolidado tras fusionar ACT y subir los pins (§5); `rbac:sync` real en la BD viva (roles «behind v5»); fila `WorkforceDashboard` de `pilots/screens-inventory.csv`. Detalle en §7.

## 1. Qué construyó cada lote (qué · cómo · tests · verificación)

Seis olas con ficheros exclusivos por lote (Ola 0 cimientos · Ola 1 servicios y demo · Ola 2 rutas · Ola 3 front · Ola 4 navegación, docs y API del panel · Ola 5 panel de costes). Las cifras son las de cada informe de lote y de `scratchpad/RRHH/*`; las puertas por ola en §3. Comando de tests: API `cd apps/api && node --env-file-if-exists=../../.env --import tsx --test <f>`; admin-web `node --import ../api/node_modules/tsx/dist/loader.mjs --test <f>`; raíz `node --test tests/<f>.test.mjs`.

### Ola 0 · RRHH-1 · Esquema, migración, cifrado, claves y tipos
- **Qué:** modelos `Employee` (PII cifrada `taxId` / `socialSecurityNumber` / `email` / `phone` / `iban` + `taxIdLookupHash`; únicos `[legalEntityId, employeeNumber]` y `[legalEntityId, taxIdLookupHash]`), `CollectiveAgreement`, `AgreementRule` (versionada por `[agreementId, key, validFrom]`), `LaborStandard` (`[propertyId, usaliDepartment, driver, validFrom]`), `StaffingPlan` (+`createdBy` para la SoD; `[propertyId, year, season]`), `StaffingPlanLine` (`schema.prisma:1757-1893`); ampliaciones nulables / con default en `StaffProfile` (`employeeId`, `usaliDepartment`, `jobTitle`), `EmploymentContract` (`agreementId`, `weeklyHours`, `partTimePct`, `fixedDiscontinuous`, `contributionGroup`, `endReason`), `AbsenceRequest` (`requestedBy`, `decidedAt`, `reason`), `LaborForecast` (`usaliDepartment` default `all`, `source`, `driversJson`, `requiredFte`, `estimatedCost`, `generatedAt`, único `[propertyId, forecastDate, usaliDepartment]`), `PayrollPeriod` (`mode` default `external`, `closedAt`), `Property.agreementId`; `PII_FIELDS.Employee` + `LOOKUP_HASH_FIELDS.Employee` (`crypto-fields.ts:179/:254`); 5 claves `hr.employee.read` / `hr.employee.manage` / `hr.config.manage` / `hr.standards.manage` / `hr.staffing.approve` (`permissions.ts:160-164`), plantillas `payroll_hr` 13 → 18, `general_manager` +2, `manager` / `operations_director` / `owner` +1, `ROLE_TEMPLATE_VERSION` 4 → 5; `packages/shared/src/hr-types.ts` (1.027 l.: vocabularios, DTOs sin PII, `HR_AGREEMENT_DEFAULTS` ES-15/33/39-HOST y ES-28-HOSP, `HR_STANDARD_DEFAULTS` por banda 2/3/4★, `PAYROLL_RATES_2026`, `HR_ERROR_CODES` ×20 + mensajes ES, `HR_AUDIT_ACTIONS`); `module-manifest.ts` `workforce_labor` + 5 claves.
- **Cómo:** migración `20260920173000_rrhh_plantilla_nomina` (217 l.) = SQL de `prisma migrate diff --from-schema-datasource --to-schema-datamodel --script` verbatim bajo la cabecera de la casa + `CHECK absence_requests_requested_ne_approved` a mano; 6 `CREATE TABLE`, 8 índices únicos, 2 índices, 21 `ADD COLUMN` nulables o con default, 0 DROP / TRIGGER / FUNCTION / FK (convención plana sin `@relation`, como `PayrollPeriod`); el nombre del diseño `20260918100000_…` colisionaba con `20260918100000_rbac_departamentos`.
- **Tests:** `tests/hr-schema-contract.test.mjs` 24/24 (nuevo), `rbac-sod-contract` 21/21 (254 → 259, v5, describe nuevo «nadie reúne payroll.manage y hr.staffing.approve»), `crypto.test.mts` 15/15 (+4 Employee), `migrations-squash-contract` 8/8, `rbac-nav-contract` 13 + 1 skip, `api-route-permissions-contract` 32/32; typecheck 5 workspaces 0 errores.
- **Verificación:** `db:migrate:deploy` 24 → 25 · `db:drift:check` «No difference detected.» · `check-migrations-vs-schema` 302 tablas / 46 enums · `rbac:sync -- --dry-run` +5 creadas · 0 stale · 69 roles behind v5 (sin escritura) · psql: `\d employees` 23 columnas, CHECK presente, triggers públicos 4 (sin cambio) · recuentos Faranda idénticos (`scratchpad/RRHH/rrhh1-counts-{before,after}.txt`).

### Ola 1 · RRHH-2 · Expedientes, convenios, contratos y tipos 2026
- **Qué:** `modules/hr/employees.service.ts` (854 l.: `listEmployees` con select sin columnas PII, `getEmployee` con `pii: true` que descifra solo `piiFields` del ámbito —NIF / NAF / IBAN con `hr.employee.manage`, correo / teléfono con `hr.employee.read`— y audita `HR_PII_READ` sin valores, `createEmployee` con validación NIF / NIE módulo 23 y duplicado por `taxIdLookupHash` (409), `patchEmployee`, `terminateEmployee` en cascada: contratos, fichas, RBAC), `agreements.service.ts` (606 l.: catálogo, reglas vigentes por `asOf`, `assignAgreementToProperty`, `resolveAgreementForProperty` contrato > centro, `seedAgreementCatalog` idempotente desde `HR_AGREEMENT_DEFAULTS`), `hr-errors.ts`; `contracts.service.ts` con `agreementId` / `weeklyHours` / `partTimePct` / `fixedDiscontinuous` / `contributionGroup` y `payCount` = 12 + `extra_pay_count` del convenio (14 sin convenio); `periods.service.ts` con `PAYROLL_RATES_2026` (6,50 / 32,15 %).
- **Tests:** `hr/__tests__/employees.test.mts` 11 · `agreements.test.mts` 10 · `payroll-calc.test.mts` 8 (re-pin 1.800 € → 270,00 / 117,00 / 578,70 / 1.413,00) · `tests/integration/hr-employees.test.mts` 6/6 (tenant aislado, SELECT crudo: envelope `v1.<iv>.<ct>.<tag>` y HMAC de 64 hex) · `treasury-banking.test.mts` 12/12 (re-pin 297,00 / 130,00 / 643,00 / 1.573,00 manteniendo el neto del fixture CSB43).
- **Verificación:** por servicio + SQL en `hotelos_rrhh`; boot smoke de la instancia `:3923` (`/health` 200); `employees` / `collective_agreements` / `employment_contracts` de Faranda 0/0/0 antes y después.

### Ola 1 · RRHH-3 · Motor de previsión, drivers, estándares, plantilla máxima, KPIs y alertas
- **Qué:** `labor-forecast.engine.ts` (puro: `minutes_per_unit`, `units_per_shift`, `fte_per_100`, `posts_by_band` con noche ≥ 1; FTE = horas / 8 × `coverageFactor` o puesto 24/7 = horas × 365 / jornada anual; `monthlyFte`; `estimateDailyCost`; `driver_missing:*` → null, nunca 0; `evaluateLaborAlerts` understaffed / overstaffed / over_approved / forecast_degraded), `drivers.service.ts` (cierres reales, OTB, previsión `pms_import`, LOS de 28 días, cubiertos por régimen), `standards.service.ts` (versionado por `validFrom`, `reset-defaults` por estrellas, 400 `HR_STANDARD_INVALID`), `staffing.service.ts` (upsert de borrador, `approve` con `hr.staffing.approve` + 409 `APPROVAL_SELF_DECISION`, `checkStaffingHeadroom` → aviso `HR_STAFFING_EXCEEDED`, nunca bloqueo), `labor-forecast.service.ts` (único escritor de `labor_forecasts`: ventana ≤ 92 días, upsert por la clave única, `deleteMany` de departamentos sin estándar, jornada anual del convenio del centro o 1.792 + degraded), `kpis.service.ts` (`getHrKpis` con `degraded[]`, `listHrAlerts`).
- **Tests:** `labor-forecast-engine` 23 (LT agosto pisos 29,5 h → 5,2 FTE; recepción 2-2-1 → 8,1) · `drivers` 15 · `staffing` 10 · `kpis` 9 · `dashboards/__tests__/workforce` 3 · `tests/integration/hr-labor-forecast.test.mts` 13/13 (28 snapshots + 4 reservas + 2 forecasts sintéticos: 36 filas, regenerar → mismos ids, SoD del plan, headroom, KPIs y alertas).
- **Verificación:** en proceso con los servicios reales sobre un tenant propio (`scratchpad/RRHH/rrhh3/runtime-check.mts`, salida `.out`): 6 días · 24 filas · 8 degradadas · 0 filas `all`; Faranda `labor_forecasts` 0 antes y después. Decisión de esquema: la clave única de `labor_standards` no lleva `unit`, así que sala + cocina sobre `covers_breakfast` se funden en 13,067 min (9 estándares por centro en 4★, no 10; deuda en CLAUDE.md 19a).

### Ola 1 · RRHH-4 · Turnos, fichajes y ausencias por ficha, SoD y motor de reglas
- **Qué:** `modules/hr/rules.engine.ts` (323 l., puro: `rest_between_shifts`, `max_daily_hours`, `weekly_rest` por semana ISO, `overtime_annual` con fracción de jornada; `headcountThreshold` RD 901/2020), `advanced-record-store.ts` (`resolveStaff` por `staffProfileId` o alias `employeeCode` / nombre completo → ficha, si no 400 `HR_EMPLOYEE_REQUIRED`; `shiftWarnings` sobre el convenio del contrato o del centro; `createTimeClock` con `metadataJson={action}`; `createAbsence` persiste `requestedBy` y `reason`; `transitionAbsence` con 409 `APPROVAL_SELF_DECISION` para approved / rejected y `decidedAt`; lista `workforce_labor:absence_requests` con filtro `status`), `advanced-record-schemas.ts` (`ABSENCE_TYPES` tasados, `AbsenceListFilterSchema`).
- **Tests:** `rules-engine.test.mts` 13/13 · `advanced-record-schemas.test.mts` 11/11 · integración `hr-absences-sod.test.mts` 5/5 (nuevo) · `l2-motor-generico` 12/12 · `l2-rutas-api` 7/7 (fichas del tenant aislado, sin nombres libres).
- **Verificación:** instancia slot C `:3943` con preload `scratchpad/RRHH/rrhh4/preload-hydrate.mts` (solo `hydrateTenantMirrors`, necesario con `TENANT_BOOTSTRAP_SKIP=true`): turno por alias 200 con `warnings` [rest 10/12, daily 10/9], alias inexistente 400, solicitud → PATCH por el solicitante 409 → por otro manager 200 `approved` + `decidedAt`; SQL con `requested_by ≠ approved_by`; tenant limpiado; invariantes Faranda idénticas.

### Ola 1 · RRHH-7 · Seed `org_hr`
- **Qué:** `packages/database/prisma/seed-hr.ts` (1.190 l., autónomo; `db:seed:hr` en `packages/database/package.json`; `--reset` / `--dry-run`; `demo-guard.ts` con `org_hr` / `prop_hr`): tenant `org_hr` / `le_hr` / `prop_hr` «Hotel HR (prueba)» 4★ con 60 habitaciones, 5 departamentos, 4 centros de coste USALI, 12 usuarios `*@hr.test` con rol de plantilla (`general_manager`, `payroll_hr`, `housekeeping_manager`, `housekeeper` ×3, `receptionist` ×3, `fnb` ×2, `maintenance`), 24 expedientes ficticios (12 con usuario y ficha; NIE sintético con letra válida, NAF y IBAN con control; cifrado por la extensión), convenio ES-15-HOST con 21 reglas, plantilla máxima alta / baja aprobada, 357 turnos + 603 fichajes en hora de pared Europe/Madrid, 3 ausencias (una aprobada con solicitante ≠ decisor), periodo 2026-09 abierto en modo externo, 90 cierres demo + 30 previsiones; tras el corrector (SEC-08) las previsiones y estándares los escriben `resetLaborStandardDefaults` + `generateLaborForecast` (previsiones `pms_import:seed-hr-demo` + 58 reservas demo con régimen → 112 filas, 0 días degradados).
- **Tests:** `tests/seed-hr-contract.test.mjs` 10/10 (allowlist, sin apellidos reales, correos solo `@hr.test`, sin `Math.random`, único escritor tras el corrector) · `demo-seed-contract` 9/9.
- **Verificación:** `db:seed:hr -- --dry-run` sin escribir; `--reset` ×2 idempotente (borra y recrea los mismos recuentos) y `ensure`; SQL: `tax_id` = envelope de 79 caracteres con `tax_id_lookup_hash` en 24/24 y 0 filas con NIF / correo / teléfono / IBAN en claro; Faranda idéntica (30 claves, `rrhh7-counts-*`), solo cambian `permissions` 254 → 259 (catálogo global, roles de Faranda intactos) y `organizations` 3 → 4.

### Ola 2 · RRHH-6 · Rutas `/hr/*`, incidencias y redacción
- **Qué:** `schemas/hr.schemas.ts` (zod `.strict()` en español, `parseOr400` «clave no admitida: '<clave>'», ninguna consulta admite PII), `modules/hr/hr.routes.ts` (`registerHrRoutes`: expediente list / get / post / patch / terminate · convenios get / post + reglas get / put · estándares get / put / reset-defaults · planes get / post / approve · previsión generate + get · kpis · alerts · ausencias get + decide; `redactHrPii` / `redactHrPiiInUrl` / `maskHealthAbsence`), `modules/hr/route-permissions.partial.ts` (clave EFECTIVA por ruta, riesgo medium / high / critical), `modules/payroll/payroll.routes.ts` + `incidences.service.ts` (`GET /payroll/incidences?period&propertyId&format=json|csv`: altas, bajas, cambios de contrato y ausencias aprobadas sin NIF; CSV `;` con BOM; auditoría solo con recuentos), `server.ts` (registro tras `registerStaffProfileRoutes`, `POST /payroll/contracts` pasa los campos nuevos, Sentry `beforeSend` y serializador de `req` redactan PII), `lib/tenancy.ts` (resolvers `employee`, `collectiveAgreement`, `staffingPlan` con 404 opaco idéntico al del servicio), `docs/api-contracts.md` viñeta «RRHH · plantilla y previsión».
- **Tests:** `hr-schemas.test.mts` 16 · `incidences.test.mts` 11 · `route-read-keys.test.mts` 9 → 12 · `tests/integration/hr-routes.test.mts` 11/11 (dos organizaciones aisladas, STRICT_ENV: 401 / 403 / 400 / 404 opaco cruzado / PII ausente / SoD / generate → GET / CSV / mode) · contratos raíz 800 (798 · 2 skip) · manifiesto +22 entradas · typecheck:all 15 PASS.
- **Verificación:** instancia `:3923` (`connection_limit=4` porque Postgres estaba a 90/100 conexiones) con sesiones reales de `org_hr` (`scratchpad/RRHH/rrhh6-curl.sh`): `rrhh@hr.test` descifra 5 campos con `?pii=1`, `direccion@hr.test` solo correo / teléfono, `camarera1@hr.test` 403, id inexistente 404 `HR_EMPLOYEE_NOT_FOUND`, `salario` en el cuerpo → 400, generate 200 (28 filas), KPIs 200, ausencia de salud enmascarada para quien no tiene `hr.employee.read`, incidencias JSON y CSV (`incidencias-2026-09-hr.csv`, BOM, sin NIF), dirección 403 en incidencias (`workforce.payroll_export`). Puerta quick 11/12 (la roja: `api-reference.test` por 17 rutas sin etiqueta española, cerrada después en `api-reference.service.ts`).

### Ola 3 · RRHH-8 · Plantilla (`HrEmployeesScreen` + `EmployeeDrawer`)
- **Qué:** `screens/hr/HrEmployeesScreen.tsx` (CocoaPage alojada «Plantilla», `FinanceScopeSelector`, segmentos Activos · Bajas · Fijos discontinuos —+ Excedencias tras RF-08—, búsqueda por nombre / número nunca por NIF, `CocoaTable` con vencimiento de contrato, estados vacío / error / sin permiso; sin la clave no se llama al API), `EmployeeDrawer.tsx` (Datos · Contrato · Baja; PII nunca precargada: «Mostrar» descifra una vez y se descarta al cerrar; «Corregir datos cifrados» con inputs vacíos; alta de contrato con convenio del centro que rellena pagas y jornada; baja con `CocoaDialog` destructivo), `employee-form.ts` (validaciones espejo de `hr.schemas.ts`), `services/hrApi.ts` + `hr-contracts.ts` (mensajes ES por `HR_ERROR_MESSAGES_ES`, etiquetas, `payCountFromRules` espejo); 0 `style={` nuevos.
- **Tests:** `screens/hr/__tests__/employee-form.test.mts` + `hr-employees-contract.test.mts` 31/31 · `admin-web-spanish-copy-contract` 6/6 · `admin-web-no-raw-fetch` 4/4.
- **Verificación:** navegador (arnés temporal sobre Vite `:5193` + API `:3923`, sesión inyectada): alta → `POST /hr/employees` 201 «Expediente 0025 creado.» → detalle enmascarado → «Mostrar» → 5 campos descifrados en una petición → baja → 200 y la fila pasa a Bajas; SQL: `status inactive`, `tax_id` cifrado, `HR_EMPLOYEE_CREATED` / `HR_PII_READ` / `HR_EMPLOYEE_TERMINATED`; `jefe.pisos@hr.test` (sin `hr.employee.read`): «Sin acceso a la plantilla» y NINGÚN GET emitido.

### Ola 3 · RRHH-9 · Previsión de plantilla y Panel RRHH
- **Qué:** `HrForecastScreen.tsx` (centro + rango 14 / 28 + `CocoaDatePicker`; `CocoaSplitView` con necesidad por día, `DegradedValue` y badge «Incompleta»; drivers del día; tabla necesario / horas / planificado / disponible / máximo aprobado / cobertura; `CocoaChart.Bars`; estándares editables con versión nueva y «Restablecer valores del sector»; plantilla máxima con borrador en `CocoaDrawer` y «Aprobar» solo con `hr.staffing.approve` y solo en borradores; «Generar previsión» con `workforce.schedule.manage`), `HrOverviewScreen.tsx` (`CocoaKpiStrip` de 6 con `degraded` por código, barras 14 días, alertas, vencimientos con nombre solo bajo `hr.employee.read`), `hr-forecast-helpers.ts` (puro; `DEGRADED_REASON_LABELS_ES`; `approvedPlanFor` tras RF-13; `laborCostCaption` tras RF-09), `services/hrForecastApi.ts`.
- **Tests:** `hr-forecast-helpers` 22 · `hr-forecast-contract` 11 · `hr-overview-contract` 6 = 39/39.
- **Verificación:** slot B (`:3933` / `:5203`): como `rrhh@hr.test` generar → «56 filas · 14 días incompletos» (los snapshots `demo` del seed no contaban para `drivers.service`: SEC-08 lo alinea), reset-defaults → 9 estándares, edición + PUT → 2 versiones en BD, «Nuevo borrador» → 201, sin «Aprobar»; como `direccion@hr.test` generar y estándares deshabilitados con nota, «Aprobar» solo en el borrador → 200 (`created_by` rrhh · `approved_by` dirección; auditoría `HR_STAFFING_PLAN_APPROVED`); Panel RRHH 12 personas · 11,63 FTE de 27 · 23 alertas · 2 vencimientos.

### Ola 3 · RRHH-10 · Nómina ampliada y selector de ficha
- **Qué:** `PayrollScreen.tsx` (callout de modo del periodo, badges Aprobado / Exportado / Externo, «Aprobar» solo con `payroll.approve` y diálogo con nota, «Pagar» hasta aprobado, «Incidencias del mes» con mes + centro y descarga CSV, comandos ⌘K; presupuesto `style={` 9 = 9), `services/payrollApi.ts` (`approvePayrollPeriod`, `listPayrollIncidences` / `downloadPayrollIncidences`, `payrollErrorMessage` con las reglas SoD), `workforceApi.ts` (`{ staffProfileId }` en `clockIn` / `clockOut` / `createShift`, `recordWarnings`, `workforceErrorMessage`), `WorkforceDashboard.tsx` (CocoaSelect de fichas en fichaje y turno; toast con los avisos del motor de reglas; 0 inputs de texto libre).
- **Tests:** `payroll-cost-screen-contract` 20 → 26 · `workforce-staff-select` 6/6 · pines externos 80/80.
- **Verificación:** slot C (`:3943` / `:5213`): `rrhh@hr.test` → Calcular 2026-09 (bruto 21.600,00 · IRPF 2.893,00 · SS 8.348,43 · neto 17.303,00) → Incidencias con ámbito de sociedad 404 `ENTITY_SCOPE_REQUIRED` → selector de centro añadido → CSV descargado; `direccion@hr.test` → Aprobar → `PAYROLL_PERIOD_APPROVED` con `sod.rule calculator_ne_approver`; Exportar A3 (12 recibos); Personal y turnos: turno con HR-004 creado (`ShiftCreated`, `warnings []`), fichar como `rrhh` 403 `workforce.timeclock.use`, como `jefe.pisos` «Entrada registrada para HR-005».

### Ola 4 · RRHH-11 · Navegación, docs y CLAUDE.md
- **Qué:** `screens/tabs/finanzas/NominasTabs.tsx` (contenedor `NavItemTabs` con loaders PayrollScreen / HrEmployeesScreen / HrForecastScreen / HrOverviewScreen), `App.tsx` (`PayrollScreen: NominasTabs` + 3 claves), `financeScope.ts` (+3 `entity_default`), `guideContent.ts` (narración de «RRHH y nóminas», 384 / 400), `.discoverability-whitelist.json` (+`EmployeeDrawer`); pilots (fuera del worktree, copias previas en `scratchpad/RRHH/rrhh11-*.bak`): `tanda5-nav-tree.csv` fila 63 → «RRHH y nóminas» (pestaña base «Nóminas») + 3 filas `merge-into` (`/finanzas/nominas/plantilla`, `/prevision`, `/panel`; roles `finanzas|direccion|admin|rrhh|auditoria`), `tanda5-nav-tree.md` §1 y §3, `screens-inventory.csv` +3 filas con `api_paths_principales`; `nav-tree.generated.json` regenerado (70 · 107 · 200); `docs/runbooks/rrhh-plantilla-nomina.md` (nuevo, 12 secciones), `docs/manual/30-rrhh.md` 386 → 512 (tipos 2026, Aprobar, Incidencias, §8 Plantilla, §9 Previsión, §10 Panel RRHH), apéndice A.1-A.3 del diseño, CLAUDE.md deuda 19 + docs prioritarios + `db:seed:hr`.
- **Tests:** `check-discoverability` verde (250 pantallas · 200/200 URL · 0 broken) · `check-route-access` 15 × 200 · `manual-contract` 15/15 · nav-tree / route-maps / sidebar / visibility 58 pass · 1 skip; `rbac-nav-contract` con el inventario compartido 19/21 (28 pares clasificados después por el corrector: SEC-03).
- **Verificación:** slot A: `rrhh@hr.test` ve «RRHH y nóminas» con 4 pestañas que renderizan (Nóminas · Plantilla «23 expedientes» · Previsión · Panel RRHH); `direccion@hr.test` igual; `camarera1@hr.test` sin el ítem y «Sin acceso» en `/finanzas/nominas`.

### Ola 4 · PANEL-A · API del panel de costes de personal
- **Qué:** `GET /payroll/labor-cost-panel?from&to[&propertyId]` (`payroll.read` · medium; `modules/payroll/payroll.routes.ts`, entrada en el partial de payroll), `labor-cost-panel.source.ts` (lector SQL propio del diario: 64x + grupo 7 por (centro, mes, cuenta, cc, `source_type`) con la regla de estados de `financial-statements/source.ts` y sin `payroll_cost_import`; lotes `posted` por `usali_department`; RN reales con `realizeDays` sin snapshots `demo`), `labor-cost-panel.service.ts` (agregador puro: diario → departamento por cc USALI o `sin_desglose`; sin diario → lote; ambos → diario + `overlap`; ninguno → `source: null`; ventas 70x con `salesSource` ledger / reference; ingreso por departamento con `computeUsaliPnl` bajo `accounting.read`; CPOR y coste por empleado solo con denominador; `deltaPrevious`; ranking solo en sociedad; `degraded[]` por (centro, código) `LABOR_PANEL_*`), `docs/api-contracts.md` viñeta «Panel de costes de personal».
- **Tests:** `labor-cost-panel.test.mts` 18/18 · `tests/integration/labor-cost-panel-routes.test.mts` 6/6 (401 / 403 / 400, sociedad y centro, 404 opaco cruzado, `ENTITY_SCOPE_REQUIRED`, `payroll.read` sin `accounting.read` → `revenue` null + `LABOR_PANEL_USALI_FORBIDDEN`).
- **Verificación:** slot B `:3933`; `org_hr` sociedad 2026-07..09 → 200 en 29 ms (2026-09 28.544,43 por el diario, 4 departamentos; degraded COST_MISSING / SALES_MISSING / RN_MISSING / HEADCOUNT_MISSING); Faranda SOLO lectura como dirección general 2026-01..08 → 200 en 0,87 s: 8 centros, 13.502 asientos; LT 2026-05 44.798,47 `sin_desglose` (640 34.657,64 + 641 −551,94 + 642 10.692,77; cotejado con SQL previo) 33,57 % s/ ventas, RN 1.660 CPOR 26,99; LT 2026-08 `import` 44.376,01 = rooms 20.493,18 / fnb 20.193,65 / admin 3.689,18, referencia 22 empleados · 172.338,00 → 25,75 %; AS / MC / PG / RA agosto por lote; OC sin 70x; FN / LL sin dato; totales sociedad 2.460.758,13 / 4.268.464,90 / 38,73 % / RN 12.630 / Δ +12,71 %; manager de LT: centro 200, sociedad 404 `ENTITY_SCOPE_REQUIRED`, otro centro 404 idéntico.

### Ola 5 · PANEL-B · Pestaña «Costes de personal» de Mi día
- **Qué:** `screens/costs/DirectorLaborCostsScreen.tsx` (364 l., 0 `style={`; `useFinanceScope` entity_default con oficina central; mes + «Mes / Acumulado del año»; `CocoaKpiStrip` de 4 —coste con Δ, % s/ ventas, coste por empleado, CPOR con aviso de cobertura de RN—; callouts «Sin desglose por departamento», «Diario y lote en el mismo mes», «Datos incompletos (n avisos)»; barras por departamento; ranking o «Meses del centro»; tabla centro × departamento con pie Total; sección Fuentes; el `0.00` con `source: null` NUNCA se pinta), `labor-costs-helpers.ts` (627 l., puro), `services/laborCostPanelApi.ts`; cableado `MiDiaTabs.tsx` + `App.tsx` + `financeScope.ts` + `guideContent.ts` (391 / 400); pilots +1 fila `merge-into` `FrontDeskDashboard` «Costes de personal» `/hoy/costes-personal` (roles `direccion|finanzas|admin|propiedad|auditoria`) + `screens-inventory.csv`; `nav-tree.generated.json` 70 · 108 · 201; `nav-tree.test.mts` pins 108 / 201; `docs/runbooks/finanzas-contabilidad.md` §20; `docs/manual/10-direccion.md` §11.
- **Tests:** `labor-costs-helpers` 11 · `director-labor-costs-contract` 7 · `nav-tree.test.mts` 33/33 · `guideContent` 13/13 · route-access + tabs-b 44/44 · manual-contract 15/15 · brand 10/10.
- **Verificación:** slot A: `direccion@hr.test` ve la pestaña en Mi día con 28.544,43 € «diario contable (64x) · sept 2026» y tres «—»; `camarera1@hr.test` «Sin acceso» y sin pestaña; dirección del piloto ago 2026 + «Acumulado del año»: tabla con FN / LL en «—» («sin datos 8»), el resto «diario 7 · lote 1» con badge Sin desglose, pie 2.460.758,13 €, ranking de 8, Δ ▲12,7 %; LT ago 2026: 44.376,01 € · 25,8 % (ref.) · CPOR 25,68 € · plantilla 22; captura a escala 0,5 sin desbordes.

### Puerta completa (2026-09-20 14:5x-15:08) y corrección de la propia puerta
- 1.ª ejecución (`scratchpad/RRHH/gates-full.json` / `gates-full.log`): 12/14 — `nav-tree --check` (CSV compartido con las 6 filas ACT) e `integración` 1.024 (1.012 pass · 4 fail): `tests/integration/structure-l4.test.mts` (C6 nómina oficina, exportación empleador, R1 tesorería por centro, retenciones fila 02) seguía con los tipos 6,35 / 30,5. Corrección (12 líneas, ninguna aserción retirada ni relajada): SS trabajador 127 → 130, SS empresa 610 → 643, neto 1.573 → 1.570, 476 737 → 773, nóminas 3.146 → 3.140, pasivos fiscales 1.037 → 1.073 y 2.074 → 2.146.
- 2.ª ejecución (`gates-full-2.log`): 14/14 con `NAV_TREE_CSV=scratchpad/RRHH/full-nav-tree.filtered.csv` (CSV compartido sin las filas ACT): integración 1.024 (1.016 pass · 0 fail · 8 skip).

### Revisores (2026-09-20 15:1x-15:36)
- **Funcional-runtime** (`scratchpad/RRHH/revfunc-{1,2,3}.{sh,out}`, instancia `:3923`, tenant `org_hr`): RF-01…RF-10 confirmados con runtime y SQL; RF-11…RF-17 low.
- **Seguridad-datos-regresiones** (`scratchpad/RRHH/rev-sec/*`: puerta quick propia 11/12, `rbac-nav-contract` con el inventario compartido 19/21, volcado de plantillas y `SOD_STATIC_PAIRS`): SEC-01…SEC-08 confirmados; SEC-09…SEC-15 low.
- Dedupe + refutación: **0 refutados** (§4).

### Corrector (2026-09-20 15:4x-16:44)
- 74 ficheros tocados (lista en el informe del corrector, todos dentro de los lotes salvo `docs/design/COCOA-22-MIGRACION.md` + `cocoa-22-inventory.json` regenerados con `node scripts/cocoa-22-inventory.mjs` y `cocoa-22-waves.mjs --write`, y `finance-contracts.ts` con la frase de `PAYROLL_MODE_CONFLICT`). Detalle por hallazgo en §4. Puertas del corrector: quick 9/12 (antes de dos ajustes: frase ES de `PAYROLL_MODE_CONFLICT` en `finance-contracts.ts`, inventario Cocoa) → completa 12/14 (integración 1 fallo no reproducible: rerun serial 0 fallos, `corrector/integration.log`; cocoa waves desactualizada tras regenerar el inventario) → completa 13/14 (`corrector/gates-full-2.json`). BD: solo `org_hr` (`db:seed:hr --reset` y `ensure`) y tenants aislados; enlace temporal `../pilots` creado y retirado para ejecutar `rbac-nav-contract` con el inventario (21/21).

### Otros ficheros tocados fuera de los lotes (pines y etiquetas; 12 ficheros, +105 / −22)
`tests/documentos-contract.test.mjs` (catálogo 254 → 259, v5, tamaños de plantilla), `apps/api/src/lib/__tests__/rbac-catalog.test.mts` (v5, «v0→v5»), `apps/api/src/scripts/__tests__/rbac-sync.test.mts`, `refresh-demo-dataset.test.mts` (+`org_hr` / `prop_hr`), `tests/seed-checkin-contract` y `seed-ux-day-contract` (allowlist con `org_hr`), `apps/api/src/lib/env.ts` + `.env.example` + `scripts/env-contract.json` (`SEED_HR_PASSWORD`, `SEED_HR_ALLOW_PRODUCTION`; censo regenerado), `modules/developer/api-reference.service.ts` + test (etiquetas ES de `employees`, `agreements`, `standards`, `staffing-plans`, `incidences`, `labor-cost-panel`, `terminate`, `reset-defaults`, `decide`), `screens/users/users-rbac.ts` (prefijo `hr` → M12; 82 prefijos), `screens/tabs/index.ts` (`NominasTabs`), `routes/__tests__/backoffice.routes.test.mts` (201 URL), `tabs/__tests__/tabs-b-containers.test.mts`.

## 2. Modelo de datos, migración y datos del carril

| Elemento | Antes (a069906) | Después | Dónde |
|---|---|---|---|
| Migraciones | 24 (`20260920160000_checkin_pago_en_recepcion`) | 25 (`20260920173000_rrhh_plantilla_nomina`, 217 l.) | `packages/database/prisma/migrations/` |
| Modelos / enums | 296 / 46 | 302 / 46 (+`Employee`, `CollectiveAgreement`, `AgreementRule`, `LaborStandard`, `StaffingPlan`, `StaffingPlanLine`) | `schema.prisma:1757-1893` |
| Columnas nuevas en tablas existentes | — | 21, nulables o con default (`staff_profiles` 3, `employment_contracts` 6, `absence_requests` 3, `labor_forecasts` 7 + único, `payroll_periods` 2, `properties` 1) | migración |
| CHECK | 0 | 1 (`absence_requests_requested_ne_approved`) | migración (a mano) |
| Triggers públicos | 4 | 4 (sin cambio; `check-fresh-install.sh` sigue censando 4) | psql |
| PII cifrada | `PII_FIELDS` sin Employee | `PII_FIELDS.Employee` (5 campos) + `LOOKUP_HASH_FIELDS.Employee` (`taxId → taxIdLookupHash`) | `packages/database/src/crypto-fields.ts:179/:254` |
| Catálogo RBAC | 254 claves, plantillas v4 | 259 (+5 `hr.*`), v5; `payroll_hr` 18 claves; `SOD_STATIC_PAIRS` 34 → 35 | `packages/shared/src/permissions.ts`, `rbac-types.ts` |
| Manifiesto de rutas | — | +25 entradas: 22 en `modules/hr/route-permissions.partial.ts` (21 `/hr/*` + `POST /workforce/me/absences`) y 3 en el de payroll (`GET /payroll/incidences`, `GET /payroll/labor-cost-panel`, `GET /workforce/properties/:propertyId/staff-profiles`); `POST /payroll/staff-profiles` admite `employeeId` | `security/route-permissions.ts` |
| Árbol de navegación | 70 ítems · 104 pestañas · 197 URL | 70 · 108 · 201 (+`/finanzas/nominas/plantilla`, `/prevision`, `/panel`, `/hoy/costes-personal`) | `nav-tree.generated.json`, CSV compartido 305 filas |
| Pantallas Cocoa | 245 en el inventario | 251 (+4 pantallas, `EmployeeDrawer`, `NominasTabs`; 0 `style={` nuevos, `inlineStyles` 624 sin cambio) | `docs/design/cocoa-22-inventory.json` |
| Tests | api unit 3.569 · admin-web 2.014 · contratos raíz 765 | 3.730 (+161) · 2.119 (+105) · 800 (+35); integración 1.029 | `gates-base.json` → `gates-final.json` |

Datos en `hotelos_rrhh` al cerrar: `org_hr` sembrado por el corrector (`db:seed:hr --reset`: 24 expedientes, 12 fichas, 12 contratos, 357 turnos, 603 fichajes, 3 ausencias, 112 previsiones con 0 días degradados, 9 estándares, 2 planes aprobados, 1 convenio + 21 reglas, periodo 2026-09 abierto) más el runtime posterior de los revisores (periodos 2026-10 / 2026-11 y contratos de prueba en `org_hr`); Faranda: `employees` 0, `labor_forecasts` 0, `journal_entries` 143.228, `payroll_cost_lines` 415, `reservations` 13.457 / 13.556 según la suite, sin escrituras; `permissions` global 259 (por `syncPermissionCatalog()` del seed, como `seed-checkin`); roles de Faranda 24/24 intactos, «behind v5» hasta el `rbac:sync` real.

## 3. Puertas (línea base 2026-09-20 10:02 → final 16:52)

Todas con `NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv bash scripts/gates.sh --quick|--json …` salvo donde se indica el CSV filtrado. JSON en `scratchpad/RRHH/`.

| Puerta | Base `gates-base` | Ola 0 `ola1` | Ola 1 `ola2` | Ola 2 `ola3` | Ola 3 `ola4` | Ola 4 `ola5` | Ola 5 `ola6` | Completa 2.ª `gates-full`* | Corrector `corrector/gates-full-2` | **Final `gates-final`** |
|---|---|---|---|---|---|---|---|---|---|---|
| typecheck:all | 15 PASS · 1 SKIP | 15 · 1 | 15 · 1 | 15 · 1 | 15 · 1 | 15 · 1 | 15 · 1 | 15 · 1 | 15 · 1 | **15 PASS · 0 FAIL · 1 SKIP (guest-web) · 21,7 s** |
| api unit | 3.569 / 3.568 · 1 skip | 3.573 | 3.670 | 3.701 | 3.701 | 3.719 | 3.719 | 3.719 | 3.730 | **3.730 · 3.729 pass · 0 fail · 1 skip** |
| admin-web unit | 2.014 / 2.013 · 1 skip | 2.014 | 2.014 | 2.014 | 2.096 | 2.097 | 2.115 | 2.115 | 2.119 | **2.119 · 2.118 · 0 · 1** |
| ai-core | 119 | 119 | 119 | 119 | 119 | 119 | 119 | 119 | 119 | **119 / 119** |
| worker | 34 | 34 | 34 | 34 | 34 | 34 | 34 | 34 | 34 | **34 / 34** |
| contratos raíz | 765 / 763 · 2 skip | 790 | 800 | 800 | 800 | 800 | 800 | 800 | 800 | **800 · 798 · 0 · 2** |
| discoverability | 197 | 197 | 197 | 197 | ROJA (4 huérfanas hasta RRHH-11) | 200 | 201 | 201 | 201 | **201 URL** |
| nav-tree --check | al día (70 · 104 · 205) | al día | al día | ROJA† | ROJA† | ROJA† | ROJA† | al día con CSV filtrado (70 · 108 · 205) | ROJA† | **ROJA†** |
| route-access | 15 × 197 | 15 × 197 | 15 × 197 | 15 × 197 | 15 × 197 | 15 × 200 | 15 × 201 | 15 × 201 | 15 × 201 | **15 × 201** |
| cocoa waves --check | §6 al día | OK | OK | OK | OK | OK | OK | OK | OK | **§6 al día** |
| rbac:sync --dry-run | OK · 186 ms | OK | OK | OK | OK | OK | OK | OK | OK | **OK · 210 ms** |
| migrate status + drift | 24/24 · 0 | 25/25 · 0 | 25 · 0 | 25 · 0 | 25 · 0 | 25 · 0 | 25 · 0 | 25 · 0 | 25 · 0 | **25/25 · «No difference detected.»** |
| admin-web build | — | — | — | — | — | — | — | 2,99 s | 3,00 s | **3,01 s** |
| integración | — | — | — | — | — | — | — | 1.024 · 1.016 · 0 · 8 skip | 1.029 · 1.021 · 0 · 8 | **1.029 · 1.021 pass · 0 fail · 8 skip** |
| **Verde** | 12/12 | 12/12 | 12/12 | 11/12 | 10/12 | 11/12 | 11/12 | 14/14* | 13/14 | **13/14** |

\* 2.ª ejecución completa tras corregir `structure-l4.test.mts`, con `NAV_TREE_CSV=scratchpad/RRHH/full-nav-tree.filtered.csv`; la 1.ª (CSV compartido) fue 12/14. † Ver §3.1. Las 2 rojas puntuales de la puerta quick de las olas 3 y 4 (discoverability por las 4 pantallas de `screens/hr` aún sin ruta; `cocoa-22-contract` regla 15 por el inventario) se cerraron en RRHH-11 y en el corrector.

### 3.1 La puerta roja: `nav-tree --check`, una sola causa, ajena al carril

El CSV compartido `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` (mtime 14:21) lleva desde las 12:19 seis filas «nueva (Tanda ACT)» (`RealEstateAssetScreen` + 5 pestañas `/finanzas/activo-inmobiliario/*`, líneas 295-300) cuyo `screenKey` no existe en `apps/admin-web/src` de esta rama. Regenerar `nav-tree.generated.json` con ese CSV metería 6 rutas sin componente (`check-route-validity` fallaría y arrastraría route-access y discoverability), así que RRHH-11, PANEL-B y la puerta completa regeneraron desde una copia filtrada (`scratchpad/RRHH/{rrhh11,panelb,full}-nav-tree.filtered.csv` = CSV compartido sin esas 6 filas): `--check --csv <filtrada>` «up to date (70 items, 108 tabs, 205 legacy routes)»; contra el CSV crudo «stale» (csvRows 298 → 304). El CSV completo construye limpio (71 ítems · 112 pestañas · 205 redirecciones; `scratchpad/RRHH/rrhh11-nav-tree.full.json`), así que en main basta regenerar tras fusionar ACT. El integrador de la puerta final regeneró para diagnosticar, comparó y RESTAURÓ el JSON byte a byte (`cmp` idéntico).

## 4. Hallazgos de la revisión (2 revisores → dedupe → refutación → corrector)

18 confirmados (5 high · 13 medium), 0 refutados, 14 low. Estado tras el corrector: **fixed** = corregido en código con test; **decisión** = no exige código en el carril.

| Id | Sev. | Hallazgo (fichero) | Estado · qué hizo el corrector |
|---|---|---|---|
| RF-01 | high | Cualquier usuario con `workforce.timeclock.use` fichaba por CUALQUIER ficha del centro y con hora arbitraria (`advanced-record-store.ts` createTimeClock; verificado como housekeeper: clock-in por `hr_sp_005` y clock-out por `HR-008` retroactivo → 200) | **fixed** · con solo `timeclock.use` se ficha SIEMPRE la ficha propia (cuerpo sin ficha = la propia; otra por id o alias → 403 `HR_TIMECLOCK_SELF_ONLY`; `at` ignorado); `timeclock.manage` ficha por terceros y honra `at`; `StoreScope.permissions`; tests `hr-absences-sod.test.mts`, `advanced-record-schemas.test.mts`; `WorkforceDashboard` solo ofrece la ficha propia sin `timeclock.manage` |
| RF-02 | high | Un periodo `approved` se recalculaba con `payroll.manage` borrando la aprobación, y `export` aceptaba periodos no aprobados (`periods.service.ts:319`, `export.service.ts:217`; runtime: approve → calculate → export sin aprobación) | **fixed** · `calculatePeriod` → 409 `PAYROLL_PERIOD_APPROVED` sobre approved / exported; `POST …/export` → 409 `PAYROLL_NOT_APPROVED` sin aprobación y conserva `approvedBy`; UI Exportar / Recalcular según aprobación; tests `treasury-banking.test.mts`, `payroll-cost-screen-contract`. Sin revocación explícita de la aprobación (documentado en runbook §12 y diseño A.3) |
| RF-03 | medium | `GET /hr/absences` con `workforce.read` devolvía las ausencias de todos con motivo, solicitante y decisor; el empleado no podía solicitar la suya (`route-permissions.partial.ts:62`) | **fixed** · con solo `workforce.read` las listas de ausencias y fichajes se ciñen a las fichas del actor (`workforceSelfScope`, `PageInput.staffProfileIds`); nueva `POST /workforce/me/absences` (`workforce.timeclock.use` · medium); sin crear `workforce.self.manage` (catálogo v5 intacto); tests `hr-absences-sod`, `route-read-keys` |
| RF-04 | medium | Un día pasado sin cierre ni reservas se etiquetaba `actual` con ocupación 0 y FTE «reales» sin degradar (`drivers.service.ts:237`; runtime 2026-04-10 en `prop_hr`: recepción 24 h / 4,89 FTE con 0 habitaciones) | **fixed** · fallback por reservas a 0 habitaciones → `source null` + `no_realized_data` (fila degradada; un cierre auditado a 0 sí es dato); `drivers.test.mts`; etiquetas en `DEGRADED_REASON_LABELS_ES` |
| RF-05 | medium | Position control no cableado: `checkStaffingHeadroom` sin llamadas y `POST /payroll/contracts` sin `warnings` (`contracts.service.ts:103`) | **fixed** · `createContract` llama a `checkStaffingHeadroom` tras persistir y responde `{ ...contrato, warnings }`; toast en `EmployeeDrawer` y `PayrollScreen`; `hr-routes.test.mts` (5 contratos sobre plan rooms 4,50 → «5.00 FTE frente a 4.50 aprobados») |
| RF-06 | medium | `computeSlip` aplicaba 6,50 / 32,15 a todos los contratos (temporales sin 6,55 / 33,35) y no prorrateaba bruto ni bases por `partTimePct` (`periods.service.ts:236`) | **fixed** · `temporal` y `sustitucion` al 6,55 / 33,35 (`PAYROLL_TEMPORARY_CONTRACT_TYPES`, `ssRatesFor`); `grossSalary` = bruto a jornada completa, bruto y bases × `partTimePct / 100`; `payroll-calc.test.mts` (+3); runbook §6 |
| RF-07 | medium | «Plantilla activa · N personas» contaba contratos, no personas (`kpis.service.ts:105`; runtime 15 «personas» con 12) | **fixed** · `activeHeadcount` = personas distintas (`employeeId ?? userId ?? ficha`), FTE sigue sumando contratos; aviso «otro contrato activo» al crear un segundo contrato sobre la misma ficha; `kpis.test.mts`, `hr-routes.test.mts` |
| RF-08 | medium | Un expediente en `leave` no aparecía en ningún segmento de Plantilla (`HrEmployeesScreen.tsx:126`) | **fixed** · segmento «Excedencias» (`status=leave`); `hr-employees-contract`, `employee-form.test`; manual |
| RF-09 | medium | «Coste del mes» del Panel RRHH solo leía el lote importado e ignoraba la nómina calculada / aprobada del mes (`kpis.service.ts:160`; runtime «No disponible» frente a 28.544,43 € en el panel de dirección) | **fixed** · `getHrKpis` cae a la nómina calculada del mes (Σ bruto + SS empresa) con `HrKpisDto.monthLaborCostSource: import | payroll | null` y caption «nómina calculada · sin dato de ventas»; `kpis.test`, `hr-forecast-helpers.test`, `hr-overview-contract` |
| RF-10 | medium | Sin el 409 `PAYROLL_MODE_CONFLICT` de D §7.1(3): un centro podía devengar 640/642 dos veces (lote + nómina calculada) y no figuraba como aplazado (`cost-import.service.ts:28`) | **fixed** · 409 en `calculatePeriod` con lote `posted` en la celda y en `createPayrollCostImport (post)` / `postPayrollCostImport` con periodo aprobado / exportado (`PayrollCostPayrollPeriodRef.approved`, código en `PAYROLL_COST_ERROR_CODES`, frase ES en `finance-contracts.ts`); `payroll-cost-import.test.mts` (ambos sentidos), `treasury-banking.test.mts` (el demo `org_123` tiene el lote manual posted de 2026-08 en `prop_123`: la suite calcula su nómina en `prop_canary` y asevera el 409 en `prop_123`); runbook §6, diseño A.1, manual |
| SEC-01 | high | `StaffProfile.employeeId` sin ningún escritor en el API: fuera del seed el expediente nunca veía fichas ni contratos, la baja no los cerraba y «Nuevo contrato» desde Plantilla era imposible (`staff-profiles.service.ts:277`) | **fixed** · `POST /payroll/staff-profiles` admite `employeeId` (misma organización y sociedad del centro: 404 «Expediente no encontrado.» / 400 `STAFF_PROFILE_EMPLOYEE_MISMATCH`; hereda `usaliDepartment` y `jobTitle`); `createEmployee` / `patchEmployee` con `userId` retro-enlazan fichas sin expediente (`linkedStaffProfileIds` en la auditoría); `StaffProfileDto.employeeId`; `staff-profiles.test.mts`, `hr-routes.test.mts` (alta → ficha por HTTP → 5 contratos → baja cierra los 5 y desactiva la ficha) |
| SEC-02 | high | Regresión: el fichaje de Personal y turnos exigía una ficha de `GET /payroll/staff-profiles` (`payroll.read`) y las plantillas operativas con `timeclock.use` sin `payroll.read` recibían 403 (`WorkforceDashboard.tsx:185`) | **fixed** · nueva `GET /workforce/properties/:propertyId/staff-profiles` (`workforce.read` · medium; DTO sin `hourlyCost` ni `userEmail`) consumida por `WorkforceDashboard`; tests `staff-profiles.test`, `hr-routes.test` (housekeeper 200, recepción 403, otra org 404), `workforce-staff-select`. Pendiente fuera del worktree: fila `WorkforceDashboard` de `pilots/screens-inventory.csv` (§5) |
| SEC-03 | high | «Contratos raíz» solo en verde porque en el worktree falta `pilots/screens-inventory.csv`; con el inventario compartido `rbac-nav-contract` fallaba 2/21 por 28 pares sin clasificar | **fixed** · `JUSTIFIED_GAPS` clasifica los 28 (hr.employee.read de accountant / controller / compliance / auditor en HrEmployeesScreen y HrOverviewScreen → `inventory`, porque las pantallas solo piden `/hr/employees` con la clave y `pending` está prohibido por el propio contrato; compliance sin `workforce.read` / `workforce.labor_cost.view` / `payroll.read` → `sister`); 21/21 con el inventario (enlace temporal retirado) y 13/13 sin él |
| SEC-04 | medium | `nav-tree --check` roja con el CSV oficial; el «allGreen» de las olas solo valía con la copia filtrada | **decisión** (§3.1, §5): fusionar ACT y regenerar con el CSV consolidado, o aceptar explícitamente el CSV filtrado para esta rama; el JSON de la rama no se regeneró con las filas ACT |
| SEC-05 | medium | `pnpm-lock.yaml` modificado en el worktree (intocable, fuera de todos los lotes) | **decisión** · fuera del commit (`git reset -q hotelos/pnpm-lock.yaml`); el desfase lock ↔ `package.json` es tarea aparte (`--frozen-lockfile` en CI) |
| SEC-06 | medium | Pareja SoD estática `{payroll.manage, hr.staffing.approve}` ausente de `SOD_STATIC_PAIRS` (`rbac-types.ts:407`) | **fixed** · añadida (35 parejas); `rbac-sod-contract` (lista de pares + aserción sobre `sodPairs`); `rbac:sync --dry-run` OK |
| SEC-07 | medium | Tipos de ausencia de salud (art. 9 RGPD) enmascarados en `/hr/absences` pero expuestos en `GET /dashboards/workforce` (`analytics.read` · low), que pinta la pantalla Personal (`workforce.service.ts:394`) | **fixed** · `buildWorkforceDashboard` recibe `permissions` y enmascara (`type null` + `restricted`); `server.ts` pasa `request.userContext.permissions`; front alineado con `ABSENCE_TYPE_LABELS_ES` (test que lo compara con `packages/shared`); `workforce.test.mts`, `workforce-staff-select` |
| SEC-08 | medium | `seed-hr.ts` era un segundo escritor de `labor_forecasts` / `labor_standards` con cálculo propio, y `drivers.service` excluía sus snapshots `demo` y previsiones `seed-hr-demo`: «Generar previsión» en `org_hr` degradaba el 100 % (`seed-hr.ts:1096`) | **fixed** · el seed llama a `resetLaborStandardDefaults` + `generateLaborForecast` (contexto rrhh), previsiones `pms_import:seed-hr-demo` desde hoy, 58 reservas demo `HRDEMO-*` con régimen BB/HB, `flushAuditQueues`; `db:seed:hr --reset` ejecutado: 112 previsiones · 0 días degradados; `seed-hr-contract` pina el único escritor |

Low (14): RF-11 `INVALID_TRANSITION` como «La solicitud ya fue decidida.» (`workforceApi.ts`, `hrForecastApi.ts`) · RF-12 `HR_PII_READ` agregado por (organización, actor, expediente, ámbito) en 60 s · RF-13 `approvedPlanFor` elige el plan aprobado cuya temporada cubre el mes (con vuelta de año) · RF-14 la vista Contrato precarga pagas / jornada del convenio del centro · RF-15 Vencimiento «—» sin contrato · RF-16 total «N FTE necesarios (parcial)» y barras «Necesario (parcial)» en días degradados · RF-17 `financeScope` no pide `GET /organizations/me/structure` sin `accounting.read` / `accounting.reports.read` · SEC-09 `POST …/labor-forecast/generate` a riesgo `high` · SEC-10 `generateLaborForecast(input, deps = { now })` en vez de dormir 5 ms · SEC-11 `HR_AUDIT_ACTIONS` + `HR_STAFFING_PLAN_CREATED` / `_UPDATED` / `PAYROLL_INCIDENCES_EXPORTED` con `satisfies HrAuditAction` · SEC-12 `mode` / `closedAt` en `mapPeriod` y en `PayrollPeriodRecord` de `treasury-types.ts` (+ `approved`, `calculatedByUserId`, `approvedByUserId`, `approvedAt`), envoltorio `withPayrollPeriodMode` retirado · SEC-15 `GET /hr/employees?search=` rechaza con 400 un NIF / NIE / IBAN — los 12 **fixed**; SEC-13 (ficheros tocados fuera de los lotes: solo alcance del commit e informe, §1 último bloque) y SEC-14 (instancia ajena en `:3923` durante la revisión; `lsof` vacío al cerrar) sin cambio de código.

Refutados: **ninguno**.

## 5. Pendientes (con dueño)

| # | Pendiente | Dueño | Dónde |
|---|---|---|---|
| 1 | Regenerar `nav-tree.generated.json` con el CSV consolidado tras fusionar ACT (`node scripts/build-nav-tree.mjs --csv /Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv`) y subir los pins: `navigation/__tests__/nav-tree.test.mts` (71 ítems · 113 pestañas · 206 URL), `routes/__tests__/backoffice.routes.test.mts` (206), `tabs-b-containers`; hasta entonces `nav-tree --check` roja con el CSV crudo | orquestador (fusión) | §3.1 |
| 2 | `rbac:sync` real (`--upgrade-templates` o top-up) sobre la BD viva: 69 roles «behind v5», +5 claves `hr.*`, +1 pareja SoD; no se ejecutó desde el carril (datos Faranda) | orquestador tras fusionar | CLAUDE.md 19c, runbook §8 |
| 3 | `pilots/screens-inventory.csv` fila 34 `WorkforceDashboard`: añadir `/workforce/properties/:p/staff-profiles` a `api_paths_principales` (fichero compartido fuera del worktree) | orquestador | SEC-02 |
| 4 | `pnpm-lock.yaml` fuera del commit (hecho por el integrador); desfase lock ↔ `package.json` como tarea aparte con `--frozen-lockfile` | César / orquestador | SEC-05 |
| 5 | Pre-commit: `core.hooksPath = .husky` (config del repo) se resuelve contra la raíz del árbol (`~/anfitorio-demo[-wt-*]/.husky`, inexistente) mientras el hook vive en `hotelos/.husky/pre-commit`: `git hook run --ignore-missing pre-commit` no encuentra nada y el hook NO se dispara en ningún commit de este repo; el integrador lo ejecutó a mano desde `hotelos/` (`scratchpad/RRHH/integrador-precommit.log`: discoverability OK · typecheck 15 PASS · 1 SKIP · 21,5 s). Corregir con `git config core.hooksPath hotelos/.husky` + `cd "$(git rev-parse --show-toplevel)/hotelos"` al inicio del hook | César | `hotelos/.husky/pre-commit` |
| 6 | Manual: las 4 capturas de `docs/manual/img/rrhh/` son anteriores a la tira de pestañas y al selector de fichas; §8-§10 sin captura; regenerar con `docs/manual/tools/capturas.mjs` sobre UXDAY con la tanda fusionada | DOC (siguiente tanda) | `30-rrhh.md` |
| 7 | `pilots/tanda5-nav-tree.md` §3 (recuentos por token) desfasada desde T9/CHK para todos los tokens salvo `rrhh`; §2 son instantáneas históricas | orquestador | pilots |
| 8 | Demo `org_hr` del panel de dirección: `prop_hr` solo tiene 2026-09 por el diario (devengo de la nómina), sin 70x, sin lote `posted` ni RN → % ventas, coste por empleado y CPOR «—»; para una demo útil el seed debería sembrar ventas 70x por mes, un lote de coste `posted` con referencia y cierres `night_audit` | siguiente tanda | PANEL-A/B |
| 9 | CPOR laboral con RN residuales en el piloto (RA `night_audit` 172 RN en 66 días; AS / MC / PG 2026-07 parciales): la pantalla pinta la fuente y un aviso de cobertura; si se prefiere degradar por umbral (RN < 5 % de inventario × días), un solo punto en `loadRoomNightsByMonth` | César (dato) / siguiente tanda | `labor-cost-panel.source.ts` |
| 10 | `labor_standards` sin `unit` en la clave única (sala y cocina fundidos, 9 estándares por centro en 4★, no 10); `Shift` sin `breakMinutes`; `hr.staffing.review` y `hr.staffing.enforce` inexistentes; `StaffProfile.userId` sigue obligatorio (expediente sin usuario → sin ficha ni contrato); `payroll_hr` con `timeclock.manage` sin `timeclock.use` y sin `accounting.entity.read` (incidencias «toda la sociedad» → elegir centro); `receptionist` / `night_auditor` / `sales` / `revenue` con `timeclock.use` sin `workforce.read` (fichan por API, no ven Personal y turnos); `general_manager` sin `workforce.schedule.manage` | siguiente tanda / César (§6) | CLAUDE.md 19, runbook §12 |
| 11 | Sin revocación explícita de la aprobación de un periodo (`payroll.approve`): para corregir un aprobado hay que pagarlo y contabilizar la diferencia o retirar la aprobación en BD auditada a mano | siguiente tanda | runbook §12, diseño A.3 |
| 12 | Preexistentes observados y no tocados: `lib/format money()` sin separador de miles en importes de 4 cifras («4228,80 €» junto a «28.544,43 €»); en `PayrollScreen` «Recalcular» / «Pagar» no gateados en la UI por `payroll.manage` / `payables.pay` (el API responde 403); carrera `useApiData` + `clearApiCache` en el evento de sesión (reproducida solo con arnés) | siguiente tanda | RRHH-9/10, PANEL-B |
| 13 | Carriles: `corepack pnpm dev -- --port <p>` NO fija el puerto en Vite 6 (usar `dev --port <p> --strictPort`); con `TENANT_BOOTSTRAP_SKIP=true` hace falta el preload `scratchpad/RRHH/rrhh4/preload-hydrate.mts` para que un tenant creado por script responda; Postgres del Mac a 90/100 conexiones obligó a `connection_limit=4` (cerrar instancias huérfanas) | orquestador | runbook §10 |
| 14 | Fuera de la tanda (diseño A.3, plan-olas «Fuera»): `WorkdayRecord` + trigger de fichajes, llamamientos de fijos discontinuos, comunicaciones TGSS, documentos PRL por persona, `WorkCalendar`, `LeaveBalance`, `SchedulePeriod`, `ContractSalaryVersion`, `PayrollSlipImport`, IRPF AEAT, Siltra / A3 / Sage oficiales, modelo 190, `LaborBudget`, app móvil del empleado, `CostKpiSnapshot` / `CostBudget` / umbrales / benchmarks del panel; bases mínimas / máximas y solidaridad en el recibo | siguiente tanda | diseño A.3 |

## 6. Decisiones para César (opción por defecto aplicada en el árbol)

| # | Decisión | Defecto aplicado | Alternativa |
|---|---|---|---|
| 1 | Modo de nómina | **Externo único**: la gestoría calcula, el ERP importa el agregado como hoy; el modo calculado queda como preparación con `PAYROLL_RATES_2026` (6,50 / 32,15; temporales 6,55 / 33,35; jornada parcial prorrateada; sin bases mín. / máx. ni MEI por tramos); IRPF AEAT, Siltra / A3 oficiales, modelo 190 y recibos por persona NO se construyen | activar el modo calculado por centro (exige lo aplazado) |
| 2 | `users.read` para `payroll_hr` (pendiente desde T8a) | **No**: «Nueva ficha» y los selectores usan `/payroll/staff-profiles` y `/workforce/properties/:id/staff-profiles` | añadirla a la plantilla (v6 + `rbac:sync`) |
| 3 | Convenio por centro | catálogo ES-15-HOST (RA / LT), ES-33-HOST (PG / MC / LL / OC), ES-39-HOST (AS), ES-28-HOSP (FN) solo en `HR_AGREEMENT_DEFAULTS` y sembrado en `org_hr`; **en Faranda NADA** (ni catálogo ni `Property.agreementId`) hasta confirmación; sin convenio los contratos usan 14 pagas y la previsión 1.792 h + degraded | `seedAgreementCatalog` + `assignAgreementToProperty` por centro cuando César confirme el convenio real (y si hay convenio de empresa) |
| 4 | Mapa de los 15 departamentos reales de Sage → USALI / 64x | **No se toca la carga**: ene-jul «sin desglose» en el panel; agosto por el reparto del lote | `ledger_analytics_maps` con el mapa cuando César lo decida |
| 5 | Estándares de dotación | `sector_default` por estrellas (2 / 3 / 4★) sembrados con `reset-defaults`; calibración `measured` solo con datos de César | medir con los cuadrantes reales |
| 6 | Aprobación de la plantilla máxima | solo `general_manager` (`hr.staffing.approve`), SoD preparador ≠ aprobador, **sin bloqueo** (`hr.staffing.enforce` no existe): los contratos que superan el máximo solo avisan | que la dirección de hotel también apruebe; bloqueo duro |
| 7 | Panel de costes de dirección | pestaña de Mi día `/hoy/costes-personal` (roles direccion · finanzas · admin · propiedad · auditoria), sin ruta ni ítem nuevos; RN solo reales (LT `pms_import`, RA `night_audit`; AS / MC / PG desde estancias) y snapshots demo excluidos; FN / LL «sin datos»; CPOR de sociedad con aviso «n de m centros» en vez de «—» | degradar la sociedad cuando un centro carece de RN (una línea en `cporDegraded`) |
| 8 | `workforce_labor` en Faranda | no se activa (exige `maintenance`); las pantallas de RRHH cuelgan de Nóminas (core); `/operaciones/personal` sigue gateado | activar el módulo por centro |
| 9 | `StaffProfile.userId` obligatorio | sí: un empleado sin acceso a la app tiene expediente pero no ficha ni contrato individual (relevante solo en modo calculado); `employeeId` ya se admite en la ficha | relajarlo (deuda documentada) |
| 10 | CCC provinciales, códigos de centro de trabajo, `cccPrincipal` | vacíos (avisos en exportes e incidencias) | César aporta CCC y código de centro por hotel y sociedad, y el formato que importa su gestoría (A3 / Sage / CSV) y la cuenta RED |
| 11 | Roles de las pestañas de RRHH en el CSV | los del recon §3.7 (`finanzas|direccion|admin|rrhh|auditoria`), con los pares `inventory` / `sister` justificados en `rbac-nav-contract` | quitar `finanzas|auditoria` de las filas 302-305 del CSV |
| 12 | Demo | tenant `org_hr` / `le_hr` / `prop_hr` con usuarios `*@hr.test` (contraseña `hr-demo`) y 24 empleados ficticios con NIF sintético cifrado; nunca personas reales ni Faranda | — |
| 13 | SoD de ausencias | cubre `approved` y `rejected` (D §9 «decide»); el solicitante SÍ puede cancelar (fija `decidedAt`) | SoD solo en `approved` |
| 14 | Lectura de convenios | `GET /hr/agreements` y `…/rules` con `hr.employee.read` (D §9 decía `hr.config.manage`): el selector de convenio de la ficha lo necesita quien lee la plantilla | restringir a `hr.config.manage` |
| 15 | Alcance aplazado | 12 lotes; lo listado en §5 nº 14 va a otra tanda | — |

## 7. Fusión: ficheros tocados, exclusiones y servidores

- **Commit del integrador** en `tanda-rrhh` (nunca en main, sin push): `git add -A hotelos` + `git reset -q hotelos/pnpm-lock.yaml` + `commit -F` con el mensaje de §8; autor `cesareme <yakutatsa@gmail.com>`. Contenido: 78 modificados (+2.971 / −364) + 60 nuevos (21.696 l.) + este informe + el bloque de `ESTADO-VERIFICADO.md`. `pnpm-lock.yaml` queda `M` sin seguimiento en el commit.
- **Directorios / ficheros nuevos:** `apps/api/src/modules/hr/` (12 + 9 tests), `apps/api/src/modules/payroll/{payroll.routes,incidences.service,labor-cost-panel.service,labor-cost-panel.source}.ts` + 2 tests, `apps/api/src/schemas/hr.schemas.ts`, `apps/api/src/modules/dashboards/__tests__/workforce.test.mts`, `apps/admin-web/src/screens/hr/` (7 + 5 tests), `screens/costs/` (2 + 2 tests), `screens/tabs/finanzas/NominasTabs.tsx`, `screens/operations/__tests__/workforce-staff-select.test.mts`, `services/{hrApi,hr-contracts,hrForecastApi,laborCostPanelApi}.ts`, `packages/shared/src/hr-types.ts`, `packages/database/prisma/seed-hr.ts`, `packages/database/prisma/migrations/20260920173000_rrhh_plantilla_nomina/`, `docs/runbooks/rrhh-plantilla-nomina.md`, `tests/{hr-schema-contract,seed-hr-contract}.test.mjs`, `tests/integration/{hr-employees,hr-labor-forecast,hr-absences-sod,hr-routes,labor-cost-panel-routes}.test.mts`.
- **Ficheros compartidos fuera del worktree ya editados** (con copias `.bak` en `scratchpad/RRHH/rrhh11-*`): `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` (fila 63 + 302-305), `tanda5-nav-tree.md` (§1 :37 y :104, §3 rrhh), `screens-inventory.csv` (+4 filas).
- **Conflictos previsibles:** `CLAUDE.md` (deuda 19, docs prioritarios, allowlist, seeds: tomar `tanda-rrhh` y fusionar a mano si otro carril tocó los mismos bloques), `docs/audits/ESTADO-VERIFICADO.md` (`merge-lane.sh` concatena), `nav-tree.generated.json` + pins (regenerar tras ACT, §5 nº 1), `docs/design/cocoa-22-inventory.json` / `COCOA-22-MIGRACION.md` (regenerar con los scripts), `scripts/env-contract.json` (`env-census.mjs --write`), `server.ts` y `route-permissions.ts` (spreads `...hrRoutePermissions` y registros tras `registerStaffProfileRoutes`), `packages/shared/src/permissions.ts` (v5: si otro carril sube la versión, renumerar), migración (renumerar si otro carril aporta una marca posterior a `20260920173000`).
- **Tras la fusión:** `rbac:sync` real en la BD viva; `db:migrate:deploy` (25); reiniciar `:3000` / `:5173` del orquestador (no tocados por la tanda); `node scripts/build-nav-tree.mjs --csv …` + pins; `node scripts/cocoa-22-inventory.mjs` + `cocoa-22-waves.mjs --write`; `node scripts/env-census.mjs --write`.
- **Servidores:** todas las instancias de la tanda (slots A-D) se arrancaron desde el worktree y se pararon por PID; al cerrar `lsof -nP -iTCP:3923,5193,3933,5203,3943,5213,3953,5223 -sTCP:LISTEN` vacío; `:3000`, `:5173` y el PID 4986 intactos.

## 8. Mensaje de commit

```
feat(rrhh): plantilla, previsión, nómina y panel de costes de dirección (Tanda RRHH)
```
Cuerpo por lote (RRHH-1 … PANEL-B, corrector, puertas 13/14, `pnpm-lock.yaml` fuera) y línea final `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; el sha lo devuelve el integrador al orquestador y queda en `git log tanda-rrhh`.
