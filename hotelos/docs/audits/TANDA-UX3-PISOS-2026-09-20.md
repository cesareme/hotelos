# Tanda UX-3 · «Feel» de pisos y mantenimiento de ehotelOS — informe de cierre (2026-09-20)

**Encargo:** `docs/design/UX-PISOS-MANTENIMIENTO-FEEL.md` (136 líneas; §1 principios P1-P7 heredados de UX-1 y reglas de pasillo, §2 tareas p1…p6, §3 fricciones F1-F16, §4 rediseño por pantalla con la copia exacta, §5 confirmar / deshacer, §6 tablet y accesibilidad, §7 medida, §8 lotes, §9 decisiones D1-D10 y riesgos R1-R6) sobre el worktree `~/anfitorio-demo-wt-ux3/hotelos` (rama `tanda-ux3`, base `a069906` = main con CHK y T9), BD propia `hotelos_ux3` (24 migraciones aplicadas + la de la tanda = 25, deriva 0). **Commit del integrador en `tanda-ux3`** (§12): 68 ficheros — 35 modificados (+2.859/−669) y 33 nuevos (10.904 líneas) —; `hotelos/pnpm-lock.yaml` aparece `M` desde ANTES de la tanda (deriva preexistente del lock frente a los `package.json` de HEAD, como en UX-1), ningún lote lo ha tocado y queda FUERA del commit (`git reset -q hotelos/pnpm-lock.yaml` antes de commitear). `:3000` (PID 4986) y `:5173` intactos; todo se verificó con instancia propia (`API :3931 RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true RATE_LIMIT_MAX=5000` + Vite `:5201` con `VITE_API_URL=http://127.0.0.1:3931`, arrancadas desde el worktree y apagadas por PID al cerrar; ver §8).

Este informe lo escribe el lote **Q1** (medida final, táctil, cierre). La sección 1 resume lo que cada lote dejó en el árbol; la 2, lo que Q1 construyó; la 3, las medidas antes / después de las seis tareas con ratón y en tablet; la 4, los objetivos táctiles, la densidad y la barra del pulgar; la 5, el contraste; la 6, los recuentos del principio P4; la 7, las puertas finales con sus cifras; la 8, los datos escritos y los servidores; la 9, las decisiones D1-D10 tal como quedan y los pendientes con dueño; la 10, los ficheros de Q1 y del commit; la 11, el corrector (revisión adversarial UX-3-REV: hallazgos confirmados y corregidos); la 12, el cierre del integrador (ficheros confirmados por `git status`, puertas finales con cifras, hallazgos confirmados / refutados / corregidos, decisiones para César con la opción por defecto aplicada y el commit).

Datos: todo sobre el tenant aislado `UXDAY` (`org_uxday` / `prop_uxday`, seeds `db:seed:ux-day -- --reset` + `db:seed:ux-day-pisos`, usuarios `*@uxday.test`); ningún dato real de Faranda se tocó (la copia `hotelos_ux3` solo se leyó fuera de `org_uxday`); ningún nombre de persona real aparece en el código, los tests, los JSON ni este informe (las personas del seed se nombran por su correo).

## 1. Qué construyó cada lote (rutas)

| Lote | Estado | Qué hay en el árbol | Dónde |
|---|---|---|---|
| U0 · Seed, arnés y baseline | done | seed de pisos sobre el día de prueba (4 usuarios `@uxday.test`, 3 secciones «Planta 1 / Planta 2 / Plantas 3-4», 5 sucias con tarea p1, 1 en curso, 2 limpias, partes `wo_uxday_p5a/b`, `p6a/b`, uno en curso y una emergencia; idempotente, `--dry-run`, guard demo), arnés de medida, specs p1…p6 (baseline sin estricto), spec táctil, baselines JSON | `packages/database/prisma/seed-ux-day-pisos.ts`, `packages/database/package.json` (`db:seed:ux-day-pisos`), `apps/admin-web/e2e/measure/{_measure-pisos.ts,p1…p6-*.spec.ts}`, `apps/admin-web/e2e/pisos/{_pisos.ts,pisos-target-size.spec.ts}`, `.gitignore` (`results-pisos.json`), `docs/audits/ux-pisos/{measure,target-size-pisos}-baseline-2026-09-20.json`, `tests/seed-ux-day-pisos-contract.test.mjs` |
| D0 · Diseño y primitivas | done | el diseño; `deferredCommit` copiado de la ficha de reserva (`ReservationWorkspaceScreen.tsx:325-349`, recepción intacta, test de identidad); copia única de botones y avisos con número (`PISOS_ACTIONS` / `PISOS_TOASTS` / `MANT_ACTIONS` / `MANT_TOASTS`, `workOrderRef`, `photoCount`) | `docs/design/UX-PISOS-MANTENIMIENTO-FEEL.md`, `apps/admin-web/src/screens/operations/deferred-commit.ts` + `__tests__/deferred-commit.test.mts`, `apps/admin-web/src/content/pisos-actions.ts` + `content/__tests__/pisos-actions.test.mts` |
| P1 · Tablero de pisos y tablero de habitaciones | done | `busy` por habitación; «Marcar limpia» / «Inspeccionar» optimistas con «Deshacer» 8 s y escritura diferida (`keepalive` en `pagehide`); «Nueva tarea» con «Asignar a» (datalist con los asignados vistos y el usuario de sesión) e Intro; etiquetas por diccionario (nunca «Mantenimiento: blocked»); `density="operational"`; ⌘K; en el tablero de habitaciones «Bloquear habitación» → diálogo nominal, «Desbloquear habitación» y «Marcar sucia» con «Deshacer» (POST inverso), «Marcar limpia» / «Inspeccionada» diferidas; avisos con número | `apps/admin-web/src/screens/operations/{HousekeepingDashboard,RoomRackScreen}.tsx`, `housekeeping-task-actions.ts`, `services/housekeepingApi.ts`; tests `__tests__/{housekeeping-board-feel,room-rack-block-dialog}.test.mts` |
| P2 · Mi turno | done | `sections[]` + `sectionId/sectionName` por habitación en `GET /dashboards/housekeeping-mobile` (join de `HousekeepingSection`/`SectionRoom` activas, degradable `housekeeping_sections`); chips de sección antes de los de prioridad, recordados por propiedad en `localStorage` (D8, «Mi sección»); tarjeta «Siguiente» arriba con el primario grande; «Iniciar» · «Limpia» · «Inspeccionada» optimistas, «Limpia» con «Deshacer» diferido y cierre de la tarea abierta (D1); ayuda plegada bajo la lista con el dedo (D9); `CocoaActionBar` también en tablet; badge «Baja» en `outline` (hallazgo U0 de contraste) | `apps/admin-web/src/screens/operations/HousekeepingMobileScreen.tsx`, `apps/api/src/modules/dashboards/housekeeping-mobile.service.ts`, `content/screen-instructions/housekeeping.ts`; tests `__tests__/housekeeping-mobile-feel.test.mts`, `apps/api/src/modules/dashboards/__tests__/housekeeping-mobile-sections.test.mts` |
| P3 · Mis averías y tablero de mantenimiento | done | chips «Mías · Todas» («Mías» = `assignedTo` igual al nombre o correo de la sesión; por defecto si hay alguna); «Tomar» optimista con «Deshacer» (PATCH inverso, D7); «Resuelta» optimista + diferida 8 s (`resolved` es terminal); «Nota»; ayuda corregida (F12); tablero con «Asignarme» / «Asignar a» (datalist), «Bloquear habitación» → `CocoaDialog` nominal «Bloquear la 305» / «Mantenerla en venta» (`tone="destructive"`, `submitOnEnter`), «Resolver» diferido con «Deshacer»; mapas locales → diccionario; `operational`; tono «Baja» a `success` | `apps/admin-web/src/screens/operations/{MaintenanceMobileScreen,MaintenanceDashboard}.tsx`, `services/maintenanceApi.ts`, `content/screen-instructions/maintenance.ts`; tests `__tests__/{maintenance-take-contract,maintenance-feel}.test.mts` |
| M1 · Fotos del parte (API) | done | migración aditiva `20260920170000_ux3_parte_fotos` (`work_order_media.content_base64`, `mime_type`, `size_bytes`, `created_at`, `created_by`; sin tabla nueva, reversible con DROP COLUMN, sin backfill); `POST /work-orders` acepta `photos[]` (≤ 3, ≤ 1,5 MiB decodificada, jpeg/png/webp por magic bytes, `bodyLimit` 6 MiB + 64 KiB) y responde 201 con `media[]`; `POST /work-orders/:id/media` con `{ contentBase64, mimeType }` (manage, D3); `GET /work-orders/:id/media` (metadatos) y `GET /work-orders/media/:mediaId` (bytes, `Cache-Control: private, no-store`, 404 opaco entre organizaciones) bajo `maintenance.read`; `mediaCount` en `/dashboards/maintenance-mobile`; manifiesto +2 | `packages/database/prisma/{schema.prisma,migrations/20260920170000_ux3_parte_fotos/migration.sql}`, `apps/api/src/modules/maintenance/maintenance.service.ts`, `apps/api/src/server.ts`, `apps/api/src/security/route-permissions.ts`, `docs/api-contracts.md`; tests `apps/api/src/modules/maintenance/__tests__/work-order-media.test.mts`, `tests/integration/ux3-parte-fotos.test.mts` |
| P4 · «Reportar» con foto, galería y manual | done | `ReportIncidentDrawer` (chips «Fuga de agua» · «Bombilla» · «Aire acondicionado» · «TV/Wi-Fi» · «Cerradura» · «Otro» → título «Hab. 203: <motivo>»; «Foto» con `CocoaFileInput accept="image/*" capture="environment" multiple`, ≤ 3, miniaturas con «Quitar», compresión por `capture-compress.ts` (lado largo 1.600 px, JPEG 0,82), foto > 1,5 MiB rechazada con el peso; «Enviar a mantenimiento» `size="large"`, Intro envía; aviso «Avería de la 203 enviada a mantenimiento · 1 foto»); galería «N fotos» en Mis averías (`CocoaSheet`); manual 40 y fichas 09/10 con la copia nueva; runbook del kit del moderador; whitelist de discoverability | `apps/admin-web/src/screens/operations/{ReportIncidentDrawer.tsx,report-incident.ts,HousekeepingMobileScreen.tsx,MaintenanceMobileScreen.tsx}`, `services/maintenanceApi.ts`, `apps/admin-web/.discoverability-whitelist.json`, `docs/manual/40-pisos-mantenimiento.md`, `docs/manual/formacion/fichas/{09-habitacion-limpia-e-inspeccionada,10-parte-de-mantenimiento}.md`, `docs/runbooks/ux-pisos-pruebas.md`; test `__tests__/report-incident.test.mts` |
| **Q1 · Medida final, táctil y cierre** | done (este informe) | §2 | §2 |

## 2. Lote Q1 · qué se construyó

- **Specs de medida adaptadas al camino nuevo** (`apps/admin-web/e2e/measure/p1…p6-*.spec.ts`, mismos dos bloques por spec —ratón 1280 × 900 y tablet 820 × 1180 con `hasTouch` + `pointer: coarse` por CDP— y mismas personas del seed; contrato `tests/seed-ux-day-pisos-contract.test.mjs` 24/24 después): **p1** «Limpia» en Mi turno de la camarera (optimista; la spec espera el `POST /rooms/:id/housekeeping-status` y el `PATCH /housekeeping/tasks/:id` diferidos, D1) y, como la tarea cerrada saca la habitación de Mi turno, la gobernanta la inspecciona desde el tablero de pisos («Inspeccionar» → `POST /rooms/:id/mark-inspected` diferido) en un segundo contexto de navegador; éxito = `inspected` y la tarea fuera del tablero. **p2** chip de sección (primera con habitaciones pendientes) + tarjeta «Siguiente» (sección titulada «Siguiente», primera habitación de la cola del API para esa sección); tras recargar afirma «Mi sección» y el chip pulsado sin tocar nada (D8); la ayuda con ratón se anota y no cuenta (§6). **p3** «Nueva tarea» → «Asignar a» con el correo de la camarera → Intro (ratón, `submitOnEnter`) o «Crear tarea» (tablet); aviso «Tarea creada para la habitación NNN · asignada a …»; éxito = tarea `departure_clean` `assigned` a ese correo en el board. **p4** «Reportar» → chip «Fuga de agua» → `setInputFiles` con un PNG generado en Node (1.800 × 1.350, 466,8 KB, `makeTestPng`) que el cajón reduce por canvas → «Enviar a mantenimiento»; la cámara se anota como paso (no es toque ni tecla); éxito por API con el encargado: `POST` 201, título «Hab. NNN: Fuga de agua», `GET /work-orders/:id/media` con 1 elemento y `items[].mediaCount 1` en `/dashboards/maintenance-mobile`; conserva el camino de texto con `completed: false` «sin foto» por si el cajón no tuviera campo de foto (contrato de U0). **p5** «Todas» solo si el parte no está a la vista (Mis averías arranca en «Mías») → «Tomar» (espera el PATCH) → «Resuelta» (espera el `POST /resolve` diferido); afirma que ambos avisos ofrecen «Deshacer». **p6** abrir la orden → «Bloquear habitación» → diálogo nominal «Bloquear la NNN» (afirma «Mantenerla en venta») → confirmar (`POST /block-room`, aviso con número, habitación `blocked` y no vendible) = tarea principal medida por el arnés; después «Resolver» libera (aviso «Parte … resuelto · habitación NNN liberada.» con «Deshacer», `POST /resolve` diferido) medido con un segundo arnés y anotado en `phases` del mismo resultado (`amend`). Las esperas son por condición (respuesta del API), nunca ventanas fijas.
- **Arnés** `e2e/measure/_measure-pisos.ts`: `snapshot(name)` (contadores acumulados con nombre de fase), `amend(patch)` (reescribe el resultado ya escrito con `phases` / `context` / `note`), tipo `MeasurePhaseP`; los objetivos siguen ≤ 3 por tarea (p2 ≤ 1) y se afirman con `MEASURE_STRICT=1`; `ms` incluye las ventanas de «Deshacer» cuando la spec espera una escritura diferida (como T6 de UX-1).
- **Helpers** `e2e/pisos/_pisos.ts`: `DEFERRED_WRITE_MS` (8 s) y `waitForApiResponse(page, método, sufijo)` (registrada ANTES del toque, plazo = ventana + viaje); `fetchMiTurnoData` (habitaciones y `sections[]`), `fetchWorkOrderMedia`, `fetchMisAverias`, `openTasksOf`; localizadores del camino nuevo `sectionChip`, `nextRoomCard`, `roomNumberOfCard`, `scopeChip`, `blockRoomDialog`, `toastOffersUndo`; `makeTestPng` (PNG RGB válido con degradado, zlib + CRC, sin fichero en disco).
- **Spec táctil** `e2e/pisos/pisos-target-size.spec.ts`: además de 0 objetivos < 24 px, los < 44 listados y el contraste, afirma en las cuatro páginas de pisos y mantenimiento `data-density-mode="operational"` resuelto a `comfortable` con el dedo (F11) y, en Mi turno y Mis averías, la barra del pulgar presente con todos sus controles ≥ 44 px (`actionBarTargets`), en los dos viewports y los dos temas; el tablero de habitaciones se declara sin densidad ni barra (recepción intacta).
- **JSON finales** `docs/audits/ux-pisos/measure-final-2026-09-20.json` (corrida estricta 1, 11:53Z; idéntica a `apps/admin-web/e2e/measure/results-pisos.json`) y `docs/audits/ux-pisos/target-size-pisos-2026-09-20.json` (40 corridas agregadas con la misma forma que la baseline más `actionBarTargets`, `badgeUiBelow3Light` y `actionBarMinPx`); segunda corrida de medida en `scratchpad/UX-3/measure-final-run2.json`.
- **Diseño §2** con las cifras reales (columna «Después (Q1)» y regla de tanda con el resultado); este informe; bloque «Estado verificado (Tanda UX-3 …)» en `docs/audits/ESTADO-VERIFICADO.md` y tres líneas en `CLAUDE.md`.

## 3. Medidas antes / después por tarea (camino óptimo automatizado, §7)

Fuente: `results-pisos.json` de `e2e/measure` copiado a `docs/audits/ux-pisos/` — baseline de U0 (`measure-baseline-2026-09-20.json`, 09:53Z, sin estricto, antes de tocar UI) y final de Q1 (`measure-final-2026-09-20.json`, **11:53Z**, `MEASURE_STRICT=1`, **12/12**: seed rearmado con el API parado, instancia propia `:5201/:3931`). Segunda corrida (11:55Z, mismo procedimiento sobre el seed rearmado; `scratchpad/UX-3/measure-final-run2.json`, `measure-final-run2.log`): **12/12 estricto, clics, teclas y peticiones idénticos** en las doce medidas; ms ±0,2 s (p1 17,30 / 17,35 s, p5 8,21 / 8,24 s) salvo p3 ratón 0,26 → 0,49 s. Clics = acciones de puntero contadas (clic con ratón, toque en tablet; `select` cuenta 1); teclas = pulsaciones y caracteres (Intro incluido); peticiones = llamadas al API desde el inicio de la tarea hasta el evento de dominio (en p1 también las de la segunda persona desde que su tablero está cargado); ms = reloj de pared entre ambos, **incluidas las ventanas de «Deshacer» de 8 s** cuando la tarea acaba con una escritura diferida (p1 dos ventanas, p5 y la fase de desbloqueo de p6 una): se lee «≤ presupuesto», no cifra exacta. Ratón y tablet miden igual en todas salvo p3 (Intro con ratón).

| Tarea (§2) | Objetivo | Baseline (U0) · ratón / tablet | Final (Q1, estricto) · ratón / tablet | Cumple |
|---|---|---|---|---|
| p1 · marcar limpia (camarera) e inspeccionar (gobernanta) | ≤ 3 · optimista · deshacer 8 s · ≤ 16 pet. | 2 clics · 0 teclas · 4 pet. · 1.397 ms / 2 · 0 · 4 · 1.411 (sin deshacer, `refresh()` completo, la tarea no se cierra) | **2 clics · 0 teclas · 4 peticiones · 17.302 ms** / **2 · 0 · 4 · 17.365** (2.ª corrida 17.348 / 17.527): «Limpia» + «Inspeccionar», los dos avisos con «Deshacer» (`undoOfferedClean` / `undoOfferedInspect` true), la tarea de salida cerrada (D1); los 17 s son las dos ventanas de 8 s + la carga del tablero de la gobernanta | sí (2 ≤ 3; = baseline) |
| p2 · ver mi turno y la siguiente habitación | ≤ 1 (chip recordado) · ≤ 14 | 1 · 0 · 7 · 1.183 / 1 · 0 · 7 · 1.155 (el clic era cerrar la ayuda; lista de todo el hotel, sin «Siguiente») | **1 clic · 0 teclas · 7 peticiones · 1.202 ms** / **1 · 0 · 7 · 1.161** (chip «Planta 1 · n»; la «Siguiente» = `rooms[0]` de la sección; tras recargar «Mi sección» y 0 toques; con ratón la ayuda sigue sobre la lista la primera vez y no cuenta) | sí (1 ≤ 1; = baseline con un clic útil en vez de cerrar la ayuda) |
| p3 · crear una tarea de limpieza asignada | ≤ 3 con asignación · ≤ 12 | 2 · 0 · 2 · 328 / 2 · 0 · 2 · 336 (sin asignar, sin Intro) | **1 clic · 17 teclas · 2 peticiones · 261 ms** (tarjeta → correo en «Asignar a» → Intro) / **2 toques · 16 teclas · 2 · 322** (tarjeta → correo → «Crear tarea»); tarea `assigned` a la camarera | sí (1 / 2 ≤ 3; no peor que 2; ahora con asignación) |
| p4 · reportar una avería con foto | ≤ 3 + cámara · 0 tecleo · `mediaCount 1` · ≤ 12 | **no completable** (2 clics · 41-42 teclas · 2 pet. · 263-270 ms; cajón solo texto, `completed: false`) | **3 clics · 0 teclas · 2 peticiones · 411 ms** / **3 · 0 · 2 · 371** («Reportar» → chip «Fuga de agua» → Foto (paso) → «Enviar a mantenimiento»; `POST` 201; PNG de 466.800 B enviado como JPEG de 31.554 B; `GET …/media` 1; `mediaCount 1`) | sí (3 ≤ 3; de no completable a completada) |
| p5 · tomar y resolver un parte | ≤ 3 · deshacer · «Mías» · ≤ 12 | 2 · 0 · 4 · 124 / 2 · 0 · 4 · 154 (`refresh()` completo entre toques, «Resuelta» irreversible) | **3 clics · 0 teclas · 3 peticiones · 8.211 ms** / **3 · 0 · 3 · 8.233** («Todas» + «Tomar» + «Resuelta»; ambos avisos con «Deshacer»; la ventana de 8 s de «Resuelta» está dentro de ms) | sí (3 ≤ 3); **+1 frente a la baseline** por el chip de alcance: Mis averías arranca en «Mías» cuando el técnico tiene partes (§4.4) y el parte a tomar está en la cola; las dos acciones de dominio siguen en 2 y las peticiones bajan 4 → 3 (§9.1) |
| p6 · bloquear y desbloquear una habitación | bloquear ≤ 3 con diálogo nominal · desbloquear + deshacer · ≤ 16 | 3 · 0 · 4 · 206 / 3 · 0 · 4 · 428 (= bloquear 2 sin diálogo + resolver 1) | **bloquear 3 clics · 0 teclas · 2 peticiones · 185 ms** / **3 · 0 · 2 · 518** (abrir + «Bloquear habitación» + «Bloquear la 117 / 118»; aviso «Habitación NNN bloqueada.») · **desbloquear 1 clic · 1 petición · 8.069 / 8.086 ms** («Resolver» con «Deshacer», ventana incluida) → 4 acciones en total | sí (bloquear 3 ≤ 3; desbloquear 1 = 1); **+1 frente a la baseline** por la confirmación nominal que exige §5 (bloquear afecta a inventario); peticiones 4 → 3 |

Objetivos §7 que sí se pueden afirmar con la medida automatizada: **p1 ≤ 3 · p2 ≤ 1 · p3 ≤ 3 · p4 ≤ 3 con `mediaCount 1` · p5 ≤ 3 · p6 bloquear ≤ 3**: los seis dentro del objetivo; p4 pasa de no completable a completada con foto; **peticiones iguales o menores en las seis** (p5 y p6 bajan 4 → 3 al desaparecer el `refresh()` completo); primer repintado tras cada acción optimista (toast visible) < 100 ms medido por la propia spec (el aviso aparece antes de que viaje nada). Dos tareas suman un toque deliberado respecto a la baseline: p5 (chip «Todas», decisión §9.1) y p6 (diálogo nominal de bloqueo, §5). La regla «ninguna peor que la baseline» se cumple en clics para p1-p4 y en peticiones para las seis; p5 y p6 llevan el toque extra que el diseño acepta (§2 «Regla de tanda», §7) y, desde el corrector (§11, REV-M01), el estricto del arnés LEE la baseline y afirma clics ≤ baseline + `BASELINE_CLICK_ALLOWANCE_P` (p5 1, p6 1) y peticiones ≤ baseline. Lo que NO mide esta tanda: éxito y tiempos con camareras y técnicos reales, errores, SEQ y UMUX-Lite (kit en `docs/runbooks/ux-pisos-pruebas.md`; decisión de César, §9).

Presupuestos de peticiones por tarea (`TARGETS_P` de `_measure-pisos.ts`, provisionales [S]): p1 16 · p2 14 · p3 12 · p4 12 · p5 12 · p6 16; las seis muy por debajo (máximo 7 en p2, que cuenta el shell al aterrizar).

## 4. Tamaños de objetivo, densidad por dispositivo y barra del pulgar (§6)

Medido con el proyecto `touch` (`e2e/pisos/pisos-target-size.spec.ts`, `hasTouch` + CDP `pointer: coarse` / `hover: none`, iPad apaisado 1024 × 768 y tablet vertical 820 × 1180, claro y oscuro, seed rearmado; **20/20 pruebas, 40 corridas** con los cajones: Nueva tarea, Reportar incidencia, ficha de la orden, Añadir nota y ficha de la casilla; `docs/audits/ux-pisos/target-size-pisos-2026-09-20.json`, 11:56Z):

| Pantalla (persona) | Objetivos 1024 × 768 · 820 × 1180 (pantalla / con cajón) | < 24 | < 44 | Densidad (`data-density-mode` / resuelta) | Barra del pulgar (mín. px) |
|---|---|---|---|---|---|
| Tablero de pisos `/operaciones/pisos` (gobernanta) | 157 / 163 · 155 / 161 | 0 | 0 | operational / **comfortable** | no (tablero de escritorio) |
| Mi turno `/operaciones/pisos/mi-turno` (camarera) | 76 / 86 · 74 / 84 | 0 | 0 | operational / **comfortable** | **sí · 44** («Actualizar» + edad de los datos) |
| Tablero de mantenimiento `/operaciones/mantenimiento` (encargado) | 40 / 45 · 38 / 44 | 0 | 0 | operational / **comfortable** | no |
| Mis averías `/operaciones/mantenimiento/mis-averias` (técnico) | 36 / 39 · 34 / 37 | 0 | 0 | operational / **comfortable** | **sí · 44** |
| Tablero de habitaciones `/recepcion/reservas/tablero` (gobernanta) | 92 / 98 · 90 / 96 | 0 | 0 | sin densidad declarada (recepción, UX-1 U10) | no |

- **Objetivos < 44 px justificados: ninguno.** En las 40 corridas `below44Total = 0` (baseline de U0: también 0, porque las cinco pantallas ya usaban `CocoaButton` y `CocoaFileInput` con caja de 44 px con el dedo); lo que cambia frente a la baseline es la densidad (`operationalPages` 0 → 16 de 16 corridas de pantalla en las cuatro páginas; F11) y la barra del pulgar (`actionBarPresent` 0 → 8 corridas, `actionBarMinPx` 44; F11: `CocoaActionBar mobileOnly={!coarse}`).
- Cajón «Reportar incidencia» a 820 × 1180: chips de motivo, «Foto», «Quitar» y «Enviar a mantenimiento» ≥ 44 px (P4 midió 44 en runtime; aquí `<44 = 0` en las cuatro corridas `mi-turno-*-reportar`); campo «Detalle» de una línea con Intro.
- Diálogo nominal de bloqueo: botones «Bloquear la NNN» / «Mantenerla en venta» dentro de la ficha (`mantenimiento-*-ficha`, `<44 = 0`).

## 5. Contraste de badges (1.4.3 texto · 1.4.11 no textual), claro y oscuro

Medido en cada corrida sobre todos los `[data-cocoa="badge"]` visibles (3-112 por pantalla; prioridad, estado de limpieza, ocupación, mantenimiento, «Bloquea la habitación», «SLA vencido», «N incidencias»):

| Esquema | Texto (mín. 4,5) | Borde / punto (mín. 3) | Detalle |
|---|---|---|---|
| Oscuro | mínimo **5,91:1** | mínimo **4,57:1** · `badgeUiBelow3Dark = []` | **cumple**; el hallazgo de U0 (badge «Baja» `neutral` + `tinted` a 1,35:1 en Mi turno, 4 corridas rojas en la baseline) está corregido por P2 (`outline`) y P3 (`success`): 0 corridas por debajo de 3:1 |
| Claro | mínimo **6,03:1** | **warning outline / dot 2,02–2,2 · success dot 2,87** (danger y neutral cumplen) | **abierto, informativo** (heredado de UX-1 §9.4): el borde `outline` y el punto `dot` pintan el matiz del tono; el texto y el icono nombran el estado, así que el borde no es información necesaria (1.4.11 «required to identify»); la spec lo escribe y solo lo afirma en oscuro |

## 6. Recuentos del principio P4 (deshacer antes que confirmar)

- **Acciones con deshacer** en las cinco pantallas: 0 (F3, `mutate(` y `ACTIONS.undo` sin usos en U0) → **11**: tablero de pisos «Marcar limpia» e «Inspeccionar» (diferidas); Mi turno «Limpia» (diferida, cierra la tarea) e «Inspeccionada» (diferida); Mis averías «Tomar» (PATCH inverso) y «Resuelta» (diferida); tablero de mantenimiento «Resolver» (diferida); tablero de habitaciones «Marcar limpia», «Inspeccionada» (diferidas), «Marcar sucia» y «Desbloquear habitación» (POST inverso). Medido en runtime por las specs: p1 2 avisos con «Deshacer», p5 2, p6 1 (`undoOffered*` en `context`).
- **Diálogos** en las cinco pantallas: 0 → **2**, los dos nominales de bloqueo (`MaintenanceDashboard.tsx`, `RoomRackScreen.tsx`: «Bloquear la 305» / «Mantenerla en venta») ≤ 2 ✓. Sin deshacer por diseño: «Iniciar» (se revierte con «Limpia»), crear / asignar tarea (D5), reportar avería (el parte queda abierto), bloquear (diálogo).
- **Escritura diferida** (`deferred-commit.ts`, copia idéntica de la ficha de reserva): una ventana por habitación / parte; `pagehide` vacía con `keepalive` (en Mi turno el POST de estado y el PATCH de la tarea viajan a la vez, §11 REV-03); la revalidación periódica (Mi turno 20 s, tablero de mantenimiento 30 s) no pisa una clave con mutación en vuelo (`apiCache` omite `load()` mientras `mutating > 0`, R2) y «Actualizar» en las cinco pantallas vacía antes las pendientes y espera a que lleguen (§11 REV-02); el toast con «Deshacer» no se pausa con el ratón ni el foco y un «Deshacer» tardío avisa «ya enviada» (§11 REV-01).

## 7. Puertas finales

Tres corridas de `scripts/gates.sh` sobre el árbol de la tanda, todas desde la raíz del worktree con `NAV_TREE_CSV=/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv`: **(a)** completa de Q1 (`bash scripts/gates.sh --json scratchpad/UX-3/gates-final.json`, 11:57-12:05Z, antes del corrector); **(b)** completa tras el corrector (mismo comando; `gates-final.json` regenerado a las 15:24 local; `docs/design/COCOA-22-MIGRACION.md` §6 regenerado antes con `node scripts/cocoa-22-waves.mjs --write` porque salía desactualizado, diff de 4 líneas); **(c)** rápida del integrador sobre el árbol que se commitea (`bash scripts/gates.sh --quick --json scratchpad/UX-3-INT/gates-quick.json`, 15:27 local, `gates-quick.log`). **(b) 13/14 y (c) 11/12: la única roja es `nav-tree --check`, ajena a UX-3 (§9.3, §12).**

| Puerta | (a) Q1 | (b) final tras el corrector | (c) integrador (rápida) |
|---|---|---|---|
| `typecheck:all` | 15 PASS · 0 FAIL · 1 SKIP · 48,9 s | **15 PASS · 0 FAIL · 1 SKIP** (apps/guest-web, explícito) · 22,3 s | 15 PASS · 0 FAIL · 1 SKIP · 22,6 s |
| api unit (`--filter @hotelos/api test`) | 3.594 · 3.593 pass · 0 fail · 1 skip | **3.594 · 3.593 · 0 · 1** (base de la tanda 3.569: +25 secciones de Mi turno y medios del parte) | 3.594 · 3.593 · 0 · 1 |
| admin-web unit (`--filter @hotelos/admin-web test`) | 2.138 · 2.137 · 0 · 1 | **2.149 · 2.148 · 0 · 1** (base 2.014: +135; +11 del corrector: `undoDeferred`, `capToasts`, feel de las 5 pantallas) | 2.149 · 2.148 · 0 · 1 |
| ai-core · worker | 119/119 · 34/34 | **119/119 · 34/34** | 119/119 · 34/34 |
| contratos raíz (`node --test tests/*.test.mjs`) | 776 · 774 · 0 · 2 skip | **777 · 775 · 0 · 2 skip** (base 765: +11 `seed-ux-day-pisos-contract`, +1 literales de pisos en plan y FAQ; los 2 skip preexistentes por `pilots/*.csv` fuera del worktree) | 777 · 775 · 0 · 2 |
| discoverability | OK · 197 URL | **OK · 197 URL** (`ReportIncidentDrawer` en la whitelist) | OK · 197 URL |
| `build-nav-tree --check --csv …` | ROJO («stale») | **ROJO** («stale») | ROJO («stale») — causa externa, §12 |
| route-access | 15 tokens × 197 URL | **15 × 197** | 15 × 197 |
| cocoa waves `--check` | §6 al día | **§6 al día** (regenerado) | §6 al día |
| `rbac:sync -- --dry-run` | OK · 203 ms | **OK · 195 ms** | OK · 189 ms |
| migrate status + drift | 25/25 · «No difference detected.» | **25/25 · «No difference detected.»** (24 heredadas + `20260920170000_ux3_parte_fotos`) | 25/25 · «No difference detected.» |
| admin-web build | 3,55 s | **3,16 s** | — (rápida) |
| integración (`tests/integration/*.test.mts`, `--test-concurrency=1`, BD `hotelos_ux3`) | 994 · 986 · 0 · 8 skip | **996 · 988 · 0 · 8 skip** (+2 del corrector en `ux3-parte-fotos`: propiedad B de la misma organización → 404; 4 POST concurrentes → 3 × 201 + 1 × 400) | — (rápida) |
| e2e `measure` p1…p6 (`MEASURE_STRICT=1`, seed rearmado, instancia propia `:5201/:3931`) | 12/12 (11:53Z) · 12/12 (11:55Z) | — (no relanzada por el corrector; solo typecheck del arnés) | **12/12** (13:31Z, 1,6 min) sobre el árbol commiteado: p1 2c·0t·4p·17,4 s · p2 1c·7p·1,2 s · p3 1c+17t / 2c+16t·2p · p4 3c·0t·2p·0,39 s · p5 3c·3p·8,2 s · p6 bloquear 3c·2p·0,96-1,04 s + desbloquear 1c·1p·8,1 s; `undoOffered*` true en p1, p5 y p6; clics, teclas y peticiones idénticos a (a) |
| e2e `touch` pisos (`pisos-target-size.spec.ts`, 2 viewports × 2 temas × 5 rutas + cajones) | 20/20 · 40 corridas (11:56Z) | — | **20/20 · 40 corridas (13:31-13:32Z, 42,8 s)** sobre el árbol commiteado: 0 objetivos < 24 px · 0 < 44 px · `operational` → `comfortable` en 16/16 corridas de pantalla · barra del pulgar en 8 corridas con controles de 44 px · badges texto ≥ 5,91:1 (oscuro) / 6,03:1 (claro) · borde / punto ≥ 4,57:1 en oscuro (`badgeUiBelow3Dark` = []) · en claro warning / success 2,02–2,87 informativo (igual que Q1); API 0 respuestas 5xx · 0 errores de nivel 50 en las dos pasadas (`scratchpad/UX-3-INT/target-size-int.json`) |
| pre-commit (`hotelos/.husky/pre-commit` = discoverability + `typecheck-all`) | — | — | **OK** a mano (`bash .husky/pre-commit`, 15 PASS · 23,9 s); git no lo dispara desde la raíz del worktree (§12) |
| `git -C ~/anfitorio-demo-wt-ux3 status --short -uall` | 58 entradas (29 M · 29 ??) | 69 entradas (36 M · 33 ??) | 69 entradas antes y después de la puerta; 68 en el commit (todo menos `pnpm-lock.yaml`) |

Puertas rápidas por ola (orquestador; `scratchpad/UX-3/gates-{base,ola1,ola2,m1,p4,ola3,ola4}.json`): base 12/12 (11:05) → ola 3 8/12 con los rojos de `cocoa-22-contract` (inventario desactualizado por los `style=` de P1/P2/P3), discoverability (`ReportIncidentDrawer`) y nav-tree; tras regenerar el inventario y la whitelist, y tras el corrector (`scratchpad/UX-3-COR/gates-quick.json`, contratos raíz 777 con el inventario regenerado), 13/14 con solo nav-tree en rojo.

## 8. Datos escritos, servidores y navegadores

- **Tenant**: solo `org_uxday` / `prop_uxday`. Cada pasada completa de p1…p6 escribe: 2 habitaciones (105 y 106) `inspected` con sus tareas `hkt_uxday_p1_1/2` `done`; 2 tareas `departure_clean` nuevas asignadas a `pisos@uxday.test` (113 y 114); 2 partes «Hab. 107: Fuga de agua» con 1 foto cada uno (`work_order_media` con `content_base64`, JPEG 31.554 B); `wo_uxday_p5a/b` resueltos y asignados al técnico; `wo_uxday_p6a/b` bloqueados y resueltos con la habitación liberada (117 y 118); las escrituras de los cajones abiertos por el proyecto `touch` no existen (solo abre y cierra). Entre la corrida 1, la 2 y la táctil se rearmó el seed con el API parado (`scratchpad/UX-3/rearm.sh`: `db:seed:ux-day -- --reset` + `db:seed:ux-day-pisos`); el tenant queda rearmado al cerrar (7 partes, 0 fotos, 7 tareas, 0 inspeccionadas, 3 secciones, 7 usuarios `@uxday.test`). Ningún dato de Faranda tocado; ningún nombre real en specs, JSON ni informe.
- **Instancias**: API `:3931` (PIDs 33450 → 36113 → 40764 → 42703 → 44936, uno por rearme; `RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true RATE_LIMIT_MAX=5000`, arrancado desde `apps/api` con `--env-file-if-exists=../../.env`) y Vite `:5201` (PID 33452, `VITE_API_URL=http://127.0.0.1:3931`) apagadas por PID al cerrar; `:3931/:5201` sin escuchas al terminar; `:3000` (PID 4986) y `:5173` intactos; sin `pkill`.
- **Navegadores**: Playwright 1.63.0 del árbol pide Chromium 1243, que no está en `~/Library/Caches/ms-playwright` (solo 1228); `pnpm exec playwright install chromium` NO se ha ejecutado (sin descargas): las corridas usan `PLAYWRIGHT_BROWSERS_PATH=<scratchpad>/pw-browsers` con enlaces `chromium-1243 → chromium-1228` (runbook §6). Sintaxis de los scripts: `e2e:measure --grep "p[1-6] · "` y `e2e --project=touch pisos` (`pnpm` pasa `--` y `--project` es variádico).
- **Logs y JSON de la sesión**: `scratchpad/UX-3/{measure-final-run1,measure-final-run2,touch-final,gates-final}.log`, `measure-final-run{1,2}.json`, `touch-out/*.json` (40 corridas + capturas PNG), `gates-final.json`.

## 9. Decisiones D1-D10 tal como quedan y pendientes con dueño

| # | Decisión (diseño §9) | Estado en el árbol |
|---|---|---|
| D1 | «Limpia» cierra la tarea abierta | Aplicada (P2): `PATCH /housekeeping/tasks/:id { status: done }` tras el POST, aviso «Hab. NNN → Limpia · tarea cerrada»; «Inspeccionada» no la cierra; medido en p1 (tarea fuera del board) |
| D2 | Fotos en base64 en la fila (`content_base64`, ≤ 1,5 MiB, ≤ 3) | Aplicada (M1): migración `20260920170000_ux3_parte_fotos`; R4 acotado a ≤ 4,5 MiB por parte |
| D3 | Solo `manage` adjunta después de crear | Aplicada (M1): `POST …/:id/media` bajo `manage`; la camarera adjunta al crear; sin UI para añadir después (manual 40) |
| D4 | El técnico no bloquea | Aplicada (P3): `canBlockRooms` (manage + `ai.high_risk.confirm`); el botón no se pinta sin permiso; p6 lo mide con el encargado |
| D5 | Sin deshacer al crear / asignar tarea | Aplicada (P1): aviso con número y asignación, sin ventana |
| D6 | `apps/mobile` fuera | Aplicada: sin cambios |
| D7 | Diferida donde no hay inversa; inversa donde existe | Aplicada: limpia / inspeccionada / resuelta / resolver diferidas; tomar / sucia / desbloquear con POST inverso |
| D8 | «Mi sección» en `localStorage` por propiedad | Aplicada (P2) y medida (p2: recargar → «Mi sección», 0 toques) |
| D9 | Ayuda bajo la lista con el dedo, sin tocar `CocoaScreenInstructionsCard` | Aplicada (P2): botón «Ayuda: Mi turno» plegado; con ratón la tarjeta sigue arriba la primera vez (dismissible + persistKey) |
| D10 | Nombres de huéspedes en la tablet de pasillo | Se mantienen [S]; alternativa «iniciales» si dirección lo pide |

**Pendientes con dueño (fuera de Q1 o decisión abierta):**

1. **p5 · chip de alcance** (diseño / P3): Mis averías arranca en «Mías» cuando el técnico tiene partes, así que tomar uno de la cola cuesta 3 toques (2 de dominio + «Todas»); dentro del objetivo pero +1 frente a la baseline. Opciones: recordar el alcance por propiedad como la sección (D8) o arrancar en «Todas» cuando la cola tenga partes sin asignar. Sin cambio en esta tanda (§4.4 dice «por defecto si hay alguna»).
2. **p6 · recuento único** (orquestador): el contrato de U0 (`tests/seed-ux-day-pisos-contract.test.mjs:151`, `clicks: [1-3]` por tarea) fija ≤ 3 por tarea; la spec mide bloquear (3) como tarea principal y desbloquear (1) en `phases`. Si se prefiere un solo recuento de 4 con objetivo 4, hay que relajar esa expresión regular (fichero fuera de Q1).
3. **`nav-tree --check` en rojo** (orquestador, en la fusión): el CSV compartido `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` cambió (13:30) después del `nav-tree.generated.json` del worktree (12:43) por rutas de otros carriles; regenerarlo aquí (`node scripts/build-nav-tree.mjs --csv …`) traería pantallas que este árbol no tiene y pondría en rojo discoverability y route-access. Ningún fichero de `navigation/` es de UX-3.
4. **Docs con literales anteriores a UX-3**: RESUELTO en el corrector (§11 REV-M02/M03: `faq.md`, `plan-de-formacion.md` y `api-contracts.md` actualizados y pinzados por `tests/manual-contract.test.mjs`); quedan las capturas PNG del manual (pisos y mantenimiento) anteriores a UX-3: regenerar con `docs/manual/tools/capturas.mjs` (orquestador).
5. **`hooks/apiCache.ts`** (UX-1 U3): `refresh()` directo durante una ventana diferida pinta el estado del API sobre el optimista; las cinco pantallas lo evitan (`refreshBoard` / `refreshTurn` / `refreshRack` / `refreshQueue` / `refreshBoard` de mantenimiento, §11 REV-02); la guarda general en `apiCache.loadApiKey` (`mutating > 0`) queda como endurecimiento fuera de UX-3.
6. **API de fotos** (M1, decisión del orquestador): `POST /work-orders` responde 201 solo con `photos` (el cuerpo clásico conserva 200 por `tests/integration/l2-modulos-operaciones.test.mts:208`); `created_at` de `work_order_media` es `TIMESTAMP(3)` canónico de Prisma, no `TIMESTAMPTZ`; `lib/tenancy.ts` sin resolver `workOrder` (la guarda de tenencia vive en el servicio).
7. **`accessKey` en primarios** (diseño §1 P2): no añadido (los botones se repiten por tarjeta); solo tendría sentido en «Crear tarea» / «Enviar a mantenimiento» de los cajones.
8. **`needs_attention`** (`HkMaintenanceStatus`) no está en `content/status-dictionary.ts`: la etiqueta «Mantenimiento pendiente» vive en `housekeeping-task-actions.ts`.
9. **`deferredCommit` duplicado** (recepción y pisos): consolidar en `lib/` cuando recepción deje de ser carril paralelo (el test de identidad lo vigila).
10. **Seed**: las habitaciones de p1 son ocupadas (stayover) porque no quedan libres fuera de las reservadas por las specs t*; si la sesión con personas prefiere libres, liberar números en `seed-ux-day-pisos.ts`.
11. **Contraste en claro** de bordes y puntos warning / success (2,0–2,9:1): heredado de UX-1 §9.4 (`CocoaBadge`, tokens).
12. **Playwright**: `pnpm exec playwright install chromium` (descarga) o mantener el shim; documentado en el runbook §6.
13. **`pnpm-lock.yaml`**: `M` desde antes de la tanda; excluido del commit del integrador (`git reset -q hotelos/pnpm-lock.yaml`) y sigue `M` en el worktree: `git checkout -- pnpm-lock.yaml` en la fusión, como en CHK.
14. **Sesiones con camareras y técnicos reales** (César): kit en `docs/runbooks/ux-pisos-pruebas.md` (tablet de pasillo 820 × 1180, guantes, tareas p1…p6, SEQ / UMUX-Lite); la medida automatizada no sustituye los tiempos con personas.

## 10. Ficheros de Q1 y del commit

**Ficheros de Q1** (todos dentro de la lista exclusiva del lote): `apps/admin-web/e2e/measure/{_measure-pisos.ts,p1-limpia-inspeccionar,p2-mi-turno-siguiente,p3-crear-tarea,p4-reportar-con-foto,p5-tomar-resolver,p6-bloquear-desbloquear}.spec.ts`, `apps/admin-web/e2e/pisos/{_pisos.ts,pisos-target-size.spec.ts}`, `docs/audits/ux-pisos/{measure-final,target-size-pisos}-2026-09-20.json` (nuevos), `docs/design/UX-PISOS-MANTENIMIENTO-FEEL.md` (§2), este informe, `docs/audits/ESTADO-VERIFICADO.md` (bloque UX-3), `CLAUDE.md` (3 líneas). Ningún fichero de producto tocado por Q1; 0 `style=` nuevos; sin dependencias nuevas; `pnpm-lock.yaml` intacto por la tanda.

**Ficheros modificados fuera de las listas de los lotes** (consecuencia legítima de las puertas; van en el commit, corrector REV-L04): `apps/admin-web/.discoverability-whitelist.json` (entrada `ReportIncidentDrawer`), `scripts/env-contract.json` (`readBy` + `seed-ux-day-pisos.ts` en tres variables), `docs/design/COCOA-22-MIGRACION.md` y `docs/design/cocoa-22-inventory.json` (regenerados por `cocoa-22-contract` y otra vez tras el corrector con `node scripts/cocoa-22-inventory.mjs`: 246 pantallas, 175 puntos de deuda, `ReportIncidentDrawer` a 0 puntos, `inlineStyles` 624 sin cambio; solo cambia `lines` 108.919 → 109.015). `pnpm-lock.yaml` queda FUERA (deriva preexistente al carril, REV-L05: el lock de `main` no trae `@playwright/test` aunque `apps/admin-web/package.json` lo declara; tarea aparte en `main`).

**Commit** (hecho por el integrador en la rama `tanda-ux3`, nunca en `main`, sin push; §12): `feat(ux-pisos): feel de pisos y mantenimiento de ehotelOS (Tanda UX-3)` con cuerpo por lote (U0, D0, P1, P2, P3, M1, P4, Q1, corrector REV, integrador) y las medidas de §3; 68 ficheros (35 modificados · 33 nuevos), sin `pnpm-lock.yaml`.

## 11. Corrector (revisión adversarial UX-3-REV, 2026-09-20)

Hallazgos de la revisión (medium y low) corregidos en código o docs, cada uno con su test; puerta rápida (`bash scripts/gates.sh --quick --json scratchpad/UX-3-COR/gates-quick.json`): typecheck:all 15 PASS · api unit 3.594 (3.593 pass · 0 fail · 1 skip) · admin-web unit 2.149 (2.148 · 0 · 1) · ai-core 119 · worker 34 · contratos raíz 777 (774 pass · 0 fail · 2 skip tras regenerar el inventario Cocoa 22) · discoverability 197 · route-access OK · cocoa waves al día · rbac dry-run OK · migrate + drift «No difference»; en rojo solo `nav-tree --check` (CSV compartido, §9.3); integración `tests/integration/ux3-parte-fotos.test.mts` 13/13 sobre `hotelos_ux3`; runtime comprobado con Playwright sobre instancia propia `:3931/:5201` y el seed rearmado (`scratchpad/UX-3-COR/cor-runtime.{mjs,json,log}`, capturas `c1…c4`; 0 errores de consola).

| Id | Corrección | Ficheros | Test |
|---|---|---|---|
| REV-01 | Toast con «Deshacer» sin pausa (`pauseOnHover: false`: la ventana diferida no se pausaba con el ratón ni el foco) y `undoDeferred` (deferred-commit.ts): un «Deshacer» con la ventana agotada, vaciada o en `pagehide` avisa «Habitación NNN ya enviada: no se puede deshacer.» / «Parte X ya enviado…» (`PISOS_TOASTS.undoExpired`, `MANT_TOASTS.undoExpired`) en vez de callar; el store de toasts EXPULSA el desbordamiento (> 3: primero los más antiguos sin acción) en vez de esconderlo y repintarlo con la acción ya inútil (`capToasts`) | `deferred-commit.ts`, `content/pisos-actions.ts`, `components/Toast.tsx`, las 5 pantallas | `deferred-commit.test.mts` (undoDeferred), `CocoaToast.test.mts` (capToasts), `pisos-actions.test.mts`, contratos de las 5 pantallas; runtime C1 (hover: POST a 8,04 s, toast cerrado, `data-paused` ausente) y C1b («Actualizar» a 0,6 s → «Deshacer» → «Habitación 106 ya enviada…», 0 escrituras) |
| REV-02 | Mis averías y tablero de mantenimiento: `refreshQueue` / `refreshBoard` (cabecera, ⌘K, reintento, barra del pulgar y tras crear una orden) vacían las diferidas, esperan `done` y solo entonces revalidan (`PendingWrite { commit, done }` como Mi turno); comentario :33, informe y runbook corregidos | `MaintenanceMobileScreen.tsx`, `MaintenanceDashboard.tsx`, runbook §6, diseño §5 | `maintenance-feel.test.mts`; runtime C3 (POST /resolve a 0,86 s, GET después, tarjeta no vuelve, 1 solo POST, sin 409) y C4 (fila no vuelve a «Activas», ficha «Resuelta») |
| REV-03 | Mi turno «Limpia» en `pagehide`: POST de estado y PATCH que cierra la tarea viajan A LA VEZ con `keepalive` (`Promise.allSettled`; fuera de pagehide sigue secuencial) | `HousekeepingMobileScreen.tsx` | `housekeeping-mobile-feel.test.mts`; runtime C8 (cierre a 0,5 s: hab. 107 `clean` y `hkt_uxday_p1_3` cerrada, no vuelve a Mi turno) |
| REV-04 | «Asignarme» envuelto en `onMouseDown={preventDefault}` (el clic no dispara el blur de «Asignar a», que asignaba el texto a medias) y `queuedAssign`: una intención llegada con otra en vuelo se aplica al terminar en vez de perderse | `MaintenanceDashboard.tsx` | `maintenance-feel.test.mts`; runtime C4 («Tec» + «Asignarme» → 1 PATCH, asignado al usuario de sesión, botón deshabilitado) |
| REV-05 | `GET /work-orders/:id/media` y `GET /work-orders/media/:mediaId`: 404 opaco también entre propiedades de la misma organización (`canReadWorkOrderMedia`: `order.propertyId === context.propertyId` salvo platform admin) | `maintenance.service.ts`, `api-contracts.md`, diseño §4.6 | `ux3-parte-fotos.test.mts` (3b: técnico de A asignado solo al hotel B → 404 en lista y bytes; el del hotel A → 200) |
| REV-M01 | El estricto del arnés afirma «ninguna peor que la baseline de U0» (`readBaselineP` + `baselineViolations`: clics ≤ baseline + `BASELINE_CLICK_ALLOWANCE_P` {p5: 1, p6: 1}, peticiones ≤ baseline; solo si la baseline acabó `completed`); cabecera del arnés, diseño §2/§7 e informe §3 con las excepciones aceptadas (p5 chip de alcance, p6 diálogo nominal) | `e2e/measure/_measure-pisos.ts`, diseño, este informe | `tests/seed-ux-day-pisos-contract.test.mjs` (pin del contrato); typecheck e2e con el tsconfig de Q1 |
| REV-M02 | `docs/api-contracts.md`: entrada `GET /dashboards/housekeeping-mobile?propertyId=` (forma de `rooms[]` con `sectionId`/`sectionName`, `sections[] { id, name, code?, total }` en orden código/nombre, `degraded[]` con `housekeeping_sections`) | `docs/api-contracts.md` | — (doc) |
| REV-M03 | Plan de formación (P-2, P4, P5, M-2, M3, M5, checklists) y FAQ con la copia de UX-3 («Hab. NNN → Limpia · tarea cerrada», «Avería de la NNN enviada a mantenimiento · 1 foto», «Parte X → En curso · asignado a ti», «Parte X → <estado>», «Nota añadida al parte X.», «Bloqueada», «ya enviada: no se puede deshacer.»); sin «Incidencia reportada», «Estado actualizado.», «no cierra su tarea», «no la asigna a nadie», «Mantenimiento: blocked» | `docs/manual/formacion/plan-de-formacion.md`, `docs/manual/faq.md` | `tests/manual-contract.test.mjs` (nuevo caso: lee los literales de `content/pisos-actions.ts` y los exige en el plan y la FAQ; los literales viejos rompen) |
| REV-06 (low) | Con el dedo, la tarjeta de ayuda plegada de Mi turno no es `dismissible` ni tiene `persistKey` (una descartada con ratón dejaba el botón «Ayuda» muerto) | `HousekeepingMobileScreen.tsx` | `housekeeping-mobile-feel.test.mts`; runtime C2 (localStorage descartado → región «Housekeeping» visible, 2.059 caracteres) |
| REV-07 (low) | Mi turno: `PRIORITY_LABEL` local y los literales de `FILTERS` sustituidos por `priorityLabel` (P6: 0 mapas locales) | `HousekeepingMobileScreen.tsx` | `housekeeping-mobile-feel.test.mts` (pin «sin Record<…, string> ni literales») |
| REV-L01 (low) | `compressImageFile(file, { force: true })` en «Reportar»: la recodificación por canvas es SIEMPRE (también ≤ 1.600 px y ≤ 512 KiB, y aunque el JPEG no sea más pequeño), así el parte nunca guarda EXIF (GPS, dispositivo, fecha); anotado en api-contracts y manual 40 | `screens/documents/capture-compress.ts` (opción aditiva), `ReportIncidentDrawer.tsx` | `capture-compress.test.mts`, `report-incident.test.mts` |
| REV-L02 (low) | `getWorkOrderMediaContent` lee metadatos (`id, workOrderId, mimeType, objectKey`), resuelve la tenencia y solo después `content_base64` | `maintenance.service.ts` | `ux3-parte-fotos.test.mts` (2, 3, 3b, 5 siguen en verde) |
| REV-L03 (low) | `attachWorkOrderMedia`: recuento + inserción en `prisma.$transaction` con `SELECT … FOR UPDATE` sobre el parte | `maintenance.service.ts`, `api-contracts.md` | `ux3-parte-fotos.test.mts` (6: 4 POST concurrentes → 3 × 201 + 1 × 400, 3 filas) |
| REV-L04 / L05 | Ficheros fuera de lote listados en §10 para el commit; `pnpm-lock.yaml` fuera del commit (deriva preexistente) | este informe | — |

Sin dependencias nuevas, sin `style=` inline nuevos (recuentos por fichero iguales: Mi turno 8, Mis averías 8, tablero de mantenimiento 5, tablero de pisos 6, tablero de habitaciones 5, Toast 0), sin cambios de esquema ni de permisos. Datos: solo `org_uxday` (seed rearmado antes del runtime; partes `UXDAY-COR-C3/C4` creados y resueltos) y los tenants aislados de la suite de integración (borrados al terminar; invariantes de Faranda iguales).

## 12. Integración (cierre del integrador, 2026-09-20)

- **Ficheros confirmados** con `git -C ~/anfitorio-demo-wt-ux3 status --short -uall` y `git diff --stat`: 69 entradas = 36 modificados + 33 sin seguimiento; **68 en el commit** (todo menos `hotelos/pnpm-lock.yaml`, +64/−25 preexistente al carril: el lock de `main` no trae `@playwright/test`, `@fontsource-variable/inter` ni `zod` que `apps/admin-web/package.json` declara y aún lista `apps/ai-gateway`; tarea aparte en `main`). Modificados 35 ficheros, +2.859/−669: `apps/admin-web` 16 (5 pantallas, `Toast.tsx`, `capture-compress.ts`, `housekeeping-task-actions.ts`, 2 servicios, 2 instrucciones, 3 tests, whitelist), `apps/api` 4 (`server.ts`, `route-permissions.ts`, `maintenance.service.ts`, `housekeeping-mobile.service.ts`), `docs` 9 (`api-contracts.md`, `ESTADO-VERIFICADO.md`, `COCOA-22-MIGRACION.md`, `cocoa-22-inventory.json`, manual 40, FAQ, fichas 09/10, plan de formación), `packages/database` 2 (`schema.prisma`, `package.json`), `scripts/env-contract.json`, `tests/manual-contract.test.mjs`, `.gitignore`, `CLAUDE.md`. Nuevos 33 ficheros, 10.904 líneas: `apps/admin-web` 20 (9 e2e: 7 `measure` + 2 `pisos`; `content/pisos-actions.ts`, `screens/operations/{ReportIncidentDrawer.tsx,deferred-commit.ts,report-incident.ts}`; 8 tests), `apps/api` 2 tests, `docs/audits` 5 (este informe + 4 JSON de medida), `docs/design/UX-PISOS-MANTENIMIENTO-FEEL.md`, `docs/runbooks/ux-pisos-pruebas.md`, `packages/database` 2 (seed y migración), `tests/` 2 (contrato del seed e integración de fotos). Solo los 4 de §10 están fuera de las listas de los lotes (consecuencia de las puertas). Ningún nombre de persona en los ficheros nuevos ni en las líneas añadidas (`grep` de `@faranda.test` y de los hoteles del brief: 0 salvo una aserción negativa en `pisos-actions.test.mts`).
- **Hallazgos de la revisión adversarial UX-3-REV**: 16. **Confirmados 15** — 8 medium (REV-01…05, M01…M03) y 7 low (REV-06, REV-07, L01…L05) —, de los que **13 corregidos** en código o docs con test (§11) y **2 solo documentados** (L04: los 4 ficheros fuera de lote van en el commit; L05: `pnpm-lock.yaml` fuera del commit). **Refutado 1**: M04 «`nav-tree --check` en rojo atribuible a UX-3» — reproducido sin tocar nada (11/12 en `--quick`, igual que el orquestador): el CSV compartido `/Users/cfernandez/anfitorio-demo/pilots/tanda5-nav-tree.csv` (fuera del worktree, sin versionar; mtime 14:21, 303 filas frente a 294 en el JSON de a069906) trae 8-9 rutas de otros carriles (`/hoy/costes-personal`, `/finanzas/nominas/*`, `/finanzas/activo-inmobiliario/*`) sin pantalla en este árbol; regenerar el JSON aquí (197 → 206-207 URL) pone en rojo admin-web unit (21-23 fallos «has no narration» y listas de `screenKeys`), contratos raíz y discoverability, por lo que se restauró a HEAD (`git show`, sin `checkout`; `navigation/` limpio en `git status`). Lo resuelve el orquestador en la fusión: regenerar `nav-tree` en `main` con todos los carriles (`node scripts/build-nav-tree.mjs --csv …`) o pasar a este carril un CSV congelado a a069906 vía `NAV_TREE_CSV`.
- **Verificación del integrador sobre el árbol commiteado**: puerta rápida 11/12 (§7 col. c; solo nav-tree); `pre-commit` a mano OK; e2e `measure` estricta **12/12** y `touch` **20/20 · 40 corridas (13:31-13:32Z, 42,8 s)** con el seed rearmado dos veces (`db:seed:ux-day -- --reset` + `db:seed:ux-day-pisos`, API parado) e instancia propia arrancada desde el worktree (API `:3931` PIDs 67339 → segundo arranque tras el rearme, `RUN_SCHEDULERS=false TENANT_BOOTSTRAP_SKIP=true RATE_LIMIT_MAX=5000`; Vite `:5201` PID 67340, `VITE_API_URL=http://127.0.0.1:3931`), ambas apagadas por PID al terminar (`:3931/:5201` libres; `:3000` PID 4986 y `:5173` intactos; sin `pkill`); Chromium por el shim `PLAYWRIGHT_BROWSERS_PATH=<scratchpad>/pw-browsers` (sin descargas). Guion y salidas: `scratchpad/UX-3-INT/{e2e-final.sh,e2e-final.log,measure.log,measure-int.json,touch.log,touch-out/,gates-quick.{json,log},pre-commit-manual.log}`. Datos: solo `org_uxday` (queda con la pasada de `touch`, que no escribe: rearmado); ningún dato de Faranda tocado.
- **Pre-commit**: `core.hooksPath=.husky` (config común `~/anfitorio-demo/.git/config`) es relativo a la raíz del árbol de trabajo, que en los worktrees es `/Users/cfernandez/anfitorio-demo-wt-ux3/` (sin `.husky`: el directorio vive en `hotelos/.husky`), así que `git commit` desde la raíz no ejecuta ningún hook (`git hook run pre-commit` → «cannot find a hook named pre-commit»). El guion se ejecutó a mano desde `hotelos/` antes del commit (`bash .husky/pre-commit`: discoverability OK · typecheck:all 15 PASS · 0 FAIL · 1 SKIP · 23,9 s), sin `--no-verify`. No se cambió ninguna configuración.
- **Decisiones para César** (opción por defecto APLICADA en el árbol; qué cambiar si prefiere la otra):
  1. D1 «Limpia» cierra la tarea; «Inspeccionada» no (aviso «Hab. NNN → Limpia · tarea cerrada»). Alternativa: cerrar también al inspeccionar → `HousekeepingMobileScreen.tsx` y `HousekeepingDashboard.tsx`.
  2. D2 / D3 fotos del parte en base64 en `work_order_media` (≤ 3 × 1,5 MiB tras comprimir, sin EXIF), adjuntar después solo con `maintenance.workorder.manage` y sin UI para ello. Alternativa: adaptador S3 / disco sobre `documents/storage` y «Añadir foto» en Mis averías (tanda aparte, clave de César).
  3. D4 el técnico no bloquea habitaciones (`manage` + `ai.high_risk.confirm`); el encargado y la gobernanta, con diálogo nominal «Bloquear la NNN» / «Mantenerla en venta». Alternativa: conceder `housekeeping.task.manage` + `ai.high_risk.confirm` al rol `maintenance` en `packages/shared/src/permissions.ts` (`rbac:sync`).
  4. p5 arranca en «Mías» (+1 toque aceptado en el diseño §2: 3 ≤ 3). Alternativa: recordar el alcance por propiedad como «Mi sección» o arrancar en «Todas» con cola sin asignar → `MaintenanceMobileScreen.tsx` (vuelve a 2).
  5. p6 bloquear con diálogo nominal (3) + desbloquear (1) medidos en fases por el contrato de U0 (`clicks: [1-3]`). Alternativa: recuento único 4 con objetivo 4 → relajar `tests/seed-ux-day-pisos-contract.test.mjs:151` y `TARGETS_P.p6`.
  6. API de fotos: `POST /work-orders` responde 201 solo con `photos` (200 clásico por `tests/integration/l2-modulos-operaciones.test.mts:208`); `work_order_media.created_at` `TIMESTAMP(3)` canónico; tenencia en el servicio (sin resolver `workOrder` en `lib/tenancy.ts`). Alternativa: 201 siempre + `@db.Timestamptz` + resolver en `tenancy.ts` (3 ficheros).
  7. D10 nombres de huéspedes en la tablet de pasillo: se mantienen como en recepción. Alternativa: chip «Sin nombres» (fuera del plan).
  8. Hook de pre-commit en worktrees: `git config core.hooksPath hotelos/.husky` (o un enlace `.husky` en la raíz) para que dispare también aquí; hoy solo se ejecuta a mano.
  9. `CLAUDE.md`: Q1 dejó 4 líneas de estado (resumen + puntero al bloque de `ESTADO-VERIFICADO.md`); van en el commit porque el integrador no las edita (fuera de sus ficheros). Alternativa: retirarlas en la fusión y dejar el estado solo en `ESTADO-VERIFICADO.md`.
  10. D7 / D9 sesiones con camareras y técnicos reales y tablet física 820 × 1180: solo César (kit en `docs/runbooks/ux-pisos-pruebas.md`); la medida automatizada no sustituye los tiempos con personas.
- **Commit**: `feat(ux-pisos): feel de pisos y mantenimiento de ehotelOS (Tanda UX-3)` en la rama `tanda-ux3` (nunca en `main`, sin push), autor `cesareme`, cuerpo por lote (U0 · D0 · P1 · P2 · P3 · M1 · P4 · Q1 · corrector · integrador) y línea `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; sha en el informe del orquestador y en `git log -1 tanda-ux3`. Tras el commit `git status --short` debe mostrar solo ` M hotelos/pnpm-lock.yaml`.
