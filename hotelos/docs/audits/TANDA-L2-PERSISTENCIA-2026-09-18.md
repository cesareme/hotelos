# Tanda L2 · Persistencia y API — informe de cierre (2026-09-18)

**Encargo:** fila L2 del plan `docs/audits/TANDA-5-PLAN-2026-09-15.md` (§3), principio §2.1 («funcional = persiste en Postgres, responde 2xx/4xx tipado y lo que la pantalla ofrece existe en el API») y criterios de cierre §4. Reconocimientos de partida: `~/anfitorio-demo/pilots/app-recon-{2,4,6}.md`. Base: HEAD `99833b1` (posterior a `dd662ee`, Tanda 8a). Working tree sin commit (125 ficheros: 102 modificados + 23 sin seguimiento; `pnpm-lock.yaml` figura modificado desde el 2026-09-16 05:32 — deuda 12(a) de Tanda 4/6, anterior a esta tanda). El API `:3000` no se ha reiniciado: a las 18:16 no había ningún proceso escuchando (`curl /health` → conexión rechazada), lo arranca el orquestador con este código; ninguna instancia `:3901` queda abierta (`lsof -iTCP:3901` → 0 al cerrar la integración, 19:00).

Este informe lo cierra la **ronda de corrección 1** (corrector): recoge los ocho lotes, las puertas medidas tras la corrección, cada hallazgo de la verificación adversarial con su arreglo, las tablas y rutas retiradas, el SQL de verificación y las decisiones abiertas. El contrato funcional detallado está en `docs/api-contracts.md` § «Tanda L2 · Persistencia y API». La **integración final** (2026-09-18 18:16-19:05, integrador) añade §3.1 (puertas repetidas), §4 (hallazgos INT-01…INT-06), §7.2-§7.5 (reinicio real del API en `:3901`, matriz de acceso, rutas retiradas, limpieza) y §10 (decisiones 10-13).

## 1. Lotes

| Lote | Alcance ejecutado | Dónde |
|---|---|---|
| L2-01 · esquema y migración | `20260918130000_persistencia_l2`: 18 `DROP TABLE` (tablas muertas, cada uno precedido de comprobación de tabla vacía), 2 tablas nuevas (`scheduler_leases`, `ai_pending_confirmations`), `organization_id` NOT NULL en `notifications` (relleno desde `properties`), `offline_sync_records` y `worker_job_runs`, CHECK `invoice_sequences_year_not_null`, 14 índices únicos parciales (uniques con NULL). Helper de tenant aislado para tests (`tests/integration/helpers/l2-tenant.mts`). | `packages/database/prisma/migrations/20260918130000_persistencia_l2/migration.sql`, `schema.prisma` |
| L2-02 · rutas y manifiesto | 82 rutas retiradas (duplicados `/ai-governance/*`, `/developer/webhooks*`, recomendaciones sin propiedad, motor en memoria sin tabla, portal del huésped scaffold, envíos a autoridad F1/F2, back office en memoria); `GET /admin/worker/job-runs` nueva; manifiesto = rutas registradas = 935; líder de schedulers por lease (`lib/scheduler-leader.ts`, cada tick `holdsSchedulerLease`). | `apps/api/src/server.ts`, `security/route-permissions.ts`, `docs/api-contracts.md` §«Rutas retiradas» |
| L2-03 · motor genérico Prisma-only | `advanced-modules.service.ts` (≈600 líneas) despacha por `${moduleCode}:${recordType}` a `advanced-record-store.ts` (25 tablas propias, `select` explícito, `organizationId` + `propertyId`, zod estricto en `advanced-record-schemas.ts`, auditoría en cada escritura). Llaves móviles en `guest_portal_actions` (`mkey_<serial>`). Cuadros y salud por módulo retirados (→ `/dashboards/*`). | `apps/api/src/modules/advanced/*`, `modules/mobile-keys/wallet-pass.service.ts` |
| L2-04 · lo que vivía en memoria | `/notifications` sobre Prisma (por usuario **y** organización), `POST /offline/sync` persiste `offline_sync_records`, HITL (`ai_pending_confirmations`), setup de back office (`manual_setup_submissions`, `property_setup_form_submissions`, `property_setup_steps`, `module_health_checks`, `property_category_options`), ajustes SES / registro de viajeros en `compliance.service.ts`, remesas SEPA en `worker_job_runs` con ámbito. `demo-store.ts` 3.987 → 3.605 líneas, 115 → 89 claves. | `modules/backoffice/{setup,categories}.store.ts`, `modules/offline`, `modules/compliance` |
| L2-05 · paginación | Listas del front con > 200 filas potenciales con `take` + cursor y envelope `{ items, total, nextCursor }` (`lib/pagination.ts`), cabeceras `X-Total-Count` / `X-Next-Cursor`. | `lib/pagination.ts`, dashboards, motor |
| L2-06 · N+1 y catches | 17 bucles N+1 → consulta agregada; `createDegradedCollector` (`lib/degraded.ts`) en 15 servicios de KPI (`degraded: string[]` aditivo); money-path con errores tipados (`ACCOUNTING_LINK_FAILED` + `correlationId`). | `lib/degraded.ts`, `modules/accounting/projection.ts`, dashboards |
| L2-07 · worker honesto | Catálogo = 4 colas pg-boss reales (`notifications.scheduled`, `notifications.retry`, `notifications.sending-sweep`, `webhooks.deliver`); 88 nombres «completed sin hacer nada» retirados; cada tick escribe `worker_job_runs` (`jobs/job-runs.ts`); `handleJob` / `ADVANCED_WORKER_JOB_NAMES` fuera; tests del worker. | `apps/worker/src/*` |
| L2-08 · tests de integración | 12 suites `tests/integration/l2-*.test.mts` (motor genérico, persistencia plataforma/SES/back office, robustez, paginación, rutas API, módulos ×5) sobre organizaciones `org_l2_*` aisladas, `RBAC_STRICT=true`, auth real, sin unión demo, borradas al terminar; Faranda comparada antes/después. | `tests/integration/l2-*.test.mts`, `helpers/l2-tenant.mts` |

## 2. Copias de seguridad y proceso

- La migración `20260918130000_persistencia_l2` se aplicó en local el 2026-09-18 14:43:15 (checksum `1724980e…`) **sin** `pg_dump -Fc` previo: la última copia anterior era `~/anfitorio-demo/backups/hotelos-pre-rbac8a-20260918-122207.dump` (12:22, 11,6 MB, anterior a la tanda y a la migración). Incumplimiento de proceso (hallazgo DP-04); sin pérdida de datos verificable: los `DO $$` abortan si una tabla retirada tiene filas y las invariantes de Faranda son idénticas (§8).
- Ronda de corrección: copia previa **`~/anfitorio-demo/backups/hotelos-pre-l2-correccion-20260918-174934.dump`** (`pg_dump -Fc`, 16,3 MB, 17:49:34) antes de tocar nada. El corrector no ha borrado datos de Faranda; sí tres organizaciones de prueba residuales (§9).
- Migraciones: 14/14 aplicadas (`db:migrate:status` «Database schema is up to date»), `db:drift:check` «No difference detected», cliente Prisma regenerado, `db:install:check` en verde (§3).
- Integración final: copia previa **`~/anfitorio-demo/backups/hotelos-pre-l2-integracion-20260918-181652.dump`** (`pg_dump -Fc`, 16,7 MB, 18:17, 285 `TABLE DATA`) antes de la primera escritura por API; `db:migrate:status` 14/14 al día y `db:drift:check` «No difference detected» repetidos a las 18:17; `demo:refresh` en seco (`--json`, 0,6 s): `residual: []`, `errors: []`, inventario 2 organizaciones · 10 propiedades · 110 reservas en Rías Altas · 25 facturas con hash · 41 VeriFactu · 149 SES → no hay nada que borrar, así que `--apply` no se ejecutó (no perdería datos de ninguna pantalla del menú: las tablas del motor y de setup no figuran entre las que limpia).

## 3. Puertas (medidas tras la corrección, 2026-09-18 18:00-18:20)

| Puerta | Resultado | Antes de la corrección |
|---|---|---|
| `corepack pnpm run typecheck:all` | **15 PASS · 0 FAIL · 1 SKIP** (guest-web, explícito) · 19,7 s | igual |
| `corepack pnpm --filter @hotelos/api test` | **2.244 tests · 2.243 pass · 1 skipped · 0 fail** | 1 fail (api-reference «segments in Spanish») |
| `corepack pnpm --filter @hotelos/worker test` | **20/20** (18 + 2 nuevos de retención) | 18/18 |
| Front (`admin-web` `__tests__`) | **1.219/1.219** | igual |
| `node --test tests/*.test.mjs` (contratos raíz) | **532/532** | 523/532 (9 fail) |
| Integración completa `cd apps/api && node … --test "../../tests/integration/*.test.mts"` (46 ficheros) | **633 tests · 626 pass · 0 fail · 0 cancelados · 7 skips conocidos · 0 «too many clients»** · 14 s | 601 pass · 3 fail · 20 cancelados · 9 skips · 20 P2037 |
| `node scripts/build-nav-tree.mjs --check` | al día (68 ítems · 101 pestañas · 205 redirecciones) | igual |
| `node scripts/check-discoverability.mjs` | OK (placeholders 16/20) | igual |
| `node scripts/check-route-access.mjs` | OK (15 tokens × 192 URL) — existe pero no está en `package.json` | no ejecutado |
| `node scripts/check-role-smoke.mjs` | **no ejecutado a propósito**: rota la contraseña de cada usuario de demo (`POST /auth/change-password`) sobre Faranda y no está en `package.json` | no ejecutado |
| Cocoa 22: `cocoa-22-inventory.mjs` + `cocoa-22-waves.mjs --write` + `tests/cocoa-22-contract.test.mjs` | 226 pantallas · 193 puntos · 0 pendientes · **18/18** (`docs/design/COCOA-22-MIGRACION.md` §6 regenerado: recoge las pantallas de T8a `approvals/`, `users/`) | 18/18 |
| `corepack pnpm --filter @hotelos/admin-web build` | OK (2,5 s) | OK |
| `db:migrate:status` / `db:drift:check` | 14/14 al día · 0 drift | igual |
| `db:install:check` (`scripts/check-fresh-install.sh`, BD temporal) | **OK: 14 migraciones → 274 tablas, 1 organización, 79 permisos, sin drift, 4 funciones / 4 triggers declarados** | ERROR paso 7 (292 ≠ 274) |
| `rbac:sync -- --dry-run` | 250 claves · **+0 · 0 stale · 0 behind** (el «1 role behind» previo era el rol `owner` de una organización de prueba residual, §9) | +0 · 0 stale · 1 behind |
| Invariantes de Faranda (§8) | idénticas antes y después de cada suite y de la corrección | — |

### 3.1 Puertas repetidas por el integrador (2026-09-18 18:40-19:00, tras corregir el mapa de setup manual, §4 INT-01)

- `corepack pnpm run typecheck:all`: **15 PASS · 0 FAIL · 0 XFAIL · 1 SKIP** (guest-web) · 19,2 s. `corepack pnpm --filter @hotelos/product typecheck` limpio.
- `corepack pnpm --filter @hotelos/api test`: **2.244 tests · 2.243 pass · 1 skipped · 0 fail** · 679 suites · 10,6 s.
- `corepack pnpm --filter @hotelos/worker typecheck` limpio · `test` **20/20** (3 suites).
- `node --test tests/*.test.mjs` (contratos raíz): **532/532** · 112 suites · 1,8 s.
- Integración completa (`cd apps/api && node --env-file-if-exists=../../.env --import tsx --test "../../tests/integration/*.test.mts"`, 46 ficheros): **633 tests · 626 pass · 0 fail · 0 cancelados · 7 skipped** · 148 suites · 13,9 s · 0 «too many clients»; 0 organizaciones `org_l2_*`/`org_lr*` residuales después.
- `node scripts/check-discoverability.mjs`: 226 pantallas alcanzables · 192/192 URLs · 216 `SCREEN_COMPONENTS` · 24 alias (0 faltan) · 0 enlaces rotos · placeholders 16/20. `node scripts/build-nav-tree.mjs --check`: al día (68 ítems · 101 pestañas · 205 rutas legacy). `node scripts/check-route-access.mjs`: OK (15 tokens × 192 URLs).
- Cocoa 22: `cocoa-22-inventory.mjs` 226 pantallas · 193 puntos (`docs/design/cocoa-22-inventory.json` idéntico) · `cocoa-22-waves.mjs --write` 0 pendientes · 0 puntos · 0 lotes · `tests/cocoa-22-contract.test.mjs` **18/18**.
- `corepack pnpm --filter @hotelos/admin-web build`: 901 módulos · 2,53 s · 0 errores.
- `db:migrate:status` 14/14 al día · `db:drift:check` 0 · `corepack pnpm run db:install:check`: OK (14 migraciones → 274 tablas, 1 organización, 79 permisos, sin drift, 4 funciones / 4 triggers declarados, 4 s).
- `rbac:sync -- --dry-run`: 250 claves · +0 · 0 stale · 46 roles con plantilla · 0 topped up · plantilla v2 · 0 behind.
- Grep de rutas retiradas en `apps/admin-web/src`, `apps/guest-web/src`, `apps/mobile/src`, `apps/worker/src` y `packages/product/src` (35 patrones: `/ai-governance`, `/developer/webhooks`, `scenarios`, `automation-rules`, `saved-views`, `/crm/profiles`, `/procurement/suppliers`, `/inventory/properties/`, `labor-costs|forecast`, `/modules/:code/health`, `ai-draft-response`, `create-reservations`, `guest-self-service`, `session/:token/check-in|check-out|invoice-request|upsells`, `/compliance/authority/`, `ses-hospedajes/submissions`, `map-positions`, `property-map`, `/ai/suggestions`, `qr-codes`, `/analytics/query`, cuadros por módulo…): **0 llamadas** (2 coincidencias en `screens/PropertyMapper.tsx:166,363` son el nombre de un CSV de ejemplo y un id de comando, no rutas).

## 4. Ronda de corrección 1 — hallazgos y arreglos

Todos los hallazgos confirmados de la verificación adversarial quedan corregidos; los «low» baratos y seguros también. Cada fila indica fichero:línea del arreglo y cómo se ha verificado.

| Id | Hallazgo | Arreglo | Verificación |
|---|---|---|---|
| Puerta 2 / L2R-FC-05 | `GET /admin/worker/job-runs` hacía eco del segmento `job-runs` en inglés en la referencia de API | `SEGMENT_LABELS["job-runs"] = "la ejecución de trabajo programado"` (`apps/api/src/modules/developer/api-reference.service.ts:313`) → «Listar las ejecuciones de trabajo programado.» | `api-reference.test.mts` 10/10; api 2.243 pass |
| DP-01 | `db:install:check` contaba `CREATE TABLE` (292) sin restar los 18 `DROP TABLE` de L2 | `scripts/check-fresh-install.sh:84-88`: `EXPECTED_TABLES = CREATE − DROP`; `tests/migrations-squash-contract.test.mjs:60`: mismo criterio; `packages/database/scripts/adopt-baseline.mjs:151` (`tablesDroppedAfterBaseline`): la adopción no exige las tablas que una migración posterior retira (paso 8/8 fallaba con «schema-mismatch») | fresh-install OK 274 tablas; contratos 18/18 |
| DP-02 / L2R-FC-02 | 9 asertos de 5 contratos raíz pinaban símbolos retirados a propósito (`forecastOccupancy`, `ratePlans` en el servicio, `applyRevenueRecommendationToRateGrid`, `evaluateChannelSyncSafety`, `generateDailyRevenueForecasts`, `getHistoryForecastReport`, `generateRevenueDailySnapshots`, `generateDailyGuestRegisterBatch`, CREATE TABLE ≠ modelos) | Reescritos contra lo canónico: aplicación de recomendaciones en `modules/revenue/pricing.service.ts` (`rateDay.updateMany`, `REVENUE_RECOMMENDATION_APPLIED`), puerta de sincronización de canales en `modules/channel-manager/{readiness.core,aggregator.service}.ts`, cuadro H&F en `hf-board.service.ts` (`getHistoryForecastBoard`, `writeYesterdayDailySnapshotsForAllProperties`), scheduler SES en `server.ts` (`runDueSesSubmissions`), catálogo del worker = 4 colas de `scheduler.ts`; los espejos de revenue siguen pinados en `demo-store.ts` (semilla) y se exige que el servicio ya no los lea | `tests/{advanced-modules,revenue-channel-manager,revenue-history-forecast,spain-guest-register-compliance,migrations-squash}-contract.test.mjs` 35/35; raíz 532/532 |
| DP-03 / L2R-FC-04 | `l2-motor-generico.test.mts:413` esperaba 404 «Ruta retirada» y 400 en rutas que L2-02 borró | El caso exige el 404 genérico de Fastify en las dos rutas y que no figuren en el manifiesto (`tests/integration/l2-motor-generico.test.mts` «rutas retiradas (L2-02)»); stubs muertos `getAdvancedModuleHealth` / `getAdvancedModuleDashboard` eliminados (`advanced-modules.service.ts:474`) | suite 12/12 |
| DP-04 | Migración destructiva sin `pg_dump` previo y sin informe | Copia `hotelos-pre-l2-correccion-20260918-174934.dump` (16,3 MB) antes de la corrección; este informe (§2) | `ls -la ~/anfitorio-demo/backups/` |
| DP-06 | `worker_job_runs` crecía sin límite (≈3.312 filas/día, sin purga) | Retención por cola: `apps/worker/src/jobs/job-runs.ts:141` `pruneJobRuns` (solo `completed`/`failed`, solo su `jobName`; nunca `running` ni `treasury.sepa_remittance`) y `scheduler.ts:81-100` (`jobRunRetentionDays`, `pruneJobRunsIfDue` tras cada tick, ≤ 1 vez/hora/cola; `WORKER_JOB_RUN_RETENTION_DAYS` = 7, fallidos 4×; variable declarada en `lib/env.ts:701`, `scripts/env-contract.json`, `.env.example`, `deploy/.env.production.example`) | worker 20/20 (2 casos nuevos); `tests/env-contract.test.mjs` 9/9 |
| SEC-L2-01 / L2R-FC-01 | Resolver `mobileKey` leía `advanced_records` (tabla inexistente) → verify/revoke 500; `revokeWalletPass` sin filtro de tenant | `apps/api/src/lib/tenancy.ts:611`: `guestPortalAction.findFirst({ id: mkey_<serial>, actionType: "mobile_key" })`; `wallet-pass.service.ts:327-335`: `revokeWalletPass` exige y filtra por `propertyId`; `server.ts:2313`: `assertPropertyEntityAccess` y pasa la propiedad resuelta; `skip` retirado en `l2-modulos-plataforma.test.mts` | suite 22/22 (antes 21 + 1 skip); repro del revisor: verify/revoke de serial inexistente → **404** «Llave móvil no encontrada.» |
| SEC-L2-03 | 8 rutas por id ejecutaban en la propiedad de la cabecera `x-property-id`, no en la de la entidad → 404 para un usuario con acceso a A y B | `server.ts:3059` (`generate-beo`), `:3071` (`shifts/:id`), `:3084` (`absences/:id`), `:3257` (`quality/cases/:id`), `:3268` (`surveys/:id/responses`), `:3290` (`safety/incidents/:id`), `:3294` (`…/evidence`), `:3305` (`safety/checks/:id/results`): `assertPropertyEntityAccess` y `propertyId` de la fila (las 3 de CRM por id siguen con la cabecera: son de organización) | caso nuevo «SEC-L2-03» en `l2-motor-generico.test.mts` (manager de A y B, cabecera A, incidente de B → 200 y la fila sigue en B; manager solo de A → 404); repro R2 → 200 |
| SEC-L2-04 | `POST /surveys/:id/responses`, `/safety/incidents/:id/evidence`, `/safety/checks/:id/results` ignoraban el `:id` del path (padre desde el cuerpo; sin él → 400) | El padre es SIEMPRE el id del path: `payload: { ...requireObjectBody(body), surveyId | incidentId | safetyCheckId: params.id }` (`server.ts:3268-3307`); `generate-beo` pasa a «el path gana» (`{ ...body, eventId }`) | casos nuevos (path ajeno → 404 y nada escrito; id ajeno en el cuerpo ignorado; cuerpo sin padre → 200); repro R3: `payload.surveyId === S2` → false |
| SEC-L2-05 (low) | Escrituras del motor tomaban `organizationId` del contexto: un admin de plataforma creaba filas org-scoped huérfanas | `advanced-modules.service.ts:524` `scopeOf` async: organización de la PROPIEDAD (`organizationOfProperty`); otra organización sin plataforma → 404 opaco; auditoría con esa organización (`:433`) | repro R5: fila `{organizationId: org de la propiedad}` visible en la lista → true; motor 12/12; api 2.243 |
| SEC-L2-06 (low) | `GET /health` (pública) exponía `holder_id` = `hostname:pid` del líder | `server.ts:1739`: `schedulers: { leader, held, thisInstance, expiresAt }` (sin `holder`); `docs/api-contracts.md` actualizado | api 2.243; ningún consumidor leía `holder` |
| DP-08 (low) | N+1 residual en el cierre nocturno: un `import()` y un `findUnique` por línea posteada | `night-audit.service.ts:686-712`: un `import` por lote y un `findMany` tras el enrutado (mapa por id) | api 2.243; integración 626 pass |
| DP-11 (low) | La «instancia nueva» de las suites L2 es `buildApiServer()` en el mismo proceso | Documentado en las cabeceras de `l2-motor-generico`, `l2-persistencia-ses` y `l2-persistencia-backoffice`: prueba la lectura desde Prisma; el reinicio real lo hace el integrador con `:3901` | — |
| L2R-FC-03 | La puerta completa (46 ficheros en paralelo) agotaba `max_connections = 100` (P2037 en `ledger-import` y `l2-robustez`) | `tests/integration/helpers/l2-tenant.mts:51-61`: las 12 suites L2 limitan su pool de Prisma con `connection_limit=4` en `DATABASE_URL` (`L2_TEST_CONNECTION_LIMIT`; no toca una URL que ya lo fija). Causa: cada proceso abría hasta 2·CPU+1 = 25 conexiones × 11 procesos | puerta completa: 633 · 626 pass · 0 fail · 0 cancelados · 0 «too many clients» |
| L2R-FC-06 | `manual-setup-route-map.ts` servía al front 4 rutas retiradas y 2 tablas borradas | `packages/product/src/navigation/manual-setup-route-map.ts`: `revenue_rate_plans` sin `revenue_automation_rules`; `history_forecast` → `…/history-forecast/export` y sin `revenue_report_views`; `revenue_recommendation_rules` → `…/recommendations` + `…/pricing-rules` (`pricing_rules`, `revenue_recommendations`); `ai_governance` → `/ai/governance/policies`; `guest_portal_online_checkin` → `/backoffice/properties/:propertyId/configuration` + `/properties/:propertyId/upsell-offers` | `grep -nE 'saved-views\|automation-rules\|/ai-governance/\|/guest-self-service/\|revenue_scenarios\|revenue_automation_rules\|revenue_report_views'` → solo comentarios; product tsc OK; admin-web build OK |
| L2R-FC-07 | Faltaba este informe; `CLAUDE.md` declaraba puertas en rojo como verificadas | Este fichero; bloque «Estado verificado» de `CLAUDE.md` reescrito con las cifras de §3 | — |
| L2R-FC-08 (low) | Runbook OPERA sombra citaba `isSchedulerLeader` / `server.ts:8467` | `docs/runbooks/opera-modo-sombra.md` §10: lease `scheduler_leases` renovado en cada vuelta (`holdsSchedulerLease`) | — |
| structure-e2e:1009 | 63 ≠ 61 asientos previos en Faranda | Línea base actualizada a **63**: los asientos 2393 `payment` y 2394 `payment_refund` (cobro de 480,00 € y su devolución en Rías Altas, 2026-09-18 10:49, folio `cmu4t44a5004rfyu07u4stv2l`) son el caso provocado §7.1 de `TANDA-8A-RBAC-2026-09-18.md`; el saldo de 4300 (379,00) no cambia (`tests/integration/structure-e2e.test.mts:1013`) | suite 29/29 |
| INT-01 (integrador · media) | `packages/product/src/navigation/manual-setup-route-map.ts` (servido al front por `GET /backoffice/properties/:propertyId/manual-setup/options`) declaraba 7 endpoints que no existen en el manifiesto: `/ai/governance/policies` ×2 (la «canónica» que fijó FC-06 no existe: la gobernanza IA vive en `/ai-operations/governance/{policies,prompts,evaluations,incidents,cost}`, `server.ts:7997-8168`; `GET /ai/governance/policies` → 404 como Carmen), `/modules`, `/integrations/properties/:propertyId/providers` ×2, `/integrations/properties/:propertyId/connect` ×2, `/revenue/properties/:propertyId/rate-plans` ×2 (preexistentes) | Repuntados a `/ai-operations/governance/policies` (`:729-730`, comentario `:728`), `/modules/catalog` (`:376`), `/integrations/providers` (`:393`, `:609`), `/backoffice/properties/:propertyId/integrations/:providerCode/connect` (`:394`, `:610`), `/properties/:propertyId/rate-plans` (`:410-411`); `docs/api-contracts.md:450` corrige la canónica | script ad hoc: 40 endpoints del mapa, **0 fuera del manifiesto** (antes 5 distintos); product tsc, admin-web build, contratos 532/532 |
| INT-02 (integrador · nota T8a) | Carmen (`owner` + `general_manager`) no tiene claves de EJECUCIÓN del motor: 25 de 36 escrituras → 403 (`workforce.schedule.manage`, `workforce.timeclock.use`, `incidents.manage`, `safety_checks.manage`, `quality_cases.manage`, `surveys.manage`, `crm.manage_profiles|campaigns|loyalty`, `events.manage`, `purchase_orders.create`, `energy.manage`, `sustainability.report`, `guest_register.configure`, `revenue.configure`, `pms.checkin.execute`) | Por diseño de T8a (dirección aprueba, no ejecuta): las escrituras de §7.2 las hacen los usuarios de departamento (`direccion.rias` manager · RA, `jefatura.recepcion.rias`, `revenue`, `direccion.tilos`); Carmen escribe lo que su plantilla permite y lee todo con 200 | matriz §7.3 |
| INT-03 (integrador · nota) | Ninguno de los 8 módulos del motor (`workforce_labor`, `safety_incident_management`, `reputation_quality`, `guest_data_crm_loyalty`, `groups_events_sales`, `procurement_inventory`, `energy_sustainability`, `hotel_intelligence_platform`) está activado en ningún hotel de Faranda (`property_modules`: 6 activados por hotel; `DEFAULT_ENABLED_MODULE_CODES` no los incluye) → sus 16 rutas de lista responden 403 «El módulo X no está activado en esta propiedad.» y sus entradas de menú quedan ocultas por `modulesAny` | Para la prueba se activaron en Rías Altas por API y se desactivaron al terminar (§7.5); activarlos de verdad es decisión de César (§10.10) | — |
| INT-04 (integrador · nota) | HITL del check-in por voz no verificable en `:3901` con los datos de la demo: `POST /ai/commands/check-in-from-scan` resuelve al huésped por el documento y los `document_number` de la demo son NULL desde la pérdida de `ENCRYPTION_KEY` (2026-07-15): RES-00008 de Rías Altas → 404 «El huésped no tiene ninguna reserva abierta en esta propiedad.»; `guest_maria`/RES-18392 de org_123 → 404 «No se ha encontrado ningún huésped que coincida con el documento.» | La persistencia de `ai_pending_confirmations` / `ai_tool_calls` queda cubierta por `l2-persistencia-plataforma` (22/22: confirmación → fila `pending` → ejecución → `executed`, réplica → 409) y `l2-modulos-ia`; `GET /ai/tool-calls` responde 200 a Carmen tras el reinicio con las filas históricas de Faranda | — |
| INT-05 (integrador · info) | `pnpm-lock.yaml` aparece modificado (52 líneas) pero su mtime es 2026-09-16 05:32: es la regeneración de la deuda 12(a) de Tanda 4 (`@fontsource-variable/inter`, `zod`, `@playwright/test`, `qrcode-terminal`), no de esta tanda | Sin tocar (el brief lo prohíbe); §10.7 corrige la atribución | `ls -la pnpm-lock.yaml` |
| INT-06 (integrador · info) | El brief asume que `:3000` sirve el código anterior; a las 18:16 no había proceso escuchando (`curl -m 3 /health` → 000) | Nada que hacer aquí: lo arranca el orquestador con este working tree | `lsof -iTCP:3000 -sTCP:LISTEN` vacío |

Repro del revisor (`scratchpad/l2-sec-review-repro.mts`, en proceso, organización aislada borrada al terminar) tras la corrección: R1 verify/revoke → 404 tipado; R2 PATCH con cabecera B sobre entidad de A → 200; R3 respuesta sin `surveyId` → 200, `surveyId` ajeno en el cuerpo ignorado, `incidentId` ajeno ignorado; R5 fila con la organización de la propiedad y visible; Faranda idéntica; 0 `org_l2_*` residuales.

## 5. Tablas

| Estado | Tablas |
|---|---|
| Retiradas (18; 0 filas, sin lector ni escritor, fuera de todo flujo fiscal) | `onboarding_migration_batch_records`, `onboarding_migration_batches`, `onboarding_mapping_suggestions`, `onboarding_extracted_entities`, `onboarding_files`, `onboarding_source_connections`, `ai_onboarding_runs`, `room_feature_assignments`, `room_beds`, `reservation_resources`, `property_map_positions`, `property_imports`, `backoffice_ai_suggestions`, `revenue_report_views`, `revenue_scenarios`, `revenue_automation_rules`, `api_usage_logs`, `module_dependencies` |
| Nuevas (2) | `scheduler_leases` (líder de schedulers), `ai_pending_confirmations` (HITL del check-in) |
| Cableadas (escritor y lector reales) | `notifications` (+`organization_id`), `offline_sync_records` (+`organization_id`), `worker_job_runs` (+`organization_id`, `property_id`; retención por cola desde la corrección), `ai_tool_calls`, `property_setup_steps`, `property_readiness_checks`, `module_health_checks`, `manual_setup_submissions`, `property_setup_form_submissions`, `property_category_options`, `guest_portal_actions` (llaves móviles), ajustes SES / registro de viajeros, las 25 tablas propias del motor genérico (`shifts`, `time_clock_entries`, `absence_requests`, `safety_incidents`, `incident_evidence`, `safety_checks`, `safety_check_results`, `guest_reviews`, `quality_cases`, `surveys`, `survey_responses`, `crm_segments`, `crm_campaigns`, `loyalty_programs`, `loyalty_memberships`, `events`, `event_orders`, `purchase_orders`, `purchase_order_lines`, `utility_meters`, `utility_readings`, `sustainability_actions`, `metric_definitions`, `anomaly_events`, `scheduled_reports`) |
| Conservadas con motivo | `onboarding_projects` (resolver `onboardingProject`), `invoice_sequences.year` con CHECK en vez de NOT NULL |
| Índices únicos parciales (14) | `revenue_daily_snapshots_top_level_key`, `revenue_forecast_snapshots_top_level_key`, `forecast_accuracy_no_segment_key`, `fiscal_periods_organization_key`, `fiscal_years_organization_key`, `payroll_periods_organization_key`, `notification_templates_organization_key`, `user_role_assignments_live_property_key`, `…_live_property_group_key`, `…_live_legal_entity_key`, `…_live_organization_key`, `exchange_rates_global_key`, `esrs_indicators_organization_key`, `app_installations_organization_key` |

Recuento: 292 `CREATE TABLE` − 18 `DROP TABLE` en la cadena = **274** tablas = 274 modelos de `schema.prisma` (`db:install:check`, `migrations-squash-contract`, `check-migrations-vs-schema.mjs`).

## 6. Rutas

- **Retiradas: 82** (detalle por familia en `docs/api-contracts.md` § «Rutas retiradas»): `/ai-governance/*` (15) → `/ai/governance/*`; `/developer/webhooks*` (5) + `GET /developer/apps/:id/usage` → `/webhooks/subscriptions*`; `/revenue/recommendations/:id/apply|approve|reject` (3) → `/revenue/properties/:propertyId/recommendations/:id/…`; motor en memoria sin tabla (cuadros/salud/métricas/escenarios/reglas/vistas guardadas/perfiles CRM/proveedores/inventario A&B/labor-costs|forecast/`ai-draft-response`/`create-reservations`); portal del huésped scaffold (`check-in`, `check-out`, `invoice-request`, `upsells`, `guest-self-service/settings`); envíos a autoridad F1 (4) y F2 (2) → F3 `/properties/:propertyId/ses/submissions`; back office en memoria (10). Todas responden el 404 genérico del router, nunca el 403 del manifiesto.
- **Añadida: 1** — `GET /admin/worker/job-runs` (`admin.tenants.manage` + `requirePlatformAdmin`).
- Manifiesto = rutas registradas = **935** (`tests/api-route-permissions-contract.test.mjs`).
- Consumidores: ningún front llama a una ruta retirada (`l2-rutas-api.test.mts`, build del admin-web); el único consumidor que quedó con rutas retiradas (`manual-setup-route-map.ts`, servido por `GET /backoffice/properties/:propertyId/manual-setup/options`) se corrigió en esta ronda (§4).
- Rutas por id del motor (corrección): la propiedad de la entidad gana a la cabecera; el padre de una fila hija es el id del path.

## 7. Verificación de persistencia y aislamiento

### 7.1 Suites de la tanda (`buildApiServer()` en proceso, organizaciones aisladas)

- Motor genérico (`l2-motor-generico`, 12/12): por familia POST → GET (`items`) → PATCH → fila en Prisma → `buildApiServer()` nuevo la sigue leyendo; organización B no ve nada (lista vacía, transición 404); SoD del pedido de compra; CRM con la organización del usuario (caso Carmen); evento/BEO; reseña una sola vez; rutas retiradas → 404 genérico; casos nuevos SEC-L2-03/04.
- Plataforma (`l2-modulos-plataforma`, 22/22): admin-console, GDPR, ESRS, reporting, search, offline, mobile-keys (emisión, verify con firma del QR, firma incorrecta → `bad_signature`, revocación por recepción de B → 404, por recepción de A → `revoked`).
- Resto de suites L2 (persistencia plataforma/SES/back office, robustez, paginación, rutas API, módulos operaciones/comercial/IA/cumplimiento): en la puerta completa 626 pass · 0 fail.
- Matriz rápida de T8a (Carmen 200 · `recepcion.tilos` 403 en finanzas y 404 en Rías Altas) sobre rutas nuevas/cambiadas: cubierta por `l2-robustez` (33 `/dashboards/*`, receptionist 200/403 exactos según manifiesto, 404 opaco en hotel B y en propiedad de otra organización) y `l2-modulos-*` (3 casos por módulo: con ámbito / 403 sin clave / 404 en propiedad ajena) con usuarios de plantilla equivalentes en organizaciones aisladas.
- Reinicio real del API (`:3901`, criterio §4 «reinicio no pierde datos de ninguna pantalla del menú»): las suites lo aproximan con `buildApiServer()` en el mismo proceso (DP-11, documentado); la verificación con procesos distintos es §7.2.

### 7.2 Integrador · reinicio real del API en `:3901` (2026-09-18 18:18-18:50)

Instancia propia `PORT=3901 RUN_SCHEDULERS=false RBAC_STRICT=true` con el `.env` local (`node --import tsx src/server.ts`): proceso A (pid 44022) recibe las escrituras → se mata → proceso B (45632) sirve las lecturas → C (45703) y D (45827) para el intento de HITL y la matriz; `/health` de cada uno `schedulers: { leader: "env", held: false, thisInstance: false }`. Criterio: escribir por API → leer por SQL (`psql`) → matar la instancia y arrancar otra → leer de nuevo por API con Carmen. Sesiones reales (`POST /auth/login`) de Carmen, `direccion.rias`, `jefatura.recepcion.rias`, `revenue`, `direccion.tilos`, `recepcion.rias`, `recepcion.tilos`, `sistemas` y `reception@example.com`. Guion `scratchpad/persist-l2.mjs` (fases `write` / `read`), resultados `persist-l2-write*.json`, `persist-l2-read.json`, `verify-run2.out`.

Prerrequisito: activar en Rías Altas los 8 módulos del motor (INT-03) — `PATCH /backoffice/properties/:id/modules/:code {action:"enable"}` como Carmen (`modules.configure`): el manifiesto exige antes sus dependencias (409 `MODULE_DEPENDENCIES_MISSING`: `maintenance`, `payment_vault`, `ai_concierge`, `asset_intelligence`, `owner_mode`), 13 activaciones en total, todas 200.

| Tipo (`moduleCode:entityType`) | Escritura (usuario) | Tabla destino (Prisma) | SQL | API tras reinicio (Carmen) |
|---|---|---|---|---|
| `workforce_labor:shift` + transición `confirmed` | `POST /workforce/properties/RA/shifts`, `PATCH /workforce/shifts/:id` (direccion.rias) | `shifts` (`property_id`) | fila `shift_06df6a38`, status `confirmed` | `GET …/schedule` → 200, en `items` |
| `workforce_labor:time_clock_entry` | `POST /workforce/time-clock/clock-in` | `time_clock_entries` | `time_clock_entry_7b58eab9` | `GET …/time-clock` → en `items` |
| `workforce_labor:absence_request` + `approved` | `POST /workforce/absences`, `PATCH /workforce/absences/:id` | `absence_requests` | `absence_request_29686174`, `approved` | (sin ruta de lista; SQL) |
| `safety_incident_management:safety_incident` + `in_progress` | `POST /safety/properties/RA/incidents`, `PATCH /safety/incidents/:id` | `safety_incidents` | `safety_incident_33dbce43`, `in_progress` | `GET …/incidents` → en `items` |
| `…:incident_evidence` | `POST /safety/incidents/:id/evidence` | `incident_evidence` (padre por path) | `incident_evidence_caeabf6b` | (hija; SQL) |
| `…:safety_check` / `safety_check_result` | `POST /safety/properties/RA/checks`, `POST /safety/checks/:id/results` | `safety_checks`, `safety_check_results` | `safety_check_749c4318`, `safety_check_result_6e6018ac` | `GET …/checks` → en `items` |
| `reputation_quality:quality_case` + `resolved` | `POST /quality/properties/RA/cases`, `PATCH /quality/cases/:id` | `quality_cases` | `quality_case_b9726082`, `resolved` | `GET …/cases` → en `items` |
| `reputation_quality:survey` / `survey_response` | `POST /surveys/properties/RA`, `POST /surveys/:id/responses` | `surveys`, `survey_responses` | `survey_40101f82`, `survey_response_e45989dd` | `GET /surveys/properties/RA` → en `items` |
| `guest_data_crm_loyalty:crm_segment` (+ PATCH) | `POST /crm/segments`, `PATCH /crm/segments/:id` (cabecera `x-property-id` RA) | `crm_segments` (`organization_id`) | `crm_segment_1128e888` | `GET /crm/segments` → en `items` |
| `…:crm_campaign` + `scheduled` | `POST /crm/campaigns`, `PATCH /crm/campaigns/:id` | `crm_campaigns` | `crm_campaign_8a7a8e27`, `scheduled` | `GET /crm/campaigns` → en `items` |
| `…:loyalty_program` | `POST /crm/loyalty/programs` | `loyalty_programs` | `loyalty_program_f31381b6` | `GET /crm/loyalty` → en `items` |
| `groups_events_sales` evento + `event_order` (BEO) | `POST /events/properties/RA/events`, `POST /events/:id/generate-beo` | `events`, `event_orders` | `cmu76av7u000tfyyuaycvore0`, `event_order_cbf9f35b` | `GET …/calendar?from&to` → en `items` |
| `procurement_inventory:purchase_order` + `approved` (SoD) | `POST /procurement/properties/RA/purchase-orders` (jefatura.recepcion.rias, `purchase_orders.create`), `POST …/:id/approve` (direccion.rias, `purchase_orders.approve`) | `purchase_orders`, `purchase_order_lines` | `purchase_order_15426970` `approved`, 1 línea | `GET …/purchase-orders` → en `items` |
| `energy_sustainability:utility_meter` / `utility_reading` / `sustainability_action` | `POST /energy/properties/RA/meters`, `…/readings`, `POST /sustainability/properties/RA/actions` | `utility_meters`, `utility_readings`, `sustainability_actions` | `utility_meter_f0a4042b`, `utility_reading_951fbe06`, `sustainability_action_34a32f93` | `GET …/meters` → en `items` |
| `hotel_intelligence_platform:metric_definition` / `scheduled_report` | `POST /analytics/metrics`, `POST /analytics/properties/RA/reports` (Carmen: `metrics.manage`, `analytics.configure`) | `metric_definitions` (`organization_id`), `scheduled_reports` | `metric_definition_14de77f5`, `scheduled_report_9b934df5` | `GET …/metrics`, `GET …/reports` → en `items` |
| Offline | `POST /offline/sync` (Carmen; 1 acción `voice.command.draft`) | `offline_sync_records` (`organization_id` + `property_id`) | `action_json->>'id' = act_20260918183500`, `organization_id` = Faranda | `GET /properties/RA/offline-sync-records` → fila presente |
| SES / registro de viajeros (Los Tilos, SES desactivado en el piloto) | `PATCH /compliance/spain/properties/LT/guest-register/settings { configurationJson: { integradorL2 } }` (direccion.tilos: `guest_register.configure` + `compliance.ses.configure`) | `authority_reporting_settings` (antes `reporting.persisted: false`, `legalProfile.source: derived`) | fila con `configuration_json->>'integradorL2'` | `GET …/settings` → `reporting.persisted: true`, clave presente |
| Setup de back office (Los Tilos) | `POST …/configuration/categories/room_features/options`, `POST …/configuration/custom-fields` (`fieldKey`, `dataType`), `POST …/manual-setup/reservation_reporting`, `PATCH …/setup/organization_details { status: not_started, metadataJson }` (Carmen) | `property_category_options`, `property_custom_field_definitions`, `manual_setup_submissions`, `property_setup_steps` | `cmu76avab000wfyyuflp64jf7`, `cmu76avah000yfyyusyrccrns`, `cmu76avam000zfyyuvo5ojmr8`, `metadata_json->>'integradorL2'` | `GET …/categories/room_features` (opción en `options`), `GET …/custom-fields`, `GET …/manual-setup/reservation_reporting` (submission), `GET …/setup` (metadata) |
| Revenue (regla de precios, RA) | `POST /revenue/properties/RA/pricing-rules` (`revenue`: `revenue.configure`; `active: false`, prioridad 999) | `pricing_rules` | `cmu76avb2001gfyyurqkdpcxp` | `GET …/pricing-rules` → en la lista |
| Notificaciones | fila insertada por SQL para Carmen (ningún flujo de dominio crea `Notification`, §10.5) → `GET /notifications` la lista → `POST /notifications/:id/read` (Carmen) | `notifications` (`organization_id` Faranda) | `status = read` | `GET /notifications` → `status: "read"` |

Resultado: **36 escrituras 200** en la pasada 2 (27 + 6 transiciones del motor + offline + SES + 4 de setup + regla de precios + lectura de notificación), 36/36 filas encontradas por SQL con la propiedad/organización esperada, **22/22 relecturas por API con Carmen (200) desde la instancia B**. La pasada 1 (todo como Carmen) dejó 9 escrituras 200 y 27 rechazos 403/400 (INT-02; el 400 del campo personalizado era `code`/`fieldType` en vez de `fieldKey`/`dataType`). Tipos sin ruta de creación (`guest_review` → `respond`, `anomaly_event`, `loyalty_membership`) y HITL (INT-04) quedan cubiertos por las suites de L2-03/L2-08. `GET /admin/worker/job-runs`: 200 solo para `reception@example.com` (plataforma; `items` con `organizationId: null` de las 4 colas), 403 para Carmen, `sistemas` y `recepcion.tilos`.

Tenencia por tabla (columnas reales, `information_schema`): llevan `organization_id` **y** `property_id` `notifications`, `offline_sync_records`, `worker_job_runs`, `ai_pending_confirmations`, `ai_tool_calls`, `anomaly_events`, `scheduled_reports`; solo `organization_id` (recursos de organización) `crm_segments`, `crm_campaigns`, `loyalty_programs`, `metric_definitions`; solo `property_id` (la organización se deriva de la propiedad con `organizationOfProperty`) `shifts`, `time_clock_entries`, `absence_requests`, `safety_incidents`, `safety_checks`, `quality_cases`, `surveys`, `guest_reviews`, `events`, `purchase_orders`, `utility_meters`, `utility_readings`, `sustainability_actions`, `pricing_rules`, `authority_reporting_settings`, `lodging_legal_profiles`, `property_setup_steps`, `manual_setup_submissions`, `property_category_options`, `property_custom_field_definitions`, `guest_portal_actions`; ninguna de las dos (hijas acotadas por su padre) `incident_evidence`, `safety_check_results`, `survey_responses`, `event_orders`, `purchase_order_lines`, `loyalty_memberships` (§10.12).

### 7.3 Matriz de acceso en `:3901` (instancia D, rutas nuevas o cambiadas)

| Ruta | Carmen (RA) | `recepcion.tilos` (LT) | `sistemas` (RA) | plataforma |
|---|---|---|---|---|
| `GET /workforce/properties/RA/schedule`, `/safety/…/incidents`, `/quality/…/cases`, `/crm/segments`, `/procurement/…/purchase-orders`, `/energy/…/meters`, `/analytics/…/metrics` | 200 (envelope `items/total/nextCursor`) | **404** «Propiedad no encontrada.» (RA fuera de su ámbito) | 403 (sin clave de lectura) | — |
| `GET /notifications` | 200 (su fila) | 200 `[]` | 200 `[]` | — |
| `GET /properties/RA/offline-sync-records` · `/properties/LT/…` | 200 | **404** · 200 `[]` | — | — |
| `GET /compliance/spain/properties/RA/guest-register/settings` · `/LT/…` | 200 | **404** · 200 | — | — |
| `GET /backoffice/properties/RA/setup` · `…/configuration/categories` (LT) | 200 | **404** · 200 | 403 | — |
| `GET /revenue/properties/RA/pricing-rules` · `/LT/…` | 200 | — · 200 `[]` | — | — |
| `GET /properties/RA/ses/submissions` (canónica F3) | 200 | **404** | — | — |
| `GET /ai/tool-calls`, `GET /events` | 200 | — | 200 | — |
| `GET /accounting/journal`, `/fiscal/vat-books` (sin `book` → 400 tipado), `/payroll/cost-imports` (finanzas) | 200 / 400 / 200 | **403** ×3 | 403 | — |
| `GET /admin/worker/job-runs` | 403 | 403 | 403 | **200** (`reception@example.com`, org_123) |
| `GET /backoffice/properties/RA/modules` | 200 | — | 200 | — |
| `GET /webhooks/subscriptions` (canónica de `/developer/webhooks`) | 403 (`developer.manage_webhooks` no está en `owner`) | — | — | — |
| `GET /revenue/properties/RA/recommendations` (canónica de `/revenue/recommendations/:id/*`) | 200 | — | — | — |

### 7.4 Rutas retiradas (14 sondas como Carmen, instancia D)

`/ai-governance/policies`, `/developer/webhooks`, `/revenue/properties/RA/scenarios`, `…/automation-rules`, `…/history-forecast/saved-views`, `/crm/profiles`, `/procurement/suppliers`, `/inventory/properties/RA/items`, `/workforce/properties/RA/labor-costs`, `/advanced/properties/RA/modules/workforce_labor/health`, `/guest-self-service/properties/RA/settings`, `/compliance/authority/inbox`, `/backoffice/properties/RA/qr-codes`, `/backoffice/properties/RA/map-positions` → **404 «Route GET … not found»** (genérico del router, nunca 403 del manifiesto). Canónicas comprobadas en §7.3; `GET /ai/governance/policies` también responde 404 porque la canónica real es `/ai-operations/governance/*` (INT-01).

### 7.5 Limpieza de la integración (19:00; copia previa §2)

1. Módulos: `PATCH …/modules/:code {action:"disable"}` ×13 en Rías Altas (200) y `DELETE FROM property_modules` de las 12 filas creadas por la prueba (`guest_data_crm_loyalty`, `payment_vault`, `ai_concierge`, `asset_intelligence`, `owner_mode`, `workforce_labor`, `safety_incident_management`, `reputation_quality`, `groups_events_sales`, `procurement_inventory`, `energy_sustainability`, `hotel_intelligence_platform`): RA vuelve a sus 8 filas (6 `enabled` de julio + `maintenance` y `ai_front_desk` `disabled`; `pm_e4edc075` maintenance conserva `disabled` con `enabled_at`/`disabled_at` de hoy).
2. Filas de la prueba (`scratchpad/cleanup.sql`, una transacción): 31 `DELETE … WHERE id = …` (pasadas 1 y 2: hijas antes que padres), 2 `offline_sync_records`, la notificación `ntf_l2_integrador_20260918`, `UPDATE property_setup_steps SET metadata_json = '{}'` (`organization_details` de LT, status `not_started` sin cambio), `DELETE FROM authority_reporting_settings` de LT (antes no existía: `persisted: false`; `lodging_legal_profiles` sin fila nueva).
3. Comprobación: 17 recuentos de residuos a 0 (`shifts`/`safety_incidents`/`time_clock_entries` de RA, `crm_segments` de Faranda, `events`/`pricing_rules`/`purchase_order_lines` con «integrador-l2», `offline_sync_records` de RA, opciones y campos `l2_2026…`, `manual_setup_submissions` y `authority_reporting_settings` de LT, `metadata_json ? 'integradorL2'`, `metric_code like 'l2_2026%'`, organizaciones residuales); invariantes de Faranda idénticas (§8). `audit_events` y `event_stream` conservan los eventos de la prueba (cadena hash global, por diseño; Faranda suma 407 eventos entre las 16:53 y las 18:38 con los de la corrección y la integración).
4. `kill` de la instancia D → `lsof -nP -iTCP:3901 -sTCP:LISTEN` → 0. `:3000` sin tocar.

## 8. SQL de verificación e invariantes de Faranda (solo lectura, `psql $DATABASE_URL -Atc`)

```sql
-- O = 'cmrhw9jy30002fyvb6tsdiugt' (Faranda)
select count(*) from journal_entries where organization_id = O;                                   -- 4951
select count(*) from invoices i join properties p on p.id = i.property_id where p.organization_id = O;   -- 25
select count(*) from verifactu_submissions v join invoices i on i.id = v.invoice_id
  join properties p on p.id = i.property_id where p.organization_id = O;                          -- 33
select count(*) from ledger_imports where organization_id = O;                                    -- 34
select count(*) from payroll_cost_imports where organization_id = O;                              -- 1
select count(*) from reservation_imports r join properties p on p.id = r.property_id where p.organization_id = O;        -- 7
select count(*) from pms_shadow_revenue_imports r join properties p on p.id = r.property_id where p.organization_id = O; -- 2 (9 pms_shadow_runs)
select count(*) from reservations r join properties p on p.id = r.property_id where p.organization_id = O;               -- 110
select count(*) from permissions;                                                                 -- 250
select count(distinct template_key) from roles where organization_id = O and template_key is not null;   -- 24
select count(*) from user_role_assignments where organization_id = O and revoked_at is null;      -- 31 (+1 revocada = 32 filas: la temporal de emergencia de T8a)
select to_regclass('public.advanced_records');                                                    -- NULL (nunca existió)
select count(*) from organizations where id like 'org_l2_%' or id like 'org_lr%';                 -- 0
select finished_at, checksum from _prisma_migrations where migration_name = '20260918130000_persistencia_l2';
-- Integrador (§7.5): residuos de la prueba en :3901 → todos 0
select count(*) from shifts where property_id = 'cmrhw9jy40003fyvbuu2ec2w7';                       -- 0 (idem safety_incidents, time_clock_entries)
select count(*) from crm_segments where organization_id = O;                                      -- 0
select count(*) from events where name like '%integrador-l2%';                                    -- 0 (idem pricing_rules, purchase_order_lines)
select count(*) from offline_sync_records where property_id = 'cmrhw9jy40003fyvbuu2ec2w7';         -- 0
select count(*) from notifications where id = 'ntf_l2_integrador_20260918';                       -- 0
select count(*) from property_category_options where code like 'l2_2026%';                        -- 0 (idem property_custom_field_definitions.field_key)
select count(*) from manual_setup_submissions where property_id = 'cmu1mifcp0000fyo1wzvq7txo';     -- 0
select count(*) from authority_reporting_settings where property_id = 'cmu1mifcp0000fyo1wzvq7txo'; -- 0 (Los Tilos sigue «derivado», como antes)
select count(*) from property_setup_steps where metadata_json ? 'integradorL2';                  -- 0
select count(*) from metric_definitions where metric_code like 'l2_2026%';                        -- 0
select count(*) from property_modules where property_id = 'cmrhw9jy40003fyvbuu2ec2w7';             -- 8 (6 enabled + maintenance, ai_front_desk disabled)
select count(*) from pg_tables where schemaname = 'public';                                       -- 275 = 274 + _prisma_migrations
```

Invariantes leídas por el integrador a las 18:17 (antes de escribir), 18:45 (con las filas de la prueba) y 19:00 (tras la limpieza): **4.951 · 25 · 33 · 34 · 1 · 7 · 2 (9 runs) · 110 · 250 · 24 · 31 (+1 revocada)**, 2 organizaciones (org_123 y Faranda), 0 residuales — idénticas las tres veces.

Línea base fiscal de `structure-e2e`: 63 asientos previos (61 + cobro y devolución de 480,00 € de T8a) excluyendo `payroll_cost_import`, `pms_shadow_revenue`, `sage200_*` y sus reversos; 4300 = 379,00; el total 4.951 no cambia.

## 9. Limpieza de residuos

Durante la revisión, las suites canceladas por P2037 no ejecutaron su `after` y dejaron **tres organizaciones de prueba** en la BD local (ninguna es Faranda ni `org_123`): `org_lr_mu730ava` «LR Sociedad Test» y `org_lr2_mu730ava` «LR Otra Sociedad Test» (`ledger-import-routes.test.mts`, 14:56) y `cmu745wsn0000fyr882eqe8mh` «L2 Test Hotels mu745wh4» (`structure-l2.test.mts`, 15:28; su rol `owner` en versión 0 era el «1 role behind» de `rbac:sync`). Borradas por el corrector con el barrido genérico de `cleanupTenant` (allowlist explícita de ids y nombres, `scratchpad/cleanup-residual-orgs.mts`) tras la copia de §2; Faranda idéntica antes y después; quedan exactamente `org_123` y Faranda.

## 10. Decisiones abiertas para César

1. **DP-07** — rutas del manifiesto que siguen sirviéndose desde `demoStore` tras retirar su tabla, fuera del menú (p. ej. `GET /backoffice/properties/:propertyId/imports/:importId` → `getPropertyImport`, `server.ts:3909`): retirarlas del manifiesto (cambia el recuento 935 y los contratos) o cablearlas; propuesta: retirar en la tanda siguiente junto con los resolvers stub `revenueAutomationRule` / `revenueScenario` de `tenancy.ts` y las funciones `@deprecated L2`.
2. **DP-09** — `RetentionPolicy @@unique([organizationId, propertyId, entityType])` con `propertyId` NULL (tabla además sin escritor): exige migración `20260918131000_…` con índice parcial o retirar la tabla; no se ha abierto migración nueva en la corrección.
3. **DP-12** — `Notification.organizationId @default("")` en columna de tenant NOT NULL: el escritor ya pasa la organización; retirar el DEFAULT y añadir `CHECK (organization_id <> '')` requiere migración nueva (misma decisión que DP-09).
4. **DP-10** — `resolveStaff` (`advanced-record-store.ts:288-298`) guarda `staffName` libre en `shifts.staff_profile_id` cuando no resuelve un `StaffProfile` (comportamiento heredado de la pantalla, sin FK): exigir perfil (404) rompería el alta desde `WorkforceDashboard`, que no tiene selector; decidir con L5 (operaciones).
5. **Clave de lectura en escritura** — `POST /surveys/:id/responses` exige `surveys.read` (`route-permissions.ts:395`); cambiarla a `surveys.manage` altera la matriz RBAC (contratos y smoke) — preexistente, no tocado.
6. **Espejos de revenue en `demo-store.ts`** (`revenueDailySnapshots`, `revenueForecastSnapshots`, `revenueReportViews`, `ratePlans`… ): solo los mantienen los contratos de texto; los contratos ya no exigen que el servicio los lea, así que pueden retirarse con su pin en una limpieza posterior.
7. **`pnpm-lock.yaml`** aparece modificado en el working tree (52 líneas: `@fontsource-variable/inter`, `zod`, `@playwright/test`, `qrcode-terminal`) pero NO por esta tanda: su mtime es 2026-09-16 05:32 (regeneración de la deuda 12(a) de Tanda 4 durante la Tanda 6; los `package.json` de HEAD ya declaran esas dependencias y el lockfile de HEAD no las cubre — INT-05). El brief prohíbe tocarlo: decidir si entra en el commit de L2 (cierra la deuda 12(a)) o se excluye (`git diff HEAD -- pnpm-lock.yaml`).
8. **`check-role-smoke.mjs`** no está en `package.json` y rota contraseñas de los usuarios de demo: decidir si entra en la puerta (con BD de prueba) o se retira.
9. **Lease de schedulers** (R4 del repro del revisor, no confirmado como hallazgo): con `ttl` muy corto una segunda instancia adquiere el lease mientras el tick de la primera sigue en curso; el TTL de producción (`SCHEDULER_LEASE_TTL_MS`) debe superar la duración del tick más largo.
10. **Módulos del motor en Faranda** (INT-03): `workforce_labor`, `safety_incident_management`, `reputation_quality`, `guest_data_crm_loyalty`, `groups_events_sales`, `procurement_inventory`, `energy_sustainability` y `hotel_intelligence_platform` no están activados en ningún hotel de Faranda y el manifiesto exige antes `maintenance`, `payment_vault`, `ai_concierge`, `asset_intelligence` y `owner_mode` (409 `MODULE_DEPENDENCIES_MISSING`). Hoy las pantallas «Personal y turnos», «Seguridad e incidentes», «Compras», «Energía»… no aparecen en el menú de Carmen. Decidir qué módulos se activan y en qué hoteles (por `PATCH /backoffice/properties/:id/modules/:code` como Carmen o `sistemas`; reversible) y si las dependencias del manifiesto (p. ej. `payment_vault` para compras, `owner_mode` para analítica) siguen teniendo sentido.
11. **Carmen no ejecuta** (INT-02): con las plantillas `owner` + `general_manager` de T8a, Carmen lee todo el motor pero no crea turnos, incidentes, encuestas, campañas, pedidos, contadores ni reglas de precio (403 con la clave requerida en el mensaje). Es el diseño de T8a (dirección aprueba); si César quiere que la propiedad también opere, hay que añadir claves de ejecución a `owner`/`general_manager` en `packages/shared/src/permissions.ts` (cambia la matriz, los contratos y el smoke).
12. **Columnas de tenencia del motor** (§7.2): 21 tablas propias solo llevan `property_id` (la organización se deriva de la propiedad en cada consulta) y 6 tablas hijas no llevan ni `organization_id` ni `property_id` (acotadas por su padre). Cumple el fail-secure por ámbito de propiedad; si se quiere el filtro literal «`organizationId` **y** `propertyId` en toda consulta» por fila, hace falta una migración aditiva (`organization_id` NOT NULL con relleno desde `properties`) en la tanda siguiente.
13. **Contrato para el mapa de setup manual** (INT-01): añadir a `tests/manual-setup-route-visibility-contract.test.mjs` (o uno nuevo) la comprobación «cada `apiEndpoint`/`saveEndpoint` de `manual-setup-route-map.ts` casa con una ruta del manifiesto» (el script ad hoc del integrador da 40/40); hoy nada lo pina y el mapa llevaba 7 endpoints muertos.
14. **HITL con datos reales** (INT-04): la demo no puede ejercitar `check-in-from-scan` porque los documentos de los huéspedes son NULL desde la pérdida de `ENCRYPTION_KEY` (2026-07-15); para probarlo en `:3000` hay que registrar un huésped con documento (o resembrar `guest_maria` de org_123 con la clave actual).

## 11. Ficheros tocados por la ronda de corrección y por el integrador

Integrador (2026-09-18 18:16-19:05): `packages/product/src/navigation/manual-setup-route-map.ts` (INT-01: 7 endpoints repuntados, `:376`, `:393-394`, `:410-411`, `:609-610`, `:728-730`) · `docs/api-contracts.md` (`:450`, canónica `/ai-operations/governance/*`) · `deploy/README-INSTALL.md` (lease de schedulers, `WORKER_JOB_RUN_RETENTION_DAYS`, nota de la migración L2 en «Actualización») · `docs/design/COCOA-22-MIGRACION.md` §6 (regenerado sin cambios) · `CLAUDE.md` («Estado verificado» de la integración) · este informe (§1-§3.1, §4 INT-01…06, §7.2-§7.5, §8, §10.7 y §10.10-14). Fuera del repo: `~/anfitorio-demo/backups/hotelos-pre-l2-integracion-20260918-181652.dump` y el scratchpad de la sesión (`persist-l2.mjs`, `persist-l2-write*.json`, `persist-l2-read.json`, `verify-run2*.sql|out`, `cleanup.sql`, `api-3901-{a,b,c,d}.log`, `integ-final.log`).

Ronda de corrección:

`apps/api/src/lib/tenancy.ts` · `apps/api/src/lib/env.ts` · `apps/api/src/server.ts` · `apps/api/src/modules/advanced/advanced-modules.service.ts` · `apps/api/src/modules/developer/api-reference.service.ts` · `apps/api/src/modules/mobile-keys/wallet-pass.service.ts` · `apps/api/src/modules/night-audit/night-audit.service.ts` · `apps/worker/src/jobs/job-runs.ts` · `apps/worker/src/scheduler.ts` · `apps/worker/src/__tests__/job-runs.test.ts` · `packages/product/src/navigation/manual-setup-route-map.ts` · `packages/database/scripts/adopt-baseline.mjs` · `scripts/check-fresh-install.sh` · `scripts/env-contract.json` · `.env.example` · `deploy/.env.production.example` · `tests/advanced-modules-contract.test.mjs` · `tests/migrations-squash-contract.test.mjs` · `tests/revenue-channel-manager-contract.test.mjs` · `tests/revenue-history-forecast-contract.test.mjs` · `tests/spain-guest-register-compliance-contract.test.mjs` · `tests/integration/helpers/l2-tenant.mts` · `tests/integration/l2-motor-generico.test.mts` · `tests/integration/l2-modulos-plataforma.test.mts` · `tests/integration/l2-persistencia-ses.test.mts` · `tests/integration/l2-persistencia-backoffice.test.mts` · `tests/integration/structure-e2e.test.mts` · `docs/api-contracts.md` · `docs/deployment.md` · `docs/runbooks/opera-modo-sombra.md` · `docs/design/COCOA-22-MIGRACION.md` (regenerado) · `CLAUDE.md` · este informe.
