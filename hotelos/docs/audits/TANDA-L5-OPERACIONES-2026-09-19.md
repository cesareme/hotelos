# Tanda L5 · Operaciones y puesta en marcha — informe (2026-09-19)

**Encargo:** fila L5 del plan `docs/audits/TANDA-5-PLAN-2026-09-15.md` (§3) — estado de habitación unificado (`status` vs `housekeepingStatus`), `mark-inspected` idempotente, dashboards coherentes; parte de viajeros (`isPrimaryGuest`, `validate` coherente, establecimiento SES en el seed de demo); readiness recalculado en el GET y go-live que cambia estado y paso; night audit con folios de canceladas; Setup Center como único hub — con los criterios §4 (check-in ≤ 4 clics, 0 pantallas mock en el menú, readiness y go-live reales). Reconocimiento de partida: `scratchpad/tandaL5-recon.md` (18 filas de estado, §6 con 15 decisiones). Base: HEAD `776782a` (L3 y el importador Sage commiteados) durante la implementación; al cerrar la ronda de corrección HEAD es `994506c` (solo documentación de diseño UX-1, ancestro `776782a`). **Working tree SIN commit**: 86 entradas en `git status` (los lotes L5-A/B/C/D + el corrector); `hotelos/pnpm-lock.yaml` modificado (+52 líneas: importadores ya declarados en los `package.json` de HEAD) NO es de L5 — dejarlo fuera del commit o que César decida.

**Integrador (2026-09-19 03:38-04:00, HEAD `e6acd8c`, working tree sin commit: 94 entradas en `git status`).** Las secciones 7-9 (al final) recogen la copia previa, el flujo real por HTTP en `:3907` con los usuarios de T8a y una organización aislada, los siete hallazgos INT-L5-01..07 (tres arreglados en código: cargo de alojamiento solo a las reservas alojadas ESA noche, `admin_user_exists` que cuenta las asignaciones RBAC vivas, lectores de habitaciones sobre el mismo conjunto `active`), el cierre real de Rías Altas y Los Tilos (fechas de negocio 13/09 y 14/09 → 19/09 con reapertura auditada), la salida en vivo de seis centros por la ruta real, las puertas completas y las decisiones abiertas consolidadas. Las notas «[corr. integrador]» de las secciones 2, 3 y 5 corrigen una lectura errónea de la ronda de corrección: `business_dates.current_date` se leyó con la función SQL `current_date` (que devuelve el día de hoy) en vez de la columna; la tabla seguía en RA 2026-09-13 / LT 2026-09-14 / PG 09-16 / MC 09-17 / prop_123 09-14 (`updated_at` 2026-09-13 y 09-14), como decía el recon.

Este informe lo abre el **corrector (ronda 1)**: la sección 1 resume lo que los lotes dejaron en el árbol (para que el integrador complete el flujo real por HTTP), la 2 detalla la corrección hallazgo por hallazgo, la 3 las migraciones y los datos, la 4 las puertas, la 5 las decisiones abiertas para dirección y la 6 lo que queda al integrador. El API `:3000` **no se ha reiniciado** (pids intactos) y no se abrió `:3907`: todo por `app.inject` de los tests de integración.

En paralelo a la tanda corrieron dos cargas externas sobre la misma BD local: la contabilidad real de Sage 200 (`ledger_imports` 34 → 42, `journal_entries` 4.951 → 143.222) y las reservas reales de OPERA de cinco hoteles (reservas de Faranda 110 → 6.254 al cerrar este informe, y creciendo durante las puertas). Ninguna cifra de esas tablas es invariante de L5.

## 1. Lotes de la tanda (estado en el árbol)

| Lote | Qué hay en el árbol | Dónde |
|---|---|---|
| L5-A · estado de habitación | Máquina de estados pura + `applyRoomTransition` (idempotente, escritura condicional, auditoría `ROOM_STATE_CHANGED` + `RoomStateChanged`); `status` = ocupación / disponibilidad (`occupied | out_of_order | out_of_service`, espejo de la limpieza si está libre), `housekeepingStatus` `dirty | clean | inspected` y `maintenanceStatus` `ok | blocked | needs_attention` NOT NULL; escritores (check-in, check-out, cambio de habitación, mark-clean, mark-inspected, ruta libre con alias, bloqueo / liberación de mantenimiento, importación, bulk PATCH) y lectores (`/dashboards/housekeeping` con `roomsOccupied`, operations-director, `GET /properties/:id/dashboard`, room-rack, front-desk-queue, housekeeping-mobile, copilot, assistant, preflight) sobre `roomStateOf` / `foldRoomStateCounts`. | `apps/api/src/modules/housekeeping/room-state.service.ts`, `housekeeping.service.ts`, `maintenance.service.ts`, `pms/pms.service.ts`, `pms/inventory.engine.ts`, `dashboards/*.service.ts`, `server.ts` (rutas `/rooms/:id/*`), migración `20260919090000_operaciones_l5`; tests `housekeeping/__tests__/room-state.test.mts`, `tests/integration/l5-estado-habitacion.test.mts` |
| L5-B · parte de viajeros y SES | `guestRegisterPayloadForLink` (`isPrimaryGuest` del vínculo, `isMinor` por edad < 14, `providedByAdultGuestId`, parentesco), validador `ready_to_sign` solo con la firma como único bloqueo, `POST …/validate` devuelve el estado persistido; SES honesto: `SES_DISABLED`, `GUEST_REGISTER_INVALID`, `GUEST_REGISTER_NOT_QUEUEABLE`, `SES_SUBMISSION_IN_FLIGHT`, una fila recuperable por parte reutilizada, sin marcadores `1900-01-01 / ESP / DNI`, descarte auditado `SES_DISCARDED` (script `ses:discard-failed`), establecimiento demo en el seed (Madrid 28079 · Adeje 38001, `DEMO-0000123/4`). | `modules/compliance/{compliance,ses-submission}.service.ts`, `packages/compliance/src/spain/guest-register-validator.ts`, `scripts/discard-failed-ses-submissions.ts`, `packages/database/prisma/seed.ts`; tests `compliance/__tests__/{ses-hospedajes,guest-register-ensure}.test.mts`, `packages/compliance/src/spain/__tests__/guest-register-validator.test.mjs`, `tests/integration/l5-parte-viajeros-ses.test.mts` |
| L5-C · readiness, go-live, Setup Center | GET de readiness calcula en vivo con ventana de frescura (`readiness-freshness.ts`), `approveGoLive` escribe `properties.go_live_at`, completa el paso `go_live` y audita; Setup Center lee readiness + pasos; banner del shell solo con bloqueantes y sin `goLiveAt`; `GoLiveChecklist` con «Aprobar salida en vivo». | `modules/backoffice/{backoffice.service,setup.store,readiness-freshness}.ts`, `screens/backoffice/SetupCenterScreen.tsx`, `screens/GoLiveChecklist.tsx`, `layouts/BackOfficeLayout.tsx`; tests `backoffice/__tests__/readiness-freshness.test.mts`, `tests/integration/l5-readiness-golive.test.mts` |
| L5-D · cierre del día | Preflight contra la fecha de negocio; `POST …/run` con la puerta del preflight (`force` + motivo auditado `NIGHT_AUDIT_PREFLIGHT_OVERRIDDEN`); paso `close_settled_folios` (folios a 0 y facturados de canceladas / no-show / salidas); pantalla con revisión y reapertura de T8a. | `modules/night-audit/{night-audit.service,night-audit-preflight.service,night-audit-gate,night-audit.routes}.ts`, `screens/operations/NightAuditScreen.tsx`, `night-audit-report.ts`; tests `night-audit/__tests__/night-audit-gate.test.mts`, `tests/integration/l5-night-audit-canceladas.test.mts` |
| L5-E · cronograma interactivo | ABSORBIDO por el worktree TL (Live Timeline): fuera de esta tanda. | — |
| Integrador | Copia `pg_dump` previa; flujo real por HTTP en `:3907` (organización aislada `org_l2_intmu7q2i4yfsjk` + Faranda con los usuarios de T8a); arreglos INT-L5-01 (`night-audit-in-house.ts`: solo se carga la noche a quien está alojado esa noche), INT-L5-03 (`admin_user_exists` cuenta `user_role_assignments` vivas) e INT-L5-07 (`/dashboards/housekeeping`, `GET /properties/:id/dashboard` y la instantánea del cierre solo con habitaciones `active`); cierre real RA 13/09→19/09 (6 runs, 1 reabierto y re-cerrado, 1 revisado) y LT 14/09→19/09 (5 runs, 1 revisado); go-live real de LT, PG, MC, AS, FN y LL; helper `l2-tenant` con vocabulario cerrado; puertas completas; este informe y `CLAUDE.md`. | §7, §8, §9 |

Navegación: ningún lote añadió pantallas ni pestañas (`build-nav-tree --check` al día: 68 ítems · 101 pestañas · 205 redirecciones). RBAC: ninguna ruta nueva; el manifiesto (`security/route-permissions.ts` + partials) no cambia; `rbac:sync --dry-run` +0.

## 2. Ronda de corrección 1 (corrector)

Alcance: los dos fallos de puerta atribuibles y los 21 hallazgos del revisor (4 high / medium de operativa, 4 de cumplimiento SES, 3 de puesta en marcha front; 11 low). Sin nuevos frentes: cero rutas nuevas, cero dependencias, cero `style={}` nuevos (inlineStyles 679 = techo), sin tocar los ficheros de L6a (`modules/ai/**`), TL (`screens/timeline/**`) ni T8 salvo un cambio aditivo de una línea en `dashboards/general-manager.service.ts`.

### 2.1 Puerta 9

- **`l2-persistencia-plataforma.test.mts:277` (execute → 409 `SES_DISABLED`)**. Causa: el tenant L2 nace con SES apagado y `modules/ai/check-in.command.ts:195-198` (L6a) solo tolera `SES_ESTABLISHMENT_INCOMPLETE`. Decisión tomada (la de menor radio): el test activa `properties.ses_hospedajes_enabled` en su propia propiedad antes del escaneo y completa el parte (teléfono y residencia en `documentExtractedFields`, que `spainInputFromIdentityFields` mapea) para que el validador pase y el flujo llegue al 409 `SES_ESTABLISHMENT_INCOMPLETE` tolerado (fila aparcada), como antes de L5. **Queda para L6a** tolerar también `SES_DISABLED` en `check-in.command.ts` (ver §6). Resultado: 88/88 en el lote 1 de integración.
- Los demás fallos de la puerta 9 (structure-e2e / structure-l5 «too many bind variables» por 143.222 asientos de Sage; fiscal-models origen `libros`; flake horario de pms-shadow 00:00–02:00 CEST; carrera de reservas por la carga OPERA) son de entorno y no se han tocado.

### 2.2 Hallazgos (fichero:línea del árbol al cierre)

| Id | Arreglo | Dónde | Test |
|---|---|---|---|
| OP-01 high | `block_maintenance` sobre una OCUPADA conserva `status = occupied` (blocked + no vendible); `check_out` de una bloqueada → `out_of_order/dirty`; `applyRoomTransition` consulta la reserva alojada también al bloquear; el Room Rack clasifica la alojada con bloqueo como ocupada (la incidencia va en el badge). | `room-state.service.ts` `computeNext` (block / check_out), `applyRoomTransition` (inHouse); `dashboards/room-rack.service.ts` (orden ocupada → bloqueada) | `room-state.test.mts` (matriz 4×3×9, bloque «OP-01»), `l5-estado-habitacion.test.mts` «Corrector L5 · OP-01» (4 lectores: ocupada 1, OOO 1 con la reserva alojada) |
| OP-02 medium | `canAssignRoom` rechaza `out_of_order | out_of_service` sin bloqueo («fuera de servicio / fuera de orden»); `computeRealAvailability` excluye por `status`; la importación de onboarding crea OOO / OOS con `sellable = false`. | `pms/inventory.engine.ts` (tras `maintenanceBlock`), `channel-manager/delivery.service.ts` (`status notIn`), `onboarding.service.ts` (`outOfInventory`) | cubierto por `l5-estado-habitacion` (OOS por `/sellable`: `hk.ooo` 2, rack 2) |
| OP-03 medium | Eventos DIFERIDOS al commit: con `db` de transacción `applyRoomTransition` devuelve `deferred: true` + `emitEvents()`; `emitRoomStateEvents(...)` tras el `$transaction` en check-in, check-out, cambio de habitación y bulk. Un rollback ya no deja `ROOM_STATE_CHANGED` fantasma. | `room-state.service.ts` (`ApplyRoomTransitionResult.deferred / emitEvents`, `emitRoomStateEvents`), `pms.service.ts` (3 transacciones), `backoffice.service.ts` `bulkUpdateRooms` | `l5-estado-habitacion` (eventos por habitación siguen exactos) |
| OP-04 medium | Reapertura del ÚLTIMO día cerrado (fecha de negocio = día siguiente, sin run posterior) rebobina `business_dates.current_date` (`closed_at / closed_by` a NULL) y responde `businessDateRewound: true, currentBusinessDate`; un run `reopened` se re-ejecuta en su fila (conserva la traza de reapertura, limpia `reviewedBy`); un día anterior no rebobina (`false`) y sus correcciones se revisan sobre el run reabierto (`review` admite `reopened`). Pantalla: aviso distinto según el caso y relectura del preflight; `runReviewState / runActionsFor` actualizados. | `night-audit.service.ts` (`reopenNightAuditRun`, `runNightAudit` upsert, `reviewNightAuditRun`), `packages/shared/src/pos-types.ts`, `NightAuditScreen.tsx`, `night-audit-report.ts` | `night-audit-report.test.mts` (+3 aserciones), `l5-night-audit-canceladas` y `rbac-sod` (28/28 + 21/21) |
| CS-01 high | Interruptor SES = OR de `properties.ses_hospedajes_enabled` y `property_compliance_settings.ses_hospedajes_enabled` (`sesHospedajesEnabledFor`), la misma regla que la readiness; el interruptor de Ajustes de cumplimiento vuelve a tener efecto. | `ses-submission.service.ts` (`queueSesSubmission`) | `l5-parte-viajeros-ses` (`setSesFlag` fija los dos) |
| CS-02 high | `sesRequeueGate` compartido por `retrySesSubmission`, `processSubmission` (justo antes de enviar) y el programador: interruptor → `SES_DISABLED` (recuperable), parte `accepted | annulled | expired` → `GUEST_REGISTER_NOT_QUEUEABLE`, validador → `GUEST_REGISTER_INVALID { issues }`. El programador clasifica las filas aparcadas ANTES de re-encolar: una recuperable por parte + tipo (las antiguas → `SES_DISCARDED` «sustituida», `discarded.supersededBy`), parte aceptado → descartada, parte inválido → `failed GUEST_REGISTER_INVALID` definitivo; `SES_DISABLED` entra en `SES_RECOVERABLE_ERROR_CODES`; resultado con `superseded / notQueueable / invalid`. | `ses-submission.service.ts` (`sesRequeueGate`, `discardSesSubmissionRow`, `runDueSesSubmissions`) | `l5-parte-viajeros-ses` (retry 409 SES_DISCARDED, scheduler ignora descartadas), `ses-hospedajes.test.mts` |
| CS-03 medium | La comunicación lleva `<Sexo>` (M → 1, F → 2, otro → 0; `sesGenderOf` tolera male/female/hombre/mujer), municipio / provincia / CP / país de residencia y el bloque `<Menor>` (parentesco, nombre y documento del adulto que declara) leídos del parte de cada viajero (`guestRegisterRecord` por `guestId`) con el perfil como respaldo. | `ses-submission.service.ts` (`SesGuestSource`, `sesGuestFromSource`, `toSesGuest`, `legalBlockFor` en `processSubmission`) | `ses-hospedajes.test.mts` (mapeo completo sigue verde) |
| CS-04 medium | Descartadas fuera de `sesOverdue` (`/compliance/health`), `sesPending` del dashboard de dirección general (cambio aditivo, fichero T8) y del portfolio, del KPI «Rechazados o fallidos» de la pantalla SES y de `GET …/ses/submissions?status=failed` salvo `?includeDiscarded=true|1`. **`dashboards/property-overview.service.ts` (T8, prohibido) sigue contándolas: pendiente de coordinar.** | `compliance-health.service.ts`, `dashboards/{general-manager,portfolio}.service.ts`, `SesHospedajesSettingsScreen.tsx`, `server.ts` (`SesSubmissionListQuerySchema`), `ses-submission.service.ts` (`listSesSubmissions`) | contratos raíz (api-route-permissions, ses-hospedajes-pending-statuses) |
| L5F-01 medium | `node scripts/cocoa-22-waves.mjs --write` (cabecera §6: 95.035 → 95.990 líneas tras los ficheros nuevos del corrector); `--check` exit 0; inventario regenerado (227 pantallas, inlineStyles 679). | `docs/design/COCOA-22-MIGRACION.md` §6, `docs/design/cocoa-22-inventory.json` | `tests/cocoa-22-contract.test.mjs` 18/18 |
| L5F-02 medium | Reglas puras extraídas y fijadas: `layouts/setup-banner.ts` (`shouldShowSetupBanner`, `setupBannerMessage`), `screens/go-live-state.ts` (`goLiveHeadState`: badge, «Aprobar salida en vivo», tono del progreso), `screens/backoffice/launch-readiness.ts` (`launchStateFromResults`, `launchSectionView`, `launchBadge`); contratos de fuente sobre el botón, el `CocoaDialog`, «En vivo desde…», el mensaje de 403 y la sección de lanzamiento. | tests nuevos `layouts/__tests__/setup-banner.test.mts` (7), `screens/__tests__/go-live-checklist.test.mts` (8), `screens/backoffice/__tests__/launch-readiness.test.mts` (8) | front 1.447/1.447 |
| L5F-03 medium | Este informe; bloque «Estado verificado (Tanda L5 …)» en `CLAUDE.md` con migraciones 17/17. | `docs/audits/TANDA-L5-OPERACIONES-2026-09-19.md`, `CLAUDE.md` | — |
| OP-05 low | `POST /rooms/:id/sellable` pasa por la transición: `mark_unsellable` (libre → `out_of_service`, ocupada sigue ocupada; no vendible) / `mark_sellable` (vuelve al inventario con su limpieza), auditado e idempotente; una bloqueada por orden → 409 `ROOM_MAINTENANCE_BLOCKED`. | `server.ts` (ruta), `room-state.service.ts` (eventos nuevos) | `l5-estado-habitacion` «Corrector L5 · OP-05» (2 casos), `room-state.test.mts` |
| OP-06 low | `snapshot_room_status` pliega `groupBy(status, housekeepingStatus)` con `foldRoomStateCounts`: métricas `{ clean, dirty, inspected, occupied, out_of_order, total }` como los dashboards. | `night-audit.service.ts` | `l5-night-audit-canceladas` |
| OP-07 low | La importación degrada `maintenanceStatus: blocked` a `needs_attention` (el bloqueo lo pone una orden de trabajo) y lo anota en `warnings` del resultado de apply. | `onboarding.service.ts` (`materialiseOnboardingProject`) | contratos de onboarding (raíz) |
| OP-08 low | El aviso «N folios liquidados … se cerrarán» se mide POR FOLIO (`computeBalancesForFolios`, 3 consultas por lote) como el paso `close_settled_folios`. | `folio/folio-balance.service.ts` (helper nuevo), `night-audit-preflight.service.ts` | `l2-robustez` (presupuesto del preflight ≤ 30 sigue verde), `l5-night-audit-canceladas` |
| OP-09 low | `bulkUpdateRooms` en UNA transacción (`db: tx` en cada transición; eventos tras el commit). | `backoffice.service.ts` | `l2-persistencia-backoffice` (14/14) |
| OP-10 low | Informe (este) y bloque de CLAUDE.md. Fechas de negocio: ya están todas en 2026-09-19 con `night_audit_runs = 0` (las adelantó la carga OPERA, sin cierres registrados): ver §5.2. **[corr. integrador: falso — la ronda leyó la función SQL `current_date`, no la columna; la tabla seguía en RA 2026-09-13 y LT 2026-09-14 y `night_audit_runs` = 0. Cerrado de verdad por HTTP en §7.4: RA 6 runs y LT 5 runs, ambas en 2026-09-19.]** | — | — |
| CS-05 low | Migración de datos `20260919120000_operaciones_l5_backfill_parte_titular`: `is_primary_guest = true` donde el vínculo `reservation_guests.is_primary` lo es (15 filas en local; queda 1 parte sin vínculo titular). | `packages/database/prisma/migrations/…/migration.sql` | `db:migrate:status` 17/17, `db:drift:check` 0, `migrations-squash-contract` |
| CS-06 low | Retención RGPD desde el FIN del servicio: `checkoutAt` = salida prevista de la reserva en `guestRegisterPayloadForLink` (check-in) y en `prepareGuestRegisterRecord` (escaneo); `persistGuestRegister` ya usaba `checkoutAt ?? now`. | `compliance.service.ts` | `guest-register-ensure.test.mts` (+1) |
| CS-07 low | `SES_HOSPEDAJES_SUBMISSION_REFUSED` con `actorType: user` cuando la inicia un usuario. | `ses-submission.service.ts` (`refuse`) | `l5-parte-viajeros-ses` (negativa auditada) |
| CS-08 low | `tests/compliance-package-tests.test.mjs`: la puerta raíz lanza la suite de `@hotelos/compliance` (validador del parte, indirect-tax, invoice-totals) con el loader del paquete y exige 0 fallos. | `tests/compliance-package-tests.test.mjs` | raíz 535/535 |
| CS-09 low | Una **baja** nunca se rechaza por `SES_DISABLED` (revoca lo que el MIR tenga) ni en el encolado ni en el retry. | `ses-submission.service.ts` | `ses-cancellation` (unit) |
| CS-10 low | El descarte exige `compliance.ses.configure` (gestión del conector), en el servicio y en el CLI. | `ses-submission.service.ts`, `scripts/discard-failed-ses-submissions.ts` | `ses-hospedajes.test.mts`, `l5-parte-viajeros-ses` (403 sin clave) |
| CS-11 low | El re-seed de prop_123 / prop_canary solo converge `sesRegistryNumber`; los interruptores SES / VeriFactu son del operador. | `packages/database/prisma/seed.ts` | `demo-seed-contract` |
| L5F-04 low | Un solo go-live: `POST /onboarding/projects/:id/go-live` delega en `approveGoLive` de backoffice sobre la propiedad materializada (`project.propertyId`, fijado en apply); sin propiedad aplicada → 409 `ONBOARDING_NOT_APPLIED`. Ruta y clave del manifiesto intactas. | `onboarding.service.ts`, `apps/api/docs/openapi.yaml` | `ai-onboarding-migration-contract`, `api-integration` (sonda 403) |
| L5F-05 low | Setup Center: el fallo del GET de readiness se guarda aparte (`readinessError`); con readiness ilegible los KPIs muestran «—» + aviso, nunca «Bloqueantes 0». | `SetupCenterScreen.tsx`, `launch-readiness.ts` | `launch-readiness.test.mts` |
| L5F-06 low | Etiquetas de los pasos desde el API (`SETUP_STEP_LABELS` en `backoffice.service.ts` → `step.label` en `GET …/setup`); el front solo conserva el fallback al código. | `backoffice.service.ts` (`getSetupProgress`), `backofficeApi.ts` (`PropertySetupStep.label`), `SetupCenterScreen.tsx` | `launch-readiness.test.mts` (sin catálogo en el front), `l2-persistencia-backoffice` |
| L5F-07 low | `go_live_at` de demo: seed (prop_123 / prop_canary → 2026-06-01) y `chain-8-hotels` (8 centros → 2026-09-14), SOLO si está vacío. La BD local no se ha tocado (ver §5.4). | `seed.ts`, `seeds/chain-8-hotels.ts` | — |
| L5F-08 low | Estado vacío de `GoLiveChecklist`: sin «Recalcular ahora» (exigía `property.configure`); «Volver a cargar» sobre el GET. | `GoLiveChecklist.tsx` | `go-live-checklist.test.mts` |

### 2.3 Ronda de corrección 2 (corrector, 2026-09-19 tarde; HEAD del árbol principal `e6acd8c` «Merge branch 'tanda-timeline'», L5 sigue sin commit)

Encargo de la ronda: los mismos 21 hallazgos de la ronda 1 (lista reenviada sin re-verificar por el revisor) más dos fallos de puerta. Método: cada hallazgo se ha **re-verificado contra el árbol actual** (marcadores en código + lectura de `room-state.service.ts` entero, `reopenNightAuditRun` / `runNightAudit`, `queueSesSubmission` / `retrySesSubmission` / `sesRequeueGate` / `runDueSesSubmissions`, `canAssignRoom`, `computeRealAvailability`, la importación de onboarding, el estado vacío de `GoLiveChecklist` y la sección de lanzamiento del Setup Center) y por las puertas completas: **los 21 arreglos de §2.2 están en el árbol y las puertas que los cubren pasan** (§4, columna «ronda 2»). Ningún fichero de producto cambia en esta ronda: solo la whitelist de discoverability, tres suites de integración de L3 y este informe + `CLAUDE.md`.

| Qué | Arreglo | Dónde | Resultado |
|---|---|---|---|
| Puerta 5 · pantalla huérfana `LiveTimeline` (Tanda TL fusionada en `e6acd8c` sin montar; `screens/timeline/**` vedado a L5) | Entrada **temporal** en la whitelist con la razón: quedan por aplicar las líneas de fusión (a)-(g) del §6 de `docs/audits/TANDA-TL-LIVE-TIMELINE-2026-09-18.md` (dos variantes que decide el orquestador: ítem propio en Hoy o sustituir la pestaña Cronograma); hasta entonces Recepción › Reservas › Cronograma sigue sirviendo `LiveTimelineWorkspace`. Sale de la lista al montarla. | `apps/admin-web/.discoverability-whitelist.json` (27 entradas) | `check-sidebar-coverage` 227/227 · `check-discoverability` OK · `build-nav-tree --check` al día (68 · 101 · 205) |
| Puerta 9 · `structure-l5` (22 cancelados) y `structure-e2e` (39 cancelados): «too many bind variables … 143.223» | El helper `farandaCounts` de las dos suites contaba `journalLine` con `journalEntryId: { in: [143.222 ids] }` (límite de PostgreSQL 32.767 parámetros): ahora `countJournalLinesOf(org)` con un JOIN (`$queryRaw`; `JournalLine` no tiene relación Prisma con `JournalEntry`) y `journalEntries` por `count`. En `structure-e2e` el baseline «63 asientos previos» también explotaba (`reversalOfId: { notIn: [143.068 ids] }`): misma exclusión con una subconsulta SQL; y los 48 reversos de lotes de nómina (`reversal` sobre `payroll_cost_import`, 2026-01-31..08-31) se excluyen como su original («sin payroll_cost_import»), con lo que el baseline vuelve a ser 63 sin re-fijar cifras. | `tests/integration/structure-l5.test.mts`, `tests/integration/structure-e2e.test.mts` | structure-l5 **14/14** · structure-e2e **29/29** (antes 0 ejecutados) |
| Puerta 9 · `structure-l5` «el 390 suma también las emitidas importadas de Sage» | La carga real de Sage 200 trae libros de **2025** (2025-Q1..Q4: 34.031 filas), no de 2026: el 390 de 2026 solo suma Sage si hay filas de Sage fechadas en 2026; sin ellas basta con que se calcule (> 0). Las cifras fijas del piloto sin Sage (74,94 / 37 registros / 805,76) NO se re-fijan: el 303 real de Faranda 2026-Q3 es hoy 27 = 71 = 70,39 con 38 registros (dato para L3 / dirección, no para esta tanda). | `tests/integration/structure-l5.test.mts` (C9) | 14/14 |
| Puerta 9 · `fiscal-models` «Faranda obtiene su 303…» (`libros` ≠ `documentos`) | La expectativa de `fuentes.origen` sigue la regla del cargador (`loadVatBookRows`: «documentos» solo si un libro del trimestre no tiene filas materializadas y los documentos sí producen filas) en vez de suponer `sourceType sage200`: en local los libros de 2026-Q3 están materializados por rebuild (34 `invoice` + 3 `rectification`) → «libros». | `tests/integration/fiscal-models.test.mts` | fiscal-models **11/11** |
| L5F-07 (`go_live_at` en la BD local) | Intentado en esta ronda con copia previa (`pg_dump -t properties`) + los dos `UPDATE` de §5.4: el arnés de la sesión **denegó la escritura sobre la BD compartida** (política «recursos compartidos»), así que sigue para el integrador tal como está en §5.4 (los 10 centros con `go_live_at` NULL; seed y `chain-8-hotels` ya lo rellenan). | — | — |
| CS-04 (`dashboards/property-overview.service.ts`) | Sin cambios: fichero de T8 vedado por las reglas de la ronda aunque `tanda-t8` ya está fusionada en `main` (0 commits por delante) y **T8 nunca tocó ese fichero** (último commit `99dc3c3`, L2): la línea exacta sigue en §6. | — | — |

Datos re-leídos por SQL (solo SELECT) en esta ronda: `go_live_at` NULL en los 10 centros; `business_dates` RA / LT / PG / MC / prop_123 = 2026-09-19 con `closed_at` NULL y `night_audit_runs` = 0 **[corr. integrador: lectura errónea (función `current_date`); valores reales antes del cierre: RA 2026-09-13, LT 09-14, PG 09-16, MC 09-17, AS 09-19, prop_123 09-14]**; SES de RA: 12 `failed + SES_DISCARDED` («sustituidas») y 3 `SES_ESTABLISHMENT_INCOMPLETE` (una por parte: `grr_1134c282` missing_data, `grr_b3766169` y `grr_01c02216` accepted); prop_123 134 `SES_DISCARDED`; `is_primary_guest` 15 true / 1 false; migraciones 17/17 aplicadas; libros de Faranda: Sage 2025-Q1..Q4 + 37 filas materializadas de 2026-Q3.

## 3. Migraciones, copias y datos

- Migraciones **17/17** (`db:migrate:status` «Database schema is up to date!»; última `20260919120000_operaciones_l5_backfill_parte_titular`), `db:drift:check` «No difference detected». Copia previa del lote L5-B: `scratchpad/hotelos-pre-L5B-20260919-005221.dump`; copia de la tabla `guest_register_records` antes del backfill: `scratchpad/guest_register_records-pre-backfill-20260919-024152.dump` (11 KB). Backfill aplicado: 15 filas → `is_primary_guest = true` (0 pendientes; 1 parte sin vínculo titular sigue en false).
- **Filas SES aparcadas de RA (`cmrhw9jy40003fyvbuu2ec2w7`)**: durante las puertas la suite `l5-parte-viajeros-ses` ejecuta `runDueSesSubmissions()` (global, como el tick del API), así que el programador nuevo ya clasificó las 15 filas: 12 pasan a `failed + SES_DISCARDED` «fila aparcada duplicada del mismo parte: la sustituye la más reciente» (11 de `grr_1134c282`, 1 de `grr_b3766169`; `responsePayloadJson.discarded.supersededBy`, auditoría `SES_HOSPEDAJES_SUBMISSION_DISCARDED` de sistema) y quedan **3 aparcadas, una por parte** (`grr_1134c282` missing_data, `grr_b3766169` y `grr_01c02216` accepted), todas `SES_ESTABLISHMENT_INCOMPLETE` y bloqueadas por el interruptor apagado. Nada se ha borrado. Cuando se active SES en RA, el siguiente tick descarta las dos de partes aceptados (`GUEST_REGISTER_NOT_QUEUEABLE`) y deja la tercera `failed GUEST_REGISTER_INVALID` definitiva: **0 altas al MIR** (antes: 3 altas duplicadas + 12 fallos en cadena).
- prop_123: 134 `SES_DISCARDED` (sin cambios); ya no cuentan en `sesOverdue` / `sesPending` / KPI de la pantalla SES / `?status=failed`.
- Faranda al cerrar: reservas 6.254 (cambia por la carga OPERA), facturas 25, VeriFactu 33 (intactas); `business_dates` RA / LT / PG / MC / prop_123 = 2026-09-19 con `closed_at` NULL y `night_audit_runs` 0 **[corr. integrador: RA 2026-09-13 y LT 2026-09-14 hasta el cierre real de §7.4; hoy RA y LT = 2026-09-19 con `closed_at` y 11 runs]**; `go_live_at` NULL en los 10 centros (8 Faranda + 2 demo) **[integrador: hoy con fecha en LT, PG, MC, AS, FN y LL por la ruta real de go-live, §7.3]**; interruptores SES apagados en las dos tablas para los 8 centros Faranda, encendidos en prop_123 / prop_canary; 0 habitaciones `occupied + blocked`; 0 organizaciones `org_l2_*` / `org_l5_*` residuales.

## 4. Puertas del corrector (2026-09-19 02:4x-03:0x)

| Puerta | Resultado |
|---|---|
| `corepack pnpm --filter @hotelos/api typecheck` · `--filter @hotelos/admin-web typecheck` | OK · OK |
| API unit (`node --import tsx --test "src/**/__tests__/*.test.mts"`) | **2.356 tests · 2.355 pass · 0 fail · 1 skipped** |
| Front unit (`apps/admin-web`, tsx de `apps/api`) | **1.447/1.447** (+23 nuevos: setup-banner 7, go-live-checklist 8, launch-readiness 8) |
| Contratos raíz (`node --test tests/*.test.mjs`) | **535/535** (534/535 antes de regenerar el inventario Cocoa; +1 fichero: compliance-package-tests) |
| `node scripts/cocoa-22-waves.mjs --check` · `cocoa-22-contract` | exit 0 · 18/18 |
| `corepack pnpm --filter @hotelos/admin-web build` | OK (2,6 s) |
| `db:migrate:status` · `db:drift:check` | 17/17 · sin diferencias |
| Integración lote 1 (l5-estado-habitacion, l5-parte-viajeros-ses, l5-night-audit-canceladas, l5-readiness-golive, l2-persistencia-plataforma, l2-persistencia-backoffice, rbac-sod, l2-robustez) | **88/88** |
| Integración lote 2 (api-integration, pos-cash-night, l2-modulos-operaciones, l2-rutas-api, l2-modulos-ia, l2-persistencia-ses, structure-l2) | 106 tests · 101 pass · **5 «fallos» = la invariante «cifras de Faranda no cambian» (reservas 5.974 → 6.136 DURANTE el lote, carga OPERA)** · 2 skips; structure-l2 pasó entero (la carga Sage no volvió a romper el `journalLine.count`) |

Ronda 2 (mismo árbol + los cuatro ficheros de §2.3): typecheck api · admin-web OK; API unit 2.356 (2.355 pass · 1 skip); front 1.447/1.447; contratos raíz 535/535 (ejecutados antes de la entrada de la whitelist; ningún contrato raíz lee `.discoverability-whitelist.json`: su único consumidor, `check-sidebar-coverage`, se re-ejecutó después: 227/227); `cocoa-22-waves --check` §6 al día; `build-nav-tree --check` al día; `check-discoverability` OK (placeholders 16/20); integración lote 1 (l5-estado-habitacion 14, l5-parte-viajeros-ses 12, l5-night-audit-canceladas 6, l5-readiness-golive 14, l2-persistencia-backoffice 8, rbac-sod 16, l2-robustez 13, l2-persistencia-plataforma 5) **88/88**; structure-l5 14/14, structure-e2e 29/29, fiscal-models 11/11. API `:3000` intacto; `:3907` no abierto.

No se ejecutaron: worker (sin cambios), integración completa (49 ficheros: la corre el integrador con Faranda en reposo; con la carga OPERA en marcha la invariante de reservas de `helpers/l2-tenant.mts` fallará en 26 suites).

## 5. Decisiones abiertas para dirección

1. **Números de registro SES.HOSPEDAJES de los 8 centros Faranda** (`property_compliance_settings.ses_registry_number`, NULL; INE / CP ya completos). Sin ellos ninguna comunicación sale (409 `SES_ESTABLISHMENT_INCOMPLETE`) y las 3 filas aparcadas de RA siguen así. Con el interruptor encendido y el número puesto, el programador deja RA en estado honesto automáticamente (§3). Decisión: ¿SES apagado explícitamente en Faranda hasta tener el registro (hoy ya lo está en las dos tablas) o pedir los números a los hoteles ahora?
2. **Turno de noche por hotel.** Solo LT tiene `night_auditor` por propiedad; en RA el cierre lo puede correr `front_office_manager` (`night_audit.run`) y la reapertura tardía la deciden los 2 `general_manager` de organización. Las fechas de negocio están en 2026-09-19 sin ningún cierre registrado (`night_audit_runs = 0`) **[corr. integrador: estaban en 13/09 y 14/09; el integrador ha cerrado RA (6 días) y LT (5 días) por HTTP con el motivo auditado, §7.4]**: ¿se acepta hoy como día de arranque (primer cierre real esta noche, sin cierres retroactivos) o se rebobina RA/LT por script para cerrar los días del piloto? Con la reapertura nueva (OP-04) un día mal cerrado se corrige y se vuelve a cerrar sin script. **Queda para dirección: los 14 folios de RA con saldo (4 salidas 379,75 € + 10 canceladas; 854,75 € en total) y la reserva RES-02109 sin tarifa ni importe (alojada, 6 noches sin cargo), que bloquean cada cierre y obligan a forzarlo con motivo.**
3. **Hubs de configuración.** Setup Center es el único hub de navegación y el go-live de onboarding ya delega en el real (L5F-04); queda decidir si se RETIRA la ruta `POST /onboarding/projects/:id/go-live` (manifiesto, openapi, permiso `onboarding.go_live` en 4 plantillas, mobile) en una tanda posterior, y si se unifican los tres «códigos de establecimiento» (`sesRegistryNumber` — el único leído —, `Property.sesEstablishmentCode`, `AuthorityReportingSetting.establishmentCode`; recon §6.12).
4. **`go_live_at` de los hoteles en explotación.** **[integrador: resuelto por la ruta real — `POST …/go-live` aprobado por `direccion.general` en LT, PG, MC, AS, FN y LL (readiness `ready` recalculada en el GET); RA sigue `blocked` por `ses_establishment_profile` (sin número de registro) y `ses_hospedajes_credentials` (modo sandbox) y OC es una oficina sin edificio ni habitaciones: decisión 1 y §8.]** El seed de demo y `chain-8-hotels` ya lo rellenan si está vacío; la BD local no se ha tocado: los 8 centros Faranda y prop_123 / prop_canary siguen sin fecha y el banner «Faltan N comprobaciones para poner la propiedad en marcha» seguirá saliendo en RA (2 bloqueantes). SQL propuesto para el integrador (UPDATE, no borra): `UPDATE properties SET go_live_at = '2026-09-14' WHERE organization_id = 'cmrhw9jy30002fyvb6tsdiugt' AND go_live_at IS NULL;` y `… '2026-06-01' WHERE id IN ('prop_123','prop_canary') AND go_live_at IS NULL;` — o aprobar la salida en vivo desde la pantalla cuando no queden bloqueantes.
5. **`pnpm-lock.yaml`** modificado por detrás de los `package.json` de HEAD (no es de L5): dejarlo fuera del commit o regenerarlo en una tarea propia.
6. **Invariantes de Faranda en los tests** (`helpers/l2-tenant.mts` `FARANDA_EXPECTED_INVARIANTS`: reservas 110, asientos 4.951, lotes 34): caducadas por las cargas OPERA y Sage; decidir si se fijan al nuevo estado cuando las cargas terminen o se relaja la comprobación a facturas / VeriFactu.

## 6. Pendientes del integrador y de las tandas paralelas

- **L6a (`modules/ai/check-in.command.ts:195-198`)**: tolerar 409 `SES_DISABLED` (y `GUEST_REGISTER_INVALID`) como hoy `SES_ESTABLISHMENT_INCOMPLETE`: el check-in y la firma ya están hechos; la negativa honesta no debe deshacerlos. Mientras tanto el test de plataforma activa SES en su tenant.
- **T8 (`dashboards/property-overview.service.ts:166,247`)** (sin cambios en la ronda 2: `tanda-t8` ya fusionada y sin tocar ese fichero; basta aplicar la línea): excluir `errorCode SES_DISCARDED` de `SES_PENDING_STATUSES` como se hizo en general-manager / portfolio (una línea `OR: [{ errorCode: null }, { errorCode: { not: "SES_DISCARDED" } }]`).
- Integrador **[hecho en §7-§9 salvo montar `LiveTimeline`, que es decisión del orquestador]**: `go_live_at` por SQL (§5.4; la ronda 2 no pudo escribirlo: escritura sobre la BD compartida denegada por el arnés) **[sustituido por la ruta real de go-live: 6 centros]**; montar `LiveTimeline` según §6 del informe TL y retirar su entrada temporal de la whitelist; copia `pg_dump` completa antes del flujo real; flujo por HTTP en `:3907` como `recepcion.rias`, `pisos.tilos`, `gobernanta.tilos`, `auditoria.noche.tilos`, `direccion.rias`, `sistemas` sobre organizaciones de prueba propias (no las reservas de Faranda, en movimiento); `go_live_at` según §5.4; cerrar el día en RA/LT según §5.2; integración completa con Faranda en reposo; commit sin `pnpm-lock.yaml`.

## 7. Integrador (2026-09-19 03:38-04:00 local; HEAD `e6acd8c`, L5 sin commit)

### 7.1 Copia de seguridad y proceso

- Copia previa **`~/anfitorio-demo/backups/hotelos-pre-l5-integracion-20260919-033833.dump`** (`pg_dump -Fc`, 71,9 MB, 285 `TABLE DATA`, 5,2 s) antes de la primera escritura del integrador (la BD ya llevaba las 17 migraciones, la carga real de Sage 200 y las reservas reales de OPERA de cinco hoteles: reservas de Faranda **13.457**).
- Migraciones **17/17** («Database schema is up to date!», última `20260919120000_operaciones_l5_backfill_parte_titular`) y `db:drift:check` «No difference detected» antes y después.
- Instancia propia `PORT=3907 RUN_SCHEDULERS=false RBAC_STRICT=true node --env-file-if-exists=../../.env --import tsx src/server.ts` (tres procesos consecutivos: pid 70431 para el flujo, 74270 tras el arreglo de `admin_user_exists`, 76310 tras el de los lectores; `schedulerLeader false`; los tres matados, `lsof -iTCP:3907` → 0 al terminar). El API **`:3000` no se ha reiniciado** (pids 4986 y 78677 intactos): sigue sirviendo el código anterior a L5 hasta que el orquestador lo reinicie.
- Sesiones reales `POST /auth/login` (10 por minuto por IP) de la organización aislada **`org_l2_intmu7q2i4yfsjk`** (helper `tests/integration/helpers/l2-tenant.mts` + 7 usuarios con las plantillas de T8a: `receptionist`, `housekeeper` (pisos), `housekeeping_manager` (gobernanta), `night_auditor`, `front_office_manager`, `manager` (dirección), `admin` (sistemas), `admin_clerk`, `general_manager`, `owner`) y de Faranda `@faranda.test`: `recepcion.rias`, `jefatura.recepcion.rias`, `direccion.rias`, `direccion.tilos`, `auditoria.noche.tilos`, `pisos.tilos`, `gobernanta.tilos`, `sistemas`, `direccion.general`, `administracion.tilos` (contraseña única de demo de T8a). Guiones: `scratchpad/l5.sh`, `l5-login.sh`, `l5int-create-tenant.mts`, `l5int-rates.mts`, `l5int-cleanup.mts`; respuestas en `scratchpad/l5int-*.json`.
- Limpieza: `cleanupTenant(org_l2_intmu7q2i4yfsjk)` → 0 organizaciones, 0 usuarios, `countResidualTenants()` = 0; `rbac:sync --dry-run` vuelve a **46** roles de plantilla (69 con la organización de prueba viva). Nada de Faranda se ha borrado.

### 7.2 Estado de habitación, parte de viajeros y SES por HTTP (organización aislada, hotel A: 3 habitaciones)

| Paso | Usuario | Ruta | Resultado |
|---|---|---|---|
| Inspección ×2 sobre 101 libre y limpia | gobernanta | `POST /rooms/101/mark-inspected` ×2 | 200 `inspected/inspected` · 200 sin cambio (un solo `ROOM_STATE_CHANGED`) |
| Reserva con titular completo + menor (8 años) en `guests[]`, llegada hoy | recepción | `POST /properties/A/reservations` | 200 `RES-00001` `confirmed`, `totalAmount 0` / `priceSource none` (la parrilla del helper solo tiene octubre) — ver INT-L5-02: el `guests[]` se descarta |
| Check-in en 101 | recepción | `POST /reservations/:id/check-in { roomId, signatureObjectKey }` | 200 `checked_in`, `guestRegister { created 1, existing 0 }`, folio primario reutilizado; **101 = `occupied` con `housekeepingStatus inspected` intacto** |
| Segundo check-in | recepción | idem | 409 «no está lista para el check-in (estado: checked_in)» |
| Limpia sobre la OCUPADA | pisos | `POST /rooms/101/mark-clean` | 200 `occupied/inspected` (no libera ni degrada) |
| Ruta libre alias / texto libre | pisos | `POST /rooms/101/housekeeping-status { ready }` · `{ foo }` | 200 `occupied/inspected` · **400** «Estado de limpieza no válido: usa clean, dirty o inspected.» |
| Inspección sin clave | recepción | `POST /rooms/101/mark-inspected` | **403** «requiere: housekeeping.task.manage» |
| Cuatro lectores con 101 ocupada | gobernanta / dirección / recepción | `/dashboards/housekeeping` · `operations-director` · `GET /properties/A/dashboard` · `room-rack` | hk `{ clean 2, dirty 0, inspected 1, ooo 0, occupied 1 }` = ops `{ 2, 0, 1, 0 }` = propiedad `{ dirty 0, cleanInspected 1, ooo 0 }` = rack `{ rooms 3, occupied 1, vacantClean 2, vacantDirty 0, ooo 0 }` |
| Validar / enviar sin firma | recepción | `POST …/guest-register/:id/validate` · `POST /properties/A/ses/submissions` | `ready_to_sign` + `valid false` `[signature_required]` · **409 `GUEST_REGISTER_INVALID`** «El parte de viajeros no se puede enviar: signature_required.» (0 filas) |
| Firmar / validar / enviar | recepción | `…/sign` · `…/validate` · `POST …/ses/submissions` | `signed` · `valid true` · 200 `queued 1` → fila `accepted` (sandbox, 1 intento) |
| Reenviar el parte aceptado | recepción | `POST …/ses/submissions` | **409 `GUEST_REGISTER_NOT_QUEUEABLE`** «El parte ya está aceptado por el MIR: los históricos no se reescriben.» |
| Cierre del día #1 (19/09) | auditoría nocturna | `POST …/night-audit/run {}` | 200 sin forzar: `post_room_charges { postedCharges 1, notYetInHouse 0, totalPosted 100 }` (tarifa 100 € sembrada por el integrador para el día), `snapshot { clean 2, inspected 1, occupied 1 }`, fecha → 20/09 |
| Check-out con saldo / cobro / check-out | recepción | `POST …/check-out {}` · `POST /folios/:id/payments { 100, cash }` · `POST …/check-out {}` | **409 `BALANCE_DUE 100`** · 201 `captured` (asiento) · 200: **101 `dirty/dirty` + `departureTask departure_clean pending`**, folio `closed` |
| Tablero móvil + «Iniciar» | pisos | `GET /dashboards/housekeeping-mobile` · `PATCH /housekeeping/tasks/:id { in_progress }` | 101 `dirty` con `taskStatus pending` → 200 `in_progress` («En limpieza» es el estado de la TAREA) |
| Limpia → inspección ×2 | pisos / gobernanta | `mark-clean` · `mark-inspected` ×2 | 200 `clean/clean` · 200 `inspected/inspected` · 200 idempotente; la tarea sigue `in_progress` (INT-L5-04) |
| Cierre #2 (20/09), revisión y reapertura | auditoría / administración / dirección | `run {}` · `runs/:1/review` · `runs/:2/reopen { audit_finding }` · `run {}` · `runs/:1/reopen { other }` | 200 fecha → 21/09 · auditoría sobre su propio run **403** (`night_audit.review`), admin_clerk 200 `reviewedByUserId` · reopen del ÚLTIMO día: `reopened`, **`businessDateRewound true`, fecha 20/09** · re-run sobre la MISMA fila (`completed`, conserva `reopenedAt`, `reviewedByUserId null`) · reopen de un día anterior: `businessDateRewound false` (fecha sigue 21/09); su re-revisión responde **409 `NIGHT_AUDIT_ALREADY_REVIEWED`** (INT-L5-05); recepción → 403 `night_audit.reopen` |
| Readiness en el GET (hotel A, SES encendido) | dirección general / propietario | `GET …/readiness` ×2 | 17 comprobaciones calculadas en el primer GET (`computedAt`), el segundo dentro de la ventana devuelve el mismo `computedAt`; bloqueantes `tax_region_configured`, `default_building_exists`, `admin_user_exists`, `ses_hospedajes_credentials` (sandbox) |

### 7.3 Readiness y go-live reales

- **Hotel B de la organización aislada** (SES apagado, sin uso): `GET …/readiness` → `blocked` (`tax_region_configured`, `default_building_exists`, `admin_user_exists`); `POST …/buildings` (dirección general, `property.map.manage`) → 200; `POST /rbac/assignments` (sistemas, `users.assign`) de un `admin_clerk` al hotel → 201 **pero `admin_user_exists` seguía `fail`** (INT-L5-03: la ruta T8a no escribe el espejo `user_property_roles`); `POST …/taxes/provision` → **403 `compliance.configure`** para `general_manager`, `admin` y `owner` (T8a gatea por plantilla × ámbito × módulo: `admin` = solo Administración de sistema) y 200 para `manager` asignado al hotel por `POST /rbac/assignments`; tras el arreglo de INT-L5-03 y el reinicio: `POST …/go-live` (dirección general) → **`approved`** (`goLiveAt`, paso `go_live completed`), segundo POST `alreadyLive true`, `GET …/setup` `live true, completed 1/15`; recepción → 404 opaco (fuera de su ámbito). El `GET …/readiness` dentro de la ventana de 10 minutos siguió mostrando los bloqueantes viejos (`computedAt` 01:47:56) mientras el go-live decidió sobre comprobaciones recién calculadas: comportamiento de diseño (L5-C), pero la pantalla debe decirlo (decisión §8.6).
- **Faranda** (`direccion.general`, `general_manager` de organización; `direccion.rias` (`manager`) y `sistemas` (`admin`) responden **403 `backoffice.access`** en `GET …/readiness` por el gating de módulo de T8a): readiness recalculada en el GET en los 8 centros (17 filas cada uno; PG, MC, AS, FN, LL y OC no tenían ninguna) → **LT, PG, MC, AS, FN, LL `ready`** y aprobados por `POST …/go-live` (`goLiveAt` 2026-09-19T01:48:03Z para LT y 01:50:48-49Z para los cinco; paso `go_live completed`; segunda llamada `alreadyLive true`; `direccion.tilos` sobre LT también `alreadyLive`); **RA `blocked`** por `ses_establishment_profile` (falta el número de registro SES; «activo por uso: 24 envíos») y `ses_hospedajes_credentials` (modo sandbox), aviso `verifactu_software_declared`; **OC `blocked`** (oficina: sin edificio, tipo ni habitación vendible). `go_live_at` en BD: 6 centros con fecha; RA, OC, prop_123 y prop_canary NULL (el UPDATE de §5.4 **no** se ha ejecutado: contradiría la readiness de RA).
- Setup Center: `GET …/setup` de RA y LT devuelve los 15 pasos con `label` (L5F-06); tras el go-live LT `live true` y `completed 1`. Navegación sin pantallas nuevas: `build-nav-tree --check` al día (68 · 101 · 205), `check-discoverability` OK (placeholders 16/20, `LiveTimeline` sigue en la whitelist temporal: montarla es decisión del orquestador según §6 del informe TL), `check-route-access` OK (15 tokens × 192 URLs).

### 7.4 Cierre real del día en Rías Altas y Los Tilos (usuarios de T8a)

Antes de cerrar, el preflight de RA a 13/09 (`jefatura.recepcion.rias`, `front_office_manager` con `night_audit.run`) bloqueaba solo por `open_folios_with_balance 4` (las 4 salidas con 379,75 € de L3) y avisaba `unposted_room_charges 2` (de 22 alojadas: solo las llegadas el 19/08 y el 13/09, ya con INT-L5-01) y `dirty_in_house_rooms 3`; `POST …/run {}` sin forzar → **409 `NIGHT_AUDIT_PREFLIGHT_BLOCKED`** con la lista. Cada cierre se forzó con motivo (auditoría `NIGHT_AUDIT_PREFLIGHT_OVERRIDDEN`: 6 en RA, 4 en LT).

| Hotel · día | Alojadas | Cargos nuevos | Sin tarifa | Aún no alojadas esa noche | Importe | Folios liquidados cerrados | Con saldo | Bloqueos forzados |
|---|---|---|---|---|---|---|---|---|
| RA 13/09 | 22 | 1 | 1 | **20** | 70,70 € | **477** (canceladas 460 + no-show 17; entre ellas RES-00003/4/5, ya canceladas por la carga OPERA) | 14 (854,75 €) | folios con saldo 4 |
| RA 14/09 | 22 | 5 | 1 | 16 | 441,22 € | 0 | 14 | 5 |
| RA 15/09 | 22 | 10 | 1 | 11 | 1.045,35 € | 0 | 14 | 9 |
| RA 16/09 | 22 | 13 | 1 | 8 | 1.348,03 € | 0 | 14 | 14 |
| RA 17/09 | 22 | 20 | 1 | 1 | 2.159,14 € | 0 | 14 | 17 |
| RA 18/09 | 22 | 21 | 1 | 0 | 2.250,19 € | 0 | 14 | llegadas 33 + folios 24 + salidas sin check-out 3 |
| LT 14/09 | 28 | 2 | 1 | **25** | 285,77 € | **343** (canceladas 327 + no-show 16) | 0 | — (sin forzar) |
| LT 15/09 | 28 | 15 | 2 | 11 | 1.495,77 € | 0 | 0 | folios 2 |
| LT 16/09 | 28 | 15 | 2 | 11 | 1.495,77 € | 0 | 0 | 15 |
| LT 17/09 | 28 | 25 | 2 | 1 | 2.568,85 € | 0 | 0 | 15 |
| LT 18/09 | 28 | 26 | 2 | 0 | 2.706,56 € | 0 | 0 | llegadas 52 + folios 25 + salidas 9 |

- Total: RA **70 cargos / 7.314,63 €** y LT **83 / 8.552,72 €** (`folio_lines` tipo `room` con `posted_by` = el auditor; auditoría `FOLIO_CHARGE_POSTED` 70 + 83, `FOLIO_CLOSED` 477 + 343). **0 no-shows** (las 33 y 52 confirmadas sin check-in llegan el 18/09: solo el cierre de HOY las convertiría). `RES-02109` (RA) y `RES-02097` / `RES-02112` (LT) están alojadas sin tarifa ni importe (OPERA sin precio): 6 y 4-5 noches sin cargo — dato para recepción. Fechas de negocio: **RA 2026-09-19** (`closed_at` 01:59:15) y **LT 2026-09-19** (`closed_at` 01:49:22); `night_audit_runs` RA 6 · LT 5.
- Reapertura auditada en RA: `direccion.rias` (`manager`, `night_audit.reopen`, dentro de la ventana de 7 días) reabre el 18/09 (`reasonCode audit_finding`) → `reopened`, **`businessDateRewound true`, fecha 18/09**; `jefatura.recepcion.rias` lo re-cierra forzando con motivo sobre la MISMA fila (`alreadyPosted 21`, 0 cargos nuevos, `reopenedAt` conservado) → fecha 19/09; `direccion.rias` lo revisa (`reviewedByUserId`, runner ≠ reviewer). `jefatura` sobre su propio run → 403 (`front_office_manager` no tiene `night_audit.review`). En LT `direccion.tilos` revisa el 18/09 cerrado por `auditoria.noche.tilos`.
- Lectores sobre datos reales tras INT-L5-07: RA hk `{ clean 86, dirty 16, inspected 0, ooo 0, occupied 22 }` = operations-director `{ 86, 16, 0, 0 }` = `GET /properties/RA/dashboard` `{ dirty 16, cleanInspected 0, ooo 0 }` = rack `{ rooms 102, occupied 22, vacantClean 67, vacantDirty 13, ooo 0 }` (antes del arreglo hk decía 123/24 y ops 86/16: 45 habitaciones retiradas por la corrección de inventario de OPERA); LT hk `{ 91, 0, 1, occupied 28 }` = ops `{ 91, 0, 1, 0 }` = rack `{ 92, 28, 64, 0, 0 }`. `pisos.tilos` / `gobernanta.tilos`: habitación 101 de LT inspeccionada ×2 (un solo `ROOM_STATE_CHANGED`) — queda `inspected` (mark-clean no degrada una inspeccionada; volverá a `dirty` en su próximo check-out); `housekeeper` también lleva `housekeeping.task.manage` (200).
- SES de Faranda, sin tocar: RA 9 `accepted` + 12 `failed SES_DISCARDED` («sustituidas») + **3 `failed SES_ESTABLISHMENT_INCOMPLETE`** (una por parte: `grr_1134c282`, `grr_b3766169`, `grr_01c02216`); `GET …/ses/submissions?status=failed` → 3, `&includeDiscarded=true` → 15; `GET …/ses/establishment` → `ok false, missing [registryNumber]`, INE 15058; prop_123 134 `SES_DISCARDED`. Los 141 fallidos del brief son hoy **149 filas honestas** (134 + 12 descartadas y 3 recuperables que esperan el número de registro; ninguna llega al MIR). Partes: `is_primary_guest` 15 true / 1 false.

### 7.5 Hallazgos del integrador

| Id | Sev. | Hallazgo | Estado | Dónde / test |
|---|---|---|---|---|
| INT-L5-01 | **alta** | `post_room_charges` cargaba la noche a TODA reserva `checked_in`: con la fecha de negocio atrasada (RA 13/09) y OPERA haciendo check-in en la llegada, el cierre del 13/09 habría facturado esa noche a 20 huéspedes que llegaron después (la parrilla BAR de RA tiene precio para 13-19/09). | **Arreglado**: módulo puro `night-audit-in-house.ts` (`isInHouseOnNight` = min(llegada, día de check-in físico en la zona horaria del hotel) ≤ fecha de negocio; la salida no acota: el overstay sigue pagando); el paso lee los `stays` en una consulta, cuenta `metrics.notYetInHouse` y los lista «Aún no alojada esa noche…»; el preflight `unposted_room_charges` aplica la regla de llegada. Contrato wire intacto. | `night-audit.service.ts`, `night-audit-preflight.service.ts`, `night-audit-in-house.ts` (+ `__tests__/night-audit-in-house.test.mts`, 9), `docs/api-contracts.md`, `openapi.yaml`; `l5-night-audit-canceladas` 6/6, `pos-cash-night` 7/7; en real: RA 13/09 `notYetInHouse 20`, LT 14/09 25 |
| INT-L5-02 | media | `POST /properties/:id/reservations` acepta `guests[]` (esquema `GuestIdentitySubSchema`) pero `createReservation` solo procesa `primaryGuest` / `primaryGuestId`: el menor del ejemplo no se creó ni se vinculó (`reservation_guests` 1 fila) y el check-in generó **1 parte de 2**. No hay ruta para añadir acompañantes a una reserva (los tests de L5-B los vinculan por Prisma). | Abierto (fichero de L3 `pms.service.ts`; el check-in IA / L6a añade viajeros por escaneo). Decisión §8.4. | `schemas/reservations.schemas.ts:27`, `pms.service.ts:865-871` |
| INT-L5-03 | **alta** | `admin_user_exists` leía solo el espejo `user_property_roles`, que escriben la invitación (`auth-pilot`), `tenant-admin` y el bootstrap de onboarding, NO `POST /rbac/assignments` (T8a): un hotel cuyo personal se asigna desde la pantalla RBAC nunca podía salir en vivo. | **Arreglado**: cuenta también `user_role_assignments` de ámbito `property` no revocadas (unión con el espejo). | `backoffice.service.ts` (`computeReadiness`); `l5-readiness-golive` 14/14; hotel B aprobado tras el arreglo |
| INT-L5-04 | baja | «Limpia» / «Inspeccionada» del tablero móvil (`housekeeping-status`) y `mark-clean` no cierran la tarea `departure_clean`: tras Iniciar → Limpia → Inspeccionada la tarea sigue `in_progress` (KPI `tasksOpen` 1). El tablero móvil no tiene acción de cierre de tarea (solo Iniciar · Limpia · Inspeccionada · Reportar); el de escritorio sí (`in_progress → done`). | Abierto (L5-A). | `HousekeepingMobileScreen.tsx:168-200`, `housekeeping.service.ts:562-651` |
| INT-L5-05 | baja | La reapertura de un día ANTERIOR conserva la revisión (`reviewedByUserId`, pinado por `l5-night-audit-canceladas:466`); como no se re-ejecuta, su corrección no admite una revisión nueva (`409 NIGHT_AUDIT_ALREADY_REVIEWED`). | Abierto: decisión de producto (§8.7). | `night-audit.service.ts` `reopenNightAuditRun` / `reviewNightAuditRun` |
| INT-L5-06 | info | Gating de módulo de T8a observado: `admin` (sistemas) y `owner` de organización → 403 en backoffice / cumplimiento (`backoffice.access`, `compliance.configure`) aunque la plantilla lleve la clave; `manager` → 403 en `GET …/readiness` de RA (`backoffice.access`) pero 200 en `taxes/provision` de su hotel; solo `general_manager` recorre readiness / go-live de todos los centros. Coherente con `check-route-access` (sistemas 4 ítems / 6 pestañas). | Sin cambio; documentado para el runbook de accesos. | `security/access-decision.ts` |
| INT-L5-07 | media | `/dashboards/housekeeping`, `GET /properties/:id/dashboard` y `snapshot_room_status` agrupaban TODAS las habitaciones mientras room-rack, operations-director, el tablero móvil y la cola de recepción filtran `active: true`: en RA (45 retiradas por OPERA) 147 vs 102 habitaciones, `roomsDirty` 24 vs 16; invisible en las suites (3 habitaciones activas). | **Arreglado**: los tres lectores filtran `active: true`. | `dashboards/housekeeping.service.ts`, `server.ts` (hunk `GET /properties/:id/dashboard`), `night-audit.service.ts`; unit dashboards + night-audit + housekeeping 52/52, `l5-estado-habitacion` 14/14; en real RA 86/16 en los cuatro lectores |

Además: `tests/integration/helpers/l2-tenant.mts` siembra `status clean / housekeepingStatus clean / maintenanceStatus ok` (antes `"operational"`, fuera del vocabulario cerrado; petición de L5-A).

### 7.6 Datos de Faranda antes / después e invariantes

| Cifra | Antes (03:38) | Después (04:00, tras la integración completa) |
|---|---|---|
| Facturas · VeriFactu `accepted` · reservas Faranda | 25 · 33 · 13.457 | **25 · 33 · 13.457** |
| Permisos · plantillas · asignaciones vivas · organizaciones · residuales `org_l2_*` | 250 · 24 · 31 · 2 · 0 | **250 · 24 · 31 · 2 · 0** |
| `business_dates` RA · LT · PG · MC · AS · prop_123 | 09-13 · 09-14 · 09-16 · 09-17 · 09-19 · 09-14 | **09-19 · 09-19** · 09-16 · 09-17 · 09-19 · 09-14 |
| `night_audit_runs` | 0 | **11** (RA 6: 1 reabierto + re-cerrado, 1 revisado · LT 5: 1 revisado) |
| Folios RA `open` (confirmed · in-house · checked_out · cancelled) / `closed` (checked_out · cancelled · no_show) | 383 · 22 · 4 · 470 (+17 no_show) / 1.185 · 0 · 0 | 383 · 22 · 4 · **10** / 1.185 · **460** · **17** |
| Folios LT `open` / `closed` | 493 · 28 · — · 327 (+16) / 1.260 · 0 · 0 | 493 · 28 / 1.260 · **327** · **16** |
| Líneas `room` de cierre (RA · LT) | 0 · 0 | **70 (7.314,63 €) · 83 (8.552,72 €)** |
| `go_live_at` | NULL ×10 | LT, PG, MC, AS, FN, LL con fecha; RA, OC, prop_123, prop_canary NULL |
| `property_readiness_checks` Faranda | RA 17 · LT 17 · resto 0 | 17 × 8 |
| `property_setup_steps` completados | 0 | 1 (`go_live`) en los 6 centros en vivo; 40 filas huérfanas de suites `structure-*` (eran 32; residuo ajeno) |
| SES (RA accepted · discarded · recuperables; prop_123 discarded) | 9 · 12 · 3 · 134 | idéntico |
| Lotes Sage · asientos (no invariantes) | 42 · 143.222 | 42 · 143.222 |

### 7.7 Puertas del integrador (04:00, tras la limpieza; logs en `scratchpad/gates-int/`)

| Puerta | Resultado |
|---|---|
| `corepack pnpm run typecheck:all` | **15 PASS · 0 FAIL · 1 SKIP** (guest-web) · 18,9 s |
| `corepack pnpm --filter @hotelos/api test` | **2.365 tests · 2.364 pass · 0 fail · 1 skipped** · 713 suites (+9 `night-audit-in-house`) |
| Front unit (110 ficheros) | **1.447/1.447** |
| Contratos raíz (`node --test tests/*.test.mjs`) | **535/535** |
| `check-discoverability` · `build-nav-tree --check` · `check-route-access` | OK 192/192 (placeholders 16/20) · al día 68 · 101 · 205 · OK 15 × 192 |
| Cocoa 22 inventario · waves · contrato | `cocoa-22-inventory.json` idéntico (md5 `f9177a0d…` antes y después; 227 pantallas, 193 puntos, inlineStyles 679) · `--check` «§6 al día» · **18/18** |
| `db:migrate:status` · `db:drift:check` | 17/17 · «No difference detected» |
| Integración completa (53 ficheros, con `:3907` parado y Faranda en reposo) | **707 tests · 700 pass · 0 fail · 0 cancelados · 7 skipped** (los 7 conocidos) · 17,7 s · 0 «too many clients» |
| `admin-web build` | OK 2,49 s |
| `rbac:sync --dry-run` | 250 claves · +0 · 46 roles de plantilla · plantilla v2 · 0 behind |
| Worker | 20/20 |

### 7.8 SQL de verificación (solo lectura; `current_date` SIEMPRE cualificado)

```sql
-- O = 'cmrhw9jy30002fyvb6tsdiugt' · RA = 'cmrhw9jy40003fyvbuu2ec2w7' · LT = 'cmu1mifcp0000fyo1wzvq7txo'
select p.code, bd.current_date, bd.closed_at from business_dates bd join properties p on p.id = bd.property_id;   -- RA / LT 2026-09-19
select p.code, count(*), sum((r.reviewed_by_user_id is not null)::int), sum((r.reopened_at is not null)::int)
  from night_audit_runs r join properties p on p.id = r.property_id group by 1;                               -- RA 6·1·1 · LT 5·1·0
select count(*), sum(l.total) from folio_lines l join users u on u.id = l.posted_by
  where l.type = 'room' and u.email = 'jefatura.recepcion.rias@faranda.test';                                 -- 70 · 7314.63
select code, go_live_at from properties where organization_id = O;                                            -- 6 con fecha, RA y OC NULL
select status, coalesce(error_code, '-'), count(*) from ses_hospedajes_submissions where property_id = RA group by 1, 2;  -- 9 · 12 · 3
select (select count(*) from invoices i join properties p on p.id = i.property_id where p.organization_id = O),
       (select count(*) from verifactu_submissions v join invoices i on i.id = v.invoice_id join properties p on p.id = i.property_id where p.organization_id = O),
       (select count(*) from reservations r join properties p on p.id = r.property_id where p.organization_id = O);  -- 25 · 33 · 13457
```

### 7.9 Ficheros tocados por el integrador

`apps/api/src/modules/night-audit/night-audit-in-house.ts` (nuevo, puro) y `__tests__/night-audit-in-house.test.mts` (nuevo, 9 tests); `night-audit.service.ts` (paso `post_room_charges` + instantánea `active`); `night-audit-preflight.service.ts` (`unposted_room_charges`); `modules/backoffice/backoffice.service.ts` (`admin_user_exists`); `modules/dashboards/housekeeping.service.ts` y `server.ts` (`GET /properties/:id/dashboard`) (`active: true`); `tests/integration/helpers/l2-tenant.mts` (vocabulario cerrado); `docs/api-contracts.md` y `apps/api/docs/openapi.yaml` (regla de alojada esa noche; conjunto `active`); este informe; `CLAUDE.md`. Sin rutas nuevas, sin claves nuevas, sin dependencias, sin `style={}`, `pnpm-lock.yaml` sin tocar, ningún fichero de L6a / TL / T8.

## 8. Decisiones abiertas para dirección (consolidado del integrador; sustituye a §5 donde difiere)

1. **Números de registro SES.HOSPEDAJES** (sin cambio respecto a §5.1): RA sigue `blocked` para salir en vivo por ese dato y por el modo sandbox; con el número y el modo real, el programador deja las 3 filas aparcadas en estado honesto solo. ¿Pedir ahora los números a los 8 hoteles o mantener SES apagado explícitamente?
2. **Turno de noche por hotel**: los cierres del piloto están hechos (RA y LT en 19/09). Cada cierre de RA exige forzar por los **14 folios con saldo (854,75 €: 4 salidas + 10 canceladas)** y cada hotel tiene alojadas sin importe de OPERA (RES-02109 en RA; RES-02097 y RES-02112 en LT): ¿regularizar esos folios (cobro, factura o anulación) y poner precio a esas reservas antes del cierre de esta noche, o aceptar el cierre forzado como rutina? En RA solo `jefatura.recepcion.rias` puede cerrar (`night_auditor` solo existe en LT) y `front_office_manager` no revisa: ¿asignar `night_auditor` y `admin_clerk` a RA en `seed-rbac-demo.ts`?
3. **Hubs de configuración** (sin cambio respecto a §5.3): Setup Center único en navegación; queda retirar `POST /onboarding/projects/:id/go-live` y unificar los tres «códigos de establecimiento».
4. **Acompañantes en la reserva** (INT-L5-02): `guests[]` se acepta y se descarta; decidir si `createReservation` crea y vincula los acompañantes (parte por cada uno en el check-in) o si se retira del esquema hasta el check-in IA.
5. **Cierre de tareas de pisos** (INT-L5-04): ¿«Limpia» cierra la tarea `departure_clean` de la habitación (regla de producto) o el tablero móvil gana un botón «Hecha»?
6. **Readiness en el GET con ventana de 10 min**: tras corregir un bloqueante la pantalla puede seguir mostrándolo hasta 10 minutos aunque el go-live ya apruebe; ¿acortar la ventana, invalidarla al escribir edificios / impuestos / asignaciones, o mostrar «calculado hace N min» con «Recalcular»?
7. **Reapertura de un día anterior** (INT-L5-05): conservar la revisión (trazabilidad) impide revisar la corrección; ¿limpiar `reviewedBy` al reabrir cualquier día (como hace el re-cierre) o registrar una segunda revisión?
8. **`pnpm-lock.yaml`** e **invariantes de `l2-tenant.mts`** (reservas 110 / asientos 4.951 / lotes 34): igual que §5.5-5.6; la cifra real es reservas 13.457 · asientos 143.222 · lotes 42.
9. **`LiveTimeline`**: montarla según §6 del informe TL (variante alias o pestaña) y retirar su entrada temporal de la whitelist: decisión del orquestador, no tocada.
10. **API `:3000`**: sigue con el código anterior a L5 (sin cargos por noche alojada, sin puerta de preflight, SES sin validar): reiniciarlo con este árbol antes de que recepción cierre el día de hoy.
